# v4.336.0 · chapter-gate 通知跳转 + oh-story T5 落库接线

> 2026-09-18 发布。规格书：`进度计划/gaea-gate-jump-t5-wiring-20260918.md`。
> 两件观察池/todos 收口（用户指令「继续」）。

## 甲件：自动门通知可点击 → 章节分析面板（v4.331 观察池头名）

- `useChapterGateNotice(onOpen?)` 增可选回调（ref 保最新回调），通知
  onClick 传报告章号；CreatePage `gateChapter` 覆盖态打开分析面板，跨章时
  `GetChapter(n)` 拉该章正文供标注锚定（失败回退空串）。无回调=纯通知，
  既有行为零变化。
- 测试：钩子 +1（回调带章号 / 无效章号不回调 / 无 onOpen 不炸）。

## 乙件：oh-story T5 落库接线（todos 欠账本体）

摸底：T5 资产=`.gaea/skills/novel-agents/` 七张角色卡（dca0936b），代码侧
引用为零；规格消费面=run_skill/任务子代理，「落库」缺的是静态资产→可派发
通路（角色数据管道本就齐备，不动库）。

- SKILL.md frontmatter `runAs: subagent` + 「派发契约」段：子代理执行序=
  解析 `role=<七卡之一>` → read_file `.gaea/skills/novel-agents/agents/<role>.md`
  → 按卡执行；主会话 arguments 格式 `role=<角色> <任务>`。
- spawn 第五个内置模板 `subagent_writing`（写作域角色卡前缀）+ boot 映射
  `novel-agents → subagent_writing`：七角色子代理共享 L4 前缀缓存（V5.30）。
- 验收钉 Go +3：skill 包直读仓库资产断言 Scope/RunAs/派发契约锚点/七 role；
  boot 映射测试；cache 模板计数 4→5。
- oh-story 余项只剩「写前契约拒绝生成硬闸」（需先有「一键补大纲」，等上游）。

## 门禁

全量 ci.ps1 绿（vitest 全量、go 全量、e-check、hygiene）；drift OK@707
（零新绑定）；版本三处 4.336.0。产物=exe 50833920B
SHA256=235e3f05ca7ab062ebea99c49e24160f439a599b80db51a648f33bd1419d77ea
（releases+SUMS；桌面副本同哈希；冒烟 /api/health 200 过）。
