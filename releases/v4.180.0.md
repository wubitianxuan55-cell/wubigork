# v4.180.0 — 办公任务管理会话关联刀：会话待办区入任务管理页（2026-09-09）

> 用户反馈：办公板块「任务管理」没有与当前会话关联，全是固定内容。
> 定位：任务管理页（TasksWorkbench）三区中，子代理区/本地模型工具区本就按
> sessionPath 建册（会话关联），但页面的主体内容是 ② TaskCenter——其数据源
> `GaeaTaskList` 是**全局后台任务表**（价格抓取/语义索引等系统 cron 周期任务，
> 持续在列），天然与会话无关，视觉上就是「永远那几条固定内容」；而当前会话里
> 真正的任务（AI todo_write 待办清单）反而没有入口。

## 1. 会话待办区（新增，页面首区）

- TasksWorkbench 直订全局 controller store（`useStore((s) => s.items)`）——当前
  会话活动流随会话切换自然更替，零 props 穿层（ContextModal 弹层实例同源自动正确）；
  复用 `useTodoExtractor` 提取最新 `todo_write` 清单（倒序取无 parentId 的最新一条，
  与聊天流待办卡同一提取规则，单一来源）。
- 渲染：区块头「会话待办 n/m」+ 三态条目（in_progress 蓝点脉冲 / pending 灰点 /
  completed 绿点+划线置灰；level 子步骤缩进）；**无待办不占位**（会话没派任务
  时不噪音）。测试钉死「无→出现→清单更新以最新为准」三段。

## 2. 页面语义重排（信息架构，非删减）

任务管理页现为四区，会话/全局边界可读：

| 区 | 内容 | 归属 |
|---|---|---|
| ⓪ 会话待办（新） | todo_write 最新清单 | 当前会话 |
| ① 子代理 | 派生拓扑+实时预览（原有） | 当前会话 |
| ①b 本地模型工具 | vision/summarize 单轮运行（原有） | 当前会话 |
| ② 任务中心 | GaeaTaskList 后台任务+输出 dock/取消/重试（原有） | 全局（work 空间） |

零绑定（drift PASS@602）、TaskCenter 本体零改动（MergedPanel/ImageGen 等其他
消费方不受影响）。

## 3. 门禁

- vitest **2739/2739 首跑全绿**（+1：TasksWorkbench 会话待办区三段用例）；
  tsc 0、eslint 0、e-check OK；零 Go 改动（build 编译绿）；build strip + 冒烟 200。
- SHA256=69C89001048CB8BFD99BCE7122C69AB92A8C01A85CB4E34D23BAA45A1A71C0DD。

## 4. 欠账

- TaskCenter 全局任务与提交会话的关联需 Go 侧任务表加 session 维度
  （Schema+提交链路），属结构刀另立版本；当前以区块语义区隔（会话任务在上、
  系统后台任务在下）过渡，观察用户反馈再拍板。
- 观察池：任务管理页是否需要「会话后台任务」过滤视图（等 Go 侧 session 维度
  就位才有数据基础）。
