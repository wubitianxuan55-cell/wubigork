# v4.148.0 · diff 确认卡刀C：project 整量替换引擎强制逐条确认（2026-09-08）

> diff 确认闭环设计刀C（§2.1 裁定一/§2.5）：schedule_apply 的 **project 整量通道**（整计划生成/重排/替换）毁伤半径最大——升级为引擎强制 hardAsk：**任何权限级别（含 auto/yolo）前置逐条弹卡，不存在会话放行**；ops 增量通道维持现状闸门（拍板项 3：auto/yolo 豁免弹卡，后置回执+回滚兜底）。改动收敛在 control 包闸门分支与技能/compact 文案，**无新绑定（597 不变）、不动 event.Approval 结构**（设计红线）。

## 1. Go 闸门（internal/gaea/control/controller_approval.go）

- `gateApprover.Approve` 增 project 通道分支：`scheduleApplyIsProjectChannel(args)`（args.project 非空 JSON 对象）→ `requestApproval(ctx, tool, subject, alwaysPrompt=true)`——**先于 auto 快捷判定**，任何级别都弹；alwaysPrompt 不读不写 granted（禁会话记忆，拍板项 2 自然落地）。
- `approvalSubjectFor` 增 schedule_apply project 分支 `scheduleApplyProjectSubject`：解析 args.project → `schedule.Project.Analyze()`（与板块/回执同一引擎口径）→ 可读摘要「整计划替换（目标文件）：N 项工作，总工期 Y 天，关键 K 项；改动明细见 diff 预览」；**CPM 不过如实标注「批准也将被引擎拒绝」**；解析失败诚实降级。before 侧对比不进 subject（闸门无工作区上下文），改动明细对比由前端 diff 确认卡承担（v4.147 刀B）。
- 拒绝语义零改动：被拒 apply 未执行、未落盘、无证据卡；固定拒绝文案已含 do-not-retry 指令。

## 2. 前端形态（ApprovalModal）

- `scheduleApplyIsProjectChannel`（applyDiff.ts，Go 判定的前端镜像）→ project 通道审批卡按 **hardAsk 三钮形态**渲染（拒绝/允许一次/终止；无「本会话内允许」「始终允许」），快捷键 3/5 对应调整，卡尾提示「批准仅本次生效；拒绝后可让模型按你的意见修改后重新提交」。展示层判定，强制权威在 Go 闸门。
- ops 通道五钮不变（allow_session 保留，审批疲劳对策）。

## 3. 技能与 compact 同步（纪律落进模型侧）

- schedule-edit 技能 Body 新增 **「确认与回滚（改动落盘的安全口径）」** 分节：project 逐条批准/被拒=未写入且**禁止原样重发**（修改后重发=新一次确认；连续被拒两次停止下发要口径）/超时=拒绝如实告知/预览与回执不一致以回执为准/回滚入口（工具卡「回滚本次」与变更 tab，用户说「撤销刚才的修改」优先引导）。
- scheduleApply.Description 尾部增确认机制说明；compact.go compactDesc["schedule_apply"] 同步（≤一行口径）。锚点锁 **31→35**（schedule_skill_test.go +4：确认与回滚/禁止原样重发/以回执为准/回滚本次）。

## 门禁与走查

- Go 全量绿（+5：project ask 弹卡/auto·yolo 仍弹/禁会话记忆双弹/ops ask 弹+auto 静默/可读 subject 与循环依赖降级）；skill 锚点测试过；tsc -b/eslint 0；vitest 2499（零变更）；drift PASS@597；版本三处 4.148.0；build+冒烟过（/api/health 200）。
- 刀D（ops 通道投影 simulateOps TS 镜像 + Go/TS 对拍单测）按设计刀序待续——设计最大风险（TS 与 Go ApplyOps 漂移）的对冲主阵地。

## 产物

- `releases/gaea-v4.148.0.exe` — SHA256 见 `SHA256SUMS-v4.148.0.txt`（f923207b59ff4052b77f60fe4e41010ecfe12d2ff7ffaf5d9d49d41de189de11）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）
