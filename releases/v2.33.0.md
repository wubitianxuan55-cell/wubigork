# gaea v2.33.0「质量收敛 · 前端收敛」

> 阶段 6 第十刀（T6-10），2026-08-14。贯穿任务收官：巨型文件拆分 / any 清零 / 漂移检查恢复 / mock 契约化 / 桥接归一。
> 规划：docs/superpowers/plans/2026-08-14-gaea长期规划-阶段6-质量收敛.md

## 发布内容

### T6-10.1 巨型文件拆分（8 个巨型文件全部收敛）
- **ChatPage.tsx 1022→370 行**：拆出 pages/chat/{constants,types,utils}.ts（纯逻辑）+ components/chat/
  {ChatComposer,ChatModeBar,ChatPersonaBar,MessageList,SuggestionCard,WelcomeScreen}.tsx（UI）+ hooks/
  useChatStream（流式订阅与五路收尾）/useChatTopics（话题列表）/useChatVoice（语音持久化与朗读）/
  useCustomTemplates（任务模板）
- **ImageGenPage.tsx 911→310 行**：拆出 hooks/useImageGenConfig（参数状态）/useImageGenHistory（历史与
  分级存储回填）/useImageGenQueue（队列与提交）+ components/imagegen/meta.ts（模型/尺寸元数据单源）
- **CapabilitiesPanel.tsx 803→178 行**：拆出 components/capabilities/{ServersSection,SkillsSection,
  ToolsSection}.tsx + hooks/useCapabilitiesData.ts（数据装配与刷新）
- **Composer.tsx 786→406 行**：拆出 components/composer/ 7 个组件（ComposerAttachmentBar/DragOverlay/
  InputRow/QueueList/TableBanner/Toolbar/WorkspaceMenu）+ hooks/useComposer{Attachments,Menus,Workspace}.ts
- **mock.ts 1563→50 行**：按域拆分为 lib/mock/{chat,core,cost,memory,model,office,retrieval,settings,
  shared,state}.ts（10 文件）；11 个 no-op 逐条落实（浏览器开发环境语义化返回 + 注释说明），不再堆在单文件
- 拆分全程行为测试基线先行（既有组件测试零语义改动通过），Sidebar/CostLibraryView 见 T6-10.5

### T6-10.2 any 清零（门禁强制）
- eslint.config.js：@typescript-eslint/no-explicit-any 由 warn 升为 **error**，进 CI 门禁——全仓显式 any
  消灭（315 处→0），新增任何显式 any 即 lint 失败
- 历史 as any 逃生口由类型化兼容层替代（wails.d.ts:100 注释成文）

### T6-10.3 绑定漂移检查恢复（双向）
- scripts/gen_bindings 新增 **-names** 模式：只输出 Go 侧全部导出绑定方法名（字典序稳定排序），不写生成文件
- 新增 frontend/src/gaea/lib/bindingNames.ts（462 个方法名清单，与 Go 绑定面一致）
- gaea/lib/bridge.ts 恢复类型级双向守卫：_CheckAppBindingsHasNoStray（AppBindings 声明不得有多余方法）+
  _CheckAppBindingsCoversAll（bindingNames 每个方法必须被前端覆盖），任一方向漂移即 tsc 报错
- 新增 scripts/check-bindings-drift.ps1（运行时对照 bindingNames.ts vs Go 方法面，不一致 exit 1）+ CI
  backend job 新增漂移检查步骤

### T6-10.4 mock 契约对齐（真实返回取样）
- mock-contract-e5.test.ts RetrievalEvalRun 改写为**契约校验**：对齐 Go gaea_retrieval_eval.go +
  docs/retrieval-eval-set.md——total=12（真实查询集）、threshold=0.8、passed=recallAt10>=threshold 自洽、
  perQuery 结构（expected/topHits 均为 kind:name 形式）、首条查询锚点「打桩设备 台班价」；不再锁定
  0.85 虚构值——mock 与 Go 不一致会在数值/结构锚点处暴露
- CostImportVisionPreview / CostCompare / UnifiedSearch 用例同步补结构断言（source/kind 枚举对齐 Go）

### T6-10.5 虚拟化与性能
- **Sidebar 会话列表改 react-window List 虚拟滚动**（新增依赖 react-window ^2.3.0，rowComponent 单行渲染，
  useMemo 行数据装配）；搜索过滤接入 useDebouncedValue（250ms 防抖）
- **CostLibraryView memo 化**：React.memo + useCallback（load/loadCategories）+ useMemo（nodeById/pathById/
  priceText/sorted）——重渲染热点收敛
- 新增 hooks/useDebouncedValue（空串/空值即时同步、卸载清理定时器）+ **6 测试**；Composer 之外 2 处消费
  （Sidebar/CostLibraryView）

### T6-10.6 桥接归一（双桥接合流）
- **删除 frontend/src/api/bridge.ts**（123 行旧代理，消费方全部迁移完成）；initBridge 与兼容层并入
  gaea/lib/bridge.ts（+478 行，含漂移守卫与 bindingNames 注册）；新增绑定单处注册
- frontend/src/types/wails.d.ts 同步收敛（+236）

### T6-10.7 测试补强（零测试组件与迁移）
- Sidebar.test.tsx（+40：虚拟滚动行渲染/过滤/空态）、CostLibraryView.test.tsx（+56）、GraphView.test.tsx
  （+47 行为断言）、ChatPage.test.tsx（+26）、CharacterLibEditor.test.tsx（+8）、BindSection.test.tsx（+8）、
  useDebouncedValue.test.ts（新 6 用例）——vitest **354→361**（79→80 文件）
- 迁移一次性/零测试组件覆盖要求在本刀完成（GraphView/WhisperMemoryLibrary 行为断言已并入上文用例）

## 验证
- Go：build + vet 干净；go test ./... **109/109 包 ok**（test-all.ps1）；TestBindingsCompleteness PASS
  （462 方法，无绑定签名变更）；check-bindings-drift.ps1 OK（bindingNames 与 Go 面 462 一致）
- 前端：tsc 0 errors、eslint 0 errors（359 存量 warnings，no-explicit-any=error 0 违反）、
  vitest **361/361**（80 文件）、vite build 14.46s
- 冒烟：scripts/smoke.ps1（/api/health 200，{"status":"ok"}）
- 注：exe 无版本资源为历史已知状态（windres 缺失，v2.20.0 实测同款），不影响使用

## 产物
- gaea-v2.33.0.exe（32.8MB，SHA256=8FADBB7385D794DB69842171D4F95E678849FA8481686F646A4BBA6E94F4E92F）
- SHA256SUMS-v2.33.0.txt
