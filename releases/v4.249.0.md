# v4.249.0 — 后端性能刀E：面板/杂项收官（2026-09-12）

## 为什么是这一刀

普查档最后一刀：面板刷新路径的固定成本与资源隐患。前端轮询刷新时，
上下文看板对同份日志反复全量解析（2MB 日志每刷 9.1ms/6.7MB 分配）；
会话历史列表对每个 .jsonl 全量解码；wssearch 正文缓存无界增长；以及
若干正则/字符串的顺手项。七项一次销清，普查档五刀全清收官。

## 修复

1. **会话条目缓存**（#5）：`readEntriesForCached`——事件日志 append-only，
   size+mtime 未变即内容未变；看板/节点详情/轨迹/Agent 网络四个绑定
   接入（resync 保持直读）；fold/detail/trajectory 层核实只读共享；
   上限 8 会话。**看板刷新 9.1ms→22μs（408×）**。
2. **会话列表预览缓存+轻量解码**（#7）：`previewSessionCached`
   （size+mtime 键，transcript 整体重写即失效，上限 64）；解码只取
   role，content 仅首条用户消息才反序列化。
3. **wssearch 正文缓存上限 64**（#10，改前只增不删）。
4. **graph linker 正则消除**（#16）：QuoteMeta 字面量匹配改纯字符串
   操作，O(实体数) 次编译归零。
5. **sanitizeFilename** 改 strings.Builder（#18）；三处函数内
   MustCompile 提包级（#20）。
6. **SemanticRecall 死代码删除**（#19，零生产消费方核实；
   DocText/Cosine 有消费方保留）。#17 归档分页=legacy file backend
   专属路径，观察不修。

## 测试

Go +2：条目缓存失效（追加立即可见）/ 预览缓存失效（重写立即可见）。
app bench 1 组入库。零绑定零前端。

## 门禁

ci.ps1 全绿 / 版本三处 4.249.0。

## 产物

build.bat（冒烟 /api/health 200 + 桌面副本同哈希）→ `build\bin\gaea.exe`
**49,031,168 B** SHA256=**4F6E6A5A23A33D871B67053017C7EFDF7C10F3E664D238F7102AB64F38E1465B**。

## 收官

普查档五刀 A/B/C/D/E 全清（v4.245~v4.249）。剩余：#17 legacy file
backend 归档分页观察项、真机走查（等闲置窗口）。
