# v4.414.0 · Plan mode 计划审批门（dsh plan-mode 蒸馏）

> DSH 留池⑦（实质头名：①子代理控制面等上游稳定）。规格
> 进度计划/gaea-plan-mode-20260926.md（蒸馏=取道不取器，471 行上游 → 内核 1 文件+2 挂点+1 斜杠动词）。
> Go 4 文件改+2 新文件，绑定面 720 零变更，前端零改动。

## 落地

- **exit_plan_mode 工具**（internal/gaea/agent/plan_mode.go）：恒注册（boot.go，与
  request_permission 同段会话级元工具纪律——工具目录跨模式切换稳定）；计划模式下把
  markdown 计划（强制 `#`~`######` 标题开头）提交 ask 通道审批（前端泛型选项卡，
  零新绑定）：批准=闸翻转 off+批准叙事落历史；继续计划=错误带回反馈语义；headless
  无 Asker=诚实拒绝（执行闸必须人来开，刻意不仿 ask 的自选降级）。
- **PlanGate 窄接口**经 callContext 盖章（executeOne 与 asker/PermissionRequester
  同位同纪律），AgentRunner 实现；子代理经 SubagentMetaTools 排除（防代替父会话
  退出计划模式）。
- **/plan 斜杠动词**（controller_submit.go，/compact 同款）：on 注入政策 user 消息
  （`[plan mode]` 研究只读纪律+exit 用法）、off 注入退出叙事、status/未知回落状态
  展示；running 中拒绝。
- **与上游两处关键偏离（取道不取器）**：①系统前缀冻结（V10.36）→政策/叙事走
  user 消息注入，不动 system 段；②状态 v1=runner 内存态（事件日志 kind=plan_mode
  +恢复折叠留二刀，最敏感面零碰）。
- **沙箱/权限独立**（与上游同判）：计划模式只动提示+审批流，不做硬工具门；写操作
  照走既有 permission 闸。

## 测试与门禁

- Go plan_mode_test.go 六例：inactive 错误/批准翻转+叙事/继续计划反馈语义/headless
  拒绝/标题校验（含六级合法+七级拒）/真闸直测（翻转+两条 user 注入+系统前缀不动）。
- agent/control/boot 三包回归绿；全量 ci.ps1 绿 + 漂移闸 OK@4.414.0。

## 产物

- exe 见 SHA256SUMS-v4.414.0.txt（仅本地；冒烟 200 过）。
- 保留策略留 v4.410~v4.414 删 v4.409.0.exe。

## 文档

- 规格 进度计划/gaea-plan-mode-20260926.md+CHANGELOG/README+releases/README
  （437→438+34 席插 v4.414 裁 v4.382）+AGENTS 迁 1 插 1（一百三十一迁：v4.409.0
  入 archive）+progress/todos（dsh 留池⑦清账）。
