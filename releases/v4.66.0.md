# v4.66.0「子代理会话提升：保存为新会话 · TasksWorkbench 轮询收敛」

> 发布日期：2026-09-04 ｜ tag：`v4.66.0` ｜ 基线：v4.65.1 ｜ 绑定面
> **560→561（+GaeaPromoteSubagent）**｜ dsh Side Chat promote 语义补完。

## GaeaPromoteSubagent（保存为新会话）

- 后端（internal/app/gaea_subagent_promote.go）：把子代理运行
  （subagents/<ref>.jsonl）忠实投影为**独立新顶层会话**——原运行完全不动
  （逐字节测试钉住），每次提升产生新副本（快照语义，注释说明理由）。
- 会话存储摸底（实施依据）：sessionPath=文件（`<纳秒>-<model>.jsonl`），
  事件日志与 legacy 镜像**双写**才会被 ListSessions 发现；ProjectMessages
  支持的 kind 清单核实后做 `session.ToLogEntries` 转换（自带 turn 边界 +
  合成 request_header）。
- **投影往返双校验**：写前 `ProjectMessages(ToLogEntries)` 恒等断言 + 写后
  按真实 Restore 链复验，不等价不落盘并报错——保证提升出的会话可恢复、
  可继续对话。诚实降级（slog.Info）：子代理 system 提示不随迁（防双重
  注入）、孤立/重复 tool 结果丢弃、未响应工具调用剥离（剥空整条丢弃）。
- 防护：ref 仅接受 sa_（mt_ 明确拒绝）、running 拒绝（移动快照）、空间
  自描述与 writeSpaceForLocked 同规则（play 恒 play）+ fail-closed 校验。
- 前端（SubagentThread 头部）：非 mt_ 非运行中显示「保存为新会话」按钮
  （Save 图标，busy 转圈），成功/失败 toast（三语 +3 键）。
- 绑定面收口：gen_bindings 自动归 office 门面 + bindingNames/bridge
  （PromoteSubagent 映射）/spaceBindings（work 分面）+ 分类数量锁
  267→268；wails generate module 刷新生成物。+8 Go 用例。

## TasksWorkbench 轮询收敛（欠账销账）

- net+runs 双源自管轮询（Promise.all + 5s interval + 静默吞错）全部迁入
  subagentRunsStore/agentNetworkStore——子代理数据四消费点（App/
  SubagentsPanel/Sidebar/TasksWorkbench）至此**全部共享单轮询**；
- 失败态不再静默：error 横幅+重试（有内容）/错误文案+重试（空态），与
  SubagentsPanel 同款；detectNewSubagents 检测语义保持（ready 时检测、
  基线不触发、偏好开关）。
- 新 TasksWorkbench.test.tsx 11 用例（双源渲染/失败重试/新子代理检测/
  会话切换重订阅/卸载注销等）。TaskCenter 自管轮询留档（GaeaTaskList
  另一数据源，不在本系列）。

## 验证

- Go 全量 `go test ./...` exit 0；tsc -b / eslint 0；vitest 全量
  **221 文件 / 1647 用例**（净增 11）；drift PASS（**561**）；build.bat
  冒烟 /api/health 200。

## 产物

- gaea-v4.66.0.exe ｜ SHA256 见 releases/SHA256SUMS-v4.66.0.txt
