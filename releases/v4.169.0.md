# v4.169.0 — 瘦身 P2 主干：双空间并列落地（rail 切换器+分域导航）+ home 空间感知化 + 走查待证项收口（2026-09-09）

> 瘦身执行层第四版：P2 形态主干收官——**工位/乐园并列结构在 UI 上成立**（masterplan §轨道一·2/3）。
> 三线并发子代理（线A rail 双空间分域 / 线B home 空间感知 / 线C 走查项收口）+ 主代理收口
> （三语字典契约键统一、后端 nav 差异取证、审计文档、门禁复跑、版本发布）。
> 审计：docs/gaea-slim-p2-dualspace-2026-09.md（证据层）；前置：刀0 白名单解耦（v4.167）+ 刀1/刀2 schedule 并入办公（v4.168）。

## 1. 线A：rail 双空间分域导航 + 工位/乐园切换器（子代理 81d72797）

- `frontend/src/layouts/MainLayout.tsx`：CommandRail 新增 `space`/`onSwitchSpace` props + rail 顶部
  **工位/乐园切换器**（SHELL_SPACES 驱动，labelKey 三语，data-testid=v3-rail-space-switch / v3-rail-space-<id>）；
  rail 主体改 `getActiveMenuBoardsForSpace(space)`——当前空间板块 + shared（首页/模型中心）恒在，
  settings（inMenu:false）与 schedule 不在 rail，**independent（编程）只经 foot 单列**。
- `frontend/src/v3/foundation.css`：`.v3-rail-space` 切换器样式（两态：active=primary-container+glow 光条；
  hover 微缩放；focus-visible 描边；全 token 无硬编码色）。
- **P2 走查待证项①消解：rail code 双入口**（基线 §3.1 勘查项）——主体列表第 7 位不再渲染 code，
  独立板块全仓唯一入口=rail foot（与 space.ts `isIndependentBoard` 语义一致）。
- 快捷键 Ctrl+1~9 语义不变（仍按当前空间 getActiveMenuBoardsForSpace 序）。
- 测试：manifests.test 新增 getActiveMenuBoardsForSpace 断言（work/play 各 6 项 id 序精确锁定；
  code/settings/schedule 剔除）；新 CommandRail.test.tsx（切换器渲染/分域/编程单入口/点击回调）。

## 2. 线B：home 空间感知化（子代理 38441e4d）

- `frontend/src/boards/launcher.ts`：新增 `LAUNCHER_FEATURED = { work:'gaea', play:'chat' }`（每空间旗舰锚点）。
- `frontend/src/components/ModuleLauncher.tsx`：Bento 按当前空间过滤（deriveLauncherModules 传 space——
  shared 保留、**independent 编程不再上首页**、settings 两空间皆在）；hero 空间 chip（工位/乐园，t(labelKey)）；
  **工位右舷新增「最近文档」面板**（localStorage 单源 loadRecentFiles 前 5，点击→办公板块，零新绑定）；
  乐园右舷维持（会话+写作进度；晨报本就 work-only）。
- `frontend/src/components/module-launcher.css`：`.ml-space-chip` + 最近文档行样式（token 化）。
- 测试：launcher.test 新增 LAUNCHER_FEATURED + 双空间分域派生断言（work/play 各自清单、code 双侧剔除、settings 双侧保留）。

## 3. 线C：走查待证项代码层收口 + 刀2 G-2（子代理 b28c55c4）

- **knowledge 孤儿页处置**（走查待证项③）：`frontend/src/main.tsx` 移除 registerPage('KnowledgePage') + 删除
  `frontend/src/pages/KnowledgePage.tsx`——normalizeManifests 无条件过滤后端 knowledge 板块 ⇒ 组件零可达入口；
  知识库功能等价=记忆中枢「知识库」子面（KnowledgePanel）。后端 D7 板块保留（GetBoardManifests 仍返回，导航侧过滤）。
- **刀2 G-2 基线数计数关闭**：GschedSummary 增 `baselineCount`（project.baselines?.length）；ScheduleFileCard 新增
  「基线 N」StatChip（活跃基线快照+漂移卡体保留）；gschedSummary.test +3（多槽/空/坏形状）、ScheduleFileCard.test +2。
- **后端 nav 子项差异取证**（走查待证项②，只读）：cost 后端 7 子面 vs 前端静态 4（前后端优先双源回退，前端缺测算项目/
  造价参考/复盘笔记）；novel 后端 6 vs 前端 5（前端缺导出）——**判定=有意的双源回退**：normalizeManifests `nav: r.nav ? … :
  base?.nav` 后端字段优先，运行时以后端为准，前端静态清单仅兜底，无功能缺口，不改码只落档（详见审计文档 §4）。

## 4. 主代理收口

- **三语字典契约键统一**（单一负责人）：shell.space.* 由旧语义「书房/庭院」更正为「工位/乐园」及新 title；
  新增 home.recentDocs/recentDocsHint/recentDocsEmpty、schedCard.baselineCount——三个子代理线共用契约键，
  消除字典足迹并发冲突。
- 审计文档 `docs/gaea-slim-p2-dualspace-2026-09.md` 落档（§1-3 各线证据 + §4 nav 差异 + §5 度量看板 + §6 真机待走查清单）。
- 基线文档 `docs/gaea-slim-baseline-2026-09.md` §3 IA 走查重走查：rail 双入口/首页平铺/并列缺口三处闭合（或转真机清单）。
- 刀2 审计文档 `docs/gaea-slim-knife2-audit-2026-09.md` G-2/G-3 状态回写（G-2 已关闭；G-3 真机走查挂账）。

## 5. 门禁（主代理统一复跑）

- Go **127/127 包全绿**（零 Go 产品改动，仅版本常量 + 测试加固：TestGaeaGitNotARepo 在 test-all.ps1 的
  TMP=.tmp（仓库内）配置下确定性假红——git 向上寻根命中外层仓库、「非仓库」前提被环境破坏；加固=沙箱选址
  检测 gitTopLevelOf 命中即改系统用户缓存区另建，双配置全绿，全量终跑 127/127）;
- vitest 2720→**2737**（313 文件全绿，+17：线A manifests 4+CommandRail 5、线B launcher 3、线C gschedSummary 3+ScheduleFileCard 2；全量复跑 exit 0）；
- `tsc -b` 0 · `eslint .` 0 · drift **PASS@602**（零绑定）· locale 死键 **1448 键 0 死**（progEntry 死键随编程瓦片移除清零）；
- vite build + wails build + 冒烟见 §8 产物。

## 6. 度量看板（masterplan §3 六行，v4.169 实测）

```
每空间一级板块数（工位/乐园） ｜ entry chunk(gz KB) ｜ 冷启动可交互(ms) ｜ 常驻内存(MB) ｜ exe(MB) ｜ >50KB 源文件数
工位 4 / 乐园 4（双双达标 ≤6/≤4）｜ 375.96（基线 375.36，+0.6 系切换器/首页面板）｜ 待真机 ｜ 待真机 ｜ 46.2 系 ｜ 16（未触结构面）
```

## 7. 欠账（如实列示）

- 壳内真机走查（WebView2）：rail 切换器/分域渲染实态、home 最近文档面板、.gsched「基线 N」布局、knowledge 无残留入口（审计 §6）；
- 审计刀D 真机取证（printSvg print/拖拽/粘贴）挂账；
- 观察池刀3（工作台全幅内嵌办公）待一周评估（knife2 审计 §5 已基线评估=中等改造，维持）。

## 8. 产物

- `releases/gaea-v4.169.0.exe`（46.22 MB / 48,465,920 B，gitignore 不入库）
- SHA256：`79df700d8ef502c3119fb201f9134efa7ff4c4d1d9b1598faef2b1cc2d0f1416`
  （见 `SHA256SUMS-v4.169.0.txt`）
- 冒烟：HTTP 桥接 127.0.0.1:18999 `/api/health` → `{"status":"ok"}` 200，进程已回收；桌面副本同步。