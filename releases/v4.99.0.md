# v4.99.0「三线并行：图像域 T1 收口 · 办公 U2 回合投影 · GaeaListDir 硬化」

- 发布日期：2026-09-05
- 绑定面：**581 零变更**（drift PASS）。前端侧 ImageHubAssets/ChapterArtList 从 LegacySurfaceNames **转正** AppBindings/spaceBindings（Go 侧方法不变，分类锁 278→280）。
- 三线并行（文件所有权互斥子代理 + 主代理收口），基线 v4.98.0。

## 线 A：图像域 T1 收口（进度文档 T1 待办三件）

1. **GenerateDiagram 登记接线**（media.diagram 原语第二试点）：
   - 生成成功且 Mermaid 校验通过后，.mmd 落盘 `.gaea/uploads/diagram-<ts>.mmd`（与聊天画图工具同目录）并写 Asset Ledger（`capability=media.diagram`、`source_board=imagegen`、`kind=diagram`、`allowRoots=[.gaea/uploads]` 防穿越、`params={format:mermaid}`）。
   - 受 `App.Startup` 显式武装位闸控（imageHubRuntimeArmed），测试进程/未武装不落盘；落盘/登记任一失败只 `slog.Warn`，`GenerateDiagram` 返回契约零变化；图示走活跃对话引擎无显式模型 → **model/cost 诚实留空**。
   - Go 用例 +4：登记字段正确/失败只 warn/.mmd 落盘/未武装不写盘（既有回归钉仍绿）。
2. **素材库成本表轻扫**（TaskCenter 素材库 tab）：一次性读取改为 **tab 激活重读 + 30s 低频轮询**（复用 usePollingGate 可见性门控，页面隐藏/面板收起整表停；缩略 dataURL 按 path 缓存，轮询不重读文件；刷新保留选中卡）。
3. **画室素材库独立页 AssetLibrary**：imagegen 模块顶部「生成模式」导航新增「素材库」入口（非壳层导航）。
   - grid 缩略卡（auto-fill 140px），懒加载分页（12 张/页「加载更多」，已转换按 path 缓存）；.mmd 无位图诚实占位「缩略图不可用」。
   - 筛选：原语（生图/图示）与来源（绘梦/小说/角色）均从登记数据派生，未知板块诚实显示原始值。
   - 点选卡片展开溯源详情条：模型/成本（空→「未定价」不伪装 0）/来源/角色/时间/提示词截断/路径/空间/原语 tag。
   - play/work 双空间 Segmented 切换（各自 ledger 独立读取，默认 work）；返回生成台即还原工作台，生成状态不受影响。
   - ChapterArtList 未接入独立页（其语义=按章号项目清单，与全库浏览不匹配；章节配图的溯源字段已由 ledger source_board=novel 覆盖）。

## 线 B：办公 U2 回合投影与审阅收口（蒸馏规划 §4.2 拍板 2/3/4/5，绑定面 0）

1. **officeTurnProjection 前端纯函数**（`frontend/src/gaea/lib/officeTurnProjection.ts`，30 用例）：
   - 输入形状取自实有 wire（WireTool/store.Item），零发明字段；dispatch/result 按 callId 配对。
   - 工具分类对齐 Go evidence.go 三口径：写入=WRITE_TOOL_NAMES+format_convert/chart_gen/diagram_gen（落盘路径取 output）+xlsx_apply；读取=read_file/grep/vision/ls；写后对同路径成功读取升级为**验证回读**，其余为生命周期读取。
   - 失败 result → status=error、err 保留进 files[].lastWriteErr，**不推进** writtenPaths/verified（失败不提交转换）；孤儿 result 照常消费并标记、重复取首个、坏事件丢弃。
   - 配 officeToolEventsFromWire / officeToolEventsFromItems 双适配器。
2. **统一 Office 回合卡**：DeliverableCards（正文轮尾卡）与 VersionTimeline（时间线卡）共用 `deliverablePhaseOf` + 同一胶囊徽标语言（data-testid `office-turn-phase-draft/ready`）；**不复制操作 footer**——保留/回滚/验收仍只在登记面与证据链。
   - **红线守住**：登记表数据源（useTurnDeliverables）、证据链入口、产物置前行为全部未动；DeliverableCards.regress.test.tsx **原样未改全绿**。
3. **预览浮窗语义状态机**（`previewAutoFrontReduce` 纯函数 + App 接线）：
   - writeDispatch → 置 intent 并移出关闭集（新写重新武装）；writeResult ok 且本回合未弹过 → open（失败只清 intent 不弹）；read 恒 no-op（**读不弹**）；userClose → 关闭集+清 intent（**关闭优先不复活**）；turnEnd → 清关闭集/已弹集（**终态清理**），intent 保留（**写意图跨回合**）。
   - 接线守卫：挂载/会话恢复/resync 快照只建基线不触发（恢复会话绝不弹）；范围收窄 Office 文档扩展名 + 专注模式/窄屏(<1240) 不弹 + 同文件同回合至多一弹；open 走 openPaneFileOrPreview（pane 文件 tab 优先，回落大预览）。
4. **草稿轻量版**：不引入持久草稿实体——证据链该路径存在 xlsx_apply（Plan→Apply 批准）且 status∈{applied,verified} → **ready**；有任何写盘记录 → **draft**；journal 不可用时登记条目兜底；非 Office 文档/无写盘证据 → null（宁缺勿误）。merge/discard = 既有「标记已验收」与「回滚/恢复」，零新按钮。
5. U2 目标测试 101/101（officeTurnProjection 30 + DeliverableCards 5 + regress 2 + VersionTimeline 16 + DeliverablesPanel 45+3）；依赖面复验 73/73。
6. 如实记录：统一回合卡与浮窗状态机依赖真实办公回合/预览数据，?mock=1 无法驱动，验收面=上述 174 个 jsdom 用例 + regress 锁（A1 先例口径）；真模型端到端待用户复验。

## 线 C：Go 小刀 GaeaListDir 硬化（v4.96 登记）

1. **Go**：`internal/app/gaea_listdir.go` 新建——IsAbs 分支（`filepath.Clean` 直用，口径对齐 resolvePreviewPath；相对路径行为逐字节不变，回归用例锁定）；错误不再吞，结构化错误码 `Error [CODE]: message`（对齐 U1 FORMAT_* 先例）：**GAEADIR_NOT_FOUND / GAEADIR_NOT_DIR / GAEADIR_READ_FAILED**。
   - **签名变化**：`GaeaListDir(rel string) []DirEntry` → `([]DirEntry, error)`（成功负载不变；失败从空切片改 reject 错误串；TestBindingsCompleteness PASS，drift 581 不变）。绑定包装 1 行与 gen_bindings 输出逐字一致。
   - osStat/osReadDir 注入缝：Windows chmod 无法剥夺目录读权限，注入 fs.ErrPermission 使权限用例全平台确定性（真实 OS 权限剥夺留真机观察）。
2. **前端**：verifyArtifacts 新增 parseErrorCode/faultFromDirCode——`classifyDirProbe` **错误码优先**，无码退回「不存在」文案匹配兜底（旧后端兼容）；VerifyArtifactsThumbs 全空分支先按码定性（省一次 Preview 探测），无码/READ_FAILED 走原 Preview 探测链路；懒加载/四态诚实降级语义不变，零新 i18n 键。
3. 签名连带（既有 catch 分支语义）：deliverablesTurn「目录已删」从全标缺失变按存在处理；FileTree 错误态开始显示真实错误文案（原永不触发分支）；VerifyThumbs「是文件不是目录」从 noPages 变 missing（更诚实）。

## 收口（主代理）

- **wailsjsCompat 消费族欠账部分销账**：ImageHubAssets/ChapterArtList 转正 AppBindings（shared/play）+ spaceBindings 分类（锁 278→280）+ api/image.ts 三态回退（`window.go?.app?.App ?? bridgeApp`，v4.58 engines.ts 模式）；dev mock 补 `mock/imagehub.ts` 中性样例（生图×2/图示/视频，模型成本留空口径与 modelcatalog 一致）。
- i18n：新增 `imagehubT1.*` 27 键 + `officeTurn.*` 4 键 ×三语（重复键/缺失键体检 none）。
- **?mock=1 真浏览器走查 PASS**：首页→绘梦→顶部「素材库」入口→AssetLibrary（3 卡渲染/图示筛选=1/点选详情/创作空间=1 视频卡/返回生成台恢复）+ 任务中心素材库 tab（play 空间 1 卡+计数徽标+溯源字段）；DOM 断言+截图双验收。

## 门禁

- Go：build/vet 0，`go test ./...` 全量 0 FAIL。
- 前端：`tsc -b` 0、eslint 0、vitest **248 文件/1954 用例全绿**（净增 3 文件/56 用例；首轮 FilePreviewModal 1 例负载 flaky，单独复跑与全量复跑均绿——既有已知模式）。
- 契约：`check-bindings-drift.ps1` PASS（**581**）。
- 版本四处：app_info.go / wails.json / versioninfo.rc / frontend/package.json = 4.99.0。
- 冒烟：build.bat + /api/health 200；SHA256SUMS-v4.99.0.txt 当场生成。

## 欠账与后续

- 图像域 T1 余件：识图「读/懂」画室试用入口（粘贴图→识别）；T2 后续 ipadapter/pulid 一致性方法实现。
- U2 余件：真模型端到端（办公面板原地更新）待用户复验；写弹默认开（契约「写弹读不弹」直落），如需 opt-in 加一行 `shouldAutoOpenDeliverables()` 门槛即可。
- GaeaListDir 真实 OS 权限剥夺场景真机观察；未来「权限不可读」独立文案键 `deliverPanel.thumbsDenied` 候选。
- ChapterArtList 已转正未入素材库独立页（按章清单语义，留待按章视图刀）。
- 老账不变：run_skill AllowedTools 真机观察、阶段三 3c 人工沙箱浏览器、微信文件收发（抓包前置）、pptx 真编辑（设计文档 6 项待拍板）。
