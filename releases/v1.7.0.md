# gaea v1.7.0「设置中心重构 · 模型统一」(2026-08-02)

> 设置中心全面重构（小说/方案/办公/轻语/更新信息）+ 模型绑定统一左下角卡片 + 代码审查修复 9 项并发与绑定 bug。
> 版本 1.6.4 → 1.7.0，tag v1.7.0，构建 36,140,032 字节。
> 本机单用户定位：小说目录固定 C:\AI\xiaoshuo，本地模型（herdsman / ComfyUI）配置写死。

## 一、代码审查修复（9 项，含并发与绑定 bug）
- 轻语引擎绑定改用 per-call EngineID 覆盖（原全局 SetActiveEngine 切换有并发竞态且绑定不生效）
- 小说 9 处直接调用改传绑定模型（原全局模型名发往错误引擎 / 绑定模型被忽略）
- SetActiveASRModel 加模型存在性校验（防静默配置无效 ASR）
- config.Save 加锁（并发丢 key）+ Config.Get/SetFeatureModel 原子读写
- assistant manager 返回副本防 data race
- 底栏轮询合并 wmic 调用（4 进程 → 2 进程）
- 删除 VoiceSetChatTarget 死绑定
- useVoiceChat finalTranscript 累积修复
- 补测试：功能绑定校验/往返/监控、ComfyUI 工作流结构、findPython standalone-env

## 二、ComfyUI 生图完善
- findPython 候选加 standalone-env（ROCm PyTorch，系统 Python CPU-only 无法跑 13GB FP8 UNET）
- --windows-standalone-build 标志覆盖 standalone-env
- Krea2/ZIT 工作流结构测试锁定（EmptyLatentImage / ConditioningZeroOut / CFG 1.0 / 模型三要素）

## 三、绑定模型统一左下角卡片
- FeatureModelBar 内联条 → 玻璃卡片（运行中/已停用/未绑定三态）
- 5 板块（聊天/轻语/小说/方案/办公）统一左下角浮动，聊天页补上遗漏渲染
- 模型加载预警只统计本地模型（herdsman/ollama + ComfyUI 运行中），云端 xai/deepseek 不计入

## 四、模型中心重构
- 功能模型绑定独立标签页（引擎管理下方），2 列卡片网格（每功能一张卡）
- 语言模型页移除绑定区（只留 LLM 模型墙）

## 五、设置中心全面重构（6 标签）
- 「工作空间」→「小说」标签；默认小说目录 C:\AI\xiaoshuo（前端 store + 后端 config + placeholder 三处对齐）
- 小说面板移除绘梦图片保存目录（归属绘梦）
- 新增「方案」标签（方案编写模型卡 + 生成说明）
- 「办公」标签补全：后端 GaeaSaveSettings + 完整可编辑表单（模型/Agent 参数/权限/沙箱）
- 「系统」标签重设计：更新信息（关于卡 + Timeline 更新日志，后端 GetAppInfo 解析 CHANGELOG）+ 删除无用 v4.0 迁移
- 「轻语」标签：原轻语界面设置面板合并（AI 伴侣/人格/互动/数据管理），adultMode localStorage 持久化

## 六、其他 UI 修复
- 轻语空态多余标题删除；左侧栏 200 → 240（悬浮卡片不再遮挡对话窗口）
- 轻语界面移除独立设置弹窗（统一设置中心）

## 验证
- go build + go vet + go test 全量（含新增测试）
- tsc -b + vite + wails build 全绿
