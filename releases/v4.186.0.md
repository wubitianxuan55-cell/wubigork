# v4.186.0 — 场景重排接 UI：上移/下移 + ReorderScenes 落盘（2026-09-10）

> v4.185 欠账收刀。场景化之后场景顺序就是阅读顺序与 blob 投影顺序，
> `ReorderScenes` 绑定（NovelB 门面在册）此前零消费方——本刀接上。

## 实现（前端单面，零新绑定）

- `ChapterEditor` 每个场景框头部增 上移/下移 两钮（aria-label「场景 N 上移/下移」，
  键盘可达；首位禁上移、末位禁下移、非场景制章（blob/分支）整组禁用）。
- `moveScene`：**乐观换位**——本地 `scenes`/`sceneIds` 同步 swap 并经 onUpdate
  回喂父层，同时 `ReorderScenes(chapterNum, 新 id 序)` 落盘（blob 投影由 Go 侧
  同一调用内同步，v4.185 语义）；落盘失败回滚本地换位并诚实提示。
- 任一框缺 id（本地降级态）只换本地不调绑定——无 id 的框对后端不可排序，
  硬调会错位。
- 绑定面 602 零变更；零 Go 改动（版本三处外）。

## 门禁

- vitest +3（换位落盘 id 序 / 失败回滚还原 / 边界与 blob 模式禁用；moveBtns
  断言改 container 作用域——双实例同挂 document 时 getByRole 撞重复）、
  tsc 0、eslint 0、e-check OK、drift PASS@602、零新增 locale 键。
- 版本三处 sync 4.186.0；build strip + 冒烟 200；**构建口径=tag 树**（commit 后
  干净 worktree 构建；首构建抓出测试文件 fireEvent(null) 类型错——moveBtns 改
  container 作用域后漏复跑 tsc，已修并 amend，教训=最后一次编辑后必须重跑
  `tsc -b` 再发）。
- SHA256=`81a79557f35845cfdad64e6b2ea5088ccbe29ccc9afa33484f8b7a5c1d856f38`
  （releases/SHA256SUMS-v4.186.0.txt；gaea-v4.186.0.exe ≈ 46.2MB）。

## 欠账

- 场景拖拽排序手感（现按钮步进，真机观察后再议 DnD）；场景元数据
  （POV/地点/情绪）编辑面仍无 UI 消费方（需 UpdateMeta 出绑定面，另刀）。
- 壳内真机走查（真机池）不变。
