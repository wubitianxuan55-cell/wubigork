# v2.37.0「正确性纵深 · 收官」（2026-08-15）

> 阶段 7 第二~四刀（T7-2 可见性收口 / T7-3 名实相符 / T7-4 前端性能收尾）并行实施 +
> 3.0 架构主线 Step 0（修债）+ Step 1（会话事件日志）。
> 三刀 + 两步各自独立提交（可分别 git revert 回退）；本次统一构建发布 v2.37.0（含 Step 0 搭车 + Step 1 地基）。
> 规划：docs/superpowers/plans/2026-08-14-gaea长期规划-阶段7-正确性纵深.md；架构：docs/2026-08-15-gaea3-architecture-design.md。

## T7-2 可见性收口（v2.35.0）—— 后端吞错清零 + 成本凭据 + 批量事务
- **qrlogin**：PollQRStatusWithCode 对齐 PollQRStatus（非 200 / 坏 JSON 返回 error，不再静默吞错）
- **chatWebSearch**：搜索失败/空结果注入占位提示（slog.Warn，不阻断对话）
- **SaveConfig**：内存同步补齐约 30 键；无内存项 Warn 标注需重启；补 KeyTTSSpeed setter（原 tts_speed 无法持久化）
- **LocalTranslate**：可取消 ctx + 长文本分段（≤1500 rune）+ 单段重试 + 部分结果保留（Partial）
- **成本进料**：textFallback 截断 6000 rune（与 vision 对齐）；GaeaCostImportApply 整批事务（全成或全回滚）
- **测评**：并发≤4/重复≤20 钳制；基地址从 engineMgr 取；导出原子写
- **凭据**：token legacy 明文迁移后删除；Delete 清理 legacyPath；剧照 ID SHA-256 清洗防路径穿越
- 新增 41 测试（qrlogin 4 / chatWebSearch 4 / SaveConfig 4 / translate 6 / 截断 4 / benchmark 5 / 事务 5 / token 4 / portrait 5）

## T7-3 名实相符（v2.36.0）—— PDF/OCR + 检索索引 + dashboard 真实数据
- **PDF**：FlateDecode 压缩文本流 zlib 还原后再 BT/ET 提取（数字型 PDF 不再误判扫描件）
- **OCR**：单页失败跳过继续（全败才报错）；OvisOCR2 max_tokens 1024→4096 + finish_reason=length 截断检测；totalOCR 按实际页数
- **语义检索**：按需/缓存（就绪只嵌 query，不一致才按需 Ensure）；search 结果 MaxResults=100 上限
- **BM25**：新增线程安全 Cache（NewRanker 签名未动，cost.go 兼容）
- **dashboard**：DailyStats 按文件 mtime 真实聚合；成就系统全部真实化（不再估算）
- **filewatch**：WatchErr 周期检查→全量重建 + 回退轮询兑现承诺；记忆索引改 ReadAllChapterSummaries 目录扫描（章节断档不再终止）
- 新增约 33 测试（pdf_flate 5 / ocr_pages 6 / semantic 4 / watch 4 / search_limit 3 / bm25_cache 4 / dashboard 4 / memory_gap 3）

## T7-4 前端性能收尾（v2.37.0）—— 写路径静默清零 + 三态错误 + 渲染收敛
- **store.ts**：写路径静默清零（send/cancel/approve/answerQuestion 失败→logBridgeError + 用户可见 notice；approve 失败保留弹窗）；reconcileFinalAnswer 前缀启发式改完整文本比较
- **三态错误**：outlineStore/KnowledgePanel/FileTree 加载 {data,loading,error} + 重试
- **渲染收敛**：Transcript onToggle/onCapture useCallback + TurnBlock/ProcessCard memo；MarkdownContent React.memo；Toast error 档 role="alert"（info/warn 不设 role）
- 新增 41 用例（store 19 / outlineStore 5 / KnowledgePanel 2 / FileTree 2 / Toast 3 / Transcript 6 / MarkdownContent 4）

## Step 0 修债（搭车）
- office 模块注册（office.create→GaeaSend，D8 决策）+ MainBrainChat 全链路测试（8/8）
- 版本源三处对齐 + sync-version.ps1 / check-version-drift.ps1（UTF-8 BOM）

## Step 1 会话事件日志（3.0 地基，机制层）
- append-only 日志（seq=行数 / json.Valid 拒绝非法 / torn-tail 修复 + 合成 closers）
- 投影层 ProjectMessages + checkpoint（压缩后落盘 + 重放）+ 旧格式迁移（旧文件保留）
- 派生 API（标题/成本统计）+ 事件 sink（模型可见必入日志）；session.log_format="legacy|event" 回退开关
- GaeaHistory 黄金测试逐字节一致（红线守住）；session 67 测试全绿
- 遗留：运行时"日志即真相"app 层接线（Resume→Restore/Save→日志/压缩→checkpoint）留待 gen_bindings 阶段

## 验证
- go build ./... 干净；go vet 干净
- 逐包测试：session/event/boot/config/search/memory/stats/bm25/auth/characterlib/channels/weixin 全 ok
- internal/app 全量仅 1 个既有 flaky 测试（TestDrainAndPersistAll_FinalRoundLands，单独 3/3 通过，T7-1 遗留）；docmd 4 个 GBK 环境失败为基线问题
- 前端 tsc 0 errors / eslint 0 errors / vite build 通过；vitest 新增 41 全过（27 个 jsdom localStorage 环境失败为基线）
- TestBindingsCompleteness 兜底（本批无新绑定）

## 发布
- 产物：gaea-v2.37.0.exe（34.5MB）
- SHA256：37A56F54DF653E3D9E8A5751EA282CEB34BF5BBCA2672D26439BF7BAEBA7A62B
- 冒烟：见下方（/api/health 200）

## 提交
- 4dbba0c Step 0 / 72fae6c Step 1 / 0a9fb6f T7-2 / d7934eb T7-3 / 5364281 T7-4
