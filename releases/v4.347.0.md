# v4.347.0 · GLM-5.3-FlashX 新模型接入目录 + Flash 补官方绝对价

> 2026-09-19 发布。智谱 9 月中旬新发 GLM-5.3-FlashX（GLM-5.3-Flash 的加速版，
> 320B 总参/18B 激活混合架构，推理 200 tokens/s），官方四页交叉核实后接入
> 模型中心 GLM 静态目录。纯数据刀：内嵌目录 +1 条目、Flash 价格口径翻新，
> 零新绑定 705、零代码逻辑变更。

## 官方规格（2026-09-19 核实）

数据源=docs.bigmodel.cn 四页交叉（模型概览 / 模型详情
`cn/guide/models/vlm/glm-5.3-flash` / 定价页 / coding-plan 活动页）：

- **模型 ID**：`glm-5.3-flashx`（Model Code 逐字：`glm-5.3-flash/glm-5.3-flashx`）。
- **上下文/最大输出**：1M / 128K（与 glm-5.3 同档）。
- **能力**：原生多模态（图片/视频/文件输入），caps 同 Flash =
  `["vision", "tools", "reasoning"]`；思考仅 `enabled` 不可关。
- **国内绝对价**（定价页首次给出）：输入 2 元/M、输出 7 元/M；缓存命中
  0.57 元/M、缓存存储限时免费（记 price_note，ModelInfo 无缓存价字段）。
- **coding 套餐**：官方明示「套餐当前暂未开放 GLM-5.3-FlashX」（仅 Flash
  全量上线）——不配积分系数、不进 `glm_alias.go` 别名表。
- **顺带翻新**：定价页同时给出 Flash 绝对价 0.8/2.8 元/M（缓存命中 0.23）
  ——目录里「官方仅相对价、不填绝对价、估算回退内置表 USD」的 2026-09-02
  口径作废，Flash 改目录层 CNY 直读（费用估算从 z.ai 国际站 USD 近似变为
  官方国内价精确值，约 8.5 倍口径修正：0.65 USD→3.6 CNY / M+M）。

## 落地

- `glm_catalog.json`：追加 `glm-5.3-flashx` 条目（尾部追加纪律）；Flash 条目
  补 `price_in/price_out/currency` 并改写 price_note；`updated` → 2026-09-19。
- `glm_catalog.go` 头注释：数据来源与核实日期增补（此文件是目录数据的
  唯一注释位——JSON 无注释）。
- 消费链零改动：模型中心 GLM 引擎 fetchModels 走静态目录，新条目自动
  出现在模型列表（含能力标记与价格）；估算计费 `glmCatalogPrice` 目录层
  命中，CNY 直用不过汇率；覆盖文件/远程缓存三层合并语义不受影响。

## 测试

- `glm_catalog_test.go`：锚定清单新增 `glmCatalogV2FlashXIDs`（接在 v2 清单
  后）；目录计数 45→46 全对齐（无覆盖/覆盖追加/热重载/坏 JSON 回退 ×4）；
  Flash 价格锁值改目录层 CNY 口径；FlashX 元数据锁（ctx/out/caps/价格）+4；
  积分守卫名单加 FlashX（应无系数）；`TestGLMPricingVerified`/
  `TestEstimateCostCNY_GLMValueLocks` 锁值翻新（flash 0.8+2.8 CNY 直用、
  flashx 2+7=9）；CatalogSource 透传日期锚定更新。
- `catalog_remote_test.go`：计数随目录 +1（47/46/47 三处）。
- 定向：modelengine 包全绿；gofmt 0。

## 门禁

gofmt 0 / modelengine 包全绿 / drift OK@705（零绑定变更）/ 版本三处 4.347.0
（sync-version.ps1）；产物=exe 50844672B SHA256=dd51dc7c1c08e279fc4ef3107cd79a2be481d1f09299d8b1d7740e3dda8c9e65
（releases+SUMS；桌面副本同哈希；冒烟 200 过）。

process 记录：首轮全量 ci 撞 `ContextView` 2 例超时（v4.345 治理档案在册的
负载型 flaky）——足迹零交叠，隔离复跑 28/28 绿（7.3s），复跑全量绿收口，
与 v4.346 首轮 BookHealthPanel 同一家族。另：pnpm 直调 `pnpm vitest` 会生成
占位 `pnpm-workspace.yaml`/`pnpm-lock.yaml` 噪音文件（本项目前端走 npm），
已删；定向复跑一律 `npm.cmd run test -- <file>` 与 ci 同链路。

## 坑

- 官方文档站正文页动态渲染，搜索引擎摘要与首页抓不到规格表——
  `llms.txt` 索引 + `.md` 后缀直出 markdown 是正道
  （`/cn/guide/models/vlm/glm-5.3-flash.md` 直接给出 Model Code/参数表）。
- 目录「无绝对价→回退内置表（z.ai 国际站 USD）」是快照口径不是永久态——
  官方补绝对价后必须回头改目录+锁值测试，否则估算长期用错币种近似
  （本次 Flash 即拖了 17 天：0.65 USD vs 官方 3.6 CNY）。
- coding 套餐边界要逐模型核实：FlashX 与 Flash 同批发布但套餐只收 Flash，
  想当然配积分系数/别名就会把套餐计费口径写错。

## 未做（下刀）

- coding 套餐若后续开放 FlashX：补 `points_*` + 别名按官方口径另刀
  （用户侧可先用覆盖文件 `glm_catalog_path` 数据驱动先行）。
- 其余付费模型绝对价官方页仍未给出，维持内置表估算回退。
