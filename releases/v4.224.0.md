# v4.224.0 — 架构修正：规范知识出内核，GB/T 9704 排版细则改技能按需加载（2026-09-11）

> 用户拍板的架构红线，本刀即刻执行：「公文写作规范这类领域知识不应植入代码，
> 应以 skill 技能按需加载，防止 gaea 臃肿」。

## 原则

**内核只留通用机制；领域规范、行业模板、检测规则一律做成技能资产，按需加载。**
后续所有需求先问「能否做成技能」，代码只收通用机制。

## 动作

1. **撤销 v4.223 的 Go 侧植入式 checker**：删 internal/office/standard/format.go
   与 format_test.go；registry.go 回退 LintDocument（WithLayout 无消费者）；
   gaea_lint.go 回退 layout 接线；docxedit.ReadDocumentXML 一并撤（无消费者不养）。
2. **改为技能包 `.gaea/skills/gongwen-9704/`**（与 docx/pptx 技能同域同形态）：
   - `SKILL.md` 承载全部规范知识：红头要素表七项 + 排版细则表六项 + twips/字号
     换算备查 + 判定纪律（容差内即符合 / 不适用≠缺失 / 实测值进建议 / 宁缺勿误）；
   - `scripts/docx_layout.py` 承载排版事实提取（stdlib zipfile+ElementTree，
     零第三方依赖）——解析也是技能资产，不进内核；
   - 已用最小 docx 实测：边距换算与逐段事实（字体/字号/行距/缩进）提取正确。
3. **既有 Go checker（红头要素/造价表式）同属「规范在码」**：列观察池候选，
   迁移涉及既有 GaeaDocumentLint 行为变更，待拍板，不随本刀擅动。

## 门禁

Go 全量 0 FAIL / 绑定 624 零变更 / 版本三处 4.224.0 / 技能脚本最小 docx 实测通过。

## 打包记录

- 产物：`build\bin\gaea.exe` **48,837,120 B** SHA256 `c021cfe1c3f4abe158779e2e6e1f8cd54b4765d19c1c04c76d8b173f4b6cd1cb`
  （冒烟 /api/health 200 + Desktop 副本同哈希；exe 不入库，只存哈希）。
- 未打 tag（随 v4.200+ 现状）。
