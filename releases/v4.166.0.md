# v4.166.0 — 瘦身 P1 快赢：轮子四刀全收 + 审计刀B/C + locale 死键 + 依赖验活（2026-09-09）

> 瘦身执行层首版（规划=docs/gaea-slim-masterplan-2026-09.md，P1=低风险机械刀热身）。
> 主代理收口发布（轮子刀为机械替换，测试先行锁行为，零功能损失）。

## 1. W1：字节+双门收口

- 新中立层 `frontend/src/gaea/lib/bytes.ts`：`b64ToBytes(b64)` —— 全仓 base64→字节
  唯一解码规范（裸 base64 入参，解码失败抛 atob 原生错误由调用方处理；未来引擎普及
  可整体换 `Uint8Array.fromBase64` 调用点零改动）。
- 新中立层 `frontend/src/gaea/lib/saveFile.ts`：`downloadBlob` / `dataUrlToBlob` /
  `saveExportBlob` —— 从 schedule/exportArtifact 迁入（schedule 侧 re-export 保
  既有 import 零改动），供任意域（图生图下载/小说设定导出/办公交付…）复用，防
  schedule 域依赖倒挂；壳判定唯一规范 = pickFile.inShellEnv。
- 全仓 b64 手搓解码 ~20 处收口：DocxPreview / docxOutline / docxText / pptxTextDiff /
  pickFile / schedule api / exportArtifact svgToPdf / TTSPlayer / ChatPage / SchedulePage /
  useVoiceChat / useImageGenHistory / ImageGenPage …
- **inShell 判定合一**：`exportArtifact.inShell()` 改薄委托 `pickFile.inShellEnv()`
  （全仓唯一规范；v4.162 前是各域手写同款判定）。

## 2. W2：slug×5 收敛

- 新 `internal/gaea/strutil/title_slug.go`：`TitleSlug(title)` —— 规范语义=小写 /
  保留 Unicode 字母数字（中文算字母）/ 其余折叠成单个 '-' / 修剪首尾 '-' /
  空结果回退 "entry" / 60 rune 截断。
- 并入五处：`cost.SlugName`（保留 "cost" 兜底语义，hasTitleSlugRune 区分真 "entry"）、
  `knowledgeimport.slugName`、`app.slugFromTitle`、`modelengine.customSlug`（保留
  ASCII 白名单前置与 40 截断）、`controller_memory.slugifyName`（SaveSession 界面文案）。
- **legacy golden matrix 测试**：`title_slug_test.go` 记录五个旧实现在同一语料上的
  输出并钉死 canonical 跟从多数（如 "Café Résumé 42" → café-résumé-42 跟 knowledge/app，
  不跟 cost/modelengine 的 ASCII 损伤版；"!!!" → entry 不跟 cost 的 "cost"）。

## 3. W3：novel diff 收口

- DiffReview 手搓弱贪心行 diff（前向 3 行窗口）→ `lib/diff.ts` 统一 LCS。
- 测试锁死贪心会漂移而 LCS 不会的构造：公共行前插 4 行（贪心把公共行标删+增）、
  删除首行（贪心把保留行标删+增）；空串=0 行边界不产出幽灵删除/新增行；全同零增删。

## 4. 审计刀B：下载类 ×4

| 点 | 修复 |
|---|---|
| ImageGen 历史下载（useImageGenHistory） | 壳内 dataURL→Blob 走 saveExportBlob 系统另存为；浏览器保留原 a.download |
| ImageGen 页下载（ImageGenPage） | 同上（同款收口，组件域双出口齐收） |
| NovelSetting 导入导出 | 壳内导入 pickFileAsFile 系统对话框还原 File 喂原 FileReader 管线；导出 saveExportBlob；浏览器回退原 input / a.download |
| 办公 md 分支（App.tsx ExportMenu） | md 分支并入 exportConversation 统一交付管线（ExportDeliverable，壳内落盘）；downloadMarkdown 零调用保留待下轮清 |

## 5. 审计刀C：上传类 ×3

| 点 | 修复 |
|---|---|
| ControlPanel（图生图参考图） | 壳内 pickFileAsFile，扩展名后置校验 fail-closed message 提示；浏览器回退原生 input |
| VisionTrial（画室识图） | 壳内 pickImageAsDataUrl 直接喂 handleImageDataUrl（复用 SavePastedImage 落盘漏斗，零新绑定） |
| SkillModal（SKILL.md 导入） | 壳内 pickFileAsFile 还原 File 维持「仅展示文件名」半桩同款语义；浏览器回退动态 input |

## 6. locale 死键清零

- `scripts/slim-locale-deadkeys.mjs`：默认 dry-run / `--write` 落盘；逐键扫描静态使用
  + 反引号动态前缀保护（memory.promoteTo / rewind.）；en/zh/zh-TW 同步删。
- **实测：1445 键 / 静态 1438 / 动态保护 7 / dead keys 0**（三语源各删 223 行共 669 行）。

## 7. 依赖验活（产物面）

- 26 个运行时直依赖逐个 import 计数：存疑名单 **gsap(2)/docx-preview(1)/unist-util-visit(2)
  全部洗清**（有真实使用）；
- **codemirror 顶包死重移除**：源码零引用、lockfile 零依赖者（此前是
  @codemirror/* 子包的隐式提供者）；CodeEditor 直接使用的
  @codemirror/state|view|commands|language 四子包**显式进 dependencies**（根除隐式依赖）；
- 坑（复现 v4.164）：npm 11.6.2 uninstall 再次写坏 lockfile（缺 wasi-threads/tslib
  条目）——先例=git restore + 手动删条目 + `npm ci --dry-run` 验证，勿信 npm 11 重写。

## 8. 门禁

- vitest：2684→**2712**（311 文件，+28；全量复跑全绿，依赖变更后二跑仍绿）；
- Go 115 包全量 0 FAIL（首跑 1 flaky 复跑清，在册同族）· `tsc -b` 0 · `eslint` 0 ·
  drift **PASS@602**（零绑定变更）；
- vite build 过（27.7s）；build.bat 构建+自动冒烟：见产物。

## 9. 欠账（如实列示）

- 审计刀D（真机取证：printSvg iframe print/拖拽/粘贴 + GaeaPickFiles Filters 可选参数）；
- P0 基线七面四表+功能等价快照+IA 现状走查入档（随后续版附带）；
- nanoid high 升级刀（v4.164 遗留，lock 已钉住版本）；
- `lib/export.downloadMarkdown` 死代码下轮清理。

## 10. 产物

- `releases/gaea-v4.166.0.exe`（46.2 MB，gitignore 不入库）
- SHA256：`c315391c5b04364fcc357f5ca67db0180855f847a247007c785d06b6bff1f525`
  （见 `SHA256SUMS-v4.166.0.txt`）
- 冒烟：HTTP 桥接 127.0.0.1:18999 `/api/health` → `{"status":"ok"}` 200，进程已回收；
  桌面副本同步。