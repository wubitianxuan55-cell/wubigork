# v4.407.0 — 角色库生成取消（剧照/设定卡可中止）

> 2026-09-24 发布。图像等待体验线程观察池头名。纯接线，绑定面 **717 零
> 变更**（复用 CancelImageGeneration）。规格：`进度计划/gaea-charlib-cancel-20260924.md`。

## 病根

v4.406 给剧照/设定卡接了进度但没有出口——撞上 20 分钟冷载时只能干看着等
超时。

## 落地

1. **Go**：剧照/设定卡两链改走 `beginImageGen/endImageGen` 对——全局可取消
   机制（ctx 退出 + ComfyUI /interrupt 双达）+ ID 守卫完成清理；顺带修掉
   v4.406 无条件 clear 会擦掉并发绘梦任务进度的潜在缺陷。`resetComfyCancel`
   镜像队列路径（取消后可再次提交）。
2. **并发语义**：begin 覆盖 imageGenCancel——编辑器后提交时取消目标=编辑器
   这次（最近提交=用户直觉）；绘梦队列自身 ctx 由其 deferred end 持有不丢失。
3. **前端**：进度行内联「取消」钮（生成中可见）；`context canceled` 拒绝
   语义化为「已取消生成」，不再以错误样式惊吓。

## 测试

- Go `TestCharacterGenerateSheetCancel`：假 ComfyUI /history 挂起模拟冷载，
  取消 → context canceled 退出 + /interrupt 恰一次 + 状态复位、可再提交。
- 前端：取消钮点击调用全局取消；取消拒绝显示「已取消生成」。
- 全量 ci 门禁绿 + tsc 0 + eslint 0。

## 门禁

全量 `scripts/ci.ps1` 绿 EXIT=0；版本漂移闸 OK@4.407.0；绑定面 717。

## 升级说明

- 无操作变化。角色档案生成剧照/设定卡期间，进度行尾部出现「取消」按钮——
  点击中断 ComfyUI 当前任务，界面提示「已取消生成」。

## 产物

`gaea-v4.407.0.exe`（冒烟 200 过；SHA256 见 SUMS）。保留策略 5 版：留
v4.404.1~v4.407，删 v4.405.0.exe。

## 观察池

sin 面板取消钮；评分 LLM 流式；iGPU 压力预检。
