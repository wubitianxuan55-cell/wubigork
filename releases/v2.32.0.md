# gaea v2.32.0「质量收敛 · 辅助合集·名实相符」

> 阶段 6 第九刀（T6-9），2026-08-14。4 子代理并行实施。
> 规划：docs/superpowers/plans/2026-08-14-gaea长期规划-阶段6-质量收敛.md

## 发布内容

### T6-9.1 微信生命周期与失效自愈（3 测试）
- Stop 幂等：stopMu 保护 stopCh（关闭即置 nil），二次 Stop 不 panic 无副作用
- Start 支持重启：running.Swap 幂等防重复拉起；stopCh 重建 + sessionExpired 重置，Stop→Start 轮询真正恢复
- 会话过期（errcode=-14）触发 OnSessionExpired 回调后**退出轮询**（删除 5 分钟空转），app 层注入回调 emit
  notice 事件「微信助手 X 会话过期，请重新扫码绑定」；wxSessionExpired 经 WhisperWeixinStatus 透出
- getUpdatesFn/notifyStartFn/notifyStopFn 测试注入点（生产默认实现不变）

### T6-9.2 凭据与表治理（3 测试）
- wxToken DPAPI 加密：Manager.save() 落盘前 secure.EncryptString（dpapi: 前缀）；Load 带前缀解密、
  旧明文一次性重写迁移、解密失败返回含助手 ID 的明确错误（不静默清空）；内存保持明文（List 回显不变）
- weixin_* 4 张死表（无任何读写引用，已 grep 核实）：SchemaV13 四条 DROP TABLE IF EXISTS 追加迁移链，
  ClearStructuredData 清表列表移除

### T6-9.3 OCR 兜底名实相符（4 测试）
- 超时杀进程树：startOvisServer 改 proc.StartTracked（Job Object），60s 未就绪 proc.KillTracked 杀整棵树
  + 同步 Wait 回收，失败路径零孤儿 llama-server；ovisStartWait/ovisHealthy/ovisBuildCmd 注入点
- 单图降级：OCRImageText 在 OvisOCR2 不可用时降级 tesseract（同 PDF 流水线参数），双不可用才报安装提示
- 文案名实相符：single_prompt.go:43 删除「Windows 原生 OCR」（实现不存在）；RapidOCR 核实真实存在保留

### T6-9.4 配置原子写（4 测试）
- Save 改 saveConfigFile：同目录临时文件 → 写入 → fsync → Chmod → Rename 覆盖；任一步失败清理临时文件
  保留原文件（中断不截断）；renameFile 注入点
- Load 损坏恢复：解析失败备份 .gaea_config.json.corrupt-<ts>（不丢数据）+ slog.Warn，默认值继续启动

### T6-9.5 CosyVoice 路径/端口可配置 + 退避重试（4 测试）
- 新配置键 cosyvoice_dir/cosyvoice_port（默认 C:\AI\cosyvoice / 8010 与历史一致，端口校验 1-65535）
- tts_service.go 写死常量全删：ttsCosyVoiceCmdFor/ttsURL 由配置推导；ttsReady/startTTSService 方法化
- 启动失败指数退避重试（1s/2s/4s 最多 3 次）再 emit 错误；ttsEnsuring 防重入不变

### T6-9.6 token 改 header（Go 3 + 前端 6 测试）
- 服务端 tokenOK 删除 ?token= 查询参数兜底，仅 Bearer + X-Gaea-Token（常量时间比较不变）
- 前端弃用 EventSource 改 fetch 流式 SSE（Authorization: Bearer）；parseSSEFrame/parseSSEStream 纯函数
  （跨 chunk 拼接/CRLF/keep-alive 注释行/流尾 flush）；断线 5s 重连/activeSSE 语义/bridgeProbed 全保留
- httpToken.ts 删除 URL query 读取；bridge.ts 401 文案更新

## 验证
- Go：改动包（channels/assistant/whisper-db/config/httpbridge/office-docmd/app TTS 域）全绿 + vet 干净 +
  TestBindingsCompleteness PASS（462 方法，无绑定变更）；全量 go test ./...：internal/app 18.7s ok、
  docmd ok（GBK 编码类失败仅出现在前台串行跑——已用 c426d3f 基线 worktree 复现同款失败，环境编码
  问题非回归）；hook/skill/tts 3 包被 AV 实时防护锁 test.exe（fork/exec Access denied，单独重跑全绿，
  历史日志同款先例，环境抖动）
- 前端：tsc 0 errors、vitest **354/354**（79 文件，+6）、eslint 0 errors（72 存量 warnings）、
  vite build 15.17s ok
- 冒烟：scripts/smoke.ps1（/api/health 200）

## 产物
- gaea-v2.32.0.exe（32.8MB，SHA256=CC8EEF7AF693B934BBDA629853F0BA3BFD9B5566B08DBD881FBD4B0FD01761B0）
