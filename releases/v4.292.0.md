# v4.292.0 — tail×反推串联：末 N 章导入完成后一键反推续写大纲（2026-09-14）

## 来源

拆书导入线按反馈项「tail×AI 反推串联」——tail 模式的产品意图（MuMu 蒸馏 §0.4：**用末尾几章反推「这本书接下来该怎么写」**）此前只完成了一半：末 N 章能导入，但反推还得用户自己去创作间点。本刀把两步缝成一条链。

## 落地

- **`HomePage`**：按提取范围（tail）导入成功后，弹「立即反推这部分大纲？」确认——确认后派发 `novel:goto-tab`（跳创作间）+ `novel:auto-reconstruct`（触发反推）两个前端事件；稍后再说则一切照旧。
- **`CreatePage`**：监听 `novel:auto-reconstruct` 事件，触发既有的任务化反推流（Start 入队 → 轮询 → 确认弹窗 → 应用）——反推全程任务化后台执行（v4.291），页面可自由导航。
- 事件挂载点安全：NovelPage 的 tab 组件常驻渲染，监听器在事件派发时必然在位。

## 测试

- `HomePage.test` +1：tail 选末 10 章 → 导入成功 → 确认弹窗出现 → 点「去创作间反推」→ 两个事件按序派发。
- `CreatePage.test` +1：派发 `novel:auto-reconstruct` → 任务化反推自动开跑（轮询到 succeeded → 篇幅路由确认弹窗 → Apply 收骨架条目）。

## 门禁与产物

- `scripts/ci.ps1` 全绿（exit 0）；drift OK@**660**（零绑定面变化）；版本三处 **4.292.0**。
- `releases/gaea-v4.292.0.exe` — 50,041,344 B，SHA256=`7AE305A5C672BB6852F680CDD7CDEE69909470887428E5E892122AD639A244D2`；`releases/SHA256SUMS-v4.292.0.txt`；桌面副本同哈希；冒烟 `/api/health` 200。

## 未做（下刀）

- 反推任务取消绑定（任务中心已有通用取消）、反向角色补建、骨架 AI 丰富化、style.md 编辑入口——均按反馈。
- 观察池：ProgrammingPage / ContextView / builtin 负载 flaky（多次隔离绿）；SERP 首搜抖动；线上源站漂移。
