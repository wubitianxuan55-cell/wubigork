# wubigrok v5.20.1

patch 版本 — 死代码清理 + bug修复

## Bug 修复（自 v5.20.0）

| 提交 | 描述 |
|------|------|
| `b1f9fd1` | 世界观编辑不再注入角色和大纲上下文 |
| `e7e3dfe` | 修复世界观编辑不注入当前设定 |
| `dcf88e2` | 修复创作面板分支生成不显示 |
| `b36e0ea` | 修复章节生成时文本框不显示内容 |
| `d11f406` | 修复章节生成续写时文本重复 |
| `69c9d95` | 修复事件监听器叠加导致文本重复 |
| `888761e` | 修复 AI 控制台章节响应只显示 200 字符 |

## 死代码清理

- 删除 `internal/ai/router.go` 整个文件（152行，Router/NewRouter/ChatWithRoute/ChatStreamWithRoute/TaskType/ModelPreference 全部零引用）
- 删除 `types.StoryMemory` / `MemoryResult` 类型 + `ProjectContext.Memories` 字段
- 删除 `stats.BarChart()` + `(*Summary).Report()`
- 删除 `util.FastRetryJSON` + `init()` 无意义函数
- 删除 `stats/dashboard.go` 未使用 `today` 变量
- 删除 `app/shelf.go` 不可达 `return nil`
- 修复 `tts/sapi.go` `escapePath` 空函数（PowerShell 单引号转义 `' → ''`）
- 清理 `app/plot_branch_handler.go` TODO 注释

## 验证

- `go build ./...` ✅ 零错误
- `go vet ./...` ✅ 零警告
- `go test ./...` ✅ 16/16 包通过

## 构建

- Tag: `v5.20.1`
- 产物: `C:\AI\wubigrok\build\bin\wubigork.exe`（约 14.7 MB）
