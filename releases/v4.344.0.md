# v4.344.0 · t7 最终形态：编辑器持久标注高亮（overlay 常驻 + 编辑退场）

> 2026-09-19 发布。t7 观察池「编辑器 overlay 持久高亮」正式落地（v4.343
> 轻量版的完全体）；纯前端（EditorPanel/ChapterAnalysisPanel/CreatePage），
> 零新绑定 705、零新依赖。

## 论点

v4.343 的定位单条高亮验证了镜像背景层在真机可用（字体/滚动条/换行对齐零
偏移），把最后的顾虑清掉了。本刀升级为**持久全量**：打开章节即常驻显示
全部标注高亮——作者编辑时始终看得见分析锚点，重写工作流闭环完成。

## 落地

1. **分段工具收敛**：新建 `create/annotationMarks.ts`（buildAnnSegments +
   runeToCodeUnit），分析面板高亮视图与编辑器镜像共用同一实现——rune↔
   code-unit 口径全仓唯一出处（ChapterAnalysisPanel 本地副本删除，既有
   11 用例零改动全绿=重构等价证明）。
2. **EditorPanel 持久化**：单条 `hl` 替换为 `annotations` prop 驱动的
   `marks`（useMemo 派生）；镜像渲染全部段（交叠钳制沿用 Q5 口径）；
   `dirty` 纪律——正文编辑即整体退场（偏移漂移结构性规避），切章/标注
   清单重载恢复；滚动条占宽 gutter 在镜像显形时 rAF 实测同步。
3. **CreatePage**：随激活章加载 `NovelChapterAnnotations` 传给编辑器
   （alive 守卫防竞态）；章节分析面板复用同一加载链。
4. locate 句柄契约不变（光标+滚动定位），与常驻 mark 解耦——v4.339
   语义零变化。

## 测试

- EditorPanel.test 重写 +2（共 11/11）：annotations 供给 → 镜像常驻双
  mark（rune 换算断言：rune[1,3)→「一二」、rune[5,6)→「五」）+ 编辑后
  整体退场 + locate 光标契约不受 dirty 影响；越界标注不产生 mark。
- ChapterAnalysisPanel 11/11（重构等价）；CreatePage 15/15（补
  NovelChapterAnnotations 桩——bridge appStubs 此前缺该方法的漏桩）。
- tsc -b 0；eslint 0。

## 坑

- **hooks 顺序陷阱**：effect 引用了 200 行后才声明的
  `activeChapterNum`（TDZ）——声明先于引用，挪位修复；tsc 不抓 TDZ，
  靠读代码顺序发现。
- **测试桩漏项会放大成组件树崩**：bridge appStubs 缺一个方法 → effect
  unhandled rejection → 整树卸载 → 后续断言全部"找不到元素"。报错信息
  与根因相距甚远，先看 unhandled errors 段。

## 门禁

tsc -b 零错 / eslint 零告警 / 三域 38/38 / 全量 ci.ps1 绿 / drift OK@705
（零新绑定）/ 版本三处 4.344.0；产物=exe 50843648B
SHA256=95ecb91d66a9d1828a26fb8d7c660d1a4f0bba3db7a7c8292c47c654fd74d5fc
（releases+SUMS；桌面副本同哈希；冒烟 200 过）。

## 未做（下刀）

- 真机走查清池班（v4.319~v4.344 挂池；持久 overlay 的真机目检随下一班）。
- 技能 ≥5 次核数 ≈09-30；t2 余项（反向角色补建/骨架 AI 丰富化按反馈）。
