## v4.316.0 · 阶段七第三刀：路由学习（任务级账目+成本归因+建议制改绑，7.1-2）（2026-09-15）
> 用户指令「继续并行使用子代理，优化迭代 gaea」。阶段七规划 §1.2「路由学习」实施刀：让 gaea 知道每个功能域的钱花在哪、值不值、该不该换。契约先行（规格书 `进度计划/gaea-route-learning-7-1-2-20260915.md`）→ A/B/C 三线并行实现 → 主代理收口。B 线原实现子代理 40 分钟零落盘、两次检查点未应——中断接管由主代理代写（子代理卡死判据先例：长时间零落盘+检查点静默）。
>
> **判据①任务级账目覆盖全部功能绑定键**：`ChatRequest/ChatSimpleOptions` 加 `Feature` 带外字段（`json:"-"` 贴 EngineID 先例，请求体形状零变化）→ `internal/ai` recordUsage/parseStreamEvents/prepareStreamRequest 全链透传（故障转移副本天然随行，两处已核验）→ `modelengine.statsKey` 扩三维 `feature|engine|model`（normalizeFeatureKey：whisper→chat 归一防脏桶）→ statsFile v2→v3（v2 旧文件全量按 feature="" 兼容读入重键、首次落盘升 v3；只读会话不重写）→ 24 个 handler 文件约 40 调用点一行式透传，chat/novel/office/gaea/characterlib/routine/sin 七键+whisper 别名全覆盖（grep 复核零遗漏）；copilot.go 三入口（CmdKEdit/OfficeEditText/XlsxEditOps）获批扩迹带 feature 参数；provider bridge 工厂 Provider 打标签（boot 主 agent→"gaea"、app 侧显式引擎→"office"，SetFeature 签名未动零测试波及）。残留如实报：platform_handler 风格分析经 internal/style 构造（足迹外，落未标记桶，novel 键另有 9 处覆盖不阻塞）；gaea_diagram 两处不走路由（全局引擎，未标记属如实呈现）。
>
> **判据②成本归因视图**：模型中心新「成本归因」tab（PieChartOutlined，居「空间策略」后）——AttributionSection：KPI 行×4（总成本 CNY/总 token/调用数/成功率，KpiTile 复用）+按功能绑定键分组表（一行=feature×engine×model 三维桶：引擎/模型/调用/Token 入+出/成本/均时长/成功率；已知功能键空态常显、账本独有键成本降序、「未标记」桶最后；组内行成本降序）+EmptyState 空态+StrategySection 风格错误条；useAttributionState hook（Promise.all 加载/apply 后仅重拉 suggestions，绑定面靠 feature-model-changed 事件自动刷新）；api/engines.ts +4 函数（getRouteLedger/getRouteSuggestions/applyRouteSuggestion/ignoreRouteSuggestion，贴 getModelCallStats 先例走 App() 兼容通道）。testid：mc-attribution/mc-attribution-kpi/mc-attribution-row-${feature}（空 feature→unmarked 后缀）/mc-suggest-${feature}(-apply/-ignore)，zh 单语硬编码（模型中心惯例）。
>
> **判据③建议制改绑（宁少勿扰）**：新纯包 `internal/routesuggest`（零 IO 表驱动，先例 memoryeval/novelgate）——score=成功率×成本因子（功能域候选集内归一：越便宜越接近 1，本地 0 成本=1；全池 0 成本时成本维度失效纯成功率比较）；`ScoreGapThreshold=0.25`（分差低于阈值不出声）+`MinSamples=20`（候选与当前双门槛，无样本不给建议——证据不足不出声）+单功能域至多 1 条（取分差最大）+ID 确定性 `feature|fromEng>fromModel>toEng>toModel`（重算幂等，忽略状态不漂移；解析残渣防御：|/> 混入任一段判非法——第二个 | 藏进字段段的解析 bug 由测试抓出后修）。App 三绑定：GaeaRouteSuggestions（现算+合并忽略/已采纳状态）、GaeaRouteSuggestionApply（**显式用户动作**，内部走 `SetFeatureModel` 既有链路保 gaea/office 域即时重建；状态落盘失败只告警不回滚）、GaeaRouteSuggestionIgnore（被忽略 ID 不再出现）；状态原子写 `<DataRoot>/route_suggestions.json`（version 1 records map，temp+rename）。红线全保：routeModel 决策链零改动、不自动改绑、策略路由（离线/办公本地优先）优先级高于建议层。
>
> **判据④本地引擎同池**：候选=全部启用引擎的 llm 模型（ModelInfo.Kind 缺省回退 ClassifyModelKind），本地（herdsman/ollama/modelhub/cosyvoice）与云端同池参评、0 成本即满成本因子；mock 契约样本内置一条 herdsman 建议卡作走查锚点。真机采纳样本验收挂闲置窗口（mock 契约+表驱动已钉死同池语义）。
>
> **绑定面**：684→688（ModelB +4：GaeaRouteLedger/GaeaRouteSuggestions/GaeaRouteSuggestionApply/GaeaRouteSuggestionIgnore；gen_bindings 显式覆盖表→model 门面，GaeaUsageOverview 同族先例）；work 锁 506→510（shared，两空间模型中心共用）；bridge/model.ts 四签名+局部重述类型 RouteLedgerViewB 族（herdsman 局部重述先例防 api↔bridge 依赖环）+mock/model.ts 四桩（账本/建议走查样本数据）+新契约测试 mock-contract-route.test.ts（4 名存在性+形状关键键与 api 层/bridge 局部类型三方对齐）。
>
> **验收**：routesuggest 11 例（阈值边界 0.2499 不出/0.25 出、无样本静默、样本门槛 MinSamples-1 不出恰好达标出、单域取最大分差、ignored 静默、本地引擎胜出、全本地成本维度失效、多域独立、重算幂等、ID 往返+残渣防御）；modelengine stats 3 例（三维桶表驱动/whisper 归一/v2 兼容读+升 v3+重启恢复）；ai 3 例（非流式透传/流式透传+归一/prepareStreamRequest 纯函数）；app ledger 2 例（零样本空视图/total 顶层精确口径）+suggestions 3 例（状态往返/容错/ID 防御）；前端 AttributionSection 5 例+mock 契约 4 例+锁数 510；tsc -b 零错（AppBindings↔bindingNames 双向漂移防线过）、eslint 零告警。
>
> **门禁**：ci.ps1 全绿 exit 0（一次过）、drift OK@688、版本四处 4.316.0（app_info/wails.json/versioninfo.rc/README）。
>
> **产物**：exe 50515456B SHA256=82c0e4537d28dfb53314770054672ea094e636aac2645509227d76de88113213（releases/gaea-v4.316.0.exe + SHA256SUMS-v4.316.0.txt；桌面副本同哈希；冒烟 /api/health 200 过）。
>
> **文档**：规格书随刀入库（进度计划/gaea-route-learning-7-1-2-20260915.md）+releases/CHANGELOG/README+AGENTS 速览（迁 1 插 1，三十一迁）。
>
> **观察池**：账本三维桶为窗口聚合（与统计同窗口），「按任务实例」切片留待任务收件箱（7.3）联动；bridge 启发式依赖「app 侧显式引擎 provider=office 域」现状（6 处），后续新增显式引擎 NewLLM 需重新审视；route_suggestions.json 无 UI 清单（忽略后悔=改绑回原模型即可，ID 随绑定漂移）；真机走查（发一条聊天→归因 tab 见 chat 桶行+建议卡）等闲置窗口。
>
> **未做（下刀）**：7.2-2 journal 历史蒸馏或 7.3-1 任务收件箱；闲庭在册清欠沿既有列车。
