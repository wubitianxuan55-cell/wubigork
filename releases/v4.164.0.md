# v4.164.0 — 收敛计划 W1 卫生刀：供应链可复现 + 测试抗抖 + 过期导航 id 修复 + 壳内残留审计（2026-09-08）

> 收敛计划（docs/gaea-convergence-plan-2026-09.md）立项后首轮交付，三线并发子代理
> （A=供应链+抗抖 / B=潜伏 bug / C=只读审计）+ 主代理收口，足迹互斥。

## 1. 供应链可复现（刀 1.1）

- `frontend/package-lock.json` 入库（.gitignore 删除未锚定忽略行），**lockfileVersion 3 / 701 包**。
- CI frontend job `npm install`→`npm ci`：CI 与本地装出同一棵依赖树，杜绝「依赖发坏版本全红」。
- **坑（沉淀）**：npm 11.6.2 的 `--package-lock-only` 产物缺 `@emnapi/wasi-threads@1.2.3`、
  `tslib@2.8.1` 条目（`npm ci --dry-run` 才暴露）；改用 `npx --yes npm@10 install
  --package-lock-only` 生成后验证通过。后续升 lock 请固定 npm 10 或先 dry-run 验证。
- 验证：`npm ci --dry-run` exit 0；`git check-ignore` 确认不再被忽略。

## 2. vitest 抗抖（刀 1.2）

- `frontend/vite.config.ts` test 块 `testTimeout: 15000`（原缺省 5s）。
- CI frontend job `npm run test` 升级为**一次 flaky retry**（对齐 backend job 形态：首跑失败
  echo 后重跑，二次仍失败正常红）。
- **动因（同日实测）**：Go 全量与 vitest 全量同机满并发时，vitest 32 例 5s 超时假红
  （FilePreviewModal/MemoryPanel/TrajectoryView 等），隔离复跑 31/31 全绿——门禁可信度
  裂缝必须先补。本版全量首跑仍有 1 例负载 flaky（ProgrammingPage「重新检查」，worker
  竞争定时器竞态，非超时类），隔离复跑绿、整轮复跑 2678/2678 全绿，CI retry 语义覆盖。

## 3. DeliverablesPanel「回办公面板重新规划」失效修复（刀 1.3，v4.121 潜伏 bug 销项）

- **根因**：`DeliverablesPanel.tsx:355` 复核失败回 Plan 按钮派发
  `NAVIGATE { page: "office" }`——沿用历史模块名，而 manifest 办公板块稳定 id 已是
  `gaea`（manifests.ts:98），MainLayout 白名单（:481-483）fail-closed 静默丢弃 → 按钮
  完全无反应。消费方契约正确，错在发送方。
- **修复**：payload 改 `{ page: "gaea" }` + 回溯注释（NAVIGATE.page=壳层板块 id）。
- **回归锁**：DeliverablesPanel.test.tsx 新用例（先失败后通过——修复前断言差
  `{page:'office'} vs {page:'gaea'}`），同时锁 manifest 契约（含 gaea 不含 office）。

## 4. WebView2 壳内残留面审计（刀 1.4，只读落档）

- 新文档 `docs/webview2-shell-audit-2026-09.md`：P0×2（BaselinesPanel 签证台账 CSV 导出
  壳内静默失败 / CharacterLibEditor 添加参考图不弹框且无替代）、P1×7（ImageGen 下载×2、
  NovelSetting 导入导出、办公 md 导出、ControlPanel img2img、VisionTrial、SkillModal、
  printSvg 待真机）、P2 若干已收敛；修复刀序 A（P0×2）→B（下载类）→C（上传类）→D（真机取证）。
- print 复核：全库无 `window.print` 直呼，唯一打印路径=printSvg iframe `contentWindow.print()`，
  WebView2 下待真机；FileSystem API（showOpenFilePicker 等）全库零命中，无此类残留。

## 5. 版本账本顺带修正

- README.md 版本表 v4.163.0 重复行去重（两行留一）。
- releases/README.md 最近区块补记 v4.159~v4.163 五版（此前漏更）+ 本版入顶。

## 6. 门禁

- Go 全量 `go test ./...`：116 包 0 FAIL（零 Go 变更）。
- vitest：2677→**2678**（304 文件；首跑 2677/2678 含 1 负载 flaky，复跑全绿）。
- `tsc -b` 0 · `eslint` 0 · 绑定漂移 **PASS@602**（零绑定变更；v4.163 文案 603 为口径
  出入，以 gen_bindings 实数为准）。
- build.bat 构建+自动冒烟：见下。

## 7. 欠账（如实列示）

- nanoid high 审计项（GHSA-2v37-7h3g-55p8，传递依赖；lock 已钉住版本，升级刀另立）。
- 审计刀 A/B/C（P0/P1 修复）与刀 D（printSvg/拖拽/粘贴真机取证 + GaeaPickFiles 无 Filters）。
- LICENSE 与性质路线（A 自用 / B 产品）待用户拍板。

## 8. 产物

- `releases/gaea-v4.164.0.exe`（46.2 MB，gitignore 不入库）
- SHA256：`6752d4096deb9fea6fb14814f40f9dd0644ea059e9774b884fe3bb77a0f98ab6`
  （见 `SHA256SUMS-v4.164.0.txt`）
- build.bat 内置冒烟：`.tmp\smoke-gaea.exe` HTTP 桥接 127.0.0.1:18999 `/api/health` →
  `{"status":"ok"}` 200，进程已回收；桌面副本同步。
