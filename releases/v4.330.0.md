# v4.330.0 · 刀7续收官：POV 视图（场景圣经消费面）

> 2026-09-17 发布。规格书：`进度计划/gaea-pov-view-20260917.md`。
> 前置核对：场景生成按钮（v4 场景工程线）/全文脑图/文风指纹面板均已落——
> 刀7续仅剩 POV 视图。本刀合入 + revolution 勾选对账后，规划文档只剩刀8。

## 论点

POV 视角掩码（刀2 场景圣经编译器的核心差异化：POVView=POV 已知 /
HiddenFacts=不知情且不得泄露）此前只在生成链内部消费——作者看不到
「这个场景通过谁的眼睛在看、哪些信息被刻意瞒住了」，视角纪律不可审视。

## 落地

- **+1 绑定** `NovelSceneBibleView(chapterNum, sceneID)`（706→707，NovelB）：
  sceneID 非空=该场景编译（SceneManager.Read→CompileSceneBible，不存在报错）；
  空=整章合成（BuildSceneBibleFromChapter，v3 与纯 blob 章也可用）。
  视图 camelCase typed、切片恒非 nil（前端零判空）；编译静默降级契约——
  空区段如实空展示，不报错不编造。
- **前端 `SceneBibleDrawer`**：POV 头（「通过谁的眼睛」/未设定=全知）；
  核心区=已知（绿底）与不知情·不得泄露（红字）对照；出场角色卡（含 t5
  状态机回灌 currentState/careerMain/Sub）；未回收伏笔约束；时间锚点/主线；
  确定性编译脚注。
- **ChapterEditor**：场景操作行新增「视角」按钮（EyeOutlined）挂抽屉。

## revolution 勾选对账

- GenerationGate 完整闭环 → [x]（v4.326.0 实落，文档补记）。
- 刀7续 → [x]（场景生成按钮/脑图/指纹核对已落 + 本刀 POV 视图）。
- 文档剩余未勾项仅刀8（跨模块验收+回退演练+race 门禁，CI 侧另配）。

## 测试与门禁

- Go +3（整章视图形状恒非 nil / 未知场景报错 / 无项目报错；掩码语义由
  novelcontext 包测试钉死，此处只钉 wire 投影）。
- 前端 +3（对照区渲染 / 空区段全知视角+无隐藏约束 / 错误态重试）；
  ChapterEditor 既有 7/7 零破坏。
- 门禁：go build/vet 全过、app+novelcontext 全绿、tsc -b 零错、eslint
  零告警、vitest 全量 3169/3169 一次全绿、drift OK@707、计数锁 530、
  版本三处 4.330.0。

## 观察池

Render 全文预览（prompt 注入形态）；POV 切换沙盒；多场景圣经并排；
隐藏事实来源溯源（哪章埋的）。
