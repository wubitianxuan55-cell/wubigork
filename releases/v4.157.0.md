# v4.157.0 · Office 编辑链一致性小刀：docx 证据链补齐 + pptx 面板字符级对比（2026-09-08）

> 两个小项收口 v4.156 刚闭环的三件套编辑线：①docx_apply 补证据链（pptx/xlsx 都有、docx 没有的一致性欠账，pptx 设计 §6 拍板项 4 推荐项）；②pptx 编辑面板对比区升级 ChangesDiff（刀3 预留的层3 字符级高亮在面板侧接通）。零新绑定（598 不变）。

## docx_apply 证据链（对齐 pptx_apply/xlsx_apply 口径）

- `GaeaDocxApplyEdit`：落盘前快照（`.gaea/work/rollback/docx-<ts>.before`，尽力而为不阻断）+ Journal 记录（Tool=`docx_apply`，摘要 truncateStr 120 截断，BaselinePath 如实）——**ApplyTrackedReplace 编辑语义零改动**。回滚走既有 GaeaRollbackRecord（VersionTimeline kind:"docx" 已支持），docx 编辑从此可回滚。
- `GaeaDocxAcceptChanges` 仅 accept==true 分支（接受=清除修订标记的不可逆整理）加同款快照+记录（Tool=`docx_accept`）；accept==false 不加（拒绝=恢复原文，天然回滚语义）。
- 测试 +3：apply 快照字节=应用前+Journal 字段逐项断言 / 120 截断口径 / accept 两分支对称性。空间守卫可注入（零值 gaeaConfig → work）非降级直调。

## pptx 面板字符级对比

- PptxEditPanel 对比区自绘句级着色替换为 `<ChangesDiff>`：既有 docxTextDiff 句级 LCS 输出归一为 DiffRow（changed=相邻 del+add 对交 pairModifications 自动改蓝配对+字符级高亮；未变句子给 ctx 行由 foldContext 折叠，不伪造全量）；纯文本不传 path 无语法着色。
- 删除「句级对比（无字符级高亮）」诚实降级标注（被真实字符级取代）；「PPT 不支持修订标记，应用即生效可恢复」常驻标注保留。编辑流状态机/应用逻辑/段落选择零改动。

## 门禁

Go app 包全绿（+3 用例，Docx/Pptx/Xlsx 三面零回归）、vitest 2650 全绿（面板 8 用例语义保持）、tsc -b/eslint 0、drift PASS@598、版本三处 4.157.0、build+冒烟过。

## 产物

- `releases/gaea-v4.157.0.exe` — SHA256 `b96c549efa92e87e2b952e7c4daed8263f52e15509f8da3f0cb65de06de89a2e`（见 `SHA256SUMS-v4.157.0.txt`）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）

## 欠账清单（如实列示）

- pptx 刀2 真机走查仍挂真机池（mock Preview 无 pptx 分支，v4.28 起即如此）。
- pptx 刀4 修改队列泛化：待刀2 使用反馈拍板（设计原文口径，未动）。
- 证据卡 Status=PendingVerify 的 Verifier 通道 B 复核对 docx_apply 的页数对比口径（既有通道 B 范畴，未动）。
