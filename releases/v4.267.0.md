# v4.267.0 — 原罪工具集 v1.1：sin_export 图文导出工具入册（2026-09-13）

## 用户口径

v4.262 留存合约清账：设计档 13.2 注记「`sin_export`/`sin_illustrate` 半成品挪至
`.tmp/sin-next-knife/`，下刀接完产物链路再入册」。导出工具的产物链路（exports
目录落盘 + 轨迹文本路径，过程卡渲染）已被 v4.262 过程卡补齐，前置满足——本刀把
`sin_export` 入册。**`sin_illustrate` 维持「刻意不做待拍板」**（插图已有标记协议
+进度卡+队列+取消，工具化会多出第二条出图路径，风险>收益），留存件不删。

## 落地

- **零第二份导出逻辑**：委托既有 `SinExportMarkdown`（与前端「另存为 .md」同一
  份 Markdown：正文原样、插图标记换本地图片引用）。
- **产物落点**：`<用户配置目录>/gaea/sin/exports/`（与 art/notes 同一数据根，
  硬隔离口径不变）；**同名不覆盖**（-2…-99 后缀），历史导出不被悄悄顶掉。
- **文件名净化 fail-closed**：路径分隔/Windows 保留字符/控制符剔除、上跳不留、
  `.md` 去重、60 rune 截断；缺省用故事 id + 时间戳。
- **原子写**（tmp+rename，与便签同一纪律）；`ReadOnly=false`（新建文件如实标注）；
  schema 仅可选 `filename`；Description 明确「只在用户明确要导出时用」。
- **工具集 5→6**：`sinToolOrder` 末位（收尾动作）；前端过程卡标签「图文导出」+
  Download 图标 + 行首摘要给 filename。

## 测试

Go +3（TestSinExportToolWritesMarkdown：落盘+轨迹给路径+同名 -2 不覆盖+空故事
诚实报错；TestSinExportFileName 净化矩阵；TestSinExportInToolSet 末位）+
wantReadOnly/loop 计数锁 5→6 更新；vitest +1（图文导出标签）。
零新 App 绑定（工具是 Go 域内注册，drift 维持 642）。

## 真机走查（CDP 受信任鼠标+桥面直调，.tmp/walk-v4267-export.mjs）

**临时故事两回合端到端，结束即删，不碰用户真实故事**：回合一只写正文（不调工具）→
等落库；回合二「把故事导出，文件名走查导出」→ 模型真调 `sin_export` → 轨迹返回
「已导出图文 Markdown：…\exports\走查导出.md 共 111 字」→ **文件真实存在且含正文
断言过**（标题+用户消息+助手回复全在）→ 过程卡渲染「图文导出」行 ✓。
**走查抓到并当场修掉一个语义缺口**：首版走查让模型「写完就导」（同回合），导出发生
在回合内、本回合正文尚未落库，只导出了头部——已把该边界写进工具 Description（模型
指引：「刚写的内容要下一回合再导出」），并把走查改为两回合式复验通过。
window.onerror/unhandledrejection=0（仅启动期一条 longtask 52ms 性能观测，非错误）。
走查故事已删、走查导出件已清（剩余=用户 2 个真实故事，未动）。

## 门禁与产物

- ci.ps1 全绿（run1 一次全绿 + run2 覆盖 Description 修改：vitest 342 文件 2977 例、
  E27 PASS）；版本三处 4.267.0；drift OK@642。
- exe 49,281,024B SHA256=BD2AEE86BAEF946688AE22DD333FB842D72FAC6A8529BE80EC50AE58E476D827
  （build/bin 与桌面副本同哈希，冒烟通过：/api/health 200）。
