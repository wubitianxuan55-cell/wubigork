# v4.296.0 — 原罪·书源线收官 t5：成书「送小说导入」+ EPUB 导出（2026-09-14）

## 来源

sin 书源线最后一刀 t5（规格 `docs/gaea-sin-booksource-distill-2026-09.md` §5；t2~t5 自 DSH 并行线移交本线执行，用户拍板 2026-09-14）。成书此前只躺在原罪成书清单里——本刀给它两条出口：整本送进小说板块拆书导入，或转出 EPUB 阅读器格式。**t1~t5 至此全部收官**。

## 落地

- **送小说导入（零新后端耦合）**：成书行新增「送入小说」——确认后 sin 前端直调既有 `ImportNovelBookEx(path, title, '未分类', '默认', 'full', 0)`（书源组装的 TXT 本就是干净章节文本，走与文件导入同一落库链），成功后 message 报告章数并派发 `NAVIGATE {page:'novel'}` 跳书架（白名单+空间可达性由 MainLayout 校验，sin/novel 同在 play 空间天然可达）。
- **`ImportNovelBookEx` 契约转正**：该绑定（v4.287）此前只走 wailsApp legacy 直调，本刀按「legacy 直调转正」方向升进 bridge `NovelBindings` 契约面（含 `NovelImportResult` 载荷类型），facets 归 play，drift legacy 清单移除。
- **EPUB 导出（`SinBookSourceBookExportEpub`，SinB +1→21，绑定面 666→667）**：成书 TXT → 同名 `.epub` 落同目录（重复导出覆盖=重导出语义）。转换是**我们自己组装格式的确定性逆过程**：`sinBookParseTxt`（书名/作者/简介头 + 「第N章」章头行 + 章头间非空行=段）→ go-epub（简介节 + 逐章节，HTML 转义）→ `Write`（失败清理半截产物）。护栏复用抽出的 `sinGuardBookPath`（限成书目录内 .txt）；**删除成书连带清理同名 .epub**（best-effort 不阻断）。
- **dev mock 诚实拒绝**：`mock/novel.ts` +ImportNovelBookEx、`mock/sin.ts` +ExportEpub（浏览器无桌面文件系统，如实报错不假装）。

## 测试

- Go +4：`sinBookParseTxt` 回环（头字段/章/段）与不可识别拒绝；导出 happy path（同名 .epub 落盘非空 + 目录外/非 txt 护栏）；删除连带清理同名 .epub。
- vitest +2：送小说确认后逐参对齐导入（full 全本）并断言 NAVIGATE 事件 detail `{page:'novel'}`；EPUB 导出调用绑定并提示成功。sin 域 86/86。

## 门禁与产物

- `scripts/ci.ps1` 全绿（exit 0）；drift OK@**667**；版本三处 **4.296.0**。
- `releases/gaea-v4.296.0.exe` — 50,127,872 B，SHA256=`DD72F0781F3E3C9E810EC7F696185730E49DD740532F2E08EB90FE7514A3736E`；`releases/SHA256SUMS-v4.296.0.txt`；桌面副本同哈希；冒烟 `/api/health` 200。

## 未做（观察池）

- 真机走查：书源卡+卡片面+工具链（book_search/book_download 故事中拉素材）一条龙，复用 v4.283.1 状态化假站配方，等用户闲置窗口。
- 断点续传/章节缓存目录、cf-bypass、真实站点规则出厂——维持不做（规格 §6 拍板池口径）。
