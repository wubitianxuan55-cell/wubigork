# v4.399.0 — 原罪插图参考升级 qedit（T2 第二消费方）

> 2026-09-23 发布。v4.398 观察池头名小刀。规格：`进度计划/gaea-sin-qedit-20260923.md`。绑定面 715 零变更。

## 病根

原罪插图的角色一致性（v4.258）走 img2img 整幅重绘近似——构图被参考图锁死，「按场景描述出图+人物一致」做不对。v4.398 qedit 通道（参考图进编辑引擎三图槽）正是为此而生；原罪插图的 prompt 本来就是场景描述+点名角色，是天然的第二个消费方。

## 落地

- `sinRefPlan` comfyui 分支 → `(txt2img, qedit)`：编辑引擎独立于生图模型，不再按 krea2/z-image 判型（flux 等模型下插图参考也可用）；herdsman 维持 img2img 近似。
- 编辑权重缺失由既有 refFallback 链兜底（失败→退纯文本重试+标注）——无权重环境插图不断流。
- 文本锚点/角色选择/多角色互串防护不动；denoise 照传无害；caption 徽标语义不变。

## 测试

矩阵更新（comfyui 全模型含 flux-dev → qedit）+透传期望+TestSin 全量回归绿；全量 398 文件 3365 例绿 + tsc 0 + eslint 0。

## 门禁

全量 ci.ps1 绿 EXIT=0；版本漂移闸 OK@4.399.0。

## 产物

`gaea-v4.399.0.exe`（冒烟 200 过；SHA256 见 SUMS）。保留策略 5 版：留 v4.395~v4.399，删 v4.394.0.exe。

## 观察池

sin 参考方法可配；两级回退（qedit→img2img→纯文本）；徽标区分方法；qedit 语义 prompt 模板。
