# v4.172.0 — 瘦身 P3 版3（bridge 双轨退役批次一）：wailsjsCompat 直调族首批 12 文件迁移（2026-09-09）

> 瘦身执行层第七版：轨道四「双轨」渐进退役第一刀（masterplan；出口判据=绑定零变更 drift PASS + 全部迁移面行为零变化/错误归一 BridgeError）。
> 调研先行（子代理三分类：39 个非测试文件 = 2A 零扩展直迁 / 24B 单簇小扩展 / 13C 暂缓），本版消 A+B 首批 12 个调用方 + 契约扩展 16 方法 + 2 个连带测试适配。

## 1. 调研基线（docs/gaea-bridge-retire-survey-2026-09.md 同源结论）

- `frontend/src/wailsjsCompat.ts` 是 S2-3 兼容 shim（仅 `export *` 十门面），**不经过 bridge 的 mock/代理**——57 文件直调。
- 权威「③ 不在 bridge」清单 = `bridge/drift.ts` `LegacySurfaceNames`（292 项，扩 bridge 必须同步摘除，`_CheckAppBindingsCoversAll` 编译期钉死）。
- 39 个非测试文件：PersonaPicker/CharacterCard=100% 命中（A）；24 个 B 类（≤3 缺失方法）；13 个 C 类（语音族/角色库族/cast 族，≥4 缺失或依赖直调语义）。
- 语义风险确认：useVoiceChat/ChatPanel 依赖直调同步 throw 的 try/catch（C 类暂缓）；useChatTopics 的 mock 元组 [T[],unknown] 语义（C 类暂缓）；CreatePage/ChapterEditor 的 cast 桥（C 类暂缓）。

## 2. 线1：bridge 契约扩展（16 新方法，单子代理）

- **CoreBindings** +4：`GetAppInfo`/`GetBoardManifests`/`GetFeatureModel(feature)`/`GetFeatureModelEnabled(feature)`；
- **ModelBindings** +3：`SetFeatureModel(feature,engineID,modelName)`（3 参，按 Go）/`SetFeatureModelEnabled(feature,enabled)`/`GetActiveModel()`；
- **ImageBindings** +1：`StartLocalTTSService(engineID)`；
- **VoiceBindings** +6：`VoiceApplySettings(patch)` + Whisper 读写族 5（`WhisperGetState/GetFacts/GetTraces/DeleteFact/UpdateFact(3 参)`——Go 侧在 VoiceB 门面，业内纠偏归属）；
- **NovelBindings** +1：`NovelSearch(query)`（单参按 Go）；
- **OfficeBindings** +1：`SaveSettings(view)` + mappings `SaveSettings:"GaeaSaveSettings"`（GaeaSettings 已有 Settings 映射不动）。
- **drift.ts 摘除 16 名**；**spaceBindings +16 分类（9 shared + 7 play）锁 315→331**；mock 全补齐（含新建 mock/voice.ts、mock/novel.ts）。
- 验证：tsc 0 / eslint 0 / 契约测试 19/19。

## 3. 线2A：零依赖直迁（PersonaPicker/CharacterCard）

- `App.CharacterList` → `app.CharacterList`；`App.WhisperAssistantSave(assistant.Assistant)` → `app.WhisperAssistantSave`（Partial 结构直接满足，零断言）。
- CharacterCard.test.tsx vi.mock 目标改 bridge（async importOriginal 保全集 + 覆写 app）。44 tests 全绿。

## 4. 线2B：10 个 B 类文件（4 路并发子代理，足迹互斥）

| 文件 | 迁移方法 |
|---|---|
| CharacterMemoryModal | WhisperGetState/GetFacts/GetTraces（as unknown as 类型适配） |
| WhisperMemoryModal | WhisperDeleteFact/UpdateFact（去多余 cast） |
| boards/manifests.ts | GetBoardManifests（normalizeManifests as unknown as；fail-closed 原样）+ manifests.test/launcher.test mock 改 bridge |
| AboutPanel | GetAppInfo（cast 保留） |
| useChatVoice | ChatAppendMessages + VoiceApplySettings（去 `?.`） |
| useFeatureModel | GetFeatureModel/GetFeatureModelEnabled |
| BindSection | VoiceApplySettings（去 `?.`） |
| useEngineState + test | GetActiveModel/StartLocalTTSService |
| novelSearchUtils | NovelSearch（cast 保留） |
| OfficePanel | gaeaApp.SaveSettings + gaeaApp.Reload（AppModels.SettingsView → gaea SettingsView；App import 删除） |

- 每线 tsc/eslint/定向测试全绿（53/53、7/7、15/15 等）。

## 5. 主代理收口

- **连带测试修复×2**（本批迁移的 hook/util 被 C 类页面消费）：ChatPage.test/ChapterPage.test 原 vi.mock 仅 mock wailsjsCompat，迁移后 useChatVoice（ChatAppendMessages）/novelSearchUtils（NovelSearch）走 bridge 导致 5 例断言打空——补 bridge vi.mock 与 wailsjsCompat mock **共享同一 vi.fn 引用**（bindingsBridge via vi.hoisted + Proxy 转发），22/22 转绿。
- **坑=vitest mock 相对路径**：src/pages/ 下 bridge 路径是 `'../gaea/lib/bridge'` 不是 `'../../gaea/lib/bridge'`（后者解析到不存在的模块，vi.mock 静默不生效、断言 calls=0 且 tsc 不报——诊断文件实锤后修正；凡 mock 相对路径必须与真实 import 绝对解析一致）。
- 度量：sp wailsjsCompat 引用 57→45（含测试）；LegacySurfaceNames 292→276；entry chunk 1190.55kB（1.19.59→略降）。

## 6. 门禁（全量复跑）

- vitest **2737/2737**（313 文件全绿——首跑 5 例失败为上述连带测试，修复后复跑全绿）；
- `tsc -b` 0 · `eslint .` 0 · drift **PASS@602**（零绑定，方法名未变）· locale **0 死**（1448 键）；
- Go 全量回归绿（本版零 Go 改动，app/core/office 包确认）；
- 版本三处 4.172.0（sync-version.ps1）。

## 7. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 1190.55 kB gz 375.07 ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.23 MB ｜ wailsjsCompat 引用 57→45
```

## 8. 产物与冒烟

- 产物：`gaea-v4.172.0.exe`（46.23 MB），SHA256=`C9B020CE9B0257FDC21FE00D9C0417D0CCE4D47767BBE1BF2310B9AB1799AFE1`（releases/SHA256SUMS-v4.172.0.txt）。
- 冒烟：`scripts/smoke.ps1 -ExePath releases\gaea-v4.172.0.exe` → **/api/health 200**（冒烟进程已回收）。

## 9. 欠账（批次二/三）

- **批次二**：api/settings.ts（GetConfig/SaveConfig/GetActiveModel/SetImageBackend/Voice* 一次扩 CoreB/ModelB/VoiceB 解锁 DataPanel/ChatPanel/VoiceSettingsPanel/OfficePanel 四面板）+ useChatTopics（ChatTopic* 簇扩展 + **元组重构**）+ useChatStream（ChatSend/ChatStreamPlain + mock 事件发射）+ NovelPage/NovelSettingPage + novel 面板族（Consistency/Foreshadow/WorldviewSections/Export/CreateInspector/NewCharactersModal/readingAssistant/ChapterIllustration）+ ModuleLauncher/pages/chat/utils；
- **批次三**：语音族（useVoiceChat/VoiceSettingsPanel/useVoiceState——依赖直调同步 throw 语义需重评）、ChatPage 五个直调、cast 族（CreatePage/ChapterEditor 需先入 AppBindings 类型）、角色库族（api/characterlib + novel/api/character）、ChapterPage 四个直调、useBindState/DataPanel；
- 全部批次完成后 wailsjsCompat.ts 退役（shim 删除）。
- 壳内真机池、审计刀D 真机取证不变。