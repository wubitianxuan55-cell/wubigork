# gaea v2.14.7「聊天板块交互收尾：清空确认 + 切换聚焦 + 标题生成收敛」

发布日期：2026-08-13 ｜ 版本号：v2.14.7 ｜ 产物：[gaea-v2.14.7.exe](gaea-v2.14.7.exe)（Windows x64）

收尾聊天板块几处日常交互细节。

## 交互优化

- 清空当前对话加 `Popconfirm` 二次确认，避免误清空不可恢复。
- 选中话题后自动聚焦输入框，切换过去即可直接输入。

## 结构收敛

- 会话标题生成抽为纯函数 `autoTopicTitle`，补表驱动测试。

## 测试

- 前端用例 194 → 196（新增 `autoTopicTitle` 用例）。

## 验证

- `wails build`（含 tsc + vite build）通过；`vitest` 196 例全过。
- `go build ./...`、`go vet ./...`、`go test`（chat/app）全绿。
- SHA256：`DA8CEF4276A2614103B822CF286E2B6BF84BD453DD577256F13F5D2A7EFA2C77`
