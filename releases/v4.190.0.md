# v4.190.0 — 空间策略写路径：总闸当场切（2026-09-10）

> v4.189 欠账收刀：`space_profiles` 从只读显形到可编辑。展望口径「云端和
> 本地……在模型中心一眼看见、**当场切**」——策略维度补上切的动作。

## 实现

### Go（603→604，新增 `GaeaSpaceProfileSet(space, key, ref)`）

- `applySpaceProfileEdit` 纯函数校验+落段：
  - `ref` 空 = 清除该键；清除后全空 profile 段删除（不留 TOML 空段噪音）；
  - 非空必须经 **ResolveModel 同链解析**（与 boot 装配一致）——解析不了
    **宁拒不写**，错误消息带已配置 provider 清单（说人话）。理由：坏引用
    写进去只会得到「boot 告警+现状模型继续生效」的骗人状态，UI 不制造它；
  - 非法空间/未知键（可写面=gaea/chat/whisper/novel/office/characterlib/
    routine 七键）拒绝；
  - 持久化走 gaeaSetSessionSpace 同款锁+`gaeaConfig.Save`。
- **生效时机=下次引擎重建/重启**（boot 装配世界，运行中引擎不动）——与
  `GaeaSpaceActivate` 同口径诚实，UI 同话术。
- 返回刷新后的双空间视图（前端零二次拉取）。

### 前端（空间策略卡）

- gaea 行加「编辑」：带出当前值 → `provider/model` 输入（留空=维持现状，
  placeholder 说明）→ 保存调绑定、视图随返回刷新 → 诚实 message
  「已写入，下次引擎重建/重启生效」（清除=「已清除，回退现状模型」）。
- 后端校验失败原样 `message.error`（含 provider 清单），不吞不改写。
- mock 模块级状态支撑浏览器演示态编辑闭环（演示态非空即收，真机校验在 Go）。

## 门禁

- Go 全量 0 FAIL（+`TestApplySpaceProfileEdit`：合法写入视图可见/清除删段/
  坏引用拒写且错误带 provider 清单/非法空间/未知键）；gofmt 0。
- vitest +2（编辑闭环：带出当前值+调用参数+视图随返回刷新+成功话术；
  写入失败说人话原样透出）；全量在册 flaky 族处置见 commit 门禁行。
- tsc 0、eslint 0、drift PASS@604、e-check OK、locale 零新增键。
- 版本三处 sync 4.190.0；CHANGELOG + README 版本表 + 本说明。
- build strip + 冒烟 200 + SHA256（tag 树干净 worktree 构建，工作树他方在途
  批次不入产物）。
  SHA256=`1f9adfbe1accc5a2f435e112739ee5dc7fd56385b466a04bd9e6fa948eddbc11`
  （releases/SHA256SUMS-v4.190.0.txt；已落 Desktop 与 build/bin，删除 worktree
  前后主树 node_modules 452 包复核一致）。

## 欠账

- 生效路由交叉显示：经核已被「功能绑定」分区的当前生效路由+来源标签覆盖
  （office 域还带 gaea 路由），销案；如需功能域×空间策略交叉矩阵再另刀。
- 编辑 UI 暂只放 gaea 主控（七键写路径已通，其余键按需出 UI）。
- 壳内真机走查池不变（策略编辑闭环壳内实测挂真机池）。
