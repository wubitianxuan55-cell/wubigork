# v4.277.0 — 小说·文风指纹落地面：参考档构建 + 对照体检（并行池小说线既定余项）（2026-09-13）

## 来源

「优化完善gaea」：拍板池三项均为用户决策项（不建议/仅观察），按既定习惯从
**板块并行池**挑余项——小说线「文风指纹 + AI 味中和（→ 打分 → 定点重写闭环）」，
novel-revolution 设计档明示的「文风指纹面板」缺口。

**现状核实**：内核 `internal/novelstyle` 的确定性指纹算子自刀3起齐备
（`ComputeFingerprint`/`Delta`/`ScoreText(text, fp)`），但 **app 层零消费**——
参考档无处落、无处看、打分只走无参考档的通用阈值。既有在产面=一键去味
（`DeSlopChapterAiTaste`）+LLM 定点重写（`RewriteChapterAiTaste`）。

## 落地

**参考档（文风基线）**：

- `<projectDir>/fingerprint.json`（`types.StyleFingerprintFile`：builtAt/chapters/
  chars/fingerprint 原样 JSON——types 不反向依赖 novelstyle，解析归 app 层
  `LoadFingerprint`）；Manager 增 `StyleFingerprintPath/ReadStyleFingerprint/
  WriteStyleFingerprint`（原子写，与既有工程文件同法）。
- `NovelFingerprintBuild`：全部已写章节（v4 场景工程走 `ReadChapterAsStitch`
  拼接，v3 整章）→ `ComputeFingerprint` → 落盘。**门槛诚实拒绝**：≥3 章且
  ≥3000 非空白字，不足时中文报错不落盘（低样本下句长分布/TTR 失真）。
- v1 口径=**作者成稿即样本**（构建时点全部已写章节）；设计档 §4.1「只学作者
  手改」的结算学习路径留后续刀，面板说明文案如实写明「不会随写作自动漂移，
  去味/手改后可重建」。

**对照体检**：

- `NovelFingerprintScore(chapterNum)`：有参考档 → `ScoreText(text, fp)` +
  `ComputeFingerprint(单章)` + `Delta(observed, ref)`（函数词 z 向量 L1 距离，
  **越小越像作者**）；无参考档 → 通用阈值打分（refExists:false），两条路都可用。
  参考档损坏按无参考档降级不挡体检。
- `NovelFingerprintStatus`：未构建返回 `exists:false` 正常态（不算错）。

**前端（CreatePage 创作间）**：

- rail 新增「文风指纹」按钮 → `StyleFingerprintPanel` 受控 Modal（形态对齐
  「叙事状态账本」弹窗）：未构建空态引导 / 已构建元信息行 + 8 项摘要格子
  （句长均值±标准差/段长均值/对话占比/词汇多样 TTR/四字格密度/连接词密度/
  形副密度）+ 口头禅 Tag（topBigrams+topTrigrams+authorSignWords）/ 章节体检
  大数字 + 语义分档（<35 顺眼 / 35~59 有 AI 痕迹 / ≥60 AI 味偏重，阈值同
  CreatePage aiTaste）+ Δ 基线距离 + issues 列表（severity Tag+reason+原文
  摘录+建议）。后端摘录 Go 侧截断（前端免按 rune 切字）；数值/列表全防御
  （omitempty 缺省/Go nil slice→JSON null）。
- 打开即拉 Status（无独立刷新钮，从简）；无当前章时「体检当前章」禁用。

**绑定 642→645**（NovelB 门面 +3；wailsjs 再生后生成模型字段 camelCase 与
Go json 标签一致——v4.276 形状普查纪律延续）；bindingNames 再生、spaceBindings
三绑定 → play（数量锁 464→467）、bridge/novel.ts 契约、mock/novel.ts 演示实现
（Build 置位返回诚实假摘要）。

## 测试

- Go +6：门槛拒绝（不足不落盘）/构建+Status 回环/有参考档体检（delta 在位+
  摘录兜底）/无参考档口径（refExists:false+delta 缺省+黑名单样本有命中）/
  空章与非法章号拒收/缺章即停遍历口径。
- 形状锁（v4.276 先例）：payload marshal 键断言（camelCase+PascalCase 反例）
  + **delta=0 不被 omitempty 吞**（指针零值）+ 参考档文件键形锁定。
- vitest +5（面板 props 驱动：空态引导/全量渲染/体检 delta/issues/禁用态）；
  spaceBindings 数量锁 464→467；CreatePage/bridge/mock-contract 回归绿；
  tsc 0 / eslint 0 / drift OK@645。

## 真机走查（壳内 CDP，一次性夹具工程走查后清场）

夹具=`go run ./.tmp/makefixture` 调自家 `project.Create` 造 3 章·约 4000 字
工程（novels/ 下，gitignored），不碰用户数据：

- **桥验**：NovelB 三方法壳内可达；`OpenProject` 打开夹具；未构建
  `exists:false`；无参考档打分 `score 9/issues 1/refExists:false/delta 缺省`。
- **UI 全流程**：书斋→闲庭（游园画廊海报墙）→小说→创作 tab →「文风指纹」
  面板：未构建空态引导 ✓ → 构建 ✓「参考档已构建（3 章 · 3,999 字）」+ 8 项
  摘要格子 + 口头禅 Tag → 体检 ✓「第 3 章体检完成 / AI 味 顺眼 / 与你的基线
  距离 Δ 0.00」→ 重建覆盖（builtAt 更新）→ console 零错误（截图
  .tmp/walk-v4277-panel.png）；fingerprint.json 落盘键形 camelCase 核验。
- Δ=0.00 为夹具特性（三章同段落重复且被评章节在参考档内，函数词向量重合），
  非缺陷；无参考档路径已由桥验覆盖非零分。
- **走查脚本坑（复训）**：海报墙入口须 `scrollIntoView` 后再点（海报中心落
  视口底缘时被右下悬浮件拦截点击）；体检按钮 disabled 根因=夹具无 outline
  节点（activeChapterNum=0），补大纲节点后走通——**UI 依赖大纲树的夹具必须
  连 outline.json 一起造**。
- 清场：杀壳 → rm 夹具（**delete-pending 复训**：node rmSync 静默删不动，
  等句柄释放后 PowerShell 复删成功，existsSync/ls 双复核空）。

## 门禁

- ci.ps1 **run1 抓到一个 v4.276 存量测试缺陷并当场修**：ChatPage.test.tsx:316
  语音落库断言仍锁旧 PascalCase 载荷，而 v4.276 已把 useChatVoice 发送改
  camelCase（形状真源=Go json 标签）——断言对齐 camelCase（+注释），单文件
  15/15 绿。
- run2 go test 段负载 flaky（既有先例类：netclient httptest 竞态/schedule
  rename 文件锁；**日志被 tail 截丢未定包名——后台长输出禁 tail 的教训再踩
  一次**）；复跑 `go test ./...`（run3 全量落盘日志）exit=0 全绿。
- 门禁分段跑齐：go vet ✓（run2 段内）/ go test ✓（run3）/ lint ✓（0）/
  build ✓（tsc+vite，0）/ vitest ✓（**345 文件 2991 例全绿**）。
- drift OK@645 / 版本三处 4.277.0。
- 产物：exe 49,352,704B SHA256=2CB99312…802CD（桌面副本同哈希，冒烟 200 过）；
  归档 releases/gaea-v4.277.0.exe + SHA256SUMS-v4.277.0.txt。
