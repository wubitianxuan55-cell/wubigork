# v4.63.0「子代理会话 tab 输出对齐主对话 Codex 式渲染」

> 发布日期：2026-09-04 ｜ tag：`v4.63.0` ｜ 基线：v4.62.2 ｜ 绑定面 **559
> 零变更** ｜ 用户点名：子代理的输出也要参照父代理的 Codex 样式。

## 内容

- **消息渲染与主对话完全同源**：assistant 正文/思考改走主对话
  `AssistantMessage`（MemoMarkdown、思考折叠带展开态、复制按钮、交付卡），
  不再是子代理 tab 自带的简化行；工具调用与结果走主对话 `ToolCard`
  （可读摘要、状态徽标、折叠输出、diffstat 芯片）。
- **配对映射层**（lib/subagentRender.ts 纯函数）：transcript 的
  assistant.toolCalls 与后续 tool 结果按 toolCallId 配对成完整工具项；
  乱序结果同样配对；孤儿 tool 行降级独立卡（name+输出，诚实展示）；
  运行中结果未到的工具项标 running。
- **流式实时行同款**：P1 逐 token 流式缓冲行也换 AssistantMessage
  （streaming 光标/思考自动展开与主对话一致）。
- system 弱化单行、user 右对齐保持；运行中自动跟随底部不变。

## 验证

- tsc -b 0；eslint 0；vitest **214 文件 / 1594 用例**（新增映射层单测 4 例，
  组件断言适配：ToolCard 摘要与全文并存、思考折叠以 aria-expanded 断言）。
- 绑定面 drift PASS（559）；build.bat 冒烟 /api/health 200。

## 产物

- gaea-v4.63.0.exe
- SHA256：见 releases/SHA256SUMS-v4.63.0.txt
- 冒烟：临时副本启动 → /api/health 200，进程已回收。

## 欠账清单（沿袭 v4.62.2）

- 子代理 tab 内 follow-up 提问仍需后端绑定；mt_ 大输出的分块摘要视图；
  其余沿袭 v4.62.2。
