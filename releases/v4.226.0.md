# v4.226.0 — 规范知识出内核第三刀：DCMA 阈值表数据化（2026-09-11）

> 拍板池 §5.4 执行，选**形态 a**：质量体检视图保留（不退任何已发布产品面），
> 行业阈值表出码成数据资产。红线第三落地（进度域）。

## 缺口

DCMA 14 点的五个行业标准阈值（高浮 44 天 / 长工期 44 / 超标占比 5% / FS 占比
90% / CPLI·BEI 0.95）硬编码在 `frontend/src/schedule/dcma.ts`——不同业主/机构
口径想调阈值就得改代码发版。

## 落地（计算=机制留码，阈值=知识出码）

- **`dcma-thresholds.json`**：默认表随构建内置（resolveJsonModule）。
- **`dcmaAudit` 第 4 参 `thresholds?: Partial<DcmaThresholds>`**：部分字段覆盖，
  缺省用内置默认；判定线与体检单阈值展示串全部走生效值。
- **兼容导出保留**：DCMA_* 常量从默认表派生，历史消费方零破坏；顺带清
  naCheck 死函数（v4.216 起的 eslint warning 清零）。
- **DcmaView**：挂载时经 ReadFile 懒加载 `.gaea/skills/schedule-dcma/thresholds.json`
  （部分字段即可；缺失/解析失败静默用内置默认——增强面不挡主功能）。
- **SKILL 文档** `.gaea/skills/schedule-dcma/SKILL.md`：14 点×默认阈值对照表 +
  JSON 字段映射 + 覆盖指引 + 诚实口径（CPM 断裂 score=null / n/a 不计分）。

## 测试

- vitest +2：默认表漂移守卫（逐字段对照行业标准值）；覆盖全链（长工期阈值
  抬升→判定翻转+展示串同步>60+未覆盖字段保默认+超标占比归零）。

## 门禁

tsc -b 0 / eslint 0 / vitest 329 文件 2847 例全绿 / 绑定 624 零变更 / 版本三处 4.226.0。

## 打包记录

- 产物：`build\bin\gaea.exe` **48,841,728 B** SHA256 `cdc837d959c22b77af147120c246dae8fbe10a3500e4fb75ff7ac1df10539ca0`
  （冒烟 /api/health 200 + Desktop 副本同哈希；exe 不入库，只存哈希）。
- 未打 tag（随 v4.200+ 现状）。
