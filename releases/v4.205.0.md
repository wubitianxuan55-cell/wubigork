# v4.205.0 — 小说书房工坊收口：书架画廊接线 + 阅读检查器可见（2026-09-10）

> v4.198 把视觉语言写进 `novel-workspace.css`，但书架 JSX 仍走旧工具条；
> 阅读 tab 用 `display: none` 藏掉属性检查器，v4.184 挂进去的「章节体检」从此没有入口。
> 本刀只接线、不造新家具。绑定面 608 零变更，Go 零改动。

## 一、书架画廊真正落地

- **HomePage**：`.novel-shelf` 外壳 + 画廊头（书架 / N 部）+ **正在编辑横条**
  （书名 / 章字 / 读到第 N 章，动作=继续阅读 / 去创作）；工具条改语义 class，
  去掉内联色值；空态按钮进 `.novel-shelf-empty-actions`。
- **ProjectCardItem**：竖版书脊 + 正在编辑角标；继续阅读图标对辅助技术隐藏。

## 二、阅读页属性检查器

- 去掉「阅读 tab 整区 display:none」。目录仍在，检查器**默认折叠**（44px 微条），
  展开即章节属性 + 章节体检。专注模式仍收起左右 zone。
- 目录侧栏已开书时不再重复头栏身份卡，只留大纲。

## 三、测试

- HomePage 3 / ProjectCardItem 2 / NovelSidebar 2 / CSS 契约 2。

## 门禁

tsc 0 / eslint（改动文件）0 / 目标 vitest 9/9 / 绑定面 608 零变更 / Go 零改动 /
版本三处 sync 4.205.0。

## 欠账

- 12 主题浏览器截图抽查仍在壳内真机走查池；
- 书封生成结果仍不回写书架卡（ProjectCard 无封面字段，不在本刀）。
