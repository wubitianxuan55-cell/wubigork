# v4.167.0 — 瘦身 P2 刀0（白名单解耦）+ P0 基线落档 + 审计刀D Filters + nanoid 升级（2026-09-09）

> 瘦身执行层第二版：P2 形态第一刀（白名单解耦）落地，P0 基线证据层入档；
> 审计刀D 代码化部分（Filters 可选参数）收官 + 供应链 nanoid high 清零 + 死代码清理。
> 三线并发子代理（足迹互斥：boards/manifests / docs 基线 / npm+export）+ 主代理第四线（Go 绑定 Filters）+ 主代理收口门禁。

## 1. 线A：P2 刀0「导航白名单与 inMenu 解耦」（子代理 e7638ab8）

- **改动**：`frontend/src/boards/manifests.ts` `deriveNavigateWhitelist` filter 由
  `b.inMenu && !b.isHome` → `!b.isHome` —— **注册即可导航**，`inMenu` 只控菜单不再派生可导航性。
- **动机**：v4.164 前科根治（藏板块 inMenu=false 经 NAVIGATE 白名单 fail-closed 静默丢导航）；
  刀1「schedule inMenu=false 并入办公文档面」依赖本语义（不预先解耦则 schedule 会被白名单拦掉）。
- **影响**：白名单新增 **settings**（canonical 唯一 inMenu=false 板块，有右上角隐式入口）；
  weixin 进 fixture 白名单（角色库→青鸟 crossSpace 深链 NAVIGATE 依赖：CharacterCard.tsx:93）。
  菜单 `menuBoards`（filter(inMenu)）与 launcher 展示层语义零改动。
- **测试**：manifests.test.ts 断言语义原地更新；全量 vitest 2714 绿（子代理跑含并发改动在场）。

## 2. 线B：P0 基线快照表落档（子代理 cea62c23）

- **新文档**：`docs/gaea-slim-baseline-2026-09.md`（基线证据层，适用 v4.166.0，实测非抄录）。
- **§1 七面四表实测**：dist 10.0MB/227 文件（立项 10.03/224 ≈持平）；entry 1192.79kB；
  MemoryHub 1436.11kB（解剖实证：3d-force-graph 仍在页内、cytoscape/cynefin 已独立 chunk）；
  mermaid/katex/CodeEditor 仅宿主视图加载成立；exe 46.22MB；npm 直依赖 29（26−1+4）；
  >50KB 源文件 16 个（立项 9 全在 +7）；P1 四刀轮子收口全部实证。
- **§2 功能等价快照表**：13 板块×核心动作可达性（代码层），纠正 knowledge 非一级导航
  （后端 D7 板块被前端过滤，KnowledgePage 孤儿页——P2 走查项）。
- **§3 IA 走查**：rail 12 板块平铺不分空间、foot 独立 code 区（**勘查出 code 双入口疑似项**）；
  默认空间=work；首页 Bento 无空间过滤平铺；**「双空间并列未表达」实证成立**（仅 3 处空间感知），
  双空间并列结构 P2 改造前基线打牢。

## 3. 线C：nanoid high 升级 + downloadMarkdown 死代码清理（子代理 bc3becc3）

- **nanoid**：纯传递依赖（vite→postcss→nanoid）。审计实证受影响范围 **<3.3.18**（非 <3.3.8，
  修正任务书口径），原锁 3.3.17 仍在漏洞内 → 升 **3.3.18**（postcss 范围 ^3.3.17 天然含，免 overrides）。
  npm 11 重写 lockfile 再次写坏（丢 @codemirror/search + 乱加 peer:true）→ 按 v4.164 先例
  git restore + 手动编辑单条目 + `npm ci --dry-run` 验证；最终 diff 恰好 3 行。
  `npm audit` total=0，**GHSA-2v37-7h3g-55p8 彻底清零**。
- **downloadMarkdown**：`gaea/lib/export.ts` 删除（v4.166 审计刀B c 留下的零调用死代码，
  exportAsMarkdown 保留）；`App.export.test.ts` 注释更新 + 新增 export.ts 源级断言
  （无 downloadMarkdown / 无 createObjectURL）——「办公导出 md 必须走 exportConversation，
  禁止回退浏览器 a.download」测试意图保持。

## 4. 线D（主代理）：审计刀D 代码化部分——GaeaPickFiles Filters 可选参数

- **Go**：`GaeaPickFiles(filters string)` 新增可选扩展名过滤器（`internal/app/gaea_ui_extra.go`
  + 委托 `bindings_office.go`）——空串=不过滤，否则转 Wails `FileFilter{DisplayName, Pattern:"*.png;*.jpg"}`
  前置到系统对话框（Windows 实测支持）；保留 pickFile.ts 扩展名后置校验 = fail-closed 双保险。
- **前端**：`bridge.ts` 类型声明 `PickFiles(filters?)` + mock 同步；`pickFile.ts` 把 accept
  数组逗号拼接传给系统对话框（角色库/画室/技能导入三类上传自动获得原生对话框过滤）。
- **测试**：`pick_file_filter_test.go`（空/多扩展名/去点/大小写/混合空段 7 例）+ pickFile.test.ts
  filters 传参与无 accept 行为 2 例。
- **审计刀D 剩余**：真机取证子项（printSvg iframe print/拖拽/粘贴实际行为）仍需壳内真机走查，挂真机池。

## 5. 门禁（主代理统一收口复跑）

- Go 115 包全量 0 FAIL（首跑干净，无 flaky）；
- vitest 2684→**2715**（311 文件，+3 vs v4.166：刀0 语义更新 + Filters 2 + 清理断言 1；全量复跑绿）；
- `tsc -b` 0 · `eslint .` 0 · drift **PASS@602**（Filters 为签名变化，方法名零变更）；
- vite build 过；wails build + 冒烟见产物节。

## 6. 欠账（如实列示）

- 审计刀D 真机取证（printSvg iframe print / 拖拽 / 粘贴；Filters 已代码化完成）；
- P2 走查待证项（基线文档 §3 勘出）：rail code 双入口渲染实态、后端清单合并 nav 子项差异、
  knowledge 孤儿页确无入口；
- P2 刀1/刀2（schedule inMenu:false 并入办公文档面 + .gsched 注册办公文件面）待续；
- 双空间并列落地（空间切换器+分域导航+home 空间感知化）随 P2 主干推进。

## 7. 产物

- `releases/gaea-v4.167.0.exe`（46.2 MB，gitignore 不入库）
- SHA256：`93731155af69e1c6c1f0b14ea107e12a5302214f50a2b19a31b429900f1ba497`
  （见 `SHA256SUMS-v4.167.0.txt`）
- 冒烟：HTTP 桥接 127.0.0.1:18999 `/api/health` → `{"status":"ok"}` 200，进程已回收；桌面副本同步。