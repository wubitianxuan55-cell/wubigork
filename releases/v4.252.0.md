# v4.252.0 — 启动链收尾：远程剧照迁移异步化（2026-09-12）

## 为什么是这一刀

二遍扫描观察项 #3 收尾：Startup 内同步串行下载全部远程剧照（多为会过期
的 xAI 临时图），每张 30s 超时——N 张即 N×30s 全算进冷启动关键路径。

## 修复

`MigrateRemotePortraits` 调用点异步化（recover 守卫协程）。异步化安全前提
核实：迁移是「先收集 todo → 逐条下载（网络，不占库连接）→ 短 Exec 回写」，
与启动期其他角色库操作在单连接池上安全交错；迁移完成前角色卡显示远程
URL——与迁移前的既有状态一致，语义零变化。幂等：本版未迁移的下轮启动
重试；迁移本体已有测试覆盖（TestMigrateRemotePortraits）。

## 二遍扫描账本更新

修 6 项（#2/#3/#4/#5/#8/#10），观察 6 项（#1 browser 持锁 / #6 filewatch
就绪语义需单独设计；#7/#9/#11/#12 低危顺手级）。

## 测试

characterlib 迁移本体 + app 全量（67.9s）绿。零绑定零前端。

## 门禁

ci.ps1 全绿 / 版本三处 4.252.0。

## 产物

build.bat（冒烟 /api/health 200 + 桌面副本同哈希）→ `buildin\gaea.exe`
**49,038,848 B** SHA256=**644F081EBACC2EAABDA5CF2E2495D37CC5AF5AEF26746140B2EC678A5F054962**。
