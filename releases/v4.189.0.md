# v4.189.0 — 阶段二首刀：模型中心「空间策略」分区——总闸画面显形（2026-09-10）

> 长期规划阶段二首刀（阶段一 1A/1B 均已出口）。出口判据：打开模型中心能
> 看见本机/云端、功能域、办公引擎空间绑的策略、失败说人话——本刀补齐其中
> 缺失的一块：**空间装配 profile 零 UI 显形**。

## 缺口盘点

- 本机/云端：引擎管理分区已有 per-engine 本地/云端徽标（`is_local`）✅
- 当前功能域：功能绑定分区已有逐域绑定+生效路由（`modelRoutes`）✅
- **空间绑的策略：`[space_profiles]` 只在 boot 装配世界被消费
  （`prof.Gaea` 经 ResolveModel 覆写办公 agent 模型），UI 从不可见** ❌ ← 本刀

## 实现

### Go（602→603，新增只读绑定 `GaeaSpaceProfiles`）

- `SpaceProfileView` 逐空间（work→play 顺序恒定）汇报「配了什么+生效成什么」：
  - `gaea`（办公 Agent 模型覆写原值）+ `gaeaResolved`/`gaeaOk`（ResolveModel
    既有链解析结果；**坏引用诚实报 gaeaOk=false**——boot 告警后现状模型继续
    生效，前端原样显形说人话，不让用户猜）；
  - `models`（其余功能域覆写，仅非空项，空白 trim 掉）；
  - `permMode`/`permHardAskCount`/`permHardAskBySpace`（PermissionsForSpace
    既有链生效值：play 未配置段=产品默认 allow+显式空集）；
  - `guardrailsOn`（PlayGuardrails 既有链，play 内容护栏生效态）；
  - `modeOn`（space.mode 分区开关，off=策略不生效全域回退 work 现状）。
- `buildSpaceProfileViews` 纯函数组装（便于测试）；绑定引擎未初始化时读盘
  兜底（gaeaSpaceActiveView 同款，防「配置了但重启前查不到」假空）。
- CoreB 门面委托（gen_bindings 生成，`GaeaSpaceProfiles` 显式映射 core——
  前缀规则会误落 office）；只读视图，写仍归 gaea.toml。

### 前端

- 模型中心新「空间策略」分区（Category `strategy`，功能绑定与引擎管理之间）：
  - 双空间卡+当前生效徽标（`GaeaSpaceActive`，读取失败只降级高亮不拖垮整区）；
  - 办公 Agent 模型三态：未配置（维持现状）/`ref → resolved`/**坏引用红色
    说人话**（含检查拼写提示）；
  - 其余功能域覆写 chips、生效权限（模式+强制审批按空间 N 项/包级默认）、
    play 内容护栏行；
  - `space.mode=off` 警示条；读取失败诚实错误卡+重试（旧后端无绑定也是
    这条路，宁显示勿空白）；
  - 底部固定注明：壳层书斋/闲庭=界面导航（1B 拍板），本区是办公引擎空间
    策略，两套是定局——总闸画面不与导航混淆。

### 接线（新绑定全家桶）

wailsjs 重生成（`SpaceProfileView` 模型类）→ `types/session.ts` wire 类型 →
`bridge/core.ts` facade → `mock/core.ts`（浏览器演示态双空间样例，play 带覆写
练习 UI 三态）→ `spaceBindings.ts` shared → `bindingNames.ts` 603。

## 门禁

- Go 全量 0 FAIL（+`TestBuildSpaceProfileViews`：work 零配置/play 解析+产品
  默认权限+护栏/坏引用诚实未解析/mode=off 降级/nil 不炸）；gofmt 0。
- vitest +3（渲染三态+徽标/坏引用说人话/mode off+失败重试）；tsc 0
  （抓出 mock 数组字面量联合展宽，显式标注修复）、eslint 0。
- drift PASS@603、e-check OK、locale 零新增键（模型中心分区文案沿用
  该页 zh 硬编码先例）。
- 版本三处 sync 4.189.0；CHANGELOG + README 版本表 + 本说明。
- build strip + 冒烟 200 + SHA256（tag 树干净 worktree 构建）。

## 欠账

- 总闸画面收尾观察：功能域×空间策略的**生效路由交叉显示**（哪个功能域最终
  用哪个 provider entry）可另刀加厚；本轮空间策略分区已满足阶段二出口判据。
- `space_profiles` 的写路径（模型中心内编辑 profile）不在本刀（只读显形
  优先，写仍归 gaea.toml——boot 装配世界）。
- 壳内真机走查池不变（空间策略分区壳内实测挂真机池）。
