# v4.215.0 — 阶段六 6.2 首刀：记忆驱动项目本体——项目本体注入（2026-09-11）

> 阶段六「书斋纵深：办公主角」6.2 首刀。依赖：5.1 记忆语义图谱
> （v4.210）与引用链（v4.210/211）已就位。域内基线：
> docs/gaea-memory-graph-51-design-2026-09.md §6.2。

## 缺口

跨会话项目连续性此前只有被动形态：记忆索引折叠进前缀（v4.126）、晨报预载
（v4.16），但没有「这个项目的口径/规范/历史决策」的显式事实表——模型每次
都要从全量索引里猜哪些是本项目的硬约束，采纳了也无从引用。

## 落地

- **BuildProjectBrief 纯函数**（memory/projectbrief.go）：
  - 固化条全收（5.3 的 pinned 语义在这里第一次大规模变现：用户固化的
    = 确认过的口径/规范/决策），再补 project/feedback 型按衰减评分降序；
  - 每行带 `[MEM:<name>]` 稳定引用键——模型采纳后在句末引用，触达、
    前端徽标、悬空定稿闸（v4.210/211）全链同源；
  - 决策来源归因：`（依据 session-20260801-abc 会话 · turn 3）`（存量行
    无来源不造数，只标「固化」）；
  - rune 预算 600（projectBriefBudgetRunes），按行完整收录，截行不截半句；
    无候选返回空串=零注入，前缀逐字节不变。
- **注入点 = work 空间会话装配**（buildSystemPrompt，晨报预载同位）：
  进缓存稳定前缀，跨会话项目连续性；play/mode=off 不注入（work 专属）；
  CLI/TUI 不读该配置维持原行为（BootOptions.ProjectBrief 穿管，mirror
  MorningPreload 先例）。
- **可关闭（判据后件）**：
  - config `project_brief` 键（~/.gaea_config.json，默认开；
    KeyProjectBrief/KeyProjectBrief save fn/GetProjectBrief/SetProjectBrief）；
  - 绑定 `GaeaMemoryBrief` / `GaeaSetMemoryBrief`（写后 gaeaRebuildLocked
    新会话即时生效；GaeaSetMemoryBrief 按 GaeaSetMemoryEnabled 先例落
    Office 门面——mapGaea 前缀规则使然）；
  - MemoryPanel 头部「项目本体 开/关」胶囊（mirror 晨报预载开关）。

## 测试

- Go +2：固化优先+引用键 citations 正则可解析+决策归因文案 / 极小预算
  零注入+无候选空串+两次渲染逐字节一致（确定性）。
- vitest +1：开关读取初始状态+点击切换持久化（MemoryPanel 2/2）。

## 门禁

Go 全量 exit 0（117 包 0 FAIL）；drift PASS@613；tsc -b 0；版本三处 sync
4.215.0。未打 tag（随 v4.200+ 现状）。

## 产物

`build.bat`（wails build -ldflags "-s -w" -trimpath）→
`build\bin\gaea.exe` **48,683,520 B**（46.41 MB）；冒烟 `/api/health` 200；
Desktop\gaea.exe 双副本同哈希。
SHA256=**86A59C8BDF4D9C582B16B176C1BC6E338ACB3A12103D74519D1B582A63717CD3**
（`releases/SHA256SUMS-v4.215.0.txt`）。

## 后续（阶段六推进）

- 6.3 办公·多文件 DAG（≥3 节点跨格式任务图，每节点独立 steer/重跑/验收）；
- 6.4 DCMA 14 点体检 / 6.5 蒙特卡洛工期带（进度两翼）；
- 6.6 跨域 EVM（施工债级优先，可穿插）。
