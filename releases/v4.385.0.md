# v4.385.0 — 工程健康轮 #2：excelize 防线扩面 + 死代码清欠 + 双扫描报告

> 距 v4.370 工程健康轮已 15 版，按惯例复扫三面（govulncheck / pnpm audit / knip）。一处真实防御缺口修复（excelize 可达面扩面）+ knip 死代码清欠 4 项。Go 2 文件 + 前端 4 文件，绑定面 711 零变更。

## 扫描报告

| 扫描 | 结果 | 处置 |
|---|---|---|
| govulncheck ./... | 1 项在案（GO-2026-6452 excelize 负 shared-string 下标 panic，上游 Fixed in N/A） | v4.370 已防 xlsxpreview 入口；**本轮扩面**新追踪到的可达面（见刀1） |
| pnpm audit | 0 漏洞 | 无需处置 |
| knip@5 | 1 未用导出 + 3 未用类型 + 3 重复导出 + 5 配置提示 | 真死 4 项删除（见刀2）；重复导出/配置提示为既有 KEEP/低价值 churn 不动 |

## 落地（两刀）

1. **刀1 excelize GO-2026-6452 防线扩面**——
   - v4.370 只给 xlsxpreview（Render/NeedsRecalc）加了 panic 兜底；本轮 govulncheck 符号追踪多出三条此前未防御的可达面：`schedule.ImportXlsx`（GetRows）、`GaeaXlsxRowOps`（InsertRows/RemoveRow）、`GaeaXlsxColOps`（InsertCols/RemoveCol）——恶意/结构异常工作簿可在这些入口 panic（Wails 绑定 panic 行为未承诺）。
   - 同 v4.370 配方：入口 named-return + defer recover → 可读错误（「操作失败（文档结构异常）/Excel 解析失败（文档结构异常）」），panic 不再外溢。
2. **刀2 knip 死代码清欠（4 项，tsc+引用 grep 双验证）**——
   - `useModelCenter`（modelcenter/context.ts）：v4.352 state/actions 两通道拆分后的兼容口，全仓零消费；stale 注释同步更新。
   - 桥接死类型 3 个：`ForeshadowUrgencyPayload`、`NovelRewriteRequest`（bridge/novel.ts）、`SinBookSourceDownloadResult`（bridge/sin.ts）——仅定义无消费。
   - 重复导出 3 项与配置提示 5 项为既有 KEEP/低价值 churn，维持不动。

## 测试与门禁

- app/schedule 包全量绿（防线为纯增量 defer，既有行为零变化）；tsc 0；modelcenter 112 例绿。
- 全量 ci.ps1 绿 EXIT=0 + 版本漂移闸 OK@4.385.0。绑定面零变更。

## 坑

1. **「已防御」不等于「全防御」**——govulncheck 的符号追踪随代码演化增长（v4.370 后新增的 xlsx 编辑/导入功能引入新可达面），扫描要周期性复跑而不是一次性清账。
2. **Fixed in N/A 的上游漏洞是常驻项**——防线只能压在自家入口上；新增 excelize 消费面（读/写用户上传的工作簿）时必须同步配 recover 入口，建议纳入新功能检查单。
3. **knip 的「未用导出」要区分死代码与契约哨兵**——本轮 4 项删除全部经 grep 双验证零消费；重复导出（CostLibraryPage|default 等）是既有 KEEP 形态不跟风删。

## 升级说明

- 无行为变化：xlsx 行列操作/计划导入在正常工作簿上表现不变；结构异常工作簿从「进程内 panic」变为可读错误提示。
