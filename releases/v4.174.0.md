# v4.174.0 — 瘦身 P3 版3终局：bridge 双轨退役收官（wailsjsCompat.ts shim 删除，全仓生产代码零引用）（2026-09-09）

> 瘦身执行层第九版：轨道四「双轨」渐进退役**收官刀**。批次三全景收口——语音族/表情/章节族/小说状态族/场景生成族/角色库族/设置面板族全部迁 bridge，**`frontend/src/wailsjsCompat.ts` shim 删除**。
> 里程碑：全仓生产代码 wailsjsCompat import **清零**；LegacySurfaceNames 292→**184**（转正 108 方法）；余额 184 为有意保留的 legacy 直调面（RunChapterGate/MigrateProjectToV4 等非 UI 直调）。

## 1. 契约扩展批次三（两线共 42 方法转正）

- **3a +18**：VoiceBindings +9（VoiceStart/Stop/PlaybackDone/CancelTTS/PushAudio/SetPTTActive/WhisperClearSession/TTSSpeakBase64/WithParams）+ ImageBindings +2（GetTTSSpeakers/GetVoicePipelineConfig——**实挂 ImageB**）+ OfficeBindings +6（DataBackup* 六方法，mappings DataBackup*:"GaeaDataBackup*"）+ CoreBindings +1（ListProjects）；spaceBindings 360→378。
- **3b +38**：CharLibBindings +16（角色库族全量）+ NovelBindings +22（章节族 4/叙事状态族 5/GetEntityRelations/场景族 4/CancelCreateChapter→boolean/项目角色族 8）；**GetCharacters 同名核对**（charlib.ts 已有与 Go NovelB 同签名，直接可用）；drift 摘除 38（「v4.7x 小说革命」注释段更新 RunChapterGate 保留）；mock/charlib.ts 新建；spaceBindings 378→416。
- **3c +3**：QuickBrainstormBranches/CreateChapter(8 参)/DeleteOutlineNode（NovelB）；spaceBindings 416→419。
- **3d +4**：SetActiveASRModel/SetActiveTTSModel/SetChatVoiceModel（ImageB）+ VoiceHealth（VoiceB）；spaceBindings 419→423。

## 2. 业务迁移（8 条并发线 + 主代理收尾）

- **组1 语音族**（22/22）：useVoiceChat 8 方法（VoiceStop cleanup 双重 try/catch 保留；**VoicePushAudio 参数 Array→base64** 对齐 Go []byte 契约）+ VoiceSettingsPanel 4 方法（VoiceHealth 暂留→3d 补）+ ChatPage 5 直调 + ChatPage.test bindingsBridge 扩到 18 fn。
- **组2 cast 族**（9/9）：CreatePage/ChapterEditor 的 `App as unknown as {...}` cast 对象全部删除（**绑定再生成后 NovelB.d.ts 已含全部方法，cast 编译期冗余**）走 NovelB 门面具名导入；过期注释清理。
- **组3 角色库**（44/44）：api/characterlib.ts 18 方法迁 bridge（6 处 as unknown as；组件层经 adapter 零改动）。
- **组4 章节/小说角色**（7/7）：ChapterPage 4 方法 + novel/api/character.ts 9 方法 + 测试 mock 并入 bindingsBridge。
- **组5 收尾**：useBindState 4 方法 + DataPanel 9 方法（DataBackup* 短名化）+ 2 测试改 bridge mock。
- **CreatePage 收尾**（9/9）：7 处 App.* 迁 bridge（含 3c 补的 QuickBrainstorm/CreateChapter/DeleteOutlineNode），wailsjsCompat import 删除。
- **终局 3d**（47/47）：useVoiceState 5 方法 + VoiceSettingsPanel VoiceHealth + ChatPanel 4 方法（WhisperClearSession/GetVoicePipelineConfig/GetTTSSpeakers 等，整体 try/catch 保留）。
- **主代理收尾**：ChatPage.test/ChapterPage.test 的 wailsjsCompat 别名引用改 bindingsBridge 直取；SettingsPage.test 死 vi.mock 改 bridge；**wailsjsCompat.ts 删除**。

## 3. 主代理收口

- **wailsjsCompat.ts shim 删除**：全仓生产代码零 import（python 脚本实证）；剩余 24 处「wailsjsCompat」字样均为契约层/历史注释（bridge 转正记录，保留合理）。
- 度量：LegacySurfaceNames 292→**184**；spaceBindings 423；转正方法累计 **108**（批次一 16 + 批次二 29 + 批次三 42 + 元组修正）。
- **CharacterList 分类遗留裁定**：现 spaceBindings 中 CharacterList=work（v4.48 微信触点先例）而其它角色库族=play——层面归 play 但 CharacterList 本体未动属有意保留（微信 PersonaPicker 走 work 门面），不追改。

## 4. 门禁（全量复跑）

- vitest **2737/2737 首跑全绿**（313 文件——shim 删除后零回归；跨三批连续全绿）；
- `tsc -b` 0 · `eslint .` 0 · drift **PASS@602**（零绑定，方法名未变）· locale **0 死**（1448 键）；
- Go 全量回归绿（本版零 Go 改动，app 包 72s 通过）；
- 版本三处 4.174.0。

## 5. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 待 build ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.18 MB ｜ wailsjsCompat shim 已退役（生产零引用）
```

## 6. 产物与冒烟

- 产物：`gaea-v4.174.0.exe`（46.18 MB），SHA256=`AD7C7CED1066F7BFF077B2416D41145BEE207619868D411F9A39FDF518737E2C`（releases/SHA256SUMS-v4.174.0.txt）。
- 冒烟：`scripts/smoke.ps1` → **/api/health 200**（冒烟进程已回收）。

## 7. 收官意义与后续

- **bridge 双轨退役完成**：S2-3 兼容层 wailsjsCompat 退役，全部前端调用统一走 gaea/lib/bridge 代理（?mock=1 dev mock 回退 + BridgeError 错误归一）。massterplan 轨道四「双轨」目标达成。
- 后续瘦身项（P4 等）：config.go 58.7/engine.go 55.2/store.ts 54.3 二次拆分、entry 懒加载、exe strip；壳内真机池（rail 切换器/home 最近文档/.gsched 基线 N）、审计刀D 真机取证（printSvg print/拖拽/粘贴）不变。