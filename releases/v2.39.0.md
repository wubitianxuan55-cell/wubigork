# v2.39.0「3.0 架构主线 · Wave 3」（2026-08-15）

> 3.0 架构改造 Wave 3：Step 3b LLM Seam + Step 3c OCR/ASR/TTS Seam + Step 3d 分类统一与 8 处硬编码注册表化 + 前端 GetBoardManifests 接线。
> 四路并行实施（E 3b / F 3c / G 3d / H 前端），每轨独立提交 + 父代理集成提交（可分别 git revert 回退）。
> 架构：docs/2026-08-15-gaea3-architecture-design.md §5.3 Provider Seam（seam 三纪律：事件词汇/互斥注册/fail-closed）。

## Step 3b LLM Seam（d183af7）
- **定义**：provider.LLMProvider{Provider; Chat}，事件词汇（Chunk*/Usage/Completion）集中 llm.go；ChatFromStream 聚合 + streamChatAdapter 自动适配（注册即 LLM 能力）
- **提供者**：bridge init() 以 LLMKindWubigrok 互斥自注册；DefaultLLMKind=wubigrok（空 kind 缺省=现状）
- **消费者**：boot.NewProvider 经 NewLLM（gaea.toml providers[].kind 驱动 + fail-closed）；agent 聊天/子代理/Plan/judge/compact 只依赖 seam 接口
- 按引擎分支（herdsman/ollama 思考模式）以测试冻结；19 新测试

## Step 3c OCR/ASR/TTS Seam（078ce1d）
- **OCR**：OCRProvider{Name,ExtractImage}；ovis/tesseract 自注册；GAEA_OCR_ENGINE 驱动（auto=ovis→tesseract 保持现状，显式单引擎 fail-closed）
- **TTS**：TTSProvider{Name,Synthesize,SynthesizeWithMime}；edge/sapi/herdsman（工厂收敛 customvoice/voicedesign/voxcpm/voiceclone）/xai 自注册；TTSSpeakBase64 四级回退与 TTSSpeakStreaming 合成器链改注册表（TTSChain 首个成功即赢）
- **ASR**：ASRProvider{Name,TranscribeBase64,TranscribeBytes}；herdsman 自注册；voice.Manager SetASRProvider 接口注入（弃回调函数注入）
- isSTTModel 委托 modelengine.ClassifyModelByName（分类单源）；isTTSModelID 因 edge 关键词口径差异保守保留

## Step 3d 分类单源化 + 8 处硬编码注册表化（9a535c4）
- **分类单源**：modelengine 导出 ClassifyModelKind/ClassifyModelByName（llm/tts/stt/ocr/rerank/embedding/image 六桶，关键词表零变化）
- **#1 websearch**：6 引擎自注册 SearchEngine 注册表 + [search] engine_order 可配序
- **#2/#3 embed/rerank**：EmbeddingProvider/RerankProvider 注册表 + SetRetrievalRuntime（弃 HERDSMAN_BASE_URL 环境变量绑死）
- **#4 vision**：Provider 注册表 + SetVisionRuntime（删 GAEA_VISION_* 环境变量读取，未知 kind fail-closed）
- **#5 image_gen**：comfyui 裸字符串改 ai.ImageBackendKindComfyUI 常量
- **#6 OCR 工具**：gaeaSpecialistTools 集中注册进 ExtraTools（原缺注册修复）
- **#7 markitdown**：MarkdownConverter 注册表 + SetMarkdownConverterRuntime（cli 两级回退保持）
- **#8 billing**：FetchByKind 按 kind 注册（deepseek 形状默认）
- 6 个注册表测试文件固化

## 前端 GetBoardManifests 接线（b1cc2fd）
- loadBoardManifests 改调 wailsjs CoreB.GetBoardManifests（v2.38.0 wails build 生成），成功→normalizeManifests 合并替换活动清单，失败/空→fail-closed 回退静态 canonicalBoards
- normalize 差集：knowledge（D7 并入菜单第 9 项，BookOutlined 补图标注册表）/home（壳层补 isHome 唯一首位）/weixin（后端 page 空为准）/重叠 id 后端优先
- MainLayout subscribeBoards 订阅 + useReducer 强刷；main.tsx 补注册 KnowledgePage
- manifests.test.ts 新增 16 用例（45/45 过）

## 父代理集成（048768c）
- config.go：SearchConfig.EngineOrder + 新增 RetrievalConfig/VisionConfig/MarkdownConverterConfig 三节（零值=全默认本地端点）
- boot/sysprompt.go：装配 SetSearchEngineOrder/SetRetrievalRuntime/SetVisionRuntime/SetMarkdownConverterRuntime
- app 层 5 处 provider.New("wubigrok") → provider.NewLLM（成本/知识导入/汇总切 3b seam 接口）

## 验证
- go build ./... 干净；go vet 干净；**test-all.ps1 110/110 包**
- 前端 tsc 0 errors / eslint 0 errors / vite build 42.9s / vitest 420 过（27 个 jsdom localStorage 基线失败，零回归）
- TestBindingsCompleteness PASS（464 方法，无新绑定）；check-bindings-drift OK
- 冒烟 /api/health 200

## 发布
- 产物：gaea-v2.39.0.exe；SHA256：见 releases/SHA256SUMS-v2.39.0.txt
- 提交：d183af7(3b)/078ce1d(3c)/9a535c4(3d)/b1cc2fd(前端)/048768c(集成)

## 遗留（Wave 4 候选）
- semantic_search 工具仍定义未注册（#6 只含 OCR，是否启用由后续决策）
- billing kind 未从 ProviderEntry 贯通（controller.Balance 暂默认 deepseek）
- pickHerdsmanModel 为能力标签挑选（非分类重复），保留
- ModuleLauncher 启动器仍用静态 canonicalBoards（首屏 8 卡不变，后端新增板块不出现）
