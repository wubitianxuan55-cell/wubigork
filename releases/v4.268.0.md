# v4.268.0 — knip unused exports 甄别清账（2026-09-13）

## 用户口径

瘦身 P5 既定池项（next-stage-plan §100「knip unused exports 169 甄别」）。实跑 knip
（frontend/knip.json 口径）现报远小于存量记录：**11 unused exports + 6 unused
exported types + 8 duplicates**——169 为陈旧数字（期间巨文件拆解/死代码清偿已消化大半）。
本刀把可证死的清干净、契约哨兵挂 @public 保留、误报注明缘由。

## 甄别原则

- **全死（无任何引用）→ 删除**：MODEL_PRICES、SPINNER_WORDS、getLocale、
  nodeStageLabel、CharacterFormHelpers 的 Block/Field/FieldProps（仅 OrgField 在用，
  L 留作其依赖）、engines.ts 的 SemanticIndexStatus/BenchmarkRunDetail/BenchmarkCase
  （前端消费方用的是 lib/types 同名镜像）、bridge.ts 对
  BridgeError/PptxSlideTextEntry/SubagentTextEvent/_Check* 的死 re-export（本体在
  proxy/office/events/drift 各自文件里健在）。
- **双出口裁未用侧**：SecurityBanner/DataPanel/SecurityPanel/AnsweredByLine（默认
  导出在用，具名侧裁掉）、CostGraphView（具名在用，default 裁掉）、
  WhisperMemoryList 的 MemoryFact 再出口、exportArtifact 的 downloadBlob 再出口
  （**例外回退**：BaselinesPanel.test 的 vi.mock spy 委托真在用——knip 解析不到该
  引用属误报，挂 `@public` 消音并注明）。
- **契约哨兵保留 @public**：drift.ts 的 `_CheckAppBindings*` 两方向锁、
  spaceBindings.ts 的 `_NoStrayFacet/_NoMissingFacet/_NoFacetOverlap/
  IndependentBindingName`、wails.d.ts 的 NovelReadingAskTurn（文档契约）——
  AssertNever 契约零运行时成本，knip 视 @public 为已用。
- **knip.json ignore 补两条**：bindingNames.ts（check-bindings-drift.ps1 按文本消费
  的数据源）、bridge/drift.ts（契约档，re-export 裁掉后无模块引用但需保留）。
- **接受的重复出口（3 处不动）**：CharacterCard / mock shared emitMock+emit /
  CostLibraryPage——两侧都真实使用，属风格而非死代码。

## 测试

tsc 0；eslint 0 error；knip「Unused exports/Unused exported types」清零（余 3 处
接受的重复出口）；vitest 342 文件 2977 例全绿；零行为变化、零 Go 改动。

## 真机走查

零行为变化刀，轻量扫描（CDP）：sin/chat/schedule/home 四页导航渲染全过
（截图 .tmp/walk-v4268-sweep.png），window.onerror/unhandledrejection=0
（仅启动期一条 longtask 55ms 性能观测，非错误）。

## 门禁与产物

- ci.ps1 全绿（run2 终态：vitest 342 文件 2977 例；run1 为已知 rename 文件锁
  flaky，隔离复跑×3 绿）；版本三处 4.268.0；drift OK@642。
- exe 49,281,024B SHA256=10A2566AA62465F85FCA4FE0960DC4AA90C597583D0E66ECAE5E6D0FE18F15D5
  （build/bin 与桌面副本同哈希，冒烟通过：/api/health 200）。
