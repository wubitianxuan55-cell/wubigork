# v4.417.0 · 前端 UI 完善批三：硬编码色值收口 + 可访问性补尾

> 接 v4.415/v4.416 观察池。前端 6 文件，绑定面 720 零变更，Go 零改动，零功能删除。
> 另证伪一条池上大项（圆角令牌化），证据在案降级。

## 硬编码色值收口（跨主题对比度真实收益）

- **ContextView 趋势图例**（审计 #1 中危）：净增/净减图例 `text-[#22c55e]/[#ef4444]`
  裸 hex——绿在白底约 2:1；改 tailwind v4 变量语法
  `text-(color:--color-success/--color-destructive)`，明暗双主题自动对比达标。
- **WhisperTracePanel MiniBar 四色**（aff/sec/aro/dom 情绪维度条）：
  `#818cf8/#4ade80/#f59e0b/#f472b6` → primary/success/warning/--whisper-accent
  语义令牌。
- **WhisperEmotionPanel 六维条** `#60a5fa` → `--whisper-accent`（同文件其余消费
  同款，家族色统一）。
- **证伪保留（hex 豁免域，不收口）**：RelationGraph `#6b7280`×5 与 GraphView
  `#64748b` 为 SVG/3D canvas 字面量域（下游有 startsWith 校验与混色逻辑，
  var() 字符串会破坏判定）；TisorRadar 渐变 stops 属装饰插画域。均加注说明入池。

## 可访问性补尾（审计 #2/#4 收尾）

- **ChapterEditor 右键菜单键盘可达**（审计中危，最后一个菜单类违例）：三个可点
  div → `role="menu"` + `role="menuitem"` button；开启即聚焦首项，Esc 关闭，
  ↑↓ 在项间漫游；视觉零变化（button 样式复位 + 原 hover 逻辑保留）。
- **MemoryHub 总览计数加载占位**：overview 拉取中 rail 徽标首帧空白 → 显「…」
  （title=总览加载中），三态齐整：数字/「—」失败/「…」加载中。
- **CharacterLibEditor 大 Modal 可访问名**：closable=false 自定义眉部无 antd
  title → 补 `aria-label="角色档案编辑器"`（纯 attribute 零视觉变化）。

## 观察池证伪降级（证据在案）

- **圆角令牌化专项（222 内联+291 CSS px）**：审计前提「让主题圆角设置真正生效」
  不成立——`--md-sys-radius-*` 在 appStore 主题表中明暗恒为 8/12/16/28px（dS/lS
  同值），今日迁移=零用户可见变化+500 处搅动。降级为「仅当圆角需随密度/主题
  缩放时才立项」。间距栅格收敛同理维持观察。

## 门禁与产物

- 定向 4 套件 80 例全绿（ContextView/ChapterEditor/MemoryHubPage/CharacterLibEditor）；
  tsc -b 0；eslint 0 error；全量 ci 绿 + 漂移闸 OK@4.417.0。
- exe 见 SHA256SUMS-v4.417.0.txt（仅本地；冒烟 200 过）。
- 保留策略留 v4.414~v4.417 删 v4.413.0.exe。

## 文档

- CHANGELOG/README+releases/README（441→442+34 席插 v4.417 裁 v4.386）
- AGENTS 迁 1 插 1（一百三十五迁：v4.413.0 入 archive）+progress
