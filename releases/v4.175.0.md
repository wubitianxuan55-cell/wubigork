# v4.175.0 — 瘦身 P4 结构刀1：三巨文件拆分（config.go/engine.go/store.ts，>50KB 拆分池再 -3）（2026-09-09）

> 瘦身执行层第十版：P4 性能版结构前置刀——把 P3 遗留的 Go 侧两个 >50KB 源文件 + frontend 一个 >50KB 源文件纯结构拆分。
> 三线并发子代理（足迹互斥）+ 主代理收口；**行为零变化**（导出面/函数签名/JSON tag/默认值逐字节保留）。

## 1. 线A：internal/config/config.go（58.7→16.1KB，7 文件）

- `config.go` 16.1KB：核心骨架（funcMu + Load + resolveResourceDir/ResolveResourceDirForTest/DataRoot/dirExists）——import 去掉 math
- 新文件（同包 `package config`，消费方零改动）：
  - `config_keys.go` 8.6KB：全部 Key*/Default* 常量
  - `config_types.go` 14.7KB：configFile/Config 结构体（字段名/JSON tag 逐字节）
  - `config_features.go` 2.7KB：Get/SetFeatureModel(Enabled)
  - `config_prefs.go` 4.9KB：布尔偏好族（Sensitive/Office/ReadScreen/Intents/Offline/Failover/KeepWarm/AutoPreload/Morning）
  - `config_realtime.go` 0.9KB：RealtimeProvider/Model/APIKey
  - `config_save.go` 12.5KB：saveMu/Save/renameFile/saveConfigFile/saveSetters/parseBoolPtr
- 验证：gofmt 空 / go build / go vet / go test 全绿；逐段 Contains 断言校验 8 区逐字节命中。

## 2. 线B：internal/modelengine/engine.go（56.5→13.3KB，8 文件）

- `engine.go` 13.3KB：EngineType/常量/ModelInfo/EngineConfig/EngineStatus/Manager 结构 + IsLocal + NewManager
- 新文件（同包）：`engine_keys.go` 3.0KB（各 provider Key + GLM 端点）/`engine_custom.go` 7.0KB（自定义引擎 CRUD）/`engine_crud.go` 3.7KB（Ensure/Get/Save/DefaultModel）/`engine_connect.go` 3.8KB（Test/Refresh/BuildChatURL）/`engine_models.go` 12.6KB（fetch/分类/glmPing）/`engine_modelhub.go` 10.9KB（Unsloth Studio）/`engine_state.go` 4.0KB（stateFile）
- 核实：无 herdsman 专属函数（仅预置默认/IsLocal/分类关键词）→ 未建 engine_herdsman.go；45 func 无重复无丢失。

## 3. 线C：frontend/src/gaea/lib/store.ts（54.3→0.4KB 聚合入口，3 文件）

- `store.ts` 0.4KB：纯 `export *` 聚合（全部 63 处消费方 import 路径零改动）
- 新文件：
  - `store/controller.ts` 50.2KB：主 controller store（ToolStatus/Item/ControllerState + applyEvent/reducer/rebuildHistoryItems/useStore/useController 等全部主体逻辑）
  - `store/preview.ts` 3.3KB：usePreviewStore + PREVIEW_MAX_QUEUE（私有）
  - `store/commonts.ts` 1.5KB：useUpdatedFilesStore + useComposerInsertStore
- 验证：出口面对账 18/18（MISSING=NONE/EXTRA=NONE）；内容逐字节对账 963/963 行与 HEAD 一致；controller.ts 相对 import 深度调整（./bridge→../bridge 等 4 行）。

## 4. 主代理收口

- 足迹核验：17 项全在三个目标域（3 M + 14 新文件），无越界。
- >50KB 拆分池复查：Go 侧 **零 >50KB 源文件**（config.go/engine.go 已拆）；frontend 剩 locales ×3（纯数据字典，惯例不计）+ **mock/office.ts 51.2KB 浮现**（下轮目标）+ store/controller.ts 50.2KB（单 store 实例自然边界，>50 红线指导性，再拆需深拆 reducer 超行为零变化红线，裁定保留）。
- entry chunk **1190.55→1176.62kB**（store 拆分后体积略降）。

## 5. 门禁（全量复跑）

- Go **128/128 包 0 FAIL**（test-all.ps1）· vitest **2737/2737 首跑全绿**（313 文件）· `tsc -b` 0（store 拆分后消费方零改动）· `eslint .` 0 · drift **PASS@602**（零绑定）· locale **0 死**（1448 键）；
- 版本三处 4.175.0。

## 6. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 1176.62 kB gz 372.90 ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.18 MB ｜ Go >50KB 源文件 2→0
```

## 7. 产物与冒烟

- 产物：`gaea-v4.175.0.exe`（46.18 MB），SHA256=`729C942B1A21B4FE0DB242565808B8CE86DB6F5B812810C836DDEBE4065F9DB8`（releases/SHA256SUMS-v4.175.0.txt）。
- 冒烟：`scripts/smoke.ps1` → **/api/health 200**（冒烟进程已回收）。

## 8. 欠账（P4 续）

- **mock/office.ts 51.2KB** 浮现（下轮拆分）；
- entry 懒加载（MemoryHubPage 1.4MB chunk / GaeaPage 622KB / cynefin 690KB 按 tab 动态 import——P0 基线 §1 解剖建议）；
- exe strip（`-ldflags "-s -w"` 实测，P0 基线 46.22MB 对靶）；
- 壳内真机池（rail 切换器/home 最近文档/.gsched）、审计刀D 真机取证（printSvg print/拖拽/粘贴）不变。