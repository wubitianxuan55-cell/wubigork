# v4.64.3「任务管理树呼吸感优化——行高/间距/字号/层级微调」

> 发布日期：2026-09-04 ｜ tag：`v4.64.3` ｜ 基线：v4.64.2 ｜ 绑定面 **560
> 零变更**（纯前端呈现调整）｜ 用户点名：任务管理树过于紧凑。

## 改动（信息零删减，纯间距/字号/层级）

- **AgentTree 节点行**：行内 padding `py-1/pr-1→py-1.5/pr-1.5`、行内间距
  `gap-0.5→gap-1`、左缩进 `4px→6px`；标题 `11.5px→12px`；统计（错误数/
  ⚙工具数/耗时/token）`9.5px→10px`；模型徽标 `9px→9.5px`；状态点
  `6px→8px`（running 呼吸更醒目）；层级缩进 `depth×10→depth×12`；
- **节点卡之间**：`gap-1→gap-1.5`；
- **运行节点活动预览行**：`py-1→py-1.5`、外边距/内边距同步放大、字号
  `10px→10.5px`；
- **TasksWorkbench 区块**：「子代理」「本地模型工具」小节标题与内容留白
  加大（`pt-2 pb-1` / `pt-2.5 pb-1`）；本地模型工具行 `py-1→py-1.5`、行间
  `gap-px→gap-1`、状态点同步 8px、主行字号 `11.5px→12px`。

## 验证

- tsc -b / eslint 0；vitest 全量 **217 文件 / 1605 用例**（AgentTree +
  SubagentsPanel 15 用例定向复跑全绿，data-testid/结构零改动）；
- build.bat 冒烟 /api/health 200；重启实机走查（截图核对右栏任务树密度）。

## 产物

- gaea-v4.64.3.exe
- SHA256：见 releases/SHA256SUMS-v4.64.3.txt
