# v4.67.0「上下文标签页重设计：驾驶舱三区布局」（ui-ux-pro-max 技能刀）

> 发布日期：2026-09-04 ｜ tag：`v4.67.0` ｜ 基线：v4.66.0 ｜ 绑定面 **561
> 零变更**（纯前端呈现重组，功能零删减）｜ 用户点名「使用技能重新设计
> 上下文标签页」。

## 设计（ui-ux-pro-max → design-system/gaea/pages/context.md）

- 技能流程：`--design-system --density 8 --motion 3 --persist` 生成页面级
  设计文件（Master 星枢 v2 保持不动，页面规则继承其令牌体系）；技能采纳
  项=dense dashboard 密度（卡内 8-12px/区块 12px 栏距）、Subtle motion
  （≤200ms 过渡）、等宽字体承载数字、pre-delivery checklist。
- **布局：单列 8 卡堆叠 → 驾驶舱三区**——
  ①顶部总览条（融合原 统计卡/水位头/当前构成 三卡）：9 统计 chips +
  六分类分段堆叠条 + 水位 + 图例 + 缓存/成本 + 告警行；
  ②左主栏（过程轴）：趋势图 → 选中步骤详情（master-detail 就地联动）
  → 事件流；
  ③右辅栏 inspector（w-80，<1100px 回落单列）：「浏览器/文件活动/Agent」
  三 tab 渐进披露（偏好 localStorage gaea.context.inspectorTab，坏值回落）。
- 红线：八块信息一条不删，只重组；六分类语义色 CAT_COLORS 单源沿用；
  数据源/轮询/useLiveReload 语义不变。

## 测试同步（结构语义，非降覆盖）

- 水位断言 ≥2→1（两卡融合为一处显示）；文件活动/浏览器 4 用例先切
  inspector tab 再断言（tab 化后按需渲染）；「文件活动」tab 与卡标题
  同文改 findAllByText。13/13 全绿。

## 验证

- tsc -b / eslint 0；vitest 全量 **221 文件 / 1647 用例**；Go 全量
  （app+contextview 定向全绿）；mock 走查（?mock=1 dev + headless Edge
  截图 1600/1920 双宽）→ judge 视觉验收 **两图 pass、无 must_fix**；
  build.bat 冒烟 /api/health 200。

## 产物

- gaea-v4.67.0.exe ｜ SHA256 见 releases/SHA256SUMS-v4.67.0.txt
