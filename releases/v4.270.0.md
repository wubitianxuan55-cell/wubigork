# v4.270.0 — sin_illustrate 工具入册（原罪工具集 v1.2，拍板放行）（2026-09-13）

## 来源与拍板

维持轨走查池清完后向用户列明剩余拍板项，用户连续「继续」=按清单顺序放行。
设计档 §13.2 的「刻意不做」就此推翻：入册前提已满足——产物链（Artifacts 轨迹 +
落库回写 extra.illustrations）本刀补齐，且**单链路委托既有 SinIllustrate**（角色
锚点 + 参考槽门控 + 退回纯文本 + 画室台账 + sin/art 目录），不复制第二条出图链。

## 落地

### ① 工具入册（留存件逐字 + 循环侧补链）

- 工具集 6→7（order：outline 之后、export 之前——内容生成先于收尾动作）。
- `sin_illustrate`：prompt 必填，caption/size 可选；`ReadOnly=false`（出图落盘 +
  台账）；Description 明确与标记协议的分工（正文配图走标记，工具只在用户明确
  要图时用）并提醒「不必再为它写插图标记」防同图两生成。

### ② Artifacts 产物链（补 v4.262 留存的循环侧缺口）

- `sinToolTrace` 增 `artifacts []sinToolArtifact`（kind/path/caption）；工具实现
  `sinToolArtifactProvider` 接口，循环在 Execute 成功后取走并随 result 帧（含
  `artifacts`）与轨迹落库（extra.tools 同形）。
- **落库回写**：消息落库拿到 messageID 后，`sinPersistToolArtifacts` 把产物按
  `tool0..toolN` 并入 `extra.illustrations`——画廊/导出与正文标记图同一存储；
  cue 用 toolN 前缀避免与正文标记数字键互踩；逐条告警不阻断。

### ③ 前端过程卡缩略图

- SinToolTrace 增 artifacts 字段（types）；过程卡工具行展开明细渲染产物缩略图
  （附件通道转 data URL，caption 作图注，读取失败如实占位）；元数据「生成插图」
  + Image 图标 + 行首摘要取 prompt。

## 测试

Go +3（TestSinIllustrateToolArtifactAndText：真出图+Artifacts 记录+路径落原罪目录+
返回文本讲给模型；TestSinIllustrateToolRejectsBadArgs：坏参数如实报错不留产物；
TestSinIllustrateInToolSet：order 位次）+ TestSinPersistToolArtifacts（回写
tool0+messageID<=0 跳过）+ 计数锁 6→7 + wantReadOnly；vitest +2（缩略图渲染/
读取失败占位）+1（图文导出并列的标签断言）；零新 App 绑定（drift 642）。

## 真机走查

见文末走查行（**部分完成，如实记录**：live 模型调工具未验成，留池补验）。

## 门禁与产物

- ci.ps1 全绿；版本三处 4.270.0；drift OK@642。
- exe 与 SHA256 见文末产物行。

## 走查行（如实）

- **已完成**：桥接建数据+桥验同 pid 版本读回 ✓；组件级缩略图渲染 ✓（vitest）；
  工具集 7 工具在位 ✓（Go）。
- **未完成（留池补验）**：live 模型调 sin_illustrate 的端到端。两次尝试均未走到：
  第 1 次=空间持久化（书斋）导致 shelf 定位失败（脚本已修：先点 .ml-space-btn.is-play）；
  第 2 次=模型回合未落任何消息且日志无流错误，同时 05:19:38 出现一次非本脚本发起的
  壳启动（疑似用户活动）——按「用户在用不动真机」红线立即停止活体操作。
- 补验口径：等模型环境稳定 + 确认用户闲置窗口后，跑 .tmp/walk-v4270-illustrate.mjs
  （已含空间切换修正），断言轨迹 artifacts/图片落盘/过程卡缩略图/extra.illustrations
  toolN 回写。
