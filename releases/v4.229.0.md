# v4.229.0 — 任务表会话维度结构刀 + gongwen 技能页码/红头字色检测（2026-09-11）

> 并行双线一版销两项（文件面零交叠：A 线=tasks/db/TaskCenter，B 线=技能包目录）。
> 另有 AGENTS.md 水位五迁（housekeeping）。

## 线 A：任务表会话维度（v4.180 既定欠账）

v4.180 用户报障「任务管理没与当前会话关联全是固定内容」当轮以「会话待办区」
（todo_write 活动流投影）止血，并留欠账：任务表本身无 session 维度。

- **SchemaV20**：`ALTER TABLE tasks ADD COLUMN session_id TEXT NOT NULL DEFAULT ''`
  （旧行零成本回填；迁移编号先查迁移链——migrations 切片 V19 终点 +1）。
- **Task.SessionID**（`json:"session_id,omitempty"`）+ **SubmitSpaceSession**：
  Submit/SubmitSpace 签名不变委托新方法；INSERT / 5 处 SELECT / scanTask 全链带列。
- **诚实口径（关键取舍）**：全仓扫描 6 个任务创建点（文件索引 rebuild/watch/轮询
  ×3、价格抓取手动/单个/全部×3）全为 cron/设置类、拿不到会话上下文；子代理运行
  与 DAG 节点是独立数据系统（SubagentRuns/DagList）不写 tasks 表。**session_id
  如实全空，停在能力层，不造假接线**。
- **前端就位但熄灯**：TaskCenter 增 `sessionPath` 可选 prop +「本会话/全部」过滤
  chip（渲染层 useMemo 过滤零请求、gaea-task 事件增量天然生效）+ 三语键 + 4 用例；
  **TasksWorkbench 暂不传 sessionPath**——现有数据下 chip 恒空 = 半成品面，等首个
  会话上下文创建点（如未来 agent 触发的后台任务走 tasks.Manager）一行点亮。
- 零新绑定（624 不变）：新字段随 GaeaTaskList JSON 自动带出。

## 线 B：gongwen-9704 技能页码/红头字色检测（v4.223 立项余项）

「页码与红头字色图形检测（需页脚/字色层），按需另刀」——纯技能侧落地，规范知识
零进内核（红线）。

- **docx_layout.py**：①页脚解析——读 `word/footer*.xml` + 从 document.xml.rels
  反查 footerReference 归位 ref_types；PAGE 字段判定用 `\bPAGE\b`（防 NUMPAGES
  误认）；报页脚字号与全文（含一字线包数字样式「— 1 —」）。②字色采集——run 级
  `w:color` 按值聚合（`auto` 不采），记计数/所见最大字号/样例文本（截 20 字）。
  顶层新增 `footers`/`colors`，旧键一字不动；缺数据=空数组不报错。
- **SKILL.md**：排版细则表 6→8 项——页码（4 号半角宋体阿拉伯数字、一字线包数字；
  版心下边缘/单双页空一字取不到注「不适用」不误报）与红头字色（红色系大字；
  无颜色信息=不适用，红头或为图片宁缺勿误）；description/Overview 分层口径同步。
- **测试**：新增 `scripts/test_docx_layout.py`（stdlib unittest，zipfile 临时目录
  现场构造最小 docx 夹具）4 用例：素文档回归、页脚三形态（NUMPAGES 不误认/未引用
  照报）、字色聚合（auto 不采）、顶层键序稳定向后兼容——4/4 全过。

## Housekeeping

- AGENTS.md 五迁：v4.208/v4.207-205/v4.204/v4.203 四条（4770B）整段迁入
  docs/archive/agents-version-history-2026-09.md，水位 61252→56472B 回落 WARN 线下。

## 门禁

ci.ps1 全绿（go build/vet/test + eslint + vite build + vitest 全量 + E 系守卫 +
仓库卫生守卫）/ 绑定 624 零变更 / 版本三处 4.229.0。

## 打包记录

- 产物：`build\bin\gaea.exe` **48,850,944 B** SHA256 `47647315537c345a657e4b1b78bced23563702dbdf0954d54300b92978c17b2d`
  （冒烟 /api/health 200 + Desktop 副本同哈希；exe 不入库，只存哈希）。
