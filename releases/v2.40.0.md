# v2.40.0「3.0 架构主线 · Wave 4：Step 3 收官」（2026-08-15）

> 3.0 架构改造 Wave 4：Step 3（Provider Seam）遗留收官——semantic_search 工具注册、BalanceKind 从
> ProviderEntry 贯通、ModuleLauncher 清单化。两条子代理轨并行实施（Go 收官 / 前端清单化），
> 每轨独立提交 + 父代理集成提交（可分别 git revert 回退）。
> 架构：docs/2026-08-15-gaea3-architecture-design.md §5.3 Provider Seam（seam 三纪律：事件词汇/互斥注册/fail-closed）。

## semantic_search 工具注册（gaea_specialist_tools.go）
- 决策：纳入注册（此前「是否启用待决策」）。semantic_search 实现完整且有 E2E 测试，能力面板
  「本地专业模型」分组声明了它，config 工具文档也收录——从死代码恢复为办公 agent 可用工具
- gaeaSpecialistTools 现在集中注册 ocr + semantic_search 两个专业工具，ExtraTools 装配展开
- TestSpecialistTools_Registered 断言同时含两者，杜绝回归

## BalanceKind 从 ProviderEntry 贯通（config → boot → controller → billing）
- ProviderEntry 新增 `balance_kind`（可选；空 = 历史默认 deepseek 形状）
- boot.NewProvider 组装时透传 entry.BalanceKind → control.Options.BalanceKind → controller
- controller.Balance 改用 billing.FetchByKind(kind, url, key)：切换余额后端只改配置、代码零改动，
  未知 kind fail-closed（拒绝运行而非静默降级）——补齐 Step 3d #8 的消费端贯通
- 测试：config TOML 往返保留 / control 层自定义 kind 路由 + 空 kind 默认 deepseek + 无 url 返回
  (nil,nil) / boot 层贯通断言

## ModuleLauncher 清单化（4a6033a）
- 新增 boards/launcher.ts 纯函数模块：deriveLauncherModules(list, descMap) + LAUNCHER_DESC 文案
- ModuleLauncher 改 useSyncExternalStore(subscribeBoards, getActiveBoards) 订阅活动清单，
  删除静态 canonicalBoards 引用——后端合并清单（含 knowledge 板块）变化后首页启动器自动跟随
- 默认 8 卡（chat/novel/imagegen/gaea/memoryhub/modelcenter/characterlib/settings）像素回归锁定；
  后端并入 knowledge 后自动多出「知识库」卡（9 卡，desc 兜底=label）
- launcher.test.ts 7 用例 + manifests.test.ts 36 用例（43/43 过）

## 验证
- go build ./... 干净；go vet 干净；**test-all.ps1 110/110 包**
- 前端 tsc 0 errors / eslint 0 errors（350 存量 warnings）/ vite build 17s / vitest 427 过
  （27 个 jsdom localStorage 基线失败，与 v2.39.0 一致零回归）
- TestBindingsCompleteness PASS（464 方法，无新绑定）；check-bindings-drift OK
- 冒烟 /api/health 200

## 发布
- 产物：gaea-v2.40.0.exe（33.1MB）；SHA256：f7c5fd1bc1859b025a742dcb78b26065a5718d8aa4374ef5c8cd90d7aaaff317
- 提交：4a6033a(前端 ModuleLauncher 清单化) / 89d9fae(Go 收官：semantic_search + BalanceKind) / efa72dc(发布)

## 遗留（下一会话）
- Step 3 收官完成；3.0.0 发布条件（Step 0-3 全部落地）仅剩统一回归与版本发布
- pickHerdsmanModel 能力标签挑选保留（非分类重复，无动作）
