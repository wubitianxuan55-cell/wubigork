# v4.317.0 · 阶段七第四刀：journal 历史蒸馏（重复模式挖掘 + 结晶审阅 + 审计链，7.2-2）

> 日期：2026-09-15 · 用户指令「继续并行使用子代理，优化迭代 gaea」。
> 规格：`进度计划/gaea-journal-distill-7-2-2-20260915.md`（契约先行 → A/B/C 三线并行 → 主代理收口）。
> 来源：`docs/gaea-stage7-plan-2026-09.md` §2 7.2-2——依赖 7.1-1 评测（v4.312 已落）已解锁，
> 7.2-1 审阅管道（v4.313 已落）直接复用；**7.2 技能结晶双入口就此齐装**（演示式录制 + 历史蒸馏）。

## 论点

journal 证据链（`.gaea/work/journal/*.jsonl`，每卡=一次已应用变更）是书斋审批链的既有沉淀。
本刀把它变成程序性记忆的矿床：**确定性挖掘跨会话重复 ≥3 次的工具应用模式 → 建议卡只提示
不打扰 → 用户点「结晶为技能」→ LLM 从多次执行回放蒸馏草稿 → 复用 7.2-1 审阅弹窗编辑落盘 →
决定与结晶全程落审计状态**。差异化空位（调研 §5.1）：Letta Mods / Bionic Skills 均无历史挖掘式。

## 落地

1. **纯函数包 `internal/skilldistill`**（零 IO 表驱动，先例 routesuggest/novelgate）：
   `MineFlows`（按会话聚合、(At,Tool,Target) 确定性排序、空值跳过）+ `Distill`（连续同
   (Tool,Shape) 折叠 → 会话级序列**精确匹配**分组 → Repeat=不同会话数 ≥3 → 步数 ∈[2,12] →
   ignored 静默 → repeat 降序/同数 ID 升序 → 截 3 条宁少勿扰）+ `BuildPatternID`（"jd-"+sha256
   模式串接前 8 hex，确定性幂等）+ `ParsePatternID`（形状校验，残渣防御）+ `PatternLine`。
   V1 范围裁决：滑窗/子序列挖掘不进（误报风险，真实数据先行——观察池）。
2. **App 管道 `internal/app/gaea_skill_distill.go`**（三绑定，OfficeB +3）：
   `GaeaSkillDistillCandidates`（journal 现算只读零 LLM；ignored∪crystallized 双静默）；
   `GaeaSkillDistillDraft`（重算候选按 ID 找 → buildJournalReplay 纯函数：≤3 最近会话
   「── 第 k 次执行 ──」分段 + AfterSummary 200 rune 截断 + 整段 600 rune 防护 →
   `prompts/skill-from-journal.json`（RTCO：只依据回放不虚构/**多次执行共性进步骤、差异进
   cautions**/文件名通用化占位）→ RetryJSON 2 → parseSkillDraft → 复用 7.2-1
   SkillRecordResult 只回不落盘）；`GaeaSkillDistillDecide`（decision∈{ignore,crystallized}；
   crystallized 必带技能名，审计条目=哪些会话+何时+结晶成哪个技能；ID 防御委托
   skilldistill.ParsePatternID）。状态 `<DataRoot>/skill_distill_state.json`
   （route_suggestions 同款容错读 + temp+rename 原子写）。
3. **前端**：`SkillDistillSection`（候选卡=PatternLine 步骤序〔工具名 code〕+「重复 N 次」
   徽章 + 最近日期 + 证据 Collapse + 〔结晶为技能〕/〔不再提示〕同卡互斥 loading；空态两分
   不可用/暂无）挂 MemoryPanel 建议 tab「流程蒸馏」分区（props 全可选，缺省隐藏——既有
   调用方零改动）；`SkillRecordModal` 加 `preload`/`onSaved` 可选（preload=外部已蒸馏直接
   回填，缺省与 7.2-1 逐字节一致）；`useSessionHandlers` 四 handler（refresh/draft/ignore/
   crystallize——保存成功 Decide("crystallized") 落审计后刷新）；建议 tab 首开自动拉一次候选
   （收口补：只读零 LLM，fetch 期显示 loading 而非「不可用」误判）。三语 `memory.distill.*`
   11 键三语键集相等。
4. **收口契约面**：bridge/core.ts +3（SkillDistillView 局部重述类型经 types/memory.ts，
   herdsman 防环先例）+ mappings +3 + mock/chat.ts 三桩（走查样本候选）+ spaceBindings
   work +3 + bindingNames 再生。

## 绑定面

684→688 后再 +3 = **688→691**（OfficeB +3：GaeaSkillDistillCandidates/Draft/Decide）；
work 锁 510→**513**；drift OK@691；TestBindingsCompleteness 绿。

## 出口判据对账（阶段七 §2 7.2-2）

- ① **只提示不打扰、拒绝即静默** ✅：Decide ignore → 状态文件 → 复算跳过，测试钉死；
- ③ **审计链完整**（哪次历史/谁确认/哪版）✅：crystallized 条目带 sessions+skill+decidedAt；
- ② **结晶技能调用 ≥5 次且成功率可见** ⚠️ **欠账如实入档**：需技能调用计数机制
  （statsFile v3 先例另刀），本刀先落建议+审阅+审计三件。

## 测试

- Go：skilldistill 15 例（聚合排序/连续折叠/精确匹配分组/同会话不重复计/门槛与步数区间/
  ignored 静默/ID 确定性与异模式异 ID/排序截断/证据行截断/ParsePatternID 矩阵）；
  app 10 例（状态往返含审计条目/损坏容错/回放分段与截断/Candidates 端到端 3 会话→
  Repeat=3+最近优先/ignore·crystallized 双静默复算/目录缺失 Available=false/空 App 守卫/
  Decide 校验矩阵）。
- 前端：SkillDistillSection 6（渲染/折叠/逐参+互斥 loading/空态两分）+ SkillRecordModal 6
  （4 既有零改动 + 收口补 2：preload 不调内部蒸馏直接回填、保存后 onSaved 收技能名）+
  MemoryPanel 2 既有零改动 + spaceBindings 锁 513。
- 门禁：tsc -b 零错（双向漂移防线过）、eslint 零告警、ci.ps1 全绿（见下）。

## 执行纪律对账

三线并行（A 纯函数包 / B App 管道 / C 前端）足迹互斥；**A 线子代理跑满上下文零落盘
两次检查点未应——主代理接管代写**（v4.316 先例再+1：子代理卡死判据）；B 线 40 分钟交付
10/10；C 线交付 12/12；生成动作（gen_bindings/bindingNames 再生）主代理统一执行，
bindings_novel.go 368 行重排噪音甄别还原（v4.313 坑再证）。

## 产物

- exe：50576384 B，SHA256=e5577402da81afbf0b0135631db066494aa7e415455da67eee2dbe9b0d5e3d2d
- `releases/gaea-v4.317.0.exe` + `releases/SHA256SUMS-v4.317.0.txt`（桌面副本同哈希）
- 冒烟：`/api/health` 200 过

## 文档

CHANGELOG/README/.gaea AGENTS 速览（迁 1 插 1）+progress+todos；规格书随刀入库。

## 观察池

- 判据②技能调用计数与成功率可见（欠账，另刀：statsFile v3 先例）；
- 滑窗/子序列模式挖掘 V2（等真实 journal 数据分布）；
- journal 回放纳入 verdicts（复核结论）过滤；
- 真机走查：多会话同型任务 → 建议卡出现 → 结晶 → 新会话命中技能一条龙（挂闲置窗口）；
- releases/README V4_RECENT 实存条数与「最近 34 版」头注不符（v4.315 前已存在，非本刀引入）。

## 未做（下刀）

7.3-1 任务收件箱（收窄版任务空间制首刀）或 7.2-2 判据②调用计数小刀；闲庭在册清欠沿既有列车。
