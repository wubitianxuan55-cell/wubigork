# wubigrok v5.34.0 — 轻语模块深度对照 ackem 补齐

**发布日期**: 2026-07-29

## 概述

全面对照 ackem 项目，逐文件比对并补齐 wubigrok 轻语（whisper）模块。轻语就是 ackem 的完整移植，唯一差异是 LLM 调用使用 wubigrok 模型中心替代 ackem 内置客户端。

## 变更统计

| 维度 | 初始 | 最终 | 变化 |
|------|------|------|------|
| whisper 文件数 | 53 | **65** | +12 |
| ackem 覆盖率 | 28% | **34%** | +6% |
| P0 Bug 修复 | — | **6** | FSM/情绪/衰减/打断/Sprintf/import |
| 新增功能 | — | **30+** | 解释器/记忆/管线/Canon/人格/成人/LLM接口 |

## P0 致命 Bug 修复

| 文件 | 问题 | 修复 |
|------|------|------|
| `relationship.go:168` | merged 缺 Stage 字段 → FSM 永远卡在空值 | 添加 `Stage: prev.Stage` |
| `memory_fact.go:ScoreRelevance` | Confidence(0-1) 替代 valence(-1~1) 做情绪比较 | EmotionalContext.valence |
| `memory_habit.go:DecayAndCleanup` | 衰减对象搞反（短时→长时） | 修正为长时衰减+降级 |
| `voice_manager.go:PushAudioChunk` | 打断检测缺时间阈值累积 | interruptSpeechMs 500ms 累积 |

## 新增 12 个完整文件

| 文件 | 对齐 ackem | 功能 |
|------|-----------|------|
| `context.go` | `context.ts` | 系统提示词组装器（Tier A/B/C + psyche + 扩展） |
| `post_chat_turn.go` | `postChatTurn.ts` | 对话后管线（工作记忆→事实写入→关联纠正→伴侣日志） |
| `memory_ingest.go` | `memory/ingest.ts` | 记忆摄入管线（LLM 事实抽取+三元组+情节生成+退役） |
| `adult_mode.go` | `prompt/adult-mode.ts` | 成人模式守卫（安全门禁+状态机+29人格×表达策略） |
| `llm_client.go` | — | LlmClient 接口 + 6 种 LLM 结果类型（接入模型中心） |
| `memory_write_job.go` | `memory/memoryWriteJob.ts` | 异步写入队列（会话串行化+摄入+主动遗忘） |
| `attention_budget.go` | `policy/attentionBudget.ts` | 注意力预算（频率控制+全局免打扰） |
| `proactive_personality.go` | `companion/proactivePersonalityContext.ts` | 主动消息人格上下文+消息类型选择+骚扰模式 |
| `finalize_new_facts.go` | `memory/finalizeNewFacts.ts` | 记忆写入追踪+用户信息块组装 |
| `user_dossier.go` | `memory/userDossier.ts` | LLM 用户档案生成+system prompt 注入 |
| `health_policy.go` | `policy/evaluate.ts` | 健康检测（久坐/深夜/喝水提醒） |
| `holiday_detector.go` | `temporalAwareness/holidayDetector.ts` | 节假日检测（公历固定+农历2026-2030+浮动） |

## P1 功能补齐

| 文件 | 改动 |
|------|------|
| `interpreter.go` | 动态 intensity/sincerity 计算、DnD 结构化返回(DnDResult)、30 英文脆弱词、英文问句正则、tease 信任门控、长消息/二次兜底 |
| `types.go` | MemoryFact 新增 EmotionalContext/UpdateTrail/DerivedFrom/FactLayer 4 字段；PersonalityTemplate 类型 |
| `memory_fact.go` | ScoreRelevance 使用 EmotionalContext.valence；ComputeMoodBoost 修复 |
| `memory_retrieve.go` | ComputeMemoryEchoFacts 使用 EmotionalContext 驱动 |
| `memory_assoc.go` | Weaken/GetLastActivated/ClearActivated 方法 |
| `memory_episode.go` | Latest() 方法 |
| `params.go` | MemoryEchoAroIntensityWeight/DomTrustWeight 常量 |
| `personality.go` | 6 种人格详细模板(tsundere/yandere/deredere/kuudere/genki/oneesan) + BuildPersonalitySection |
| `canon.go` | 从 4 行扩展为 11 条法律条文（冒充防护/父亲称呼禁止/Tier B 不可改写） |
| `companion_proactive.go` | 从 3 规则扩展为 6 种消息类型 + 人格感知 prompt |
| `orchestrator.go` | AssocIndex 字段；成人模式委托到 adult_mode.go |
| `channel_turn.go` | ApplyLLMReply 接入 FinalizeTurn 管线 |
| `whisper_handler.go` | App 实现 LlmClient 接口；EnqueueMemoryWrite 异步管线 |

## 全链路

```
用户消息 → InterpretInput(L0) → UpdateRelationship(L1) → EmotionStep(L2)
→ BuildPsycheBlock(L3) → AssembleMessages(context) → 模型中心(LLM)
→ FinalizeTurn(postChatTurn) → MemoryIngestPipeline(ingest)
→ EnqueueMemoryWrite(writeJob) ✨
```

## 构建

- 全量编译通过
- 20/20 测试包通过
- whisper 52 测试全部通过
- 产物: `C:\AI\wubigrok\build\bin\wubigork.exe`

## 下一步

- P0: 提示词系统剩余 23 种人格模板 + 情绪融合
- P1: LLM 驱动模块（factExtractor/consolidator/contradictionDetector）
- P1: 分发系统（dispatch/candidateCollector/contextPipeline）
- P2: 向量存储/语义搜索
