# gaea v2.31.0「质量收敛 · 记忆·生命周期与审计」

> 阶段 6 第八刀（T6-8），2026-08-14。父代理 Go 实现 + 3 子代理并行（前端接线 / 索引截断 / 组件补测）。
> 规划：docs/superpowers/plans/2026-08-14-gaea长期规划-阶段6-质量收敛.md

## 发布内容

### T6-8.1 dream 路径决策与审计（3 测试）
- **决策成文**：docs/DREAM_WRITE_POLICY.md + gaea_dream.go / SaveDreamFacts 注释——dream 写入
  **不**纳入 hardAskTools 逐条审批（后台异步 90s 超时无法等人工确认；显式路径即用户触发），
  补偿 = 每次实际写入落审计日志，全程可追溯
- SaveDreamFacts 签名改 (source string, facts)（source=auto_dream|explicit，条数+名称）；
  审计行追加 <userDir>/dream-audit.jsonl（JSONL 追加式、损坏行跳过、尽力而为不阻断写入）
- DreamAuditEntries(userDir, max) 读取入口（倒序最近 N 条）
- 测试：自动/显式两路径各断言 1 条审计行（source/saved/names）+ 记忆未配置安全跳过

### T6-8.2 facts 生命周期清理（7 测试 + 2 绑定 + 前端按钮）
- **保留策略**：归档超 90 天（memory.ArchivedRetention）硬删；sqliteBackend/fileBackend 双后端
  CleanupArchived（返回被删行含溯源字段：名称/描述/正文/归档时间/来源会话/来源消息）
- **分页**：ListArchivedPaged(limit, offset) 总量+当前页（limit 钳制 [1,200]/默认 50），防全量返回
- **新绑定**：GaeaMemoryCleanupArchived / GaeaMemoryArchivedList（gen_bindings **462** 方法 +
  TestBindingsCompleteness PASS）；清理逐条 slog 日志 + 溯源审计 purge-audit.jsonl
  （GAEA_DATA_ROOT 可隔离，测试不触真实用户库）
- **前端**：「归档」tab「清理超期归档」按钮（Modal.confirm 危险确认 → message.success → 刷新）；
  bridge AppBindings + gaeaToGaea 映射 + mock 实现 + types 对齐
- 测试：memory 包 5（SQLite 清理/无超期不误删/分页边界/文件后端清理/文件后端分页）+ app 2
  （清理幂等+审计落盘、分页绑定）

### T6-8.3 索引截断按边界（6 测试函数）
- **口径统一**：memoryIndexBudget（3000 runes）→ memoryIndexBudgetBytes = **4096 字节**，
  与 Block() 全块 4096 字节阈值同口径（注释成文）
- truncateIndexByLines 纯函数：预算内最后一个 '\n' 处按**行边界**截断（不切半行）；
  **markdown 链接保护**（未闭合 "[" 或 "](url" 的 ")" 被截 → 回退到链接起始行之前整体舍弃）；
  只在 ASCII '\n' 处切 → UTF-8 安全不产生半个 rune；单行超预算宁丢整行不切半字
- 截断提示文案与旧实现逐字一致；预算内原样返回不追加提示
- 测试：行边界/字节预算/rune≠byte（中文+emoji）/链接完整性 5 子测/单行超预算；既有 recall 预算回归全绿

### T6-8.4 前端组件补测（13 vitest 用例）
- **GraphView 5 用例**：3d-force-graph 链式 stub 替身（vi.hoisted）；工具条与 "3 节点 · 2 边" 计数、
  类型过滤重构图（graphData 收到过滤后 nodes/links）、节点点击详情 Modal、空数据不崩溃、
  variant="home" 隐藏工具条
- **WhisperMemoryLibrary 8 用例**：domain 分组（身份×2/社交×1/未知归「其他」）、三种关键词搜索过滤
  + 清空恢复、情节 tab（情绪 emoji/强度条/关键词 chips/轮次）、事实与情节详情 Modal、
  导出归档链路（PickDirectory + WhisperExportArchive 调用）、事实/情节双空态
- 组件实现 **0 改动**（纯测试）；桥接 vi.mock("../../lib/bridge") 注入确定性数据

## 验证
- Go：go build ./... ok、go vet ./... 干净、TestBindingsCompleteness PASS（462 方法）；
  go test ./... 全量：internal/app 15.5s ok、control/memory 全绿；hook/skill 两包在本环境被 AV
  实时防护锁 test.exe（fork/exec Access denied，历史日志 go_full_final2/go_full_retry 均为 ok、
  本刀未触碰这两包）——环境抖动，非代码回归（同 v2.26.0 记录的 AV 锁先例）
- 前端：tsc 0 errors、vitest **348/348**（78 文件，+14）、eslint 0 errors（38 存量 warnings）、
  vite build 23.84s ok
- 冒烟：scripts/smoke.ps1 通过（/api/health 200）

## 产物
- gaea-v2.31.0.exe（32.7MB，SHA256=35A8445738A6A1D7991F32338C39060F44B34948020467225029CF049940AA71）
