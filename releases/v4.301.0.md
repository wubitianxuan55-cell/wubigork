# v4.301.0 — t4-C1 分析代理 V2 化：9 维结构化 + 三维评分联动 + analysis-v2.json 落盘（2026-09-14）

## 来源

MuMuAINovel 蒸馏线 t4 情节分析域首刀（`docs/distill/04-plot-analysis.md` §8.3 C1：AnalyzeChapter 升级——结构化 9 维度 + 三维评分 + 建议联动 + keyword 锚点，改造 `internal/analysis/analysis.go` + `prompts/analysis-chapter.json`）。**市场调研直接抬升优先级**（docs/gaea-market-survey-2026-09.md §9 增量轮）：Novelcrafter Codex 手工建档摩擦、Sudowrite 书长上下文极限、蛙蛙设定库主打——「设定库自动回填」是长篇一致性的行业痛点，而 V2 分析载荷正是 gaea 自动回填（t3-P1 记忆生产者）与重写消费（C3）的数据底座。

## 落地

- **模板 V2 九维化**（`prompts/analysis-chapter.json`，原地升级）：输出 schema 从旧 5 键（hook/conflict/emotion_curve/key_events/scene_rhythm/quality_score）升级为 V2 九维全量（hooks/conflict/emotional_arc/character_states 差分/plot_points/scenes/pacing/dialogue_ratio+description_ratio）+ `scores`（P/E/C 一位小数）+ `suggestions`（数量与分数联动）+ `summary`（供记忆/检索）。t1-P2 的伏笔追踪任务指令、候选清单槽位（P1）、三条强约束与自检条款**原样保留**——foreshadows 数组即 `types.ForeshadowHit` 契约，自动回收链路零改动。
- **服务端权威归一**（`normalizeAnalysisV2`）：三维钳位 [1,10]；**overall 一律重算**（`ComputeOverallScore` 唯一实现，模型自报值丢弃，禁止打安全分的机制性保障）；建议条数按 `SuggestionCountForOverall` 联动**只裁上限不代拟**（overall<4→≤5 条、<6→≤4、<8→≤3、≥8→≤1）。
- **落盘 analysis-v2.json**（`project.UpsertAnalysisV2`，按章号 upsert=PlotAnalysis 的 chapter_id unique 语义同源；章号升序；原子写）：`ChapterAnalysisResult` 携带章号/文件名/时间戳/引擎/模型/`analyzer_source`。容错注入：落盘失败只记日志不影响分析返回。
- **旧 wire 零破坏**（`deriveLegacyAnalysis`）：`AnalyzeChapter` 绑定返回键零变化——hook 取首钩、conflict 取描述、情感曲线=主导情绪：轨迹、关键事件=推进点前 5、节奏枚举映射中文、质量分=overall 取整、角色状态 V2 差分映射；前端零改动。`syncCharacterStates` 升级 V2 差分载荷（空 NewState 不写=差分缺省不猜值）。
- `SyncForeshadows` 继续消费同一 `[]types.ForeshadowHit`——t1-P2 三级匹配链路原样。

## 测试

- `analysis` +4：归一（钳位/overall 重算覆盖模型自报/建议联动裁剪——overall 6.0 落 [2,3] 档边界）/分档矩阵（2.0/5.0/7.0/9.0 四档上限）/legacy 映射（逐键断言+伏笔透传）/空载荷安全（nil/空结构）。
- `project` 落盘回环（upsert 同章覆盖异章追加/升序/persist 容错路径）。
- 既有测试零改动通过（`SyncForeshadows` 15 例、渲染 4 例、prompt 模板名清单）。

## 门禁与产物

- `scripts/ci.ps1` 全绿（exit 0）；零绑定面 drift OK@**672**；版本三处 **4.301.0**。
- `releases/gaea-v4.301.0.exe` — 50229760 B，SHA256=`739399a4a2098e63937246dc2a1444634b9083d7e01f17404523fd901b582f83`；`releases/SHA256SUMS-v4.301.0.txt`；桌面副本同哈希；冒烟 `/api/health` 200。

## 未做（下刀）

- t3-P1 记忆生产者（解锁）：`types.StoryMemory`（确定性 ID）+ 入库门槛规则表（`hook.strength≥6`/`plot_point.importance≥0.6`/`conflict.level≥7` + 每类固定重要性）+ 从 analysis-v2.json 抽取落盘 `memories/MMM-n-memory.json`——V2 生产者已就位，下一刀即可闭环「分析→自动设定库回填」。
- t4-C3 驱动式重写（`internal/rewrite/` + 版本快照，消费 V2 建议）；t4-C4 keyword 锚点→正文偏移标注与前端高亮。
- 真机走查（分析一条龙：V2 载荷/评分联动/落盘回读）等闲置窗口。
