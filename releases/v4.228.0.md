# v4.228.0 — 办公板块输出列左对齐 + 存量 lint 清障（2026-09-11）

> 用户反馈「办公板块的输出部分还是居中，而不是其它的左对齐」。对话输出列
> `.v3-reading` 基础规则 `margin: 0 auto` 把整列水平居中——前次「通用办公重设计」
> 只把列宽覆写到 `--maxw`、**没去 auto margin**，宽屏下输出仍悬在主区正中。

## 缺口

- 输出列居中：`.v3-reading { max-width: 74ch; margin: 0 auto }`（foundation.css），
  办公板块覆写只改了 `max-width`，居中语义原样保留。
- 输入框同轴：`.composer-glow` 与 Composer 内层 `mx-auto` 都在做水平居中，
  只改输出列会出现输入框独居中、正文贴左的错位。
- 共享样式泄漏面：`SchedulePage` / `MemoryHubPage` / `CostLibraryPage` 都导入
  `gaea/styles.css`，直接改共享规则会波及进度计划板块的 Claude 式居中流。

## 落地（全部 `.gaea-app-layout` 作用域覆写，零删基础规则）

- **`.gaea-app-layout .v3-reading`**：去 auto margin——输出列保持 `--maxw`
  （min(1000px, 100%)）宽度，贴主区左缘，与工作台/产物面板/轨迹视图对齐。
- **`.composer-glow`**：去 auto margin（该类仅办公 App 使用），输入框与输出列
  同一左缘；进度计划的输入框居中来自 Composer 内部 `mx-auto`，零波及。
- **`.gaea-app-layout .phase`**：阶段小标左对齐；基础居中规则保留给进度计划。
- Transcript 注释同步（「74ch 居中」→按板块分述）。

## 顺带清障

- `schedule/monte.ts` 存量 `prefer-const` error（`let idx`→`const idx`）：
  v4.217.0 入库，此后发布未跑全量 lint 故未暴露，本次 ci.ps1 门禁拦下。

## 测试

- 目检前后对照：真实 CSS + 还原 DOM 静态挂载，Edge 无头截图（1600×900）——
  before 输出列/阶段小标/输入框居中悬空，after 三者同一左缘对齐工作台。
- vitest src/gaea 184 文件 1541 用例全绿。

## 门禁

ci.ps1 全绿（go build/vet/test + eslint + vite build + vitest 全量 + E 系守卫 +
仓库卫生守卫）/ 绑定 624 零变更 / 版本三处 4.228.0。

## 打包记录

- 产物：`build\bin\gaea.exe` **48,848,384 B** SHA256 `2953cfca5d337cf5a2fa03526334ab8bc9597df3dc822c471995d74bafcf834a`
  （冒烟 /api/health 200 + Desktop 副本同哈希；exe 不入库，只存哈希）。
