# v4.400.0 — 角色设定卡生成（三视图，qedit 通道 · T2 角色资产 v2 首刀）

> 2026-09-23 发布。T2 余项按序首刀；复用 v4.398 qedit 通道+characterlib 参考图
> 画廊（零 schema）。规格：`进度计划/gaea-char-sheet-20260923.md`。
> 绑定面 **715→716**（+CharacterGenerateSheet）。

## 病根

「确定性人设复现」（T2 验收：同一角色跨场景可辨识）缺基建：高质量设定卡
（三视图并排）是最佳参考图，但用户只能手动找图或反复抽卡；qedit 通道
（参考图+指令→编辑引擎）正是生成它的正确工具——人物锚定参考、构图按指令展开。

## 落地

1. **internal/app**：`buildCharacterSheetPrompt` 纯函数（三视图并排模板+外观
   锚点 Appearance/Figure 截断 160 rune）；`CharacterGenerateSheet(chJSON)`：
   取首张参考图（其次剧照，sinCharacterRefPath 口径）→ qedit（txt2img+
   RefImages[1]+RefMethod=qedit）→ 返回 data URL 不自动保存。后端门：生效后端
   非 comfyui 如实报「仅 ComfyUI 本地档」；无参考图如实报错。
2. **前端**：api `generateCharacterSheet`；CharacterLibEditor 剧照按钮旁新增
   「生成设定卡」按钮——产物**追加进参考图列表**（设定卡即最佳参考，画廊=
   参考图列表，保存时后端本地化落盘与手动上传同管线）；无参考 warning。
3. **绑定面**：715→716（play）；spaceBindings 锁 538→539。

## 测试

- Go：prompt 纯函数（三要素+锚点+无外观可用）；绑定路径（httptest 假 ComfyUI
  全链：prompt 提交含三视图+锚点、产物 data URL；无参考报错；非 comfyui 报错）。
- 前端：Editor 按钮两态（无参考 warning 不触发/带参考调用+success）。
- 全量 ci 门禁绿 + tsc 0 + eslint 0。

## 门禁

全量 `scripts/ci.ps1` 绿 EXIT=0；版本漂移闸 OK@4.400.0；绑定面 716。

## 升级说明

- 角色档案新增「生成设定卡」：以首张参考图（无参考时用剧照）走 Qwen 参考编辑
  产出三视图设定卡并自动加入参考图列表——需本地 ComfyUI 与编辑权重（已就位
  机器直接可用）。

## 产物

`gaea-v4.400.0.exe`（冒烟 200 过；SHA256 见 SUMS）。保留策略 5 版：留 v4.396~
v4.400，删 v4.395.0.exe。

## 观察池

一致性评分（vision 文本锚点法）；多姿势模板；设定卡分张（三视图拆三图）；
sin 侧设定卡入口；评分驱动「低分提示补参考」。
