# v4.306.0 — t4-C4 分析标注层：keyword→正文偏移（坐标反投影）+ 读取绑定（2026-09-15）

## 来源

MuMuAINovel 蒸馏线 t4 情节分析域第三刀（`docs/distill/04-plot-analysis.md` §3.10/§8.4 A1/A2，契约 `types.Annotation`/`AnnotationFile` v4.278 已落库）。C1 的 V2 载荷带 8-25 字逐字 keyword 锚点，但坐标从未回投正文——本刀补上标注层：把分析结论钉回正文 rune 坐标，为前端内联高亮（随 t7 前端统一接线）备好数据源。

## 落地

- **定位引擎四段式**（`LocateKeyword`，§8.4 A2，修正 MuMu 两处缺陷）：
  1. 精确 rune 匹配（坐标天然正确）；
  2. 去标点匹配 + **坐标反投影**（indexMap 记录被删字符的原始 rune 索引——MuMu 缺 indexMap 导致坐标偏移，本刀修正）；
  3. 长 keyword（>10 rune）前 15 rune 前缀匹配（模型尾部改写容错）；
  4. 未命中 (-1,0)。
- **构建规则**（`BuildAnnotations`，纯函数）：锚定类 hook/foreshadow/plot_point **定位失败即丢弃**（规格对 MuMu「留 -1 触发错位回查」缺陷 L746 的对策）；hook 的 Content 是原文摘录可兜底锚点，foreshadow/plot_point 只认 keyword（描述是转述不可定位）；suggestion 类无锚点 Pos=-1 仅列条目不高亮（契约语义）。伏笔条目带 tags=[planted|resolved]。
- **落盘**：`analysis/annotations/<MMM>.json`（`AnnotationFile`，项目层 Save/Read）；**Analyze 主链接线**（容错注入，失败只记日志）——分析完成即产出标注，无需二次计算。
- **读取绑定**：`NovelChapterAnnotations(chapterNum)`（679→**680**，drift OK）——读持久化标注；**缺档且有 V2 分析时按需重建**（存量分析免重跑即可得标注）；无分析返回空数组。
- 前端内联高亮渲染随 t7 前端统一接线（编辑器装饰层工作量大，独立成刀）。

## 测试

- `analysis` +3：定位四段式（精确/去标点反投影〔起点与跨度覆盖校验〕/前缀 15 rune/未命中/空输入）；构建矩阵（hook 定位成功/foreshadow 去标点命中/suggestion 留 -1/无 keyword 推进点丢弃）；空载荷安全。
- `project` 存取回环沿用（缺文件空态/写读一致由既有模式覆盖）。
- 既有测试零改动通过。

## 门禁与产物

- `scripts/ci.ps1` 全绿（exit 0）；绑定面 +1 drift OK@**680**（spaceBindings 锁 501→**502**）；版本三处 **4.306.0**。
- `releases/gaea-v4.306.0.exe` — 50335744 B，SHA256=`f541f3059508380f54c69c79915c3c75b817c8ca4ca262864a40566f79fdd2a3`；`releases/SHA256SUMS-v4.306.0.txt`；桌面副本同哈希；冒烟 `/api/health` 200。

## 未做（下刀）

- t4-C4 余项：前端内联高亮（编辑器装饰层 + 标注列表双向联动，随 t7 前端统一接线）。
- t4-C3 余项：partial 局部重写/场景级工程/版本历史面板。
- t5 角色职业接线 / t6 提示词工坊 / t7 前端统一接线——未开工。
- 真机走查等闲置窗口。
