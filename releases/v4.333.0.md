# v4.333.0 · 阶段七出口对账 + 7.3-1 会话级回源收口

> 2026-09-17 发布。规格书：`进度计划/gaea-stage7-audit-resume-20260917.md`。

## 会话级回源（7.3-1 唯一结构欠账）

v4.318 遗留：任务 Session 字段落库的是会话路径，但恢复 seam
（resumeRecentSession）活在 gaea App 树内，收件箱在首页拿不到——V1 只能
回板块粒度。本刀收口：

- **pendingSessionResume 模块**：单例 pending + window CustomEvent
  `gaea:resume-session` 双态通道——gaea 板块 keepAlive=true，已挂载态事件
  直达、冷启态由 App 挂载消费 pending 兜底。
- **gaea App 挂载消费**：consume pending + 事件监听 → resumeRecentSession
  （按 projectGroups 解析跨项目、切工作区后恢复）；ref 保最新回调防过期
  闭包（projectGroups 异步加载）。
- **收件箱**：TaskInboxBoard/Panel +onResumeSession——源会话按钮有回调且
  带路径走精确回源；否则回退 V1 板块粒度（零变化）。

## 出口对账（stage7 §5 全表更新）

| 门 | 结论 |
|---|---|
| 7.1 信任地基 | 已落；挂账=本地引擎建议真实采纳样本（真机） |
| 7.2 复利层 | 已落；挂账=复跑 ≥80% 抽样（真机）+ 结晶 ≥5 次（≈09-30 核数） |
| 7.3 形态层 | 已落（②会话级本刀收口）；缺省 classic→tasks 翻转候拍板 |
| 7.4 并行清欠 | 已清（t6/t7/Gate/画室一） |
| 总判据⑤ | 欠账：v4.319~v4.332 每刀真机走查挂池——列下一班清池首项 |

## 测试与门禁

- 前端 +5（通道 3 / 精确回源与回退 2）；既有 11 用例零改动。
- go test ./... 全量复跑全绿；vitest 3185/3185；tsc/eslint 零错零告警；
  drift OK@707（零新绑定）。
- ci.ps1 首两跑分撞 go/vitest 段瞬时失败（单跑皆绿、清残留 node 进程后
  第三跑 CI OK——环境竞争先例+2）。

## 坑（沉淀）

- 测试文件尾 rfind('})') 追加吞 describe 闭合——同坑两日三犯，规约：append
  前数一遍尾括号。
- [] 依赖订阅 effect 引用依赖异步数据的回调=过期闭包——ref 保最新。
