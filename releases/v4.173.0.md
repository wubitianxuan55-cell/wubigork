# v4.173.0 — 瘦身 P3 版3（bridge 双轨退役批次二）：chat/novel/settings 三族 22 文件迁 bridge + 元组契约修正（2026-09-09）

> 瘦身执行层第八版：轨道四「双轨」渐进退役第二刀（批次二）。承接批次一（12 文件 + 16 方法），本版转正 **29 个新方法** + 迁移 **22 个调用方文件**（wailsjsCompat 引用 45→35，LegacySurfaceNames 276→247）。
> 六线并发（1 契约扩展 + 1 契约修正 + 4 业务线，足迹互斥）+ 主代理收口。

## 1. 线1：契约扩展（29 新方法，单子代理）

- **CoreBindings** +5：`GetConfig/SaveConfig/GetStats/ListSkills/ExportAll`（ExportAll 实测在 bindings_core.go:23，非 NovelB）；
- **ChatBindings** +9：`ChatTopicCreate/Delete/Rename/SetMode/Clear/ImportTopic/Send/StreamPlain/ExportMarkdown`（签名按 Go，ChatSend 6 参、ChatStreamPlain 5 参）;
- **NovelBindings** +12：`GetWorldview/SaveWorldview/ChatWorldview/GetWorldviewSections/SaveAllWorldviewSections/CheckConsistency/CheckConsistencyDeep/GetForeshadows/SaveForeshadows/SaveCharactersBatch/NovelReadingAsk/GenerateSceneIllustration`；
- **VoiceBindings** +2：`VoiceGetSettings/VoiceChatText`；**ImageBindings** +1：`SetImageBackend(4 参)`；
- 已核实跳过 3：WhisperGetPersonalities（memory.ts 已有）/ListSessions（core.ts 已有）/MemoryHubOverview（memory.ts 已有）。
- **⚠️ ChatWorldview 契约纠偏**：任务书按 (string,error) 写，实测 Go 是 `ChatB.ChatWorldview(userMsg, currentContent) (map[string]interface{}, error)`（bindings_chat.go:35）——声明为 `Promise<Record<string, unknown>>`，NovelSettingPage 消费处本就把返回值当对象用（`result?.reply`），仅需 `unknown` 收窄。
- drift 摘除 29 名（误删 MigrateProjectToV4 立即恢复）；spaceBindings +29（work 14 / play 14 / shared 1）锁 **331→360**；mock 补齐（core/chat/novel/voice/imagegen；novel.ts 重写保留 NovelSearch）。

## 2. 线1b：元组契约错位修正（ChatTopicsList/ChatMessagesList）

- **历史误读实锤**：bridge/chat.ts 原声明 `Promise<[T[], unknown]>` 元组、mock 返回 `[[], null]`——但注释自己承认「T6-3.2 **读错** 返回 error」，真实 Wails 对 Go `([]T, error)` 是**成功返回数组、失败 rejected promise**（chat/utils.ts `await App.ChatImportTopic()` 无解构即旁证）。
- 修正为数组契约：`ChatTopicsList(): Promise<chat.Topic[]>` / `ChatMessagesList(id): Promise<chat.Message[]>`，mock 同步返回 `[]`，mock-contract-t63.test 断言改 Array.isArray。
- **影响面零**（tsc 全绿证明无下游按元组消费）——useChatTopics 迁移后 `(await app.ChatTopicsList()) || []` 直接可用，无需调研预测的「元组解构」重构。

## 3. 业务线×4（22 文件）

**chat 组**（useChatTopics + useChatStream + pages/chat/utils + ChatPage.test）：
- useChatTopics：11 处 App.*→app.*；ChatTopicCreate 返回值 `{id;title;mode;[k]:unknown}` 进 `chat.Topic[]` state 需 `as unknown as` 双断言。
- useChatStream：ChatStreamPlain/ChatSend 换管道；`Record<string, any>`→`Record<string, unknown>` 触发 answered_by/emotion 两处最小 cast（保留原运行期语义）。
- chat/utils：`App.GaeaLogFrontendError`→`app.LogFrontendError`（core.ts 短名 + mappings LogFrontendError:"GaeaLogFrontendError"）；ChatImportTopic。
- ChatPage.test：bindingsBridge 扩展 11 个共享 vi.fn（Proxy 转发），mockResolvedValue 本为数组无需改。**22/22 绿**。

**novel 组**（10 源文件 + 4 测试）：
- NovelPage（GetStats，app 本是既有 bridge import）/NovelSettingPage（GetWorldview/SaveWorldview/ChatWorldview——返回值 unknown 收窄 `typeof result?.reply === 'string'`）/ExportPanel（ExportAll）/WorldviewSectionsEditor/ConsistencyPanel/ForeshadowPanel/CreateInspector（ListSkills cast 经 unknown）/NewCharactersModal/readingAssistant/ChapterIllustration（GenerateSceneIllustration）。
- 测试：NovelSettingPage.test **关键决策=app 用 Proxy 回落真代理**（「审计刀B b」壳内导入导出用例的 SaveFileAs/PickFiles/ReadFileB64 必须走真实代理路由 window.go stub，纯对象 mock 会打挂 3 例）；ConsistencyPanel/ForeshadowPanel/ChapterIllustration 普通对象 mock。**25/25 绿**。

**settings 组**：
- api/settings.ts **迁移**（8 调用全命中：GetConfig/SaveConfig/GetImageBackendInfo/SetImageBackend/GetActiveModel/VoiceGetSettings/VoiceApplySettings/Settings）。
- ChatPanel/DataPanel/VoiceSettingsPanel/useVoiceChat **暂缓→批次三**（WhisperClearSession/GetVoicePipelineConfig/GetTTSSpeakers/GaeaDataBackup×6/语音族 VoiceStart 等 6 方法未转正——宁少勿多纪律）。

**ModuleLauncher**：ListSessions/MemoryHubOverview/VoiceApplySettings/VoiceChatText 4 处迁移；全文件无遗留 App.*（批次三无需处理）。

## 4. 主代理收口

- 此前批次一的组件（ChatPage/ChapterPage 等）经 ChatPage.test 的共享 bindingsBridge 模式继续全绿；useChatTopics 的 `|| []` 语义在数组契约下保留。
- 度量：wailsjsCompat 引用 45→**35** 文件 / LegacySurfaceNames 276→**247** / spaceBindings 360。
- 版本三处 4.173.0（sync-version.ps1）。

## 5. 门禁（全量复跑）

- vitest **2737/2737 首跑全绿**（313 文件——批次一曾 5 例连带失败，本批次零失败；含 ChatPage 22/ChapterPage/NovelSettingPage 14 等迁移后测试）；
- `tsc -b` 0 · `eslint .` 0 · drift **PASS@602**（零绑定）· locale **0 死**；
- Go 全量回归绿（本版零 Go 改动，app 包 76.8s 通过）；
- 版本三处 4.173.0。

## 6. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 待 build 实测 ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.23 MB ｜ wailsjsCompat 引用 45→35
```

## 7. 产物与冒烟

- 产物：`gaea-v4.173.0.exe`，SHA256=`C3DFA08F1A56C2389C2E4D5515A0E7F09D017B0DFB763DD0CDC459EBE66750CE`（releases/SHA256SUMS-v4.173.0.txt）。
- 冒烟：`scripts/smoke.ps1` → **/api/health 200**（冒烟进程已回收）。

## 8. 欠账（批次三）

- **语音族**（useVoiceChat.ts 8 直调 + components/VoiceSettingsPanel.tsx）：VoiceStart/VoiceStop/VoicePlaybackDone/VoiceCancelTTS/VoicePushAudio/VoiceSetPTTActive 6 方法转正后两文件可迁——**依赖直调同步 throw 语义需重评**（useVoiceChat：488/550 cleanup try/catch）；
- **Chat 余项**：ChatPage 本体五个直调（WhisperClearSession/TTSSpeakBase64WithParams/TTSSpeakBase64/ChatTopicClear/ChatTopicExportMarkdown——后两者契约已转正可先迁）+ WhisperClearSession/GetVoicePipelineConfig/GetTTSSpeakers 转正解锁 ChatPanel/VoiceSettingsPanel；
- **设置面板族**：DataPanel（GaeaDataBackup×6 转正后迁）；
- **cast 族**：CreatePage（13 直调含 6 cast 方法）/ChapterEditor（sceneBridge cast）需先入 AppBindings 类型；
- **角色库族**：api/characterlib（17 缺失）+ novel/api/character（8 缺失）；
- **ChapterPage 四直调**（GetChapter/GetChapterBranch/SaveChapterContent/SaveChapterBranchContent）；
- **useBindState**（4 缺失簇）；
- 全部完成后 **wailsjsCompat.ts 退役**（shim 删除）+ 悬空注释清理（api/engines.ts:362 等）。
- 壳内真机池、审计刀D 真机取证不变。