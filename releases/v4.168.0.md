# v4.168.0 — 瘦身 P2 刀1：schedule 并入办公文档面（inMenu:false + 命令面板入口）+ 刀2 补验落档（2026-09-09）

> 瘦身执行层第三版：P2 形态「schedule→办公文档化」第一刀落地——schedule 移出顶栏菜单/启动器，
> 办公「进度计划」入口承接（命令面板 Ctrl+K 直达 + 办公文件面 .gsched 摘要卡既有链路）。
> 三线并发子代理（刀1a manifest / 刀1b 办公入口 / 刀2 补验审计）+ 主代理收口（后端同步翻转 + DataPanel 绑定签名修复 + 补验测试）。

## 1. 线A：刀1a schedule inMenu:false（子代理 a3d4a40b）

- `frontend/src/boards/manifests.ts`：schedule 板块 `inMenu: true → false`（menuOrder/space/featureModel 不动），
  注释=「瘦身刀1（v4.168.0）：并入办公文档面——inMenu:false 藏出顶栏菜单，办公『进度计划』入口承接（白名单刀0 已解耦，注册即可导航不丢能力）」。
- `manifests.test.ts`：菜单顺序/文案/图标三处断言移除 schedule（12→11 项）、settings/weixin 用例补
  `not.toContain('schedule')`；**navigateWhitelist 断言保持含 schedule**（注册即可导航，12 项不变）——白名单与菜单解耦的核心语义。
- **launcher.test.ts 同步（主代理收口）**：静态清单 12→11 卡移除 schedule + 补 `not.toContain('schedule')` 锁语义
  （launcher.ts 源零改动，inMenu 过滤是展示层语义）。

## 2. 线B：刀1b 办公「进度计划」入口（子代理 22080cf7）

- `frontend/src/gaea/App.tsx` paletteItems 新增 **cmd-schedule**（Ctrl+K 命令面板组）：
  - title 复用既有 i18n 键 `schedCard.tag`（三语齐备，不铺新字典）；icon=`LineChart`（既有）；
  - keywords=`["schedule","进度","计划","工作台"]`；
  - run=`emitFrontendEvent(NAVIGATE, { page: "schedule" })`（与 ScheduleFileCard.tsx:67 同一事件通道）。
- 办公文件面可达性核验：RecentFilesBar 复用 lib/recentFiles 单源（无扩展名过滤），.gsched 预览/引用即入列 →
  FilePreview 分发 ScheduleFileCard 摘要卡 →「在进度计划中打开」跳板块。**零新增数据源、零过度设计**。
- `App.palette.test.ts` 新增源级回归锁 3 例（巨组件不整页渲染先例）。

## 3. 线C：刀2 补验审计落档（子代理 0f96dc42，只读）

- 新文档 `docs/gaea-slim-knife2-audit-2026-09.md`：**刀2 主体已提前在产**（v4.121 刀12 办公共联 ScheduleFileCard +
  v4.139 多工程切指针），三要素实证：
  - ✅ 摘要卡+打开工作台（FilePreview.tsx:618-627 分发 + ScheduleFileCard 统计/基线/漂移/打开按钮）；
  - ✅ 最近文档可见（FileTree 无扩展名过滤 + recentFiles 单源，.gsched 打开即入列）；
  - ✅ .mpp 走导入不做内嵌预览（v4.140 二进制解析 + v4.144 导入为新工程）。
- 勘出 **G-1 前后端分裂**：前端已 inMenu:false 但后端 `builtins.go` 仍 InMenu:true（normalizeManifests 后端字段优先 → 真机/浏览器行为不一致）。
- 观察池刀3（工作台全幅内嵌办公）核验：现状不可直接嵌入（SchedulePage 自持整页 + 面板注册表无渲染器），需中等改造，维持观察池。

## 4. 主代理收口

- **G-1 后端同步翻转**：`internal/app/board/builtins.go` schedule `InMenu: Bool(true → false)` + 注释注明刀1 语义
  （防 normalizeManifests 后端字段优先导致真机仍显示 schedule 的分裂）。
- **G-1 测试锁**：`board_manifest_test.go` TestGetBoardManifestsCanonical 补 schedule InMenu=false 断言。
- **v4.167 遗留绑定签名修复**：DataPanel.tsx:83 `GaeaPickFiles()` 无参 → `GaeaPickFiles('zip')`——这是 wails 生成物
  `OfficeB.d.ts` 在 v4.167 加 Filters 参数后的**必填签名**（TS2554），顺带让「恢复备份」对话框前置过滤 .zip（选错趋零）。
- **G-5 补验测试**：FilePreview.test.tsx 新增 .gsched 分发 2 例（合法→摘要卡 / 坏 JSON→回落文本）——锁刀2 分发本身
  （既有测试只到组件级 ScheduleFileCard.test）。

## 5. 门禁（主代理统一复跑）

- Go 115 包全量 0 FAIL（首跑干净）；
- vitest 2715→**2720**（312 文件，+5 vs v4.167：刀1a 3 断言更新 + 刀1b 3 新 + FilePreview 2 新 − 既有净额；
  全量复跑全绿，首跑 1 例 CostProjectsView flaky 复跑清，在册同族）；
- `tsc -b` 0（DataPanel 修复后基线 TS2554 也清零）· `eslint .` 0 · drift **PASS@602**（零绑定）；
- vite build + wails build + 冒烟见产物。

## 6. 欠账（如实列示）

- 审计刀D 真机取证（printSvg iframe print / 拖拽 / 粘贴）；
- P2 走查待证项（基线文档 §3 勘出）：rail code 双入口渲染实态、后端清单合并 nav 子项差异、knowledge 孤儿页确无入口；
- 刀2 缺口 G-2（多基线计数口径，活跃基线快照+漂移已覆盖核心，裁决近似达成可关闭）与 G-3（.gsched kind:text 真机走查）；
- 观察池刀3（工作台全幅内嵌办公）待一周评估；
- 双空间并列落地 + home 空间感知化随 P2 主干推进。

## 7. 产物

- `releases/gaea-v4.168.0.exe`（46.2 MB，gitignore 不入库）
- SHA256：`30357f9a3ad8206c2a3deec63970bf9ff1ddc9c861e0c6183ef9df60befce853`
  （见 `SHA256SUMS-v4.168.0.txt`）
- 冒烟：HTTP 桥接 127.0.0.1:18999 `/api/health` → `{"status":"ok"}` 200，进程已回收；桌面副本同步。