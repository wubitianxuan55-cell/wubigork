# v4.206.0 — 浅主色上的字：accent-fg 取代硬编码白（2026-09-10）

> 暗色主题的 primary / glow 是浅色（如夜青 `#2dd4bf`）。办公 gaea 主色按钮
> 写死 `text-white`，浅底上白字看不见。亮色主题 on-primary 才是白。

## 改动

- **33 处** `bg-accent text-white` → `bg-accent text-accent-fg`
  （Tailwind `--color-accent-fg` = `--md-sys-color-on-primary`：
  暗色深字、亮色白字）。红/绿实底按钮仍用白字。
- **App.tsx** 补 shim：`--color-on-primary`、`--color-surface`
  （此前只写了 on-primary-container，首页发送钮等靠 `#04121a` 兜底）。

## 门禁

tsc 0 / eslint（改动文件）0 / 目标 vitest 27/27 / 绑定面 608 零变更 /
Go 零改动 / 版本三处 sync 4.206.0。
