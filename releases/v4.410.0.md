# v4.410.0 — sin 评分入口 v1（快路径）+ 评分流式辨伪关账

> 2026-09-25 发布。观察池末两项收官。纯前端，绑定面 **717 零变更**。
> 规格：`进度计划/gaea-sin-score-entry-20260925.md`。

## 落地

1. **sin 评分入口（快路径）**：右栏角色 chip 新增「评分」钮（AimOutlined）——
   评角色**最新一张**参考图（通常是刚生成的设定卡/剧照），Modal 呈现分数+
   总评+不一致点，<60 提示补参考；与角色库编辑器同款渲染。无参考图如实指
   路「先用设定卡或剧照生成一张再评分」；逐张评分全功能留在编辑器（v4.403
   快路径原则同款）。
2. **单飞+互斥**：评分与设定卡生成互斥（本地视觉模型与 ComfyUI 同 GPU）；
   未传 prop 不渲染入口（向后兼容）。
3. **评分 LLM 流式辨伪缓办**：评分响应是严格 JSON 契约，流式只能展示原始
   JSON 累积、无 UX 增益；等待语义已被评分钮 spinner+单飞+90s 上限覆盖；
   真流式要动 vision provider+事件面+部分渲染，churn 大收益小。不立项，
   重开条件=评分扩展为自然语言长评（图vs图 V2）。

## 测试

- SinCastPanel.test：+3 例（评分钮按 id 调用+复位/评分与生成互斥双向/未传
  不渲染），sin 域全绿。
- 全量 ci 门禁绿 + tsc 0 + eslint 0。

## 门禁

全量 `scripts/ci.ps1` 绿 EXIT=0；版本漂移闸 OK@4.410.0；绑定面 717 零变更。

## 升级说明

- 无操作变化。sin 右栏角色 chip 多一枚「评分」钮（瞄准图标）：单击评估该
  角色最新参考图与文字设定的一致性，弹窗给出分数与不一致点。

## 产物

`gaea-v4.410.0.exe` 51315712B，SHA256=`f7f08a2ff3adeaeb4bd9e34a65da7be82f825df8750b1c7b5d57898732204140`（全文见 SHA256SUMS-v4.410.0.txt；冒烟 200 过；桌面副本同哈希实测一致）。保留策略 5 版：留
v4.406.0~v4.410.0，删 v4.405.1.exe（SUMS 身份档案全保留）。
