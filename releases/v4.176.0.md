# v4.176.0 — 瘦身 P4 结构刀2 + entry 懒加载：mock/office 拆分 + MemoryHubPage 全 tab lazy + mock 异步 chunk（2026-09-09）

> 瘦身执行层第十一版：P4 性能版第二刀——**结构刀2**（mock/office.ts 51.2KB→1.1KB 入口）+ **运行刀**（entry 懒加载 H6/H2-H5）+ 同步修复 7 例测试（H6 带来的 mock 异步时序）。

## 1. 结构刀2：mock/office.ts（51.2→1.1KB 入口，6 文件）

- `office.ts` 1.1KB：纯 `export { buildOffice } from "./office/build"`（mock.ts 零改动）
- `mock/office/`：types 2.0 / state 1.9 / **schedule 10.5**（进度计划域状态+辅助+Schedule* 方法+window 钩子聚合）/ methods_xlsx 4.9 / methods_office 32.7 / build 0.7
- **TS2632 教训沉淀**：`let mockScheduleCurrent` 被 ScheduleProjectCreate/Delete 直接重赋值——跨模块重赋值导入绑定被 TS 禁止，故 schedule 状态与方法必须同文件（与 weixin.ts 域内私有状态先例一致）。
- 验证：tsc 0 / eslint 0 / 定向 vitest 4 文件 23 例绿。

## 2. 运行刀：entry 懒加载（调研先行，H6+H2-H5 实施，H1 递下一轮）

**调研关键发现（对 P0 假设两处修正）**：
- 页面级 lazy 早已全就位（main.tsx registerPage + lazy）；`pageLazy.ts` 是 PDF 逐页懒挂载非页面 lazy。
- **cytoscape/cynefin 与 MemoryHubPage 无关**——是 mermaid 的传递依赖/自带 parser chunk，本就懒；MemoryHubPage 1.4MB 大头是静态 **3d-force-graph（three.js）**。

**实施**：
- **H6 mock 异步 chunk**：proxy.ts 删静态 `import "../mock"` → `startMockChunk()` 单例 `import("../mock")` + 真机启动探测门控（window.go 存在即零加载）；resolveBinding 真机路径零变化、mock 冷路径异步 thunk 兜底；events.ts 事件 mock 回退走 `mockEventSharedSync/waitMockReady`。**index 1149→967.23KB（−182KB/−15.8%）**；mock 独立 110KB 异步 chunk。
  - **⚠️ 连带测试时序修复×2**：mock 异步化使 `window.__mockScheduleFile`（ScheduleModal 钩子）与 SearchModal 的 RouteIntent/UnifiedSearch 演示规则在同步断言时未就绪——两测试补 `beforeAll(await waitMockReady())`（bridge.ts 入口加 waitMockReady re-export）。
- **H2-H5 MemoryHubPage 页内 lazy**：8 组件（KnowledgePanel/ProfileLibrary/OfficeMemoryLibrary/WhisperMemoryLibrary/MaterialsLibrary/DigitalLifeLibrary/FilePreviewModal/GraphView）全 React.lazy + 双 Suspense（HubViewFallback 轻量骨架）。**MemoryHubPage 1402.5→15.95KB 页壳**；GraphView/three.js 1,354.97KB、KnowledgePanel 51.3、Markdown 212.9、katex 261.3、mermaid.core 609.8 等全部独立 chunk 按需加载。GraphView lazy 决策：默认 home tab 首开仍拉一次（与现状等价），但其余入口不摊 three.js 成本。
- **H1 mermaid 动态 import 本轮跳过**（如实理由）：mermaid 另在 footprint 外 `utils/mermaidPng.ts` 静态导入（ImageGenPage 静态可达），单独改 Markdown.tsx 无法把 mermaid 移出急切图；memoryhub lazy 已把 mermaid.core 拆为共享异步 chunk；Markdown 全局组件测试面大。**下一轮连同 mermaidPng 一起做**。

## 3. 主代理收口

- 足迹核验：9 项（5 M + 3 M 测试 + 新目录 mock/office/），全在预期域。
- 门禁：vitest **2737/2737 全绿**（首跑 7 例失败=H6 时序，修复后全绿）、tsc 0、eslint 0、drift PASS@602、locale 0 死、Go 回归绿（app/config/modelengine）。
- entry 1176.62→**967.23KB**；MemoryHubPage 页壳 15.95KB；GraphView chunk 1,355KB 独立。

## 4. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 967.23 kB gz 312.73（基线 1193→−19%）｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.18 MB ｜ Go >50KB 源文件 0
```

## 5. 产物与冒烟

- 产物：`gaea-v4.176.0.exe`，SHA256=`402A1B1A937CF6690E435C1F4E61AA6BB6D72487543A6B078E0F9F5F132DBD2F`（releases/SHA256SUMS-v4.176.0.txt）。
- 冒烟：`scripts/smoke.ps1` → **/api/health 200**（冒烟进程已回收）。

## 6. 欠账（P4 续）

- **H1 mermaid 动态 import**（Markdown.tsx + utils/mermaidPng.ts 一起，预计再减 4 页面急切 607KB）+ H7 locales 按需（−160~200KB）+ H8 SearchModal lazy（−20~40KB）；
- **exe strip**（`wails build -ldflags "-s -w" -trimpath` 发布版，dev 保留符号——build.bat 或新脚本，对靶 P0 基线 46.22MB）；
- 壳内真机池（rail 切换器/home 最近文档/.gsched）、审计刀D 真机取证（printSvg print/拖拽/粘贴）不变。