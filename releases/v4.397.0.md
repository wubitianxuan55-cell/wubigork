# v4.397.0 — 绘梦·抠图/透明底导出（T3 编辑力收官）

> 2026-09-23 发布。来源：用户指令「继续」（画室线：T3 最后一件）。
> 长期规划 T3「蒙版局部重绘 + 扩图 + 抠图（canvas 圈选/笔刷；透明底 PNG 导出，
> 对齐 NovelAI V5 原生透明卖点）」四件至此**全部落地**。规格：
> `进度计划/gaea-cutout-20260923.md`。绑定面 **714→715**（+ImageCutout）。

## 病根

涂选保留主体 → 透明底 PNG 是立绘/素材加工的高频动作（对齐 NovelAI V5 原生
透明卖点），gaea 编辑三件+变体簇已齐但抠图缺位——用户只能出方图再外部去背。

## 落地（零新模型零引擎调用）

1. **internal/ai `image_cutout.go`（新）**：`ComposeCutout(initImage, mask)` 纯
   函数——蒙版即 alpha 通道：**抠图蒙版语义：白=保留主体、黑=背景（转透明）**
   （与编辑的「白=重绘」相反，两原语各自契约文档化；蒙版产物同组件产出，语义
   由消费方定义）。标准库 image/draw 逐像素合成，硬边（羽化挂观察池）；尺寸
   不一致/非 data URL/解码失败如实报错。
2. **internal/app `mediaState.ImageCutout`**：合成 → 落盘 ImageSaveDir（复用
   saveMediaToDisk，未配置兜底 `~/Pictures/gaea`）→ 台账登记（mode=cutout 入
   params；v4.396 修复后闸正常）→ 返回 `{path, asset_id}`（byPath 回填复用
   v4.395 链路）。零模型调用不设引擎门。ImageB 门面委托。
3. **前端**：`api/image.ts` +facade 声明与 `imageCutout()` 封装；新
   `CutoutModal`（复用 MaskBrushEditor——文案「涂抹要保留的主体」；未涂
   warning；成功态显示路径+素材库提示；导出产物不进画布 results——透明图在
   结果流的展示语义挂观察池）；ResultStage 结果卡/灯箱新动作「抠图」（与
   「改图」「指令编辑」并列，ScissorOutlined）；ImageGenPage 接线。
4. **绑定面**：714→715（`ImageCutout`，play）；spaceBindings 锁 537→538；
   wailsjs 由 `wails generate module` 再生；bindingNames 再生（真实消费文件是
   `src/gaea/lib/bindingNames.ts`——根级 `src/bindingNames.ts` 是无人引用的
   陈旧副本，本次顺手删除）。

## 测试

- ai +2：合成形状（白区原像素 alpha255/黑区透明/尺寸不变）；边界（尺寸不一致/
  空 mask/非 data URL）。
- app +1：空参拒绝/闸关（默认测试态）落盘出 path 且 asset_id 空/尺寸不一致
  如实报错。
- 前端 +3：未涂 warning/涂抹后 mask 透传+成功态路径/错误态如实展示。
- 全量 398 文件 3363 例绿 + tsc 0 + eslint 0。

## 门禁

全量 `scripts/ci.ps1` 绿 EXIT=0（E 系列守卫 OK + 仓库卫生守卫 OK）；
版本漂移闸 OK@4.397.0；绑定面漂移 PASS@715；spaceBindings 538。

## 升级说明

- 结果卡新动作「抠图」：涂选要保留的主体 → 导出透明底 PNG（落盘图片目录+
  台账登记，素材库可查）。回填角色库参考图可用导出路径（角色库管线对接挂
  观察池）。

## 产物

`gaea-v4.397.0.exe`（仅本地不入库；SHA256 见 `SHA256SUMS-v4.397.0.txt`；
冒烟 /api/health 200 过）。保留策略 5 版：留 v4.393~v4.397，删 v4.392.0.exe。

## 观察池（T3 全线收官，画室线转入 T2 一致性）

羽化半径/边缘平滑；角色库参考图管线对接；模型推理抠图（BiRefNet/RMBG 类）；
透明产物进画布/历史的展示语义；抠图反选。**T2 一致性余项**（ipadapter/pulid
真实现、角色资产 v2、LoRA 训练向导）与**导演 Agent**（并行池在册）为画室线
下一切片。
