# v4.322.0 · t4-C3 收官：场景工程整章重写（单场景替换）

> 用户指令「继续」。小刀单线主代理直做；契约先行（规格书随刀入库
> `进度计划/gaea-scene-rewrite-20260916.md`）。**产品裁决（单场景替换）**：
> 整章重写产出的是整章新全文，与原场景边界无法自动对齐——重写整章本就
> 意味着重划结构，应用=新全文写回该章唯一场景；Restore 同语义对称。
> LLM 智能分场景回写与场景粒度重写入观察池。

## 论点

NovelChapterRewrite 此前对 v4 场景章直接拒绝（「整章重写暂未开放」）——
场景工程项目整章重写不可达。根因=版本库 Apply/Restore 只写 blob，而 v4 的
真源是 scenes/（scene→blob 单向同步 syncBlobFromScenes）。本刀补 v4 感知的
写回，**复用既有原语 rebuildScenesFromBlob**（CreateChapter 整章重写完成点
同款：删全部场景→从新 blob 物化单场景；v3/无场景章 no-op）——零新机制。
**零新绑定**（RewriteModal/RewriteHistoryPanel 全 chapterNum 泛型，696 不动）。

## 落地

### ① Go（novel_rewrite_handler.go 四处）

- **移除** whole 入口的 v4 拒绝段；original 改 `ReadChapterAsStitch`
  （v3 走 fallback 零变化）——重写整章语义以全章拼接视图为对象。
- **NovelApplyRewriteVersion**：WriteChapter 后 + `rebuildScenesFromBlob`
  （v4 场景章收敛为单场景+blob 同步；v3 no-op）。
- **NovelRestoreRewriteVersion**：同款（恢复原文也重置单场景，语义对称）。
- **partial 守卫**：v4 场景章拒绝局部重写（「选段与场景边界无法对齐，请
  使用整章重写」）——选段拼接回写会破坏场景结构，V1 不做（观察池）。

### ② 前端（ChapterPage = v4 场景编辑面）

- EditChrome 加可选 props（canRewrite/onRewrite/onRewriteHistory）+「整章
  重写」「重写历史」两按钮（保存旁）；仅 sceneBacked 活动页渲染。
- ChapterPage 挂 RewriteModal + RewriteHistoryPanel（onApplied →
  loadChapterIntoTab 重载该 tab 场景框）。

## 验收

- Go +2 例：①v4 场景章全链（两场景夹具→重写→版本快照=stitch→Apply→
  场景收敛 1 个+内容=新全文+blob 同步+stitch 读到新文→Restore 回原文同为
  单场景）②partial 对 v4 拒绝；既有 whole/partial 用例回归绿。
- 前端 +2 例：v4 场景章渲染双按钮（等 V4 探测再开章——mountSceneChapter
  同款时序）；v3 blob 章不渲染（CreatePage 已有入口不重复挂）；既有 9/9
  零破坏（合计 11/11）。
- tsc -b 零错、eslint 零告警、gofmt 干净；版本三处 4.322.0；drift 复核
  696（零绑定变化）。
- 产物：exe 50638848B SHA256=5b1b105ad81bff7b5dd391cc445aeda8cb77aed575a516c996e10441c38ea29e（releases/gaea-v4.322.0.exe +
  SHA256SUMS-v4.322.0.txt；桌面副本同哈希；冒烟 /api/health 200 过）。

## 出口对照

- v4 场景章整章重写全链可达（重写→版本→应用=单场景替换→恢复）✅ 测试
  钉死。**t4-C3 余项全清**：whole v4.304 / partial v4.321 / 历史面板 v4.320 /
  场景章 v4.322——重写三通道（整章/局部/去味）× 存储两制（blob/场景）矩阵
  仅剩 partial×场景一格（守卫拒绝+观察池）。

## 观察池

- LLM 智能分场景回写（应用时按原场景数/标题重拆）。
- 场景粒度重写（对齐去味逐场景先例：单场景指令重写，结构不动）。
- partial 对场景章的选段→场景映射。


## 未做（下刀）

- t6 提示词工坊 / t7 前端统一接线（小说线最后两域）。
- 7.3-2 板块降视图（等 v4.318 稳定一个零功能周）。
