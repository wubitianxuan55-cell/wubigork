# v4.289.0 — oh-story T6：书级文风档案（style.md 协议对齐，风格注入生成上下文）（2026-09-14）

## 来源

oh-story 蒸馏线 T6「风格档案协议对齐」（上游 `story-deslop/references/style-resolution.md` 的风格裁决协议 ↔ gaea 文风指纹参考档），**oh-story 线最后一项**——T1~T6 至此全部落地。

## 协议对齐映射（上游 → gaea 现状/本刀）

| 上游协议要点 | gaea 状态 |
|---|---|
| 书级文风档案（`设定/文风.md`），无文件不建占位、一句偏好也有效、空白/纯标题/待补充不算 | **本刀新增**：`<项目目录>/style.md` → 注入章节生成上下文 |
| 文风只裁决表达维度（句长/视角/标点/对话/修辞/收尾），事实与表达分开裁决，事件/信息边界仍以细纲为准 | 注入区段的协议声明原文写入 prompt |
| 书级白名单 `.deslop-whitelist`（一行一片段、# 注释忽略、不向父目录找） | v4.286 已实现；**本刀补注记格式兼容锁**（`# 来源：…；用途：…` 整行忽略） |
| 优先级：当前请求 > 本书文风 > 全局默认 | 生成 prompt 中文风区段为「作者显式偏好」增强区；用户当轮请求本就优先 |
| 风格画像（统计层） | gaea 文风指纹 fingerprint.json（v4.277），与 style.md 互补：统计基线 vs 显式偏好 |

## 落地

### `buildStyleSection`（`internal/app/create_chapter_handler.go`）

`buildChapterContextSections` 新增文风区段（伏笔 → **文风** → 世界观，共享既有的总预算截断）：

- 读 `<项目目录>/style.md`；无文件返回 ""（不建占位、不猜）；
- 逐行过滤：空行、`#` 标题行、含「待补充」的行不算偏好；一句有效偏好即注入；
- 区段头声明协议边界：「只约束表达方式（句长/视角/标点/对话/修辞/收尾等）；事件、事实与信息边界仍以细纲为准」；
- 超长截断（`ctxStyleBudget` = 1200 rune，偏好宜精不宜多）。

## 测试

`internal/app` +3：`TestChapterContextInjectsBookStyle`（偏好原样注入 + 协议边界声明在位 + # 标题行过滤）+ `TestChapterContextStyleMissingOrPlaceholderOmitted`（无文件无区段 / 纯标题+待补充无区段 / 一句偏好即注入）+ `TestChapterContextStyleTruncated`（超长截断带省略号标记）。`internal/novelstyle` 白名单注记格式兼容 +1 断言。

## 门禁与产物

- `scripts/ci.ps1` 全绿（exit 0）；零绑定面变化 drift OK@658；版本三处 **4.289.0**。
- `releases/gaea-v4.289.0.exe` — 50,027,520 B，SHA256=`4F6E8CB200FA92AF5403C905497904F02C40CE56BB63E84D25BFCE907F840442`；`releases/SHA256SUMS-v4.289.0.txt`；桌面副本同哈希；冒烟 `/api/health` 200。

## 未做（下刀）

- oh-story 线 **T1~T6 全部收官**（T5 七角色卡实跑验收在观察池）。
- 拆书导入线余项：长书后台任务态 / 角色名→角色库 ID 匹配 / tail×AI 反推串联。
- style.md 的编辑入口（项目设置 UI）：文件即资产、手改即可，按反馈另刀。
