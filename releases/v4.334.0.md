# v4.334.0 · revolution 刀8 收官：跨模块验收 + 回退演练 + 版本三处漂移根治

> 2026-09-17 发布。规格书：`进度计划/gaea-rev-knife8-acceptance-20260917.md`。
> 本刀合入 = **小说 revolution 规划刀 1–8 全线收官**（race 项 v4.331 已清）。

## 跨模块验收（Go +7，三线并发）

各模块单测在位，但跨模块串联与回退路径零覆盖——本刀把 revolution §5
验收口径逐条钉死：

- **线A novelstyle**（2 例）：AI 味检测性能钉——~5200 rune 真实感章节
  ScoreTextNoRef 单次 **3.3~5.0ms**（判据 <1s，余量约 200 倍）；去味保真——
  DeSlopRewrite 分数 100→27、rune 漂移 **0**、实体名逐字保留（各 3 次）、
  句数 14→14 恒等（词级替换不碰句读）。
- **线B 生成主轴端到端**（2 例，**史上首测**）：herdsman→httptest SSE 双路桩
  （按「小说分析编辑」系统提示词标记分流）——CreateChapter → 章落盘 →
  异步自动门 → Analyze 成功路径（此前只有 nil-engine 降级形态被测过）→
  analysis-v2.json 落盘（Overall 归一重算 8.2）/ memories 6 条 /
  annotations 4 条（3 条 pos≥0 锚点命中）/ 伏笔 planted「虎符之谜」入库 /
  rebuildScenesFromBlob 真跑重置物化 / **chapter-gate 事件经 httpbridge
  SSE 订阅捕获**（analysisDone=true、foreshadowSync planted/created=1/1）；
  桩计数 genCalls==1 && analysisCalls==1（一次生成一次分析，零多余调用）。
  50 章书级体检：奇数脏（~6900 rune 单段触发 S3）/偶数干净 →
  TotalChapters==50、QualityIssueChapters==25、AnalyzedChapters==3、
  无大纲节点 gateOutlineIssues 如实为空，全程 ~0.7s 零 LLM。
- **线C 审批链 + 回退演练**（3 例）：BuildNovelStatePatch→Settle(false)
  **AI 建议无一自动入库**（版本 0/账本空）→Settle(true) 版本 1→
  journal Replay 与 GetNovelState 逐字段一致（append-only 回放）→二次结算
  版本 2（账本只增）→坏 JSON 拒且不污染账本；app 级快照往返（改→快照→再改
  →Restore 精确回快照态）；v3→v4 迁移非破坏（**真实 v3 夹具**：场景 ≥1、
  Stitch==原文、`_v3_backup/001.md` 保留原文）。

## 版本三处漂移根治（独立发现）

app_info.go / versioninfo.rc 停在 **4.323.0**——v4.324 起十版未跑
sync-version.ps1，exe 内嵌版本号落后十版（wails.json/README 已 4.333）；
check-version-drift.ps1 闸门存在但不在 ci.ps1 内从未拦截。修：三处同步
4.334.0 + **ci.ps1 新增 version drift check 段**（防再犯）。

## 顺手清账

- eslint 5 条存量告警清零：EXTRACT_OPTIONS 归位 novelOptions.ts（与
  GENRE/STYLE 同家，react-refresh 合规）+ 4 处未用变量/导入
  （CharacterFormHelpers/MonteView `_cpm` 缝合面保留/monte.test/
  FileVersionStrip.test）。
- vitest 3185/3185 不变（零前端行为改动，3 个测试文件仅进口调整）。

## 坑（沉淀）

- `util.RetryJSON` 的 ExtractJSON 对「回复整体即裸 JSON」判为未找到——
  桩的分析回复必须包 ```json 围栏，否则 3 次重试全失败。
- `rebuildScenesFromBlob` 对无场景章是 no-op（物化靠读路径惰性）——验证
  「重置+物化」须预置旧场景。
- `project.Create` 现直接写 v4 标记：旧夹具「v3 工程」假设全部过时
  （newMaterializeApp 里 MigrateV3ToV4() 实为 no-op）；构造真 v3 须手动
  摘 `.gaea/v4`。

## 门禁

go build/vet 全过、go test ./... 全量全绿、tsc -b 零错、eslint 零告警、
vitest 3185/3185、drift OK@707（零新绑定）、版本三处 4.334.0（新闸门过）。
产物=exe 50832384B SHA256=4d2a01c5c84a8e6696fbe845d3583d0591ec79f26e083840dc01110b31c081bc（releases+SUMS；桌面副本同哈希；
冒烟 /api/health 200 过）。
