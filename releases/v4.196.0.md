# v4.196.0 — 语义索引显形与显式补齐（2026-09-10）

> 造价刀路池第 5 项（docs/gaea-cost-domain-survey-2026-09.md「检索索引被动
> 维护：仅检索时按需增量向量化，无后台任务」）。

## 读档修正：刀型收缩（以 v2.18 先例去重）

读档发现刀路池第 5 项描述**部分过时**：

- `GaeaSemanticIndexStatus`（D3-1，v2.18.0）早已提供各 kind 向量条数显形，
  前端 api/engines.ts 有消费面；`semantic.Coverage` 所在的共享索引还有
  file 类每 10 分钟的定时维护（GaeaSemanticSearch 注释在案）；
- 真剩余缺口只有两个：**覆盖数字没有内容口径**（Counts 只数向量行，正文
  改了仍算「已索引」）与**成本类没有显式补齐动作**（首次检索大库时冷启动
  向量化挤在 60s 超时里，超时即静默降级仅关键词）。

按「先读完再动、以先例方案去重」收缩刀型：不新造状态绑定，**扩展现有
Status + 只新增一个 Backfill**（曾一度做成两个新绑定，tsc drift 双向断言
与 git log 读档把它拉了回来）。

## 落地

**Go**：

- `semantic.Store.Coverage(kind, docs)`（新，非绑定）：「向量在且正文快照
  一致」的数量——与 Ensure 的缺失判定完全同口径，状态数字与补齐行为不再
  各说各话；
- `SemanticIndexStatus` 结构体扩展 `costTotal` / `costIndexed`（omitempty，
  旧消费面零破坏）；
- 新绑定 `GaeaSemanticIndexBackfill()`（MemoryB，work 空间）：对全部成本
  条目增量向量化（与搜索路径共用 Ensure，幂等），返回
  `{total, updated, indexed, missing}`；模型未配置时拒绝并说人话，绝不
  静默假装补齐；10 分钟超时（对比搜索路径 60s——补齐是显式动作，给足预算）。

**绑定面 606→607**，七处接线：gen_bindings（MemoryB 门面）/bindingNames/
mappings gaeaToGaea/spaceBindings 分类表 429/bridge cost.ts 接口/wailsjs
MemoryB 双件（手同步）/dev mock（演示态诚实报「浏览器无本地语义模型」）。

**前端（CostLibraryView 工具条）**：索引 chip 三态——

- 全覆盖：绿色「语义索引 x/x」禁用（title 注明正文变更自动重嵌）；
- 部分覆盖：琥珀「语义索引 x/y」可点 → 补齐（诚实 toast 汇报 updated）；
- 模型未配置：灰色「语义索引 未启用」（title=modelNote 人话），不可点。

**测评集**：docs/retrieval-eval-set.md 补 13-15 三条清单级综合单价描述
查询（表格与 JSON 块同步；expected 复用既有条目名，零新种子）——对应
survey「测评集 12 条偏材料/台班，清单级综合单价描述覆盖少」。

## 测试

- Go（gaea_semantic_index_test.go +2）：覆盖显形→补齐全覆盖→二次补齐
  幂等零重嵌→正文变更判未覆盖→重嵌恢复（复用 countingEmbedEnv 计数
  fake，能断言「没发生嵌入」）；模型不可用时 Status 诚实 Available=false、
  Backfill 拒绝；
- vitest（CostLibraryView.test.tsx +2）：chip 三态（部分覆盖可点、补齐后
  全覆盖禁用、模型未配置渲染为 span 不可点）。

## 门禁

Go 全量 exit 0 / vitest 全量 316 文件 2768 例 exit 0 / tsc 0 / eslint 0 /
e-check OK / drift PASS@607 / 版本三处 sync 4.196.0。

## 欠账

- 刀路池第 6 项（人材机含量基线）需行业含量区间数据源，等真实样本；
- 壳内真机走查池不变。
