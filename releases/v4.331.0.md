# v4.331.0 · 自动门可见化 + CI race 扩面（刀8 race 项收口）

> 2026-09-17 发布。规格书：`进度计划/gaea-gate-notice-race-20260917.md`。

## 论点

- 自动门（v4.326）异步跑完只落 slog+事件——「生成→体检→分析同步」主轴的
  最后一环（知会作者）缺失。
- CI race 门禁（S0.5）只盖办公核心包，小说 revolution 期六个新生包无并发
  防线（自动门/覆盖缓存/状态机的并发路径都在其中）。

## 落地

- **useChapterGateNotice**（CreatePage 挂载，零新绑定 707 不动）：订阅
  chapter-gate 通道（useChapterStream 同款 EventsOn/EventsOff 模式），
  report 载荷 → 轻通知「第 N 章 生成后自动体检：契约 X 项 · 质量 Y 项 ·
  AI 味 Z · 分析已同步」（antd message key 防叠 6s；空体检显示「完成」；
  分支章带标记；非 report 忽略；浏览器无通道静默）。
- **CI race 扩面**：race job 追加 chapter/analysis/promptstore/novelgate/
  rewrite/characterstate 六包；internal/app 注释不进（race 下已知 TempDir
  清理竞争 flaky 先例，候单独评估）。六包本地普通模式全绿。
- revolution 刀8 行补注：race 项收口；余=跨模块验收+回退演练候排。

## 测试与门禁

- 前端 +3（文案拼装三分支 / 订阅触发+防叠+非 report 忽略+卸载退订 /
  无 runtime 静默）。
- 门禁：go build 全过、六包全绿、tsc -b 零错、eslint 零告警、vitest 全量
  3172/3172 一次全绿、drift OK@707、版本三处 4.331.0。

## 坑（沉淀）

- `vi.stubGlobal('window', {...})` 整窗替换会炸 react-dom 的 document/window
  依赖——测试只换 `window.runtime` 属性并恢复。
