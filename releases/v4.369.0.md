# v4.369.0 — 真机走查班第二班：流式分段渲染真机验收 + --w-ink-3 重指向

> 用户口径「继续」——走查班第二班：v4.366~v4.368 三轮（聊天组件 memo 化/编辑输入性能/流式分段渲染）真机验收；顺手落地别名重指向小刀 --w-ink-3。前端 1 源文件（css），绑定面 706 不动。

## 走查结论（CDP 9333 附着新构建壳，只读纪律）

1. **流式分段渲染链路真机验证通过**：聊天页新建会话→发送短消息→模型正常回复（「我是一个热心且博学的AI助手。」）→回复正常落库与渲染；终态无错误、无 file:// 残留、无 console 告警。分段渲染的切分契约由新增的 findStableCut 单测（6 例含盲区修复）锁定，真机验证补上了「发送→流式→落库→渲染」全链路一环。
2. **--w-ink-3 重指向目检通过**：launcher 局部令牌 `--w-ink-3` 实测解析为 `#5b6472` = `--md-sys-color-text-tertiary`（亮态），与主题令牌一致——别名重指向两目中的最后一目收官。
3. 环境事实记录：走查中观察到聊天历史存在「请求超时」消息（用户环境的模型服务当时不可用所致，非本轮回归；重试即可）。

## 落地

- **--w-ink-3 重指向**（module-launcher.css）：`color-mix(in srgb, var(--color-text) 46%, transparent)` → `var(--md-sys-color-text-tertiary)`——六个次级文本别名收敛的最后一目；`.ml-avatar-user` 的二次混色底 12%→14% 微调对齐观感。真机目检解析值与 tertiary 一致。

## 清场

- 走查用的测试消息对（user+assistant）经 chat.db 精准删除（按 id 定位两条，会话恢复原有 6 条消息）；走查脚本/截图清理；杀壳。用户既有会话与真实工程零触碰。

## 测试与门禁

- tsc 0 + eslint 0 + 全量 ci.ps1 绿 EXIT=0（vitest 387 文件 3315 例）+ bindings drift OK@706 + 版本三处 4.369.0。

## 坑

1. **CDP 目检读局部令牌要在定义作用域元素上读**——`--w-ink-3` 定义在 `.ml` 选择器下（板块局部令牌层），从 documentElement 读恒为空串；应读 `.ml` 元素的 computed style。
2. **走查发消息会写用户真实库**——聊天无消息级删除绑定，测试消息只能按 id 从 chat.db 精准删除（删前 SELECT 确认、按主键删）；后续走查若需验证流式，先建专用测试会话再删除整个会话（ChatTopicDelete），避免污染用户会话。

## 产物

- exe **50,951,680 B** SHA256=`26f0293b4b08e03f70d9220093872c3eff335ed72642116d341ec6029fbdfc50`（releases/gaea-v4.369.0.exe + SHA256SUMS-v4.369.0.txt，exe 仅本地不入库；桌面副本同哈希实测一致；冒烟 /api/health 200 过）。
- 保留策略 5 版：留 v4.365~v4.369，删 v4.364.0.exe（SUMS 身份档案全保留）。

## 文档

- releases/v4.369.0.md + CHANGELOG/README + releases/README（保留策略行 + 发布说明计数 390→391 + 34 席索引插 v4.369 裁 v4.335）+ AGENTS 迁 1 插 1（八十四迁：v4.366 入 archive）+ progress/todos（别名重指向两目收官销号）。
