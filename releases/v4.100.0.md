# v4.100.0「三线并行：画室 T1 余件 · 办公 U4 前半 · GenUI 围栏审计」

- 发布日期：2026-09-05
- 绑定面：**581 零变更**（本刀口径；树内并行会话 Model Hub 线在途 +3 绑定未包含在本发布提交，drift 在含其改动的树上 PASS@584）。
- 三线并行（文件所有权互斥子代理 + 主代理收口），基线 v4.99.0。**纪律说明：本轮子代理禁区清单显式排除并行会话在写文件**（bindings_core/app.go/model_router/internal/modelengine/internal/config/bindingNames/engines.ts/modelcenter 一族/bridge.ts/spaceBindings.ts）。

## 线 A：画室 T1 余件（longterm-plan §T1 剩余两项）

1. **识图「读/懂」画室试用入口**（`frontend/src/components/imagegen/VisionTrial.tsx`，8 用例）：
   - imagegen 顶部「生成模式」导航新增「识图试用」tab（与素材库同模式）。
   - 动作链：paste 事件（窗口级）或隐藏 file input 选图 → FileReader dataURL → SavePastedImage 落盘 → readFileAsDataURL 读回预览 → 两动作：「识别内容」GaeaRecognizeImage(path, 自填提示词或默认通用词)、「提取文字」GaeaOCRText(path)。
   - 结果区诚实呈现：原语 Tag（识图-懂/识图-读）+ 模型名（仅当返回真带 model 字段）+ 文本/错误原文；「识图结果为文本，不产图、不入素材库登记」明示；loading/空态/未载图提示齐备。
   - 历史：localStorage `imagehubT1.visionHistory` 最近 5 条（文本截 600 字），缩略懒读缓存，点卡片回放并可原地重跑。
2. **画室「创作资产」面板**（`AssetStudio.tsx`，10 用例）：
   - 角色槽：CharacterList 分页追加（总数/加载更多），选中带出立绘 + 性格/背景/外貌/标签摘要；
   - 模板槽：复用绘梦既有 TEMPLATES（231 个平铺）+ 自定义模板，搜索 + 18/页懒加载；
   - 近期作品：imageHubAssets 当前空间前 12 张（work/play Segmented），点卡片跳素材库。
   - 回填生成台（不硬拼）：模板→既有 applyTemplate；角色→既有 applyRefCharacter（T2 参考槽路径，无参考图诚实 warning）；另有「复制设定」剪贴板手动路径（失败诚实 warning）。回填后面板自动关闭。
   - 零新绑定：四能力全部既有绑定经 appFacade() 三态回退（v4.99 同款）。
3. **走查实锤并修复真缺陷**：三个视图 tab（素材库/创作资产/识图试用）onClick 只开不关互斥方——双 tab aria-selected 同时为真且内容不切换（jsdom 分组件测试盖不住页面级缝）；修=三处 onClick 互斥关闭另两个 state，?mock=1 序列验证（studio→vision→lib）三态各自唯一。

## 线 B：办公 U4 前半（蒸馏规划 §4.3 ①②，③ pptx 联调不在本轮）

1. **渲染证据通道盘点**（`docs/gaea-u4-render-evidence-inventory-2026-09.md`，纯盘点零绑定）：
   - 6 通道对照：①bash soffice+pdftoppm+vision（office-edit 技能验证闭环，绑定 0）②Verifier 通道 B 逐页缩略（成对 PNG，before/after diff 独有）③GaeaPreview 直读渲染（docx 整本 base64/docx-preview；xlsx excelize+按需 Recalc；pptx soffice→PDF→页图，**缓存键含 size+mtime 纳秒，文件一变自动失配重转**）④GaeaPreview image 探测万能取图 ⑤GaeaReadFile ⑥排除项（screen_capture/CDP 非文档渲染通道）。
   - 结论：写后反馈证据**首选 ③+④（app.Preview 单口刷新，零绑定零后端改动）**；兜底 ②+①；缺口 6 条只列不立绑定（各附「若立绑定建议形态」，建议真模型走查后再定）。
2. **写后预览实时跟随**：
   - 信号源：App 既有 U2 工具卡跃迁 effect 派生 officeUpdatedPathsFromResult（ok 才派/写类才派/Office 扩展名才派；失败与 stopped 不派）；
   - `createPreviewRefreshScheduler`（officeTurnProjection §5 纯逻辑，800ms 防抖、同路径连写合并、**到点才做 isOpen 判定**）→ isOpen=主区大预览命中 或 pane **活动**文件 tab 命中（路径归一比较）→ `paneTabs.requestReload` 刷新总线（瞬态不落盘、会话切换清空）；
   - FilePreview 以 reloadSignal 消费：**静默重读 app.Preview**——不进 loading、不清 preview、不重挂；DocxPreview renderAsync 前后保 scrollTop，同 relPath 载荷刷新**不作废修改队列**（跨 relPath 才作废）；XlsxPreview body 变化即重读 .gbase.json sidecar；
   - **与 U2 协同**：刷新绝不置前（不碰 previewAutoFrontReduce）；已关闭文件不刷新也不弹；专注/窄屏天然 isOpen=false；刚被本回执自动置前的路径跳过刷新；基线期（恢复会话）不派信号；文本编辑态/有脏草稿跳过刷新（绝不拿旧草稿覆盖新内容）；
   - 「已自动刷新」badge 2.6s（officeTurn.autoRefreshed* 三语）。
   - 已知小代价（文档如实记）：刷新瞬间 XlsxPreview 未提交单元格草稿被重置（body 变化既有语义）；sidecar 重读复位激活的 gbase 视图（配置可能已变，诚实重置）。
   - 目标测试 108 passed（officeTurnProjection 41 含 §5 新增 10、paneTabs 12、FilePreview 25 含刷新 4 例、DocxPreview 10、XlsxPreview 18、WorkspacePane 2）。
   - 如实记录：真模型写文件→跟随场景 ?mock=1 无法驱动，验收面=上述 108 jsdom 用例，真机待复验。

## 线 C：GenUI 记忆/压缩围栏审计（P5 剩余项）

- 产出 `docs/gaea-genui-memoryfence-audit-2026-09.md`，五链路逐条结论（全部带 file:line 证据）：
  1. 记忆沉淀链：主链路（用户输入框/remember 工具）原样无害；**3 个 Go 侧低风险吸收点登记待裁决**（做梦 dreamInput 每轮拼最后一条 user+assistant 原文、BuildCompactSummary 关键词摘要前 160 字符、whisper companion_reply 前 200 字符——围栏 JSON 可能被吸进记忆条目）；
  2. 上下文压缩链：围栏是模型可见正文原样进 summarizer、token 三处口径一致全计入=正确；低风险=summarySystemPrompt 无围栏指引且 digest 永久 pin；
  3. 子代理投影/转录链：SubagentThread 在 GenuiScopeProvider 之外→volatile 渲染（不持久化/action 禁用）=原样无害；v4.83「官方 patch 口径」实为图片 token 估算与围栏无交集；
  4. 会话恢复/回放链：指纹防线有效 key miss 默认渲染=诚实降级；已知限制=实时/重放/resync 三套锚点口径不一致，跨重启状态恢复实际不生效（诚实但不理想，待裁决）；
  5. 导出链：原文本导出、交互状态不导出=无害。
- **genui 内修复 1 项**（越界修复全部只登记不实施）：InputNode.submit 对 password 输入只回传 `{valueLength, id}` 不再回传明文——堵住「交互值→[genui-action] 会话日志→压缩摘要/做梦记忆链」泄漏路径，与 handbook「秘密禁令」对齐；新增 2 用例。
- 待主代理/后续裁决清单（Go 侧，未动）：dreamInput 围栏剥离（P5 规划项本体）、BuildCompactSummary/whisper 摘要跳围栏、compact summarizer 围栏口径、resume 槽位口径统一、面板 append resync 去重、会话删除清理 resetInteractionStore。

## 收口（主代理）

- i18n：imagehubT1.* +45 键（A）、officeTurn.* +2 键（B）×三语；重复/缺失键体检 none（两命名空间共 77 键三语一致）。
- **?mock=1 真浏览器走查 PASS**：绘梦→创作资产（角色槽 3 卡含无立绘占位/模板搜索+套用卡/去素材库入口）→识图试用（全要素渲染）；三视图互斥修复序列验证（studio→vision→lib 各自唯一 selected+内容互斥）。
- 门禁：Go build/test/vet 全量 0 FAIL（树内含并行会话在途 Model Hub 改动，如实注明）；tsc -b 0、eslint 0；vitest **250 文件/1993 用例全绿**（净增 2 文件/39 用例；首轮 1 例负载 flaky，复跑+定向复跑全绿=既有已知模式）；drift PASS（584=581 本刀口径 + 并行会话未提交 +3）。

## 欠账与后续

- U4 后半：pptx 面板接续编辑与刀2 联调（随 pptx 真编辑拍板）；渲染证据缺口 6 条（真模型走查后再定是否立绑定）。
- GenUI 审计裁决清单（Go 侧 6 项 + 恢复口径）见 docs/gaea-genui-memoryfence-audit-2026-09.md §8。
- 画室余件：T2 一致性方法实现（ipadapter/pulid）、模型目录分层展示进创作语境。
- 老账不变：run_skill AllowedTools 真机观察、3c 人工沙箱浏览器、微信文件收发（抓包前置）、pptx 真编辑（6 项待拍板）、转正余件（GenerateFreeImage 等直调族，bridge.ts 与并行会话共树期间暂缓）。
