# v4.72.0「删除主区概览标签页」

> 发布日期：2026-09-04 ｜ tag：`v4.72.0` ｜ 基线：v4.71.0 ｜ 绑定面 **561
> 零变更**（纯前端，Go 零改动）。

## 背景

概览 tab 承载的会话级 Token/费用/命中率统计，已由上下文页的 Token 统计 /
预估费用 / 会话信息卡承接；顶部标签栏保留 4 个 tab 只会增加切换成本。
用户点名删除后，收敛为「对话 / 轨迹 / 上下文」3 tab。

## 删除面

- ChatTabs：移除「概览」tab 与 ChatTabId.overview；tab 顺序收敛 3 项；
- App：删除 OverviewPanel 渲染分支、概览相关 props 透传、useStatsPersistence
  调用与 statsReset 状态；历史 localStorage 选中 overview 自动回落「对话」；
- 命令面板：删除「概览面板」命令（paletteRank 无主区 tab 专属命令）；
- 删除 OverviewPanel 容器组件及其定向测试；底层 StatsPanel/统计持久化模块
  暂留未接线（如需彻底清理统计模块与死键，另行处理）。

## 验证

- tsc -b / eslint 0（全量）；vitest **223 文件 / 1677 用例**（净减 6：
  OverviewPanel 5 用例随容器删除 + paletteRank chatTab-overview 用例删除；
  ChatTabs 概览用例改写为 3 tab 等价保留）；
- mock 走查：标签栏仅剩对话/轨迹/上下文，Ctrl+K 无「概览面板」；
- 冒烟 /api/health 200。

## 产物

- gaea-v4.72.0.exe ｜ SHA256 见 releases/SHA256SUMS-v4.72.0.txt
