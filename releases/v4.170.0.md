# v4.170.0 — 瘦身 P3 结构版1：巨文件首批 4 拆（App/bridge/types/GanttView，>50KB 9→5）（2026-09-09）

> 瘦身执行层第五版：P3 结构面第一刀（masterplan 轨道三；出口判据=绑定零变更 drift PASS + 拆分池 >50KB 9→5）。
> 四线并发子代理（足迹互斥）+ 主代理收口——**纯结构拆分：行为零变化、公开导出面逐一同、测试数零新增零减少**。

## 1. 线B：App.tsx 拆分（子代理 ea7fefbd）

- `frontend/src/gaea/App.tsx` **87.3KB → 46.6KB**（<50KB）。
- palette 构建/command palette items（含 cmd-schedule）/exportConversation onPick/JSX 布局**刻意留壳内**——
  App.export.test/App.palette.test 是对 App.tsx 源码的 readFileSync 行级断言（拆走即红），留下约 4.7KB 无法迁移。
- 11 组逻辑迁新 `frontend/src/gaea/app/`：useSubagentTabs（子代理 tabs+taskPick 候选）/useTaskCards（taskActivity 注入接线）/
  usePreviewAutoFront（U2/U4 状态机+写后刷新调度）/useDeliverables（产物角标+tab 置前）/useWorkspaceLayout（右栏宽度持久化+拖拽）/
  usePreviewPanel（预览队列+专注模式+pane 拖拽）/useSessionHandlers（会话/历史/记忆回调）/useAppKeyboard（全局快捷键）/
  useTasksAutoOpen（新任务 500ms 去抖激活）/sessionCleanup（purgeDeletedSessionGenui）/layout（CHAT_MIN_WIDTH）。
- **行为等价保障**：每个 hook 调用点停在原代码块位置，deps 数组与执行顺序逐项原样（35→16 组 effect，含原 2 处 eslint-disable）；
  JSX 区与基线逐字节一致；hook 参数化后 exhaustive-deps 降级 warn 按仓库风格加带理由 disable 注释（0 error 0 warning）。
- 测试：App.export.test + App.palette.test 6/6 通过（src 级断言全保住）。

## 2. 线C：bridge.ts 拆分（子代理 f6e2ab51）

- `frontend/src/gaea/lib/bridge.ts` **85.1KB → 1.3KB**；新 `gaea/lib/bridge/` 16 文件。
- **按 bindingNames 域切**：10 分域接口（core 16.4KB/office 6.8KB/memory 10.4KB/cost 7.3KB/model/voice/chat/novel/image/charlib）+
  `appBindings.ts`（`interface AppBindings extends CoreBindings, …, CharLibBindings {}` 组合，成员冲突编译报错兜底）+
  mappings.ts（gaeaToGaea 映射表）+ proxy.ts（realApp/app/workApp/playApp/sharedApp/BridgeError/openExternal）+
  events.ts（5 个 on* 订阅 + mock 分支）+ http.ts（initBridge + RPC 代理）+ drift.ts（双向漂移检查）。
- **行为等价**：Proxy/懒探测/mock 回退/事件时序原样（PowerShell 脚本按内容锚点切片非手抄，方法签名字节级一致）；
  方法名 parity **old=315=new**（与 spaceBindings.test 锁一致）；**bindingNames 602 零变更=drift 锁不动**。
- 公开导出 17 名 re-export 逐一同（AppBindings/PptxSlideTextEntry/SubagentTextEvent/onEvent/onSubagentText/onUpdaterProgress/
  onTaskEvent/onReady/BridgeError/app/workApp/playApp/sharedApp/openExternal/initBridge/_CheckAppBindingsHasNoStray/_CheckAppBindingsCoversAll）。
- 测试：21 个引用 lib/bridge 的测试文件 186 用例全绿。

## 3. 线D：types.ts 拆分（子代理 8aaeadac）

- `frontend/src/gaea/lib/types.ts` **66.4KB → 1.2KB**；纯类型按域拆 `gaea/lib/types/` 24 文件：
  shared/wire/session/context/git/trajectory/agent/shell/files/office/subagent/deliverables/resync/capabilities/memory/knowledge/settings/updater/whisper/weixin/cost/search/tasks/programming。
- **220 声明零增减零重名**（列 0 锚定 155 interface + 63 type 与原始一致；CostSummary 成员/CostEntry/CostComponent 两条缩进接口为 +2 真值原样保留）；纯 type-only 无运行时值。
- 入口 types.ts 改纯 `export *` re-export 桶；**135 处消费方 import 零改动**；search.ts→files.ts 唯一跨域类型依赖，无循环。

## 4. 线E：GanttView.tsx 拆分（子代理 56c9871a）

- `frontend/src/schedule/GanttView.tsx` **64.8KB → 25.2KB**；新 `schedule/gantt/` 8 文件：
  ganttUtil.ts（共享常量+纯函数 predsOf/succsOf/frontierWd/fmtDate 等，对标 aoaRuler 范式）/PredEditor/CustomFieldCell/GanttToolbar（含 chatPrefs 排序分组持久化）/GanttTable/GanttTimeline/GanttBars/GanttOverlay。
- **classNames/data-testid 逐字节照搬**（sched-group-row-/sched-progress-/sched-gantt-* 等测试锚点全保留）；导出面零 diff（仍只导出 GanttView，3 处引用方零改动）。
- 刻意保留：GanttView 30+ state/memo/handler 组合层（深拆需 context/reducer，超「行为零变化」红线）。
- 测试：GanttView.test + ResourcePanel.test 57/57 全绿。

## 5. 主代理收口

- 四线足迹互斥核验（git status 仅 4 M + 4 新目录，无越界）；并发中间态法=子代理按「只修足迹内」忽略足迹外 tsc/vitest 红（如 ganttUtil CostResult 中间态），稳定后自愈。
- 度量：拆分池口径 **>50KB 文件 9→5**（首批四巨文件全拆；次批 KnowledgePanel 62.2/DeliverablesPanel 57.4/XlsxPreview 56.4/ChapterPage 55.9/herdsmanTemplates 55.7 待版2）；entry chunk 1193.59kB gz 375.59、MemoryHubPage 1436.11kB 与 P0 基线逐位持平（纯拆分零体积劣化）。
- 版本三处 4.170.0（sync-version.ps1）。

## 6. 门禁（主代理统一复跑）

- Go **127/127 包 0 FAIL**（含 TestBindingsCompleteness=PASS，drift 锁 602 零变更）；
- vitest **2737/2737**（313 文件全绿 exit 0，纯拆分零新增测试）；
- `tsc -b` 0 · `eslint .` 0（全量）· drift **PASS@602**（零绑定）· locale 死键 **0 死**（1448 键不受影响）。

## 7. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 1193.59 kB gz 375.59 ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 48.47 MB ｜ >50KB 源文件（拆分池口径）9→5
```

## 8. 产物与冒烟

- 产物：`gaea-v4.170.0.exe`（48,472,064 B），SHA256=`C19C1319C94E869B5E23AFB068E24C03454BDC3B9E919175436934132700321C`（releases/SHA256SUMS-v4.170.0.txt）。
- 冒烟：`scripts/smoke.ps1 -ExePath releases\gaea-v4.170.0.exe` → **/api/health 200，响应 {"status":"ok"}**（冒烟进程已回收）。

## 9. 欠账

- **P3 版2**：office 抽核（journal/evidence/文件读写→`internal/core`，office 树 29 处 import 集中 internal/AI 22 文件+根包 3 处已盘点）+ bridge 双轨退役启动（wailsjsCompat 单 shim 被 57 文件引用，渐进迁移）+ 次批 5 巨文件（KnowledgePanel/DeliverablesPanel/XlsxPreview/ChapterPage/herdsmanTemplates）；
- 壳内真机池（rail 切换器/home 最近文档/.gsched 基线 N/knowledge 无残留）、审计刀D 真机取证（printSvg print/拖拽/粘贴）不变。