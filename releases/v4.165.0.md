# v4.165.0 — 拍板落档（路线A+私有许可）+ 壳内残留审计刀A：P0×2 修复（2026-09-08）

> 收敛计划 §0 两项用户拍板落档 + docs/webview2-shell-audit-2026-09.md §5 刀A。
> 单线子代理（Carver）实现 + 主代理收口发布。

## 1. 拍板落档（收敛计划 §0 → 已决）

- **性质路线 = A「终极个人工具」**：产品化降为期权不作承诺；所有决策按自用标准，
  不为想象中的用户写代码。已写入 AGENTS.md 长期规划与收敛计划。
- **LICENSE = 私有 All Rights Reserved**：根目录新增 LICENSE（私有许可全文，含
  「未来开源须另行发布开源许可证覆盖对应模块并与本文件区隔」的尾注）；README
  新增「许可」段。

## 2. 审计刀A：P0×2 修复（壳内点击完全无反应的两处）

### 2.1 新中立层 `frontend/src/gaea/lib/pickFile.ts`

```ts
export function inShellEnv(): boolean
export async function pickFileAsFile(accept?: string[]): Promise<File | null>
export async function pickImageAsDataUrl(): Promise<string | null>
```

- 只 import `./bridge`（防 schedule 域依赖倒挂），供任意域复用；
- GaeaPickFiles **无文件类型过滤器**（审计刀D 项）→ 本层按扩展名后置校验，
  不合法抛错由调用方提示（fail-closed 不静默吞）；取消=返回 null；
- `pickImageAsDataUrl` 白名单 png/jpg/jpeg/webp/gif/bmp，mime 按扩展名映射，
  未知降级 application/octet-stream；ReadFileB64 64MB 上限注释已提及。

### 2.2 P0-1：BaselinesPanel 导出签证台账 CSV

- `exportLedger` 直呼 `downloadBlob` → `saveExportBlob`（壳内系统另存为，浏览器
  回退=原 downloadBlob 行为不变）。v4.162 修了 SchedulePage 主链路漏了同域邻居
  的典型（「同组件域内新出口未过统一收口」）。

### 2.3 P0-2：角色库「添加参考图」

- 壳内改 `pickImageAsDataUrl()` 读回 dataURL 追加 referenceImages；此前
  `fileRef.click()` 壳内点击完全无反应且**无粘贴/拖拽替代**，下游「以参考图生成
  剧照（img2img）」整条链被卡死。浏览器回退原生 input；hidden input 与既有
  浏览器路径用例口径原样保留。

## 3. 红rightarrow绿取证

- 刀A-2 修复前：`壳内添加参考图` 用例 `GaeaPickFiles 0 次调用`（点击无反应本体复现）；
- 刀A-1 修复前：`点击下载 CSV` 用例 `saveExportBlob 0 次调用`（仍在直呼 downloadBlob）；
- util 修复前：模块不存在（import 失败）。三条均先红后绿。

## 4. 门禁

- vitest：2678→**2684**（305 文件，+6）——全量**首跑全绿**（v4.164 的 testTimeout 15s
  抗抖当场见效，本轮无 CI retry 依赖）；
- Go 全量 0 FAIL（零 Go 变更）· `tsc -b` 0 · `eslint` 0 · drift **PASS@602**（零绑定变更）；
- build.bat 构建+自动冒烟：见下。

## 5. 欠账（如实列示）

- 审计刀B（下载类：ImageGen×2/NovelSetting 导入导出/办公 md 分支）；
- 审计刀C（上传类：ControlPanel/VisionTrial/SkillModal）；
- 审计刀D（真机取证：printSvg iframe print/拖拽/粘贴 + GaeaPickFiles Filters 可选参数）；
- nanoid high 升级刀（v4.164 遗留，lock 已钉住版本）。

## 6. 产物

- `releases/gaea-v4.165.0.exe`（46.2 MB，gitignore 不入库）
- SHA256：`ba6166a0ad1c6be9d213be3b5640595d19222eef7e2c152f9026c107eae69446`
  （见 `SHA256SUMS-v4.165.0.txt`）
- build.bat 内置冒烟：`.tmp\smoke-gaea.exe` HTTP 桥接 127.0.0.1:18999 `/api/health` →
  `{"status":"ok"}` 200，进程已回收；桌面副本同步。
