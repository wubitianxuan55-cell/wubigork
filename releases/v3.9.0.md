# v3.9.0「双空间壳 + 办公信任链」(2026-08-29)

> git tag `v3.9.0`；基线 v3.8.0（双空间内核 · 阶段 1）。本版完成 **阶段 2 双空间壳**
> （S2.1–S2.3 + S2.3b + 页面迁入 P1）+ **v4.1 办公信任链**（证据链→复核→回滚→规范体检）。
> 验证：Go 全量绿；vitest **738/738**（139 文件）；tsc/eslint 0；`vite build` 成功；
> 绑定面 **506 方法**漂移 PASS；版本五处统一 3.9.0（app_info / wails.json /
> versioninfo.rc / package.json / package-lock）。

## 阶段 2 · 双空间壳

- **S2.1 壳层重构**：工位/乐园两视图 + 空间切换持久化（`gaea.shell.space`、
  每空间最后页面 `gaea.shell.page.<space>`）；删除旧 10 板块导航（manifest.space
  派生：work/play/shared/independent，编程 DSH 独立窗口单独 rail 入口）；双首页
  （工位=任务工作台门面 / 乐园=会客厅创作间门面）；事件订阅空间过滤
  （`subscribeForSpace`）；Ctrl+K 搜索 scope（默认当前空间，「全部」显式选择）。
- **S2.2**：工作台 localStorage 空间分键（`workbenchStorage`：rightTab/布局/
  chatTab/compact/focus → `gaea.work.*`，旧 key 只读迁移）；keepAlive 跨空间卸载
  （性能门控，后台轮询归零）；**i18n 决策**（审计「诚实 zh-only」：壳层 chrome +
  设置外壳三语，页面内容层保持 zh 单语）；**页面迁入 P1**（对话降为对话流：
  chat `shared→play`，工位对话收敛到办公工作台，乐园保留会客厅）。
- **S2.3 bridge 分面 + types 生成化**：`spaceBindings` 将 gaea 桥 **214 方法显式
  分类**（work/play/shared/independent，satisfies + 双向断言编译期全覆盖）；
  `workApp/playApp/sharedApp` 三门面（类型级红线 + 运行时门控）；`WireShape`
  types 全量迁移（55 个重叠类型改别名，1375→1065 行；修 `FileSearchHit.modTime`
  / `UpdateInfo.version` / `FilePreview` 契约漂移）；`typesGenerationCheck` 编译期
  漂移校验。
- **S0.7 遗留：151 hex token 化**：VoiceSettingsPanel 浅色主题不可读真 bug 修复；
  语义色全 token；图表/品牌/图片覆盖层显式 `hex-exempt`；新增 eslint 规则
  `local/no-raw-hex` 防回归。

## v4.1 · 办公信任链（工位「审阅后」护城河）

- **证据链（v4.1a）**：`evidence.ChangeRecord`（Before/After **原文摘要** 8KB，§15
  补丁）+ `ChangeLedger`（回合台账，ctx 盖章）+ `JournalStore`（按会话 JSONL +
  turn markdown 投影 `.gaea/work/exports/journal/`）；edit_file/write_file/
  multi_edit/edit_lines/move_file/xlsx_apply 接入；**play 回合不落盘**（红线）；
  前端「证据」入口（DeliverablesPanel 证据链折叠区）+ `GaeaJournalList`。
- **Verifier + 回滚（v4.1b）**：写盘工具/xlsx 应用前**整文件基线快照**
  （`.gaea/work/rollback/`）；`GaeaVerifyRecord` 双通道复核（A 结构/引用完整性 +
  B 基线 PDF 渲染对比，降级 warn）；verdict 落 `verdicts.jsonl`；`GaeaRollbackRecord`
  基线回滚 + **手工编辑冲突保护**（零覆盖）；前端证据卡复核/回滚操作。
- **中文规范包（v4.1c）**：`internal/office/standard` GB/T 9704 红头 **7 要素 lint**
  （发文机关标志/发文字号/标题/主送/成文日期/印章位/版记 + 修复建议）；
  `GaeaDocumentLint`（md/txt/docx）+ OfficePanel「规范体检」入口。

## 绑定面与验证

- bindingNames **502 → 506**（新增 GaeaJournalList / GaeaVerifyRecord /
  GaeaRollbackRecord / GaeaDocumentLint；gen_bindings 重生成 + CI 漂移检查 PASS）；
  spaceBindings **218 方法**全覆盖断言。
- Go 全量包绿；vitest 738/738；tsc/eslint 0；vite build；版本五处统一 3.9.0；
  CHANGELOG / README / AGENTS / progress 同步。

## 提交清单（8cd1bec → a455b5e，14 个）

`a455b5e` v4.1 收官（Verifier/回滚/红头规范包）｜`66f5fe1` v4.1a2 补齐（xlsx/前端证据入口）
｜`3b152f1` v4.1a 证据链核心｜`bb76d48` 页面迁入 P1（chat→play）｜`b62e9fb` 151 hex token 化
｜`9a62ac5` i18n 决策 + v4.1 设计定稿｜`5896cb9` SettingsPage i18n｜`295c20c` S2.3b types 迁移
｜`40cdb6d` 壳层 chrome i18n｜`c6a699b` S2.3 bridge 分面｜`b6272f4` S2.3a types 漂移校验
｜`90d2e7f` RemoteBoardManifest.space 收敛｜`10c9b8a` S2.2（分键/门控/i18n）｜`8cd1bec` S2.1 壳层

## 已知遗留

- ProgrammingPage.test 全量负载偶发 flake（单独跑通过，项目已知类）。
- v4.2 造价 AI 化 / v4.3 乐园做深 / v4.4 触点 待启动（§10.4）。
