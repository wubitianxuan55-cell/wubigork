# gaea v2.27.0「质量收敛 · 绘梦·链路真实生效」

> 阶段 6 第四刀（T6-4），2026-08-14。
> 规划：docs/superpowers/plans/2026-08-14-gaea长期规划-阶段6-质量收敛.md

## 发布内容

### T6-4.1 取消真实生效
- CancelImageGeneration：cancel context 之外调用 POST /interrupt 中断 ComfyUI 当前任务
- 本地取消标记拒绝后续排队提交（ComfyUI 无删除排队任务 API，/queue 仅查询——代码注释说明）
- checkHistory 携带 ctx，取消后轮询即刻退出；取消幂等

### T6-4.2 flux 名实相符
- 文生图显式映射表 txt2imgWorkflows（krea2/z-image-turbo/flux），未知模型中文错误（静默降级消除）
- flux 真实工作流：UNETLoader flux1-schnell + DualCLIPLoader type=flux + ae VAE + 4 步 KSampler
- img2img 白名单化（krea2*/z-image-turbo）

### T6-4.3 历史图片可恢复
- 后端：imageItem 增 file_path，生成流程写入落盘路径
- 前端：历史分级存储（>200k base64 只存 path）；挂载时经 GaeaAttachmentDataURL 回填恢复；下载/剧照优先 file_path；预览按 mediaIsVideo 选元素（t2v webp 不再播不了）

### T6-4.4/4.5 校验与注入面
- parseSize 严格解析（弃 Sscanf），非法中文报错，钳制 64–2048
- findProcessByPort 弃 cmd/findstr 拼接改 exec.Command("netstat","-ano") 参数数组；端口白名单 1–65535

### T6-4.6 测试补全
- httpClient/pollInterval 可注入；httptest 五链路 20 用例 + 全链路端到端；Go 新增 31 用例
- 前端 media/historyMeta/queue 纯逻辑模块 + 19 vitest 用例

## 验证
- internal/ai 4.2s + internal/app 23s ok、vet 干净；TestBindingsCompleteness PASS（无绑定签名变化）
- 前端：tsc 0 errors、vitest **312/312**（70 文件）、eslint 0 errors（761 存量 warnings）
- 冒烟：scripts/smoke.ps1（/api/health 200）

## 注意
- flux 工作流需 ComfyUI 装有 flux1-schnell.safetensors / t5xxl_fp8_e4m3fn / clip_l / ae，未装则提交 400（中文提示）
- 旧版本遗留历史（无 image 无 file_path）无法恢复，显示占位

## 产物
- gaea-v2.27.0.exe（32.6MB，SHA256=B00C9A9B327D34370116583FC5D48A64500C7575924CC7B1225D61BCC21E90C1）
