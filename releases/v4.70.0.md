# v4.70.0「上下文页视觉重设计：数字层级 · 构成强调 · 仪表化」

> 发布日期：2026-09-04 ｜ tag：`v4.70.0` ｜ 基线：v4.69.0 ｜ 绑定面 **561
> 零变更**（纯前端，Go 零改动）｜ 用户点名「重新设计」后 v4.69 交互精修
> 静态不可见（承认），本轮改为**肉眼可见的视觉重设计**，逐项对齐 dsh-context
> styles.css 实测数值（rc 10px / padding 14×16 / title 600 / stat value 14px /
> seg 圆角 5px / 选中实心强调）。

## 视觉重设计（对齐 dsh 仪表层级，功能零删减）

- **数字层级放大**：上下文统计格 value 13→15px（强项 14→17px）；当前上下文
  大数字与百分比 **20→26px bold**；Token/耗时环心 14→16px；各行数字
  10→11.5px——关键数据一眼可见（对齐 dsh 突出的数值层级）。
- **当前上下文宽卡强调（全程最高权重）**：卡头 12.5px semibold；分段条
  **h-3→h-4 + rounded-md**，色彩饱和；图例 chips 11.5px；空闲段灰点保留；
  告警行 10.5px。
- **卡头统一**：标题 11→12.5px semibold 左置 + 副注 10.5px 右置（dsh
  lc-card-sub 同构）；六分类构成卡、趋势卡、事件卡、文件活动卡同步。
- **环形图**：Token/耗时卡 Donut 84→92px、stroke 9→10px，环心中枢 16px
  semibold。
- **趋势卡 master-detail**：v4.69 内联详情保留（第 N 轮·第 M 步在趋势卡内
  分隔线下实时呈现，默认选中最新一步）；悬停柱六分类构成详情条 10.5px。
- **事件筛选多选**（v4.69）保留：五分类 chips 各自开关，选中态 rounded-md
  px-2 py-1（更接近 dsh 分段控件观感）。
- Token 环心命中率两位小数（99.89%）；卡内滚动/截断照旧；六分类语义色
  CAT_COLORS 单源不变。

## 验证

- tsc -b / eslint 0（全量）；vitest **224 文件 / 1683 用例**；drift PASS（561）；
  冒烟 /api/health 200。
- 走查受限如实记录：mock 走查浏览器视口 756px（模拟面板仅 ~123px 高），
  宽版网格仪表（4 卡行/3fr-2fr 双列）需桌面端实机复核；jsdom 27 用例
  （ContextView 15 + cards 12）为交互面验收。

## 欠账（沿 v4.68/v4.69）

- 浏览器「对比上一步变动」请求下拉（需 per-request surface 快照，后端加折叠）；
- ContextTiming 秒粒度近似 + 真实供应商「实际」用量卡（usage 落盘增强）；
- 图例 hover 跨卡联动（浏览器同分类高亮，需 lift 共享 hover 态）；
- 宽版仪表在 <1100px 回落单列（视口小卡片纵向堆叠，桌面宽窗为全宽网格）。

## 产物

- gaea-v4.70.0.exe ｜ SHA256 见 releases/SHA256SUMS-v4.70.0.txt
