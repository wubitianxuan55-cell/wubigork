# v4.370.0 — 工程健康轮：依赖漏洞清零 + Go 工具链升级 + 构建管线修正

> 用户口径「继续」——十轮快跑后的工程健康整固：govulncheck + npm audit 双扫描，按发现修复。Go 1 源文件（xlsxpreview 防线）+ 依赖升级 + 构建脚本修正，绑定面 706 不动。

## 扫描结果与处置

| 发现 | 严重度 | 处置 |
|---|---|---|
| Go 标准库 5 处（net/url 二次复杂度 / crypto-tls 握手 / net-http ReadHeaderTimeout / encoding+xml 与 asn1 递归） | 各有攻击面 | **Go 工具链 1.26.5→1.26.6**（go.mod toolchain 指令，构建自动拉取），exe 实测 go1.26.6 |
| golang.org/x/image v0.40.0 4 处（webp/bmp 解码 panic、VP8L 越界内存分配——图片解码是用户可控输入面） | 高优 | **升级 x/image v0.40.0→v0.46.0**（连带 x/sys/x/text） |
| excelize v2.11.0 恶意 xlsx 可致 panic（GO-2026-6452，**上游 Fixed in: N/A**） | 中（预览读用户自己文件） | **应用层防线**：xlsxpreview 的 Render/NeedsRecalc 入口加 defer recover 转错误（「xlsx 预览失败（文档结构异常）」），上游修复后可移除；govulncheck 复扫后仅剩此条（上游未修，防线已兜） |
| npm @vitest/mocker 2 处 moderate（路径遍历/任意文件读，dev-only 测试工具） | 低（不进产物） | **vitest ^4.1.10→4.1.11**，npm audit 归零 |
| 前端依赖无 lockfile（pnpm 项目历史上从未锁定） | 工程风险 | **新增 pnpm-lock.yaml**（pnpm install 生成并入库，vitest 4.1.11 锁定；CI 实测兼容） |

## 构建管线修正（在册坑落地）

build.bat 的 wails 命令此前未带 `-s`（跳过前端），wails 内部隐式 `npm install` 在 lockfile 缺失 + peer 依赖冲突（eslint-plugin-prettier ^4.2.5）时直接失败——本轮 vitest 升级触碰 package.json 后即暴露。按在册坑配方修正 build.bat：**先 `npm.cmd run build` 显式构建前端，再 `wails build -s`**（wails 捕获前端输出走管道会挂起 + 隐式 install 环境敏感，这正是 AGENTS 在册坑的原文配方）。

## 测试与门禁

- govulncheck 复扫：11→1（仅剩 excelize 上游未修项，防线已兜）+ npm audit 归零。
- go build/vet 0 + office 包全量绿 + 全量 ci.ps1 绿 EXIT=0（vitest 388 文件 3315 例，vitest 4.1.11 兼容）+ bindings drift OK@706 + 版本三处 4.370.0。

## 坑

1. **pnpm 项目里 `npm install` 会生成 package-lock.json 形成双轨**——发现后立即删除并 `pnpm install` 恢复单轨（新增 pnpm-lock.yaml 入库）。
2. **lockfile 缺失是环境敏感炸弹**——无 lock 时任何依赖树变动（升级/重装）都会触发重新解析，peer 冲突在 wails 隐式 install 中爆炸；lockfile 入库后此类失败不再复现。
3. **wails 隐式 npm install 依赖环境解析成功**——显式前端构建 + `-s` 是在册坑配方，build.bat 此前未遵守，本轮落地。

## 产物

- exe **50,979,328 B** SHA256=`785a740a681d410dfff9c8744d09cb37698ca6d756e0808f31b42edbae8de95f`（releases/gaea-v4.370.0.exe + SHA256SUMS-v4.370.0.txt，exe 仅本地不入库；桌面副本同哈希实测一致；冒烟 /api/health 200 过；**go1.26.6 构建**）。
- 保留策略 5 版：留 v4.365~v4.369+新增，删 v4.365.0.exe（SUMS 身份档案全保留）。

## 文档

- releases/v4.370.0.md + CHANGELOG/README + releases/README（保留策略行 + 发布说明计数 391→392 + 34 席索引插 v4.370 裁 v4.336）+ AGENTS 迁 1 插 1（八十五迁：v4.367 入 archive）+ progress/todos（健康轮记录：excelize 上游未修留观）。
