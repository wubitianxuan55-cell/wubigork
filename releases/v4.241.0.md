# v4.241.0 — 记忆注入质量可评测（市场调研候选2，绑定 625→626）（2026-09-12）

## 为什么是这一刀

市场调研（docs/gaea-market-survey-2026-09.md §2/§5 候选2）：2026 记忆层市场共识
=「可评测」；gaea 检索面早有门禁（GaeaRetrievalEvalRun，Recall@10≥0.8），注入面
（work 会话装配真正进系统提示词的两个记忆块）却从未被量化——「注得对」没有体检。

设计档：docs/gaea-memory-injection-eval-design-2026-09.md

## 口径

对真实库的 work 视图跑与装配点**完全相同**的两个真实构建器（晨报预载 v4.16 +
项目本体 6.2），断言结构不变量：

- 预算合规×2（各 600 runes）
- 归档/跨空间零泄漏（**精确路径**=预载条目名+本体引用键逐点核对；块全文子串
  兜底明确不做——work 正文合法提及归档名不是泄漏，子串必误报）
- 引用零悬空（悬空注入=模型会引用不存在的键）

**门槛=零违规**（确定性规则，与检索测评统计门槛分立）。固化覆盖是信息面不判死
（预算挤兑下固化未全收是设计内行为）。门控三开关透明下发，关态体检照跑。

## 形态

- Go 纯核 `memory.EvalInjectionBlocks`（internal/gaea/memory/injection_eval.go）
- App 绑定 `GaeaMemoryEvalRun`（internal/app/gaea_memory_eval.go），gen_bindings
  再生，**绑定 625→626**
- 前端六处接线 + 办公记忆库工具栏「注入体检」Drawer（渐进披露，不加一级房间）

## 测试

Go +7（纯核五不变量逐形态/固化覆盖信息面/空输入；绑定真库接线/关态 Note）；
vitest +3（通过态/违规态/失败回退）。

## 坑

1. **gen_bindings 再生会回退 v4.237 的手工单参化**——脚本从 App 方法签名派生
   门面，手工修的七个变参签名被还原，E27 守卫当场拦下（守卫价值实证）。
   **以后 gen_bindings 后必须 git diff bindings_cost/office，凡含变参行即手工还原。**
2. **两个 cfg 域不可混用**：`a.cfg`=*appconfig.Config（偏好域 GetMorningPreload/
   GetProjectBrief），`ga.cfg`=gaeaConfig（引擎域 Memory.Enabled/SpaceModeIsOn）；
   且裸 `&App{}` 读 a.cfg 因 core 未初始化直接 panic（测试须
   `&App{core: &core{cfg: ...}}`）。

## 门禁

tsc 0 / eslint 0 error / ci.ps1 全绿（vitest 332 文件 2889 例）/
drift OK@626 / E27 PASS / 版本三处 4.241.0。tag 停发（v4.200+）。

## 产物

build.bat（冒烟 /api/health 200 + 桌面副本同哈希）→ `build\bin\gaea.exe`
**48,991,232 B** SHA256=**BAAF70F0BA565DAAF094B4448FE4E2BD21C4217F536DD50528DE49A59A0DCA56**。

## 下一刀

- 候选3：办公流水线交付物「成品直出」打磨（防御性优先级，先出设计）。
- 候选4：价格带数据源（待拍板，不自行动）。
