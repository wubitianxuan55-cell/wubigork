# v4.55.0「继续完善：拆分补测起步 · mock 补面 · failover 文案 label 化」

> 发布日期：2026-09-03 ｜ tag：`v4.55.0` ｜ 基线：v4.54.0 ｜ 绑定面 **559 零变更**

## 定位

「继续」= 欠账池互不相交三线并行子代理（文件所有权逐字互斥）+ 一条欠账当场
DOM 核实销账。judge 走查以 DOM 断言+探针代替（本轮无新视觉面，行为面由三层
测试+浏览器闭环走查覆盖）。

## 销账 · 右舷虚线交叠（v4.54 judge Unverified 备注）

DOM 核实**证伪**：右舷（.ml-side）内无任何 dashed 边框元素；「—」值与
「暂无引擎运行」上下相邻无交叠（y178+18 < y197）。系截图分辨率观感误判，
直接销账。

## A 线 · ChapterPage 拆分补测（第一步）

- 阅读搜索高亮三函数（textNodesOf/clearReadingHighlight/highlightSearchHitAt）
  从 ChapterPage.tsx（~822 行）原样搬入新建 `pages/chapter/readingHighlight.ts`
  （净 -49 行；ref 改显式传参，调用点同 tick 读同一 ref，时序等价论证完整；
  2.6s 清除定时器保持「触发时读 .current」语义）。
- **ChapterPage 首次拥有测试**：新建 ChapterPage.test.tsx（3 条冒烟：空态渲染/
  novel:open-chapter 事件开章/阅读模式段落）+ readingHighlight.test.ts（12 条
  纯函数单测：收集/跨内联元素偏移/命中与未命中副作用/清理）。15/15 绿。
- 纪律：严禁大拆，`applyTextHighlight` 等余下函数留待后续刀次（模块注释已标）。

## B 线 · dev mock 补 Get/SetEngineFailover

- mock/state.ts 加 `engineFailover`（默认 false 对齐后端）+ setter；mock/model.ts
  按询价九方法先例补两方法（照 KeepWarmGet/Set 同构；legacy 绑定名走
  LegacyModelMethods 交集补型，AppBindings/bindingNames 零牵动）。
- 新建 mock-contract-failover.test.ts（存在性/初值/写回回读/会话内保持 4 用例）；
  SchedulingSection.test.tsx 仅追加「false 时开关可用」用例（既有断言零改动）。
- **收口残项（主代理补）**：B 线发现纯浏览器 mock 下 UI 仍「未知」——根因是
  api/engines.ts 的 App() 直读 window.go（?mock=1 下刻意为空）。补一行回退
  `window.go?.app?.App ?? bridgeApp`（bridge 代理 realApp 优先、shell 外落
  mock；未实现方法照旧 TypeError 走既有 catch，行为不变）。
- 浏览器闭环走查（?mock=1）：getEngineFailover 返回 false（非 null）；模型中心
  →引擎管理→本地调度三开关全部点亮（此前全「未知」）；故障转移开关经 CUA 真实
  点击切换，aria/chip/mock 值三方同步（true→false 往返）。

## C 线 · model-failover toast 文案 engineLabel 化

- ModelCenterPage onFailover 接入全仓统一 `engineLabel` 解析（modelcenter/utils：
  后端 label → 内置映射 → id 兜底）：命中显示「已切换到 {显示名} 重试」；
  列表未命中回退原 id 文案；to 缺失回退通用文案（三级回退语义不变）。
- 闭包新鲜度：解构 engines 入 effect 依赖（文件内既有模式，unsub/resub 同步）。
- 新建 ModelCenterPage.failover.test.tsx 锁 4 用例（label 命中/内置映射/回退
  id/回退通用）。
- 佐证：to_engine 取值域 = engines[].id（internal/ai/client.go failoverTarget
  返回 cand.ID，仅读未改）。

## 验证

- go build/vet/test 全绿；tsc -b 0；eslint 全量 0/0；vitest **196 文件 / 1361
  用例**全绿（较上刀 +4 文件 +24 用例）；drift PASS（559）；浏览器闭环走查
  （mock 探针 + CUA 真实点击三方核对）。

## 欠账清单（发布必列）

- **销账**：右舷虚线交叠（证伪）✓、dev mock failover（+浏览器回退）✓、
  failover 文案 label 化✓、ChapterPage 补测第一步✓；
- **承转**：ChapterPage 拆分第二步（applyTextHighlight/textAtScrollTop 等仍在
  组件内）；model-failover 与 engine-health-changed 重拉存在列表竞态窗口
  （已有 id 回退兜底，属预期）；mock 缺 HerdsmanModelCatalog（引擎管理走查
  可见运行态报错，属 mock 面缺口）；
- **既有**：A2 浏览器接管 / B2 pptx 真编辑（动手前先出方案）；微信侧文件收发
  （抓包前置）；Realtime 真机、iLink 语音/视频、自动路由 v1、做梦 2.0 预取、
  -race（本机无 gcc）。
