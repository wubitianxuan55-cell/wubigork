# v4.354.0 · 前端优化第六轮：新一轮三路审计——P0 数据形状崩溃 ×2 / 异步竞态 11 处 / 资源生命周期

> 用户口径「继续」。五连发后新现场再开三路并行**只读审计**（异步竞态与并发 / 运行时数据
> 形状防御 / 资源生命周期第二层，均带 file:line 与触发步骤），按证据定刀。**纯前端**：
> 17 源码文件 + 1 测试扩展，零 Go 逻辑改动、零绑定变更（绑定面 706 不动，仅版本常量走 sync-version）。

## 一、P0 数据形状崩溃 ×2（均为「后端产出前端未声明的形状 + mock 不可复现」同型）

1. **伏笔面板 6 态漂移（「项目重置」一键自毁式崩页）**：前端 `STATUS_META` 只声明
   `planted/hinted/revealed` 三态，Go 侧状态机是 6 态并集（`pending`=外部导入初始态 /
   `partially_resolved` / `abandoned`=合法存量）且原样透传。面板自己的「清理→项目重置」
   把手动条目置 `pending` 后立即重拉 → `STATUS_META['pending']` undefined → 根
   ErrorBoundary 整页崩。修复=类型 6 态并集 + STATUS_META 补全 6 档 + `statusMetaOf`
   防御未知值（诚实显示原文）+ 流转按钮 6 态文案/语义（待规划→标记埋设、部分回收→标记
   回收、废弃→恢复埋设）。测试 +2（超集三态渲染不崩 / 未知未来值 fallback 不崩）。
2. **记忆面板 skills=null（轻量用户点「扫描」即崩）**：Go `suggestSkillsFromMemories`
   在规则类记忆 <2 条时 `return nil`，nil slice 序列化为 JSON `"skills": null`（无
   omitempty）；mock 恒 `[]` 故 `?mock=1` 永不复现。修复=setSuggestions 入口统一
   收窄归一（skills/memories/merges `??= []`），消费侧五处不再逐个防御。

## 二、异步竞态（高危 5 + 中低 6，统一「项目已有 loadToken/seq 范式补齐」）

| # | 点 | 后果 | 修法 |
|---|---|---|---|
| 1 | PromptWorkshopPanel 模板切换 | 慢响应把 A 正文写进 B 表单，**保存即用 A 覆盖 B 模板**（生成链路即时生效） | `selectSeqRef` 守卫 |
| 2 | BookSearchModal 选书→取目录 | 网络延迟窗口换书，用 B 书源 URL + A 的章节范围执行**导入** | `pickSeqRef` + backToSearch 失效在途 |
| 3 | useChatTopics.createTopic / ChatPage 清空对话 | 点历史话题（在途）→立刻新建，旧话题消息填满空话题视图，**清空对话清错对象** | 入口 `topicLoadSeqRef++`（新增 `invalidateLoads` 暴露） |
| 4 | useSinStory.loadMessages | 快速切故事旧消息覆盖当前视图，发送后两故事内容同屏串台 | `loadSeqRef` + createStory 失效在途 |
| 5 | ChapterPage.handleSave | V4 场景章逐场景串行保存（数百 ms）期间继续打字，完成侧强制 `saved=true` 打掉未保存保护→**关闭不弹确认静默丢稿** | 保存快照 + setTabs 函数式比对，不一致保持 `saved=false` 并提示「请再次保存」 |
| 6 | SearchModal | Enter 双触发（onSearch+onPressEnter 双挂）+无 seq：双倍检索、慢响应覆盖新结果、**旧意图卡可被执行** | 移除 onPressEnter + `searchSeqRef` |
| 7 | MemoryHubPage.runSearch | Enter 路径无守卫（按钮路径有 disabled），旧命中覆盖新检索 | `searchSeqRef`（同 ChapterPage 范式） |
| 8 | useImageGenConfig 切后端 | backend state 在 await 后才写，二次切换绕过相等检查，两路 setImageBackendAPI 交错 | `backendSwitching` 重入闸 |
| 9 | outlineStore.loadOutlines | 快速换书旧书大纲覆盖新书，按 A 树章号读写 B 书**错误章节** | 模块级 `loadSeq` |
| 10 | WeixinPage.toggleAssistant | Switch 受控到轮询才翻转，「点了没反应」诱发连点=两条相反 Save 并发 | `togglingId` busy 闸 + Switch loading 态 |
| 11 | TTSPlayer | 播放中卸载组件泄当前 chunk blob URL 且**音频继续外放** | effect cleanup 调 `cleanupAudio`（ref 转发防重订阅） |

辨伪 18+ 处（antd Modal async onOk 自带 confirmLoading、ChatPage 流式归属、ForeshadowPanel
等已有 seq 范式者不重报）。

## 三、资源生命周期第二层

- TTSPlayer 卸载回收（上表 #11，唯一真实泄漏 ~0.1-0.25MB/次）。
- `dirListingsCache` 无界累积 + `invalidateTurnCaches` 零调用方——结构性欠账挂池不改
  （单条 1-10KB、速率慢，接线时机需要设计）。
- blob URL/Observer/AudioContext/MediaStream 全量辨伪通过（saveFile 同步 revoke、
  useVoiceChat 四路回收等 9 处 Observer 均 disconnect）。

## 门禁与产物

- tsc 0（`-b --force` 全量复核）；eslint 0/0；全量 ci.ps1 绿（vitest 386 文件 3295 例）；
  bindings drift OK@706；版本三处 4.354.0；exe 50877440B
  SHA256=758eba83f50b5b5d23a2cb49e66f019aa4c721d160afd25896afb3f80cc1e0fd
  （releases/gaea-v4.354.0.exe + SHA256SUMS-v4.354.0.txt，仅本地不入库；桌面副本同哈希实测
  一致；冒烟 /api/health 200 过）；本地 exe 保留策略执行：留 v4.349~v4.354，删 v4.348。

## 坑（本轮新增）

1. **mock 与真壳形状分叉是最阴的洞**：Go nil slice→JSON null（无 omitempty），mock 手写
   `[]` 永不复现——前端收窄层（setXxx 入口统一归一）比消费点逐个防御可维护。
2. **「保存完成侧强制 saved=true」类语义 bug**：状态标志的语义是「缓冲区==磁盘」，
   异步完成侧不能无条件置位——快照比对是通用解。
3. **antd `Input.Search` 回车已触发 onSearch**，再挂 onPressEnter=双发；删除而非守卫。
4. **竞态修复统一走项目已有范式**（loadToken/topicLoadSeqRef/searchSeqRef/chapterLoadToken），
   不发明新机制——审计报告本身也以这些为参照系。
