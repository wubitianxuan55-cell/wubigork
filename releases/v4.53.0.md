# v4.53.0「办公欢迎界面化繁为简：四点降噪 · 右栏 6→4」

> 发布日期：2026-09-03 ｜ tag：`v4.53.0` ｜ 基线：v4.52.0 ｜ 绑定面 **559 零变更**

## 定位

用户四点拍板（办公板块欢迎界面重新设计）：①删对话上方上下文横条（已有专门的
「上下文」标签页，重复）；②删左侧面板底部模型卡（与顶栏重叠）；③右栏「任务」
与「分工」合并；④「产物」与「变更」合并。**零功能删除**：被删的只是重复入口，
能力全部保留；合并 = 两块内容**直接并成一个面板**（用户明确纠正：不是二级
标签/段切换，上下分区同屏全可见，零额外点击）。judge 视觉验收 **3/3 pass
（无 must_fix）**。蓝图 `design-system/gaea/pages/gaea.md` 已更新。

## ①② 双删（重复入口收敛）

- **ContextBar 删除**：chat 头部上下文用量条移除（`components/ContextBar.tsx`
  连同组件文件删除，grep 核实零残留引用）；上下文能力走主区 ChatTabs「上下文」
  标签页（ContextView，未动）。
- **侧栏模型卡删除**：Sidebar 底部「绑定模型」区块（FeatureModelBar feature=gaea）
  移除；FeatureModelBar 组件仅此一个消费方 → 组件文件连同 ChatPage.test 死
  mock、redesign.css `.side-model-wrap` 死规则一并清掉。模型入口统一 = chat
  头部 ModelSwitcher + 壳层顶栏 pill。

## ③④ 右栏合并（6→4 Tab）

- **清单**（lib/workspaceTabs.ts）：`["files","deliverables","changes","tasks",
  "subagents","browser"]` → `["files","deliverables","tasks","browser"]`；
  产物 keywords 保留 变更/diff/修改、任务 keywords 保留 分工/子代理（命令面板
  检索不断）；**旧 id 别名** `LEGACY_TAB_ALIASES`：changes→deliverables、
  subagents→tasks——裸 id/v2 记录/启用集三条持久化路径读档一律收敛（旧值
  可读不报废，学 sanitizeState）。
- **合并壳**（components/MergedPanel.tsx）：主区 flex-1 自滚动 + 次区 border-t
  分隔、高度随内容封顶 45%、内部滚动；两块各自保留 v3-panel-head 标题行与
  计数/动作（自动弹出/自动展开/刷新），壳只分区不抢语义。
  - 产物 = DeliverablesPanel(上) + ChangesPanel(下)；任务 = TaskCenter(上) +
    SubagentsPanel(下)。面板组件本体零改动（框架与内容解耦延续）。
- **接线**（lib/sidebarRegistry.ts）：deliverables/tasks 两个渲染器换 MergedPanel
  组合，changes/subagents 渲染分支删除；按钮条/设置卡/命令面板随清单自动
  4 卡。App.tsx：运行角标挂「任务」单键（runningDomainActive = tasks）；v4.24
  新子代理自动展开改切「任务」面板（功能不变）。

## 测试同步（三层锁更新 + 新增）

- workspaceTabs.test：清单锁改 4 id；新增旧 id 别名三路径用例（裸 id/记录/
  启用集）；firstEnabledTab/resolve 期望随新序更新；
- WorkspaceTabs.test：6 Tab→4 Tab 全量断言改键；角标用例去 subagents 键；
- MergedPanel.test 新增（同屏双块/分区样式契约）；Sidebar.test 去 FeatureModelBar
  mock；ChatPage.test 去死 mock。终轮 **192 文件 / 1337 用例全绿**。

## 验证

- go build / vet / test 全绿；tsc -b 0；eslint 0 error；drift PASS（559）；
- judge 视觉验收 3/3 pass（无 must_fix）：四点诉求逐条落实、上下分区空态/
  开关完整、无截断叠压、4 Tab 图标化正确。

## 欠账清单（发布必列）

- **新增（judge 非阻塞备注）**：任务中心空态副文案「价格抓取、语义索引」与
  办公语境不符（占位残留）；产物/变更次区分隔线偏淡可加深（设计偏好）；
- **承转**：首页矩阵末行右一空位（v4.52.0）；IAB 截图滚动撕裂用 DOM 断言
  核伪（走查技巧已录）；eslint 3 个旧 warning（未触碰旧文件）；
- **既有**：A2 浏览器接管 / B2 pptx 真编辑（动手前先出方案）；ChapterPage 拆分
  补测、微信侧文件收发（抓包前置）、Realtime 真机、iLink 语音/视频、自动路由 v1、
  做梦 2.0 预取、-race（本机无 gcc）。
