# v4.192.0 — 阶段四三小刀：角色同一人设（2026-09-10）

> 阶段四出口落地（读档 docs/gaea-character-domain-survey-2026-09.md：一套资产
> +项目工作副本，缺口=外观锚点分裂与演化漂移）。方向句验收：**角色在章节和
> 出图里是同一个人**。

## ① 外观锚点单一来源（绘梦）

`applyRefCharacter`（useImageGenConfig）在库内参考图为空时不再静默空槽：

- 回退读库内 `Appearance` 文案 → 并入提示词（可见可编辑），提示
  「已把文字设定并入提示词（角色库补参考图后可用图生图）」；
- 无参考图也无文案才提示去角色库补全；无图不硬切图生图。

## ② 副本→库显式回写（角色库）

`ImportProjectCharacters` 在既有单向约束（只增不改）内追加**描述性空字段
补全**：

- ID 命中且库内字段为空、副本有值 → 补全（Gender/Age/Personality/Background/
  Appearance/Figure/Motivation/Notes）；**非空一律不动**；
- **RoleType/Arc/Status 不补**——项目内弧线/状态以项目为准（关联即快照）；
- 返回 `{imported, filled}` 双计数（绑定回执 map 形态，方法名 604 零变更）；
- CharacterPage 迁入按钮回执诚实化：「回写完成：新迁入 X 个，补全 Y 个角色
  的空缺设定（已有设定未覆盖）」；
- 不覆盖语义由既有 `TestImportProjectCharacters_IdempotentAndOneWay` 继续
  锁（该测试三处断言改双计数，单向约束原样），新增
  `TestImportProjectCharacters_EmptyFill`（空补/非空拒/状态不补/幂等四段）。
- 不做后台自动同步：回写走显式调用，漂移是本地编辑隔离的代价，显式桥足够。

## ③ 关联即快照进 UI（角色库）

角色详情抽屉「本书局部设定」标签精化：**「仅本小说生效 · 与角色库互不影响」**
（原来只有「仅本小说生效」）——消除「改了库就该变」的误解。

## 门禁

- Go 全量 0 FAIL（characterlib 新增 EmptyFill 测试 + 存量导入测试三处断言
  改双计数；internal/app 两处导入断言改 report 形态）。
- 前端 tsc 0、eslint 0；drift PASS@604、e-check OK；AssetStudio 回归 10/10。
- 版本三处 sync 4.192.0；CHANGELOG + README 版本表 + 本说明。
- build strip + 冒烟 200 + SHA256（tag 树干净 worktree 构建，工作树他方在途
  批次不入产物）。
  SHA256=`e0764829a8a0e5f71e77a11a3054f8746a80be6f1f949810f70cfb93dc5c2aa9`
  （releases/SHA256SUMS-v4.192.0.txt；已落 Desktop 与 build/bin，删除 worktree
  前后主树 node_modules 452 包复核一致）。

## 欠账

- 非空字段的逐项确认覆盖 UI（当前只做安全空补，覆盖按需另刀）。
- 参考图反向流（绘梦出图一键存为库内 ReferenceImages）已存在于角色库页，
  绘梦侧不重复做。
- 壳内真机走查池不变（绘梦回退链/回写按钮壳内实测挂真机池）。
