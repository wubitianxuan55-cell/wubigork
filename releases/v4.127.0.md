# v4.127.0 · 进度计划基本功刀A+E+B：呈现层合规整改（2026-09-06）

> 用户走查实锤「UX/UI 太垃圾」：双代号网络图画斜线（标准要求横平竖直）、横道图前置/后置与六时参看不见。本版对标 JGJ/T 121 绘图口径与 MS Project 基本功列集，纯前端呈现层整改——**引擎零变更**（六时参/四种搭接/日历/关键路径本就完整，坏在没露出来）。绑定面 **588** 零变更。

## 背景：市场对标与差距审计（2026-09-06）

- 市场调研：AI 进度计划五种范式（对话生成/多方案比选/预测预警/动态重排/Agentic），引擎口径已与 Project 齐平，差距集中在呈现与交互层。
- 专业口径：JGJ/T 121《工程网络计划技术规程》绘图规则（箭线左→右、横平竖直、直角拐弯、避免斜线交叉）+ 六时参数（ES/EF 顺推、LS/LF 逆推、TF/FF 时差，关键线路=TF=0）+ Project 教程基本功列集（前置「3FS+2」引用、日程表、跟踪甘特）。
- 逐行审计定位：AOA 斜线=`AoaView.tsx edgeGeom()` 两点直连无正交路由（单代号 PdmView 与横道依赖线都是正交的，属 AOA 漏做）；六时参=`cpm.ts/cpm.go` 完整但表格无列。

## 刀A：双代号箭线正交画法合规（AoaView.tsx）

- `edgeGeom()` 重写为正交路由：前进边右缘出→直角拐→左缘入（H-V-H）；同列竖直直连；后退边（手动布局可能产生）底缘出→下方通道绕行→底缘入。**禁斜线**。
- 平行边（同 from>to 多条，如虚工作+实工作）按序错开 10px 通道防重叠。
- 工作/工期标注随水平段归位（第一段过短自动落到末段；竖直边贴线右侧），名称不再压线。
- 走查断言：39 条箭线 **0 斜线**（逐 path 解析 M/L/H/V 坐标验证每段横平或竖直），12 条含直角拐弯。

## 刀E：视觉纪律（schedule.css + PdmView.tsx）

- **分组行降饱和**：六色循环由饱和实底（橙/青/粉/黄/蓝/绿 `#f9a03a` 级）改为 16%~18% 透明淡底+原深色左条——保留色相导航，让位任务条/汇总条对比层级；黑汇总条（带端帽）重新可见。
- **对比度缺陷根修**：走查实锤暗夜主题 `--md-sys-color-outline` 竟为 `rgba(45,212,191,0.15)`（15% 透明青）——日期列/六格/状态栏次级文字全用它，这是「看不清」的元凶。次级文字统一切 `--md-sys-color-on-surface-variant`（实色）。
- 网络图坐标纸底纹降强度（`--sched-grid: rgba(148,163,184,0.16)`）；图例/统计牌 `position: sticky` 吸顶常驻，长图滚动不再失联。
- **单代号缩放/适配**：放大/缩小（×1.2，0.2~2 钳位）+「适配全图」按钮+缩放读数；布局尺寸变化且用户未手动缩放时自动适配一次（hooks 顺序纪律：useEffect 置于 early return 之前，eslint rules-of-hooks 实锤拦截过一次）。链式计划整网一行展开也能一屏读完（走查：16 节点自动 30% 全图可见）。

## 刀B：横道表格补齐 Project 口径（GanttView.tsx + ganttLinks.ts）

- **列集扩为 14 列**：行号|任务名称|WBS|工期|开始|完成|**最迟开始|最迟完成|总时差|自由时差**|模式|前置|**后续**|成本。关键任务 TF=0 红字；手动任务逆推不回传，时差四格诚实显「—」（不放假数据）。
- **前置/后续 Project 式引用文本**：新增纯函数 `fmtLinkRefs`——行号+类型+时距（`3SS+5,4FS`；lag=0 省略时距）；前置点击仍开编辑 Popover，后续悬停显示任务名清单。行号口径=当前任务序（1-based）。
- **依赖线按搭接类型取锚点**：新增纯函数 `ganttLinkPath`——FS=前完成→后开始、SS=起→起、FF=完→完、SF=前开始→后完成，含反向进入（负时距/倒排）与水平不足短桩绕行；不再一律画成 EF→ES 的 FS 形状。走查断言：18 条依赖线 **0 斜线**。

## 测试与门禁

- vitest 2216→**2224**（+8：ganttLinks 几何 6 例+引用文本 2 例+全量回归）；tsc 0；eslint 0（修 PdmView hooks 条件调用 1 error）。
- drift PASS@**588**；版本四处 4.127.0（app_info.go/wails.json/versioninfo.rc 经 sync-version.ps1 + frontend/package.json）。
- `?mock=demo` 浏览器走查：横道六时参列齐全/前置「3SS+5,4FS」/后续「3SS+5,4FS」格式化渲染、分组淡底 rgba(69,214,208,0.16)、单代号自动适配 30%、双代号 0 斜线；三视图截图复核（暗夜主题）。

## 已知取舍与欠账（下刀排期）

- 表格 14 列后 LEFT_W≈904px，左 AI 栏展开时图表区较窄——横向滚动+左栏折叠可用；列宽拖拽/列配置/大纲折叠/undo-redo/进度编辑入口/右键菜单=**刀C 交互包**；周/月刻度+打印导图=**刀D**。
- 单代号整网一行布局（layerByTime 时标分层特性）以适配缩放缓解，折行布局（Project 网络图页宽折行）观察后拍板。
- 双代号时标波形线（自由时差可视化）未做（时标口径收尾项）。
- 差距审计基准：AOA 手动模式 x 与时间解耦偏离时标口径（v4.123 有意设计，维持）。

## 走查坑增补

- vite dev server 会静默退出（curl 探活 200→连接拒绝），走查前先探活再跑。
- antd Segmented 的 radio input 视觉隐藏，Playwright role 点击超时；经文本 label 或 DOM dispatch click。
- 首个分组行类名是 `sched-group-c1` 不是 c0（gc 先自增再赋值）——DOM 断言按 `[class*="sched-group-c"]` 匹配。
