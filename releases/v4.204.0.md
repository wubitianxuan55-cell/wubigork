# v4.204.0 — 组价多方案对照：P25/中位/P75 当场切（2026-09-10）

> 造价刀路池 §2 半刀。价格带三档数字早就在，推荐价却钉死中位数——偏低/偏高
> 只能手改。本刀让造价页与会话开口都能选档，**不**把组价拆成逐步盖章。

## 缺口

survey §2「多方案（不同分位/费率政策）对比无」。`RecommendPrice` 已支持
median/p25/p75/mean/conservative，UI `GaeaCostCompose` 与工具 `cost_compose`
都硬编码 `"median"`。三档格是死展示。

## 落地

绑定面 **608 零变更**（不新造门、不嵌套 LLM）。

- **ComposeModal**：P25 / 中位数 P50 / P75 格可点（`aria-pressed`），应用价随档；
  默认中位数（既有「应用」回传 1200 用例不动）；均值/离散/离群仍只读。
- **cost_compose**：可选 `mode`（未知回落中位数）+ 输出「三档对照」一行，
  管家不必再调一次就能报三档。

## 测试

ComposeModal +1（点 P25 → 应用价 1000）；Go `TestCostComposeMode`（p25 理由含
「P25 分位」/未知回落中位数）；既有 BandAndEvidence 锁「三档对照」。

## 门禁

vitest ComposeModal 11/11 / Go builtin TestCostCompose 绿 / tsc 0 / eslint 0 /
绑定名 608 零变更 / 版本三处 sync 4.204.0。

## 欠账

- 组价流式打字机仍不做（规划：不拆逐步盖章）；费率政策对照需费率入库才有意义
  （survey 边界：费率仅展示追溯不参与计算）；
- 人材机含量基线仍等真实样本；
- 壳内真机走查池不变（组价选档手感挂真机池）。
