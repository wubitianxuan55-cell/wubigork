# v4.411.0 — 一致性评分写回元数据（编辑器评分历史池①）

> 2026-09-25 发布。观察池「评分写回元数据」落地（用户口径「继续」按 v4.359
> 先例=按判断推进挂池项）。绑定面 **717 零变更**（CharacterSave 签名不变，
> JSON 增可选字段）。schema：characterlib 迁移补 `reference_scores` 列。

## 落地

1. **存储**：characterlib.db characters 表补 `reference_scores` 列（JSON []int，
   与 referenceImages 索引对齐；0=未评分）。迁移幂等（hasColumn→ALTER，
   SchemaV2 同款）。读写两路同索引维护：读侧 capImageList 剔除超限内联图时
   capScores 按保留下标映射分数（非前 N 截断）；越界/缺失补 0。
2. **编辑器**：评分成功后写 form.referenceScores（保存时持久化——评分对象
   可能含未保存改动，不做隐式写库）；参考图缩略图左上角分数徽标
   （≥80 绿 / 60-79 橙 / <60 红）；移除参考图同下标裁剪分数。
3. **语义**：分数是角色元数据的一部分，sin 侧追加参考图不破坏对齐
   （短分数表读侧补 0）。

## 测试

- Go：往返/超限对齐/短分数表补 0 三例（TestReferenceScores*）。
- 前端：徽标渲染+保存持久化（saveCharacter 收到对齐分数表）+移除对齐，
  编辑器 35 例全绿。
- 全量 ci 门禁绿 + tsc 0 + eslint 0。

## 门禁

全量 `scripts/ci.ps1` 绿 EXIT=0；版本漂移闸 OK@4.411.0；绑定面 717 零变更。

## 升级说明

- 无操作变化。参考图评分后徽标即时显示；「保存角色」后分数持久化，重开
  编辑器可见历史评分。

## 产物

`gaea-v4.411.0.exe` 51303424B，SHA256=`5020b3762adcf12c979597484eaec2d15840dc07d6e57c143ea01f609d55dff6`（全文见 SHA256SUMS-v4.411.0.txt；冒烟 200 过；桌面副本同哈希实测一致）。保留策略 5 版：留
v4.407.0~v4.411.0，删 v4.406.0.exe（SUMS 身份档案全保留）。
