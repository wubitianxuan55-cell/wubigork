# v4.171.0 — 瘦身 P3 结构版2：office 抽核 + 次批巨文件拆分（>50KB 首拆池 9→0）（2026-09-09）

> 瘦身执行层第六版：P3 结构面第二刀（masterplan 轨道三；出口判据=绑定零变更 drift PASS + >50KB 拆分池 9→0）。
> 承接 v4.170.0 首批 4 拆的次批收口 + 轨道三「office 抽核」——**纯结构拆分：行为零变化、公开导出面逐一同**。
> ⚠️ 本版为接手会话收口：工作树改动由前一会话（2026-09-09 02:04–02:20）完成后遗留未提交，本会话完成全量门禁核验、修复 1 处 lint、补齐发布。

## 1. 线A：office 抽核（journal/evidence/文件读写 → internal/core）

- `internal/office` 顶层 8 文件删除（audit_log/desktop_session/executor/job_manager/job_routing/session_mode/types + executor_test/desktop_session_test）→
  新包 `internal/core/`（fs.go/jobs.go/journal.go/modes.go/routing.go/sessionmode.go/types.go + fs_test/modes_test）。
- `internal/office/aliases.go`（新）：`type ExecResult = core.ExecResult` / `type AgentJobState = core.AgentJobState` 类型别名——
  **bindings_office.go 由 scripts/gen_bindings 生成且签名受 TestBindingsCompleteness 保护，别名=同一类型，JSON/反射零变化**（绑定面签名不变）。
- `internal/app/office_handler.go` / `embed_check_test.go`：import 改 `officecore "internal/core"`，调用点同步；office 树 29 处 import 集中收口。
- 保留：`internal/office/archive.go`（事实归档，whisper 域既有逻辑，不属于抽核范围）。
- **绑定面 602 零变更**（drift PASS@602 实证）——门面签名/方法名全不动，壳内前端零感知。

## 2. 线B：次批巨文件拆分（5+1 拆，拆分池 >50KB 首拆池 9→0）

| 文件 | 拆分前 | 拆分后 | 新目录 |
|---|---|---|---|
| KnowledgePanel.tsx | 62.2KB | 40.8KB | `gaea/components/knowledge/`（CenterView/constants/EditForm/EntryRow/HistoryModal/Inspector/MergeModal/utils） |
| DeliverablesPanel.tsx | 57.4KB | 23.2KB | `gaea/components/deliverables/`（DeliverableRow/EvidenceSection/RegistrySection/shared） |
| XlsxPreview.tsx | 56.4KB | 35.5KB | `gaea/components/xlsxpreview/`（FormulaBar/MiniChart/numFmt/SheetGrid） |
| ChapterPage.tsx | 55.9KB | 35.6KB | `pages/chapter/`（editChrome/errText/readingChrome/readingOverlays/readingPanel） |
| herdsmanTemplates.ts | 55.7KB | 1.6KB | `data/templates/`（13 个分类模板文件 + categories） |
| SchedulePage.tsx（超额） | ~50KB | 37.3KB | `pages/schedule/`（CalendarEditor/ExportDialog/ProjectsManagePanel） |

- 拆法沿用先例：子组件/纯函数/常量独立成文件，父壳保留 state/handler 组合层；classNames/data-testid 逐字节照搬（测试锚点全保留）。
- 收口修复（本会话）：`pages/schedule/ExportDialog.tsx` 非组件导出（EXPORT_KIND_LABEL/EXPORT_KIND_FILE/ExportKind）仅文件内使用却带 export
  触发 react-refresh/only-export-components ×2 → 去 export 关键字本地化（拆分前就是局部 const，零行为变化），eslint 0 error 0 warning 恢复。

## 3. 主代理收口（本会话）

- 工作树核验：`git status` = 6 M + 12 删除（office 抽核）+ 5 新目录 + aliases.go，足迹无越界。
- >50KB 拆分池口径：**首拆池 9→0**（v4.170 拆 4 + 本版拆 5）；frontend 仍 >50KB 仅 locales ×3（84.9/82.7/82.5KB，纯数据词典，P0 基线已单列不计入拆分池）+ `gaea/lib/store.ts` 54.3KB（下一批候选）；Go 侧 `internal/config/config.go` 58.7KB / `internal/modelengine/engine.go` 55.2KB 入 P3 版3 候选。
- 版本三处 4.171.0（sync-version.ps1，app_info/wails/rc 三处一致）。

## 4. 门禁（全量复跑）

- Go **128/128 包 0 FAIL**（scripts/test-all.ps1，含 TestBindingsCompleteness=PASS，drift 锁 602 零变更）；
- vitest **2737/2737**（313 文件全绿——首跑 4 文件 31 例与 Go 门禁同机并发负载假红，**复跑两次全绿**，在册「并发负载 flaky 复跑清同族」先例；纯拆分零新增测试）；
- `tsc -b` 0 · `eslint .` 0（全量，修完 ExportDialog warning）· drift **PASS@602**（零绑定）· locale 死键 **0 死**（1448 键）。

## 5. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 1193.59 kB gz 375.58 ｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.23 MB ｜ >50KB 源文件（首拆池口径）9→0
```

## 6. 产物与冒烟

- 产物：`gaea-v4.171.0.exe`（46.23 MB），SHA256=`834DCDEF35DDFCD049C7E9CB7E54FB3DB55EA04793B8055384770C42C2BCEB38`（releases/SHA256SUMS-v4.171.0.txt）。
- 冒烟：`scripts/smoke.ps1 -ExePath releases\gaea-v4.171.0.exe` → **/api/health 200，响应 {"status":"ok"}**（冒烟进程已回收）。

## 7. 欠账

- **P3 版3 候选**：bridge 双轨退役启动（wailsjsCompat 单 shim 仍被 57 文件引用，渐进迁移——本版未动，需独立设计迁移批次）；Go 侧 config.go 58.7KB / engine.go 55.2KB + frontend store.ts 54.3KB 二次拆分；entry/页面级懒加载拆解。
- 壳内真机池（rail 切换器/home 最近文档/.gsched 基线 N/knowledge 无残留）、审计刀D 真机取证（printSvg print/拖拽/粘贴）不变。