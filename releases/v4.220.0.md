# v4.220.0 — 6.3 余项：流水线模板库——「月度报告」存模板一键重建（2026-09-11）

> 阶段六收官后续刀（设计 docs/gaea-office-dag-63-design-2026-09.md §6 余项之二清掉）。
> 周期性流水线（月度报告/周报汇编）不再每次重新规划：存成模板，一键重建为草稿 run。

## 缺口

6.3 首刀后流水线是一次性实体：下个月再做「读三份报表→透视→出报告」，得让模型把
同样的链重新规划一遍——图形状（goal+节点指令+依赖）没有复用形态。

## 落地

- **模板只取图形状**：internal/gaea/dag/template.go 纯函数包。
  - `FromRun`：剥状态/产物/ref/运行痕迹，节点回 pending；空名回退 goal 截 24 字，
    限长 40 rune 显式报错。
  - `Instantiate`：模板→全新草稿 run，**不自动起跑**（起跑仍是人拍板，与整链首跑同闸）。
  - `TemplateStore`：模板档落 `<cwd>/.gaea/work/dag/templates/`（run 档同域子目录，
    Store.List 只读顶层 *.json 互不混；删档即弃）。Save 前全量 Validate——成环/
    悬空依赖/坏形状拒入库，模板是「可重建」的承诺。
- **绑定 620→624**（Office 门面）：GaeaDagTemplateSave / TemplateList / TemplateNew /
  TemplateDelete。重建=模板当前形状快照，改模板不影响已重建 run；删除只删模板档。
- **前端 DagPanel**：模板折叠条（默认收起、有模板才显形）逐条「新建/删」；run 卡
  「存模板」→内联输入（默认带出 goal 截断，可清空交后端回退，Escape 收起）。
  模板拉取失败静默不挡主列表；?mock=1 预置「月度经营报告」模板可走查。

## 测试

- Go +6：template_test（FromRun 剥痕迹/名规则/Instantiate 全新/存储往返+坏形状拒
  入库+穿越拒绝/列表倒序+与 run 档隔离）；app 层 TestDagTemplateFlow=存→列→重建→
  重建链 fake-runner 跑通全 done→删→再删显式报错。
- vitest +4：DagPanel 模板区（显隐折叠/新建重拉/存模板内联输入提交/删除+拉取失败静默）。

## 门禁

Go 全量 0 FAIL / drift PASS@624 / tsc -b 0 / eslint 0 / vitest 329 文件 2845 例全绿 /
版本三处 4.220.0。

## 观察池

- 壳内真机走查：存模板→重建→起跑全链（并入 6.3 既有真机池项）。
- dag_plan 增量改图（改单节点指令不整链重规划）按需另刀。

## 打包记录

- 产物：`build\bin\gaea.exe` **48,825,344 B** SHA256 `ce794b8539869730a7df430293295048f8d8527157bbd5850dc8d8f8878e011d`
  （冒烟 /api/health 200 + Desktop 副本同哈希；exe 不入库，只存哈希）。
- 未打 tag（随 v4.200+ 现状）。
