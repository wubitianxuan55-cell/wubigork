# v4.179.0 — 瘦身清点刀：knip 死代码清除 + 依赖显式化 + 冷启动基线打点（2026-09-09）

> 瘦身总规划（docs/gaea-slim-masterplan-2026-09.md）P0-P4 收官后的清点刀：
> ①轨道二尾欠「knip 扫 unused exports」落地并扩大为死文件/幽灵依赖双清零；
> ②轨道五「运行面」真正欠账=测量——冷启动基线打点就位（数据随日常启动自动积累）；
> ③初始化链审计结案：重活均已异步，懒初始化改造无需立项。零绑定（drift PASS@602）、
> 零行为变化（纯删除死代码 + 观测打点 + lock 显式化）。

## 1. knip 首扫三发现与处置

- **死文件 ×13 全删**（零 import 全仓甄别，含 scripts 层）：`App.css`、`AppBar.tsx`、
  `MobileSheet.tsx`、`SettingsMobile.tsx`、`SettingsUpdates.tsx`、`Tooltip.tsx`（自制版，
  各处用的均为 antd Tooltip）、`PromptShelf.tsx`、`office/ParseSummaryCards.tsx` +
  `office/SourcePreviewDrawer.tsx`（互为唯一引用的死链）、`memoryhub/ComingSoon.tsx` +
  `memoryhub/ModuleCard.tsx`、`lib/typesGenerationCheck.ts`、`theme/m3-palette.ts`；
  连带清 `utils/zIndex.ts` 中指向已删组件的过时注释（档位值不动）。
- **幽灵依赖 ×13 包显式化**（20 处 import 靠传递依赖侥幸工作，升级即断）：
  `jszip`（docx/pptx 链 8 处）、`dayjs`（WeixinPage/GanttToolbar）、`katex`
  （Markdown 直引 CSS）、`unified` + `hast-util-sanitize`（sanitize.ts）、
  `@lezer/{common,css,highlight,html,javascript,json,markdown,python}`（diffHighlight.ts）——
  全部按 lockfile 现解析版本钉死声明，零版本漂移。
- **死传递依赖顺带清除**：`@codemirror/search@6.7.2`（全仓零引用）被 npm install
  自动从 lock 与 node_modules 移除。
- **三存疑依赖验活结案**（轨道二 gsap/docx-preview/unist-util-visit 名单）：
  gsap=2 hook（useGSAPCollapse/useEntranceAnimation）×6 组件消费、docx-preview=
  DocxPreview 组件、unist-util-visit=remarkFileLinks/remarkMemCitations 两插件——
  全部在用，保留。

## 2. 冷启动基线打点（轨道五：先有测量再谈优化）

- **Go 侧**（长期日志自动积累）：`app.New` 构造链耗时一条；`Startup` 分段计时
  （migrate/logging/engine/voice+weixin/charlib+stores/tts-kickoff/schedulers）+
  total 汇总一条 slog——日常启动即落 `logs/gaea-YYYYMMDD.log`，真机基线零成本采集。
- **前端侧**：index.html 解析即 `performance.mark('gaea:boot')`；React 挂载后第二个
  rAF `gaea:interactive` + measure——CDP 走查 `performance.getEntriesByName` 可读、
  dev 控制台同步打印。有意不落 gaea.log（现有前端日志通道为 Error 级，不污染错误信号）。
- **初始化链审计结案**：TTS 拉起=幂等异步、价格源/文件索引首轮=任务队列异步提交、
  四引擎模型刷新=goroutine、预载延迟 10s、健康巡检延迟 1min——同步 Startup 链
  只剩配置/嵌入 FS/DB 打开/角色库迁移等轻量磁盘活，**懒初始化改造无需立项**；
  轨道五余项收敛为「真机冷启动/常驻内存采集」（与壳内真机池同窗口）。

## 3. 门禁与度量

- vitest 2738/2738（5 例 ContextView/MemoryPanel/TrajectoryView 并发负载 flaky，
  单独复跑全绿——在册先例模式）；tsc 0、eslint 0、drift **PASS@602（零绑定）**、
  e-check OK、Go 全量 116 包 0 FAIL。
- 构建：build.bat strip + 冒烟 /api/health **200**；exe **46.16MB**（与上版持平，
  删除的死文件本就不进 bundle——本刀收益在仓库卫生与依赖正确性，非产物体积）。
- knip 复扫：Unused files **0**、Unlisted dependencies **0**。

## 4. 欠账

- knip Unused exports 169 项挂账下一版（甄别成本高，需逐项区分「测试专用/
  预留面/真死」三态）；masterplan §4 防复发规约可加「新导出须有消费方」门禁。
- 壳内真机池（冷启动前端口径采集/审计刀D 真机取证/.gsched chips）不变——待协作/
  闲置时间窗。
