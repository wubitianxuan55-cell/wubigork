# v4.63.4「mt_/长文本输出 Codex 式有界渲染」

> 发布日期：2026-09-04 ｜ tag：`v4.63.4` ｜ 基线：v4.63.3 ｜ 绑定面 **559
> 零变更** ｜ 用户点名：本地模型工具的输出也要渲染优化，最好像 Codex 一样。

## 内容

- **BoundedAssistantMessage**：mt_ 标签页（文档级输出）或超 4000 字的
  assistant 内容，快照渲染默认**限高 26rem 内部滚动**（Markdown 照常渲染），
  底部「展开全部（N 字）/ 收起输出」切换 + 字数标注——不再全量铺开成墙。
- 流式实时行不做有界（保持跟随滚动语义）；短输出照常直出。
- i18n 三语 +3 键（subagent.outputChars / outputExpand / outputCollapse）。

## 验证

- tsc -b 0；eslint 0；vitest **217 文件 / 1602 用例**（新增 mt_ 长输出有界
  渲染 + 展开/收起切换回归测试）。
- 绑定面 drift PASS（**559**）；build.bat 冒烟 /api/health 200。

## 产物

- gaea-v4.63.4.exe
- SHA256：`79c8d16f5cf0f89c8b784c70364e1b762d70fca9d83ed42f315fd1ee685bb978`
- 冒烟：临时副本启动 → /api/health 200，进程已回收。

## 欠账清单（沿袭 v4.63.3）

- Side Chat 式子代理追问（后端绑定刀）；任务树/左栏子行迁移共享 store；
  自动展开偏好缺设置中心 UI 开关。
