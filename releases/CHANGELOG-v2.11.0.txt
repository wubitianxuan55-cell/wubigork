gaea v2.11.0 正式发布 · 通用办公大优化（2026-08-10）
======================================================

四大库（成本/知识/办公记忆/工作区文件）能力闭环 + 本地语义检索栈。

一、成本库
  - cost_search/cost_save 内置工具：测算前查库、完成后沉淀（同名 UPSERT）
  - cost-estimate 模板「先查后沉淀」；办公右侧成本库 Tab（浏览/搜索/结构化引用）
  - 产物面板「沉淀到成本库」；导入 xlsx/csv + AI 归一化（预览确认）
  - 编辑/删除/批量操作；价格源订阅 + 手动/定时抓取 + 价格历史 + 异常识别（±20%）

二、知识库
  - 导入 md/txt/docx/pdf/xlsx/csv + AI 多主题拆分（预览确认）
  - 批量管理 + 审核流 + 版本历史 + 批量导出 Markdown
  - 查重（CJK 二元组 Dice）+ 相似条目一键合并

三、办公记忆
  - 两两查重 + 一键合并（自动做梦同名变体归并）

四、工作区文件
  - fileindex 扫描 md/txt/csv/docx/xlsx/pdf/pptx 提取正文（pptx 零依赖）
  - bge-m3 向量化 + 内容感知增量重建 + 10 分钟自动维护
  - 搜索面板「语义」模式 + 记忆中枢统一搜索并入「语义·文件」

五、本地语义检索栈（四库统一）
  - 子串召回 → bge-m3 语义召回 → BM25 排序 → bge-reranker-v2-m3 精排，失败降级
  - 跨库统一语义检索；Herdsman bge-m3 / bge-reranker-v2-m3 实测通过，零 token

验证：go test 全量 + go vet 干净；前端 111 例全过；tsc 与 vite build 通过。
产物：releases/gaea-v2.11.0.exe（Windows x64）。
