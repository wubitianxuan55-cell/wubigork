# v4.149.0 · diff 确认卡刀D：ops 通道投影模拟器 + Go/TS 对拍（2026-09-08）

> diff 确认闭环设计**收官刀**（§2.2/§5）：ops 通道此前在审批卡上只是意图清单——模型说改什么就显示什么，**用户看不到落盘后的样子**。本刀交付 `simulateOps`（Go ApplyOps 的逐语义 TS 镜像），审批卡在落盘前投影「这批 ops 应用后会得到什么」，走与 project 通道同一条 diff 管线渲染。设计标注的最大风险「批的是 A，落的是 B」（TS 与 Go ApplyOps 漂移）的对冲主阵地 = **Go/TS 对拍 golden fixture**：49 条用例双端同吃一份口径，任何一侧漂移都会炸掉对端测试。纯前端投影，绑定面 **597 零变更**，落盘权威永远在 Go。

## 1. Go/TS 对拍主阵地（ops_golden.fixture.json）

- `internal/schedule/ops_golden_test.go`：**口径唯一权威源**——49 条用例（21 成功 + 28 失败路径）覆盖全部 12 种 op 及其守卫分支（层级/进度/负工期/分组行禁固定成本与分配/自前置/前置不存在/日期口径/缺 savedAt/CPM 拒绝/类型非法/坏金额/重复分配对/外来 taskId/未知类型/多 op 中途失败），跑 Go ApplyOps 后把 after 状态（或错误）写进 fixture。
- 重生成命令：`go test ./internal/schedule/ -run TestApplyOpsGolden -update-golden`（改 Go ops 语义必须重生成并提交，让 TS 侧对齐失败显式化）。
- `opsSim.golden.test.ts`（vitest）：同一份 fixture 双跑 TS simulateOps，逐例比对「是否失败 + after 状态」（canonicalStringify 消化 Go omitempty 与 TS undefined 表达差）；**50/50 通过**。
- 语义要点全部钉死：指针三态（缺 key=不动/显式 0 与空串=改；mode 空串 no-op）、upsert_task 零值语义（缺 level=0 分组行/缺 progress=0）、remove_task 扁平数组 level 子孙级联+搭接清理、set_links 整体替换入边缺省 FS、set_meta 日期口径与 deadline 空串清除/null 不动、auto_chain 手动跳过+无可补报错、set_baseline 引擎纯函数不取时钟（缺 savedAt 拒绝）、remove_resource 级联、set_assignments 分组行拒绝+重复对+外来 taskId、多 op 中途失败半途状态不外泄。

## 2. opsSim.ts（TS 镜像）与卡体接入

- `frontend/src/schedule/opsSim.ts`：`simulateOps(before, ops)` 深拷贝上顺序应用（fail-closed），`SimResult = ok(project+summary) | error`；错误文案与 Go 尽量一致但不参与对拍。
- **ScheduleDiffCard ops 通道升级**：缺省路径 + before 可得时，simulateOps 投影 → 与 project 通道同一条 `diffProjects` 管线渲染真 diff（汇总行+明细行+「ops 投影」来源标注）；**三级诚实降级**：非缺省路径（绑定只服务缺省）→ before 读取失败 → ops 模拟失败（上屏「ops 投影失败（原因）：以 apply 回执为准」）→ 意图清单。project 通道回执标注同步为「文件实况对比」。
- 落盘权威永远在 Go：模拟结果只用于预览，「成功≠正确」核对仍以 apply 回执为准。

## 门禁与走查

- Go 全量绿（golden fixture 生成后复验通过）；vitest 2499→**2550**（+51：对拍 49 + 卡体 ops 投影/降级 2 重构）；tsc -b/eslint 0（tsconfig 增 resolveJsonModule 供 fixture 导入）；drift PASS@597（零绑定）；版本三处 4.149.0；build+冒烟过（/api/health 200）。
- **diff 确认闭环设计四刀（A 后置回滚 / B project diff 卡 / C 引擎强制 / D ops 投影对拍）全部收官。**

## 产物

- `releases/gaea-v4.149.0.exe` — SHA256 见 `SHA256SUMS-v4.149.0.txt`（71502c4d0ea2dcfc67b961fb796e0a7767a66fa0a38f06475985571b78d216fa）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）
