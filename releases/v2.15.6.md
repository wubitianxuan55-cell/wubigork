# gaea v2.15.6「Herdsman 深挖 P5 · 数字生命记忆联动 + 最近操作」发布说明（2026-08-13）

> 承接 v2.15.5，完成 Herdsman 深挖路线图最后一个阶段 P5：把 Herdsman 的
> 「数字生命」记忆（digital-life/life.sqlite3：虚拟人格角色、关系、记忆摘要、
> 时间线、世界事件）只读接进 gaea 记忆中枢，并展示 Herdsman 最近异步操作。
> 桌面端已打包。

## 一、数字生命记忆联动（只读）

- `App.HerdsmanDigitalLife()`：只读打开 `~/.herdsman/digital-life/life.sqlite3`
  （mode=ro，绝不写入），聚合：
  - 计数：角色 / 时间线（life_timeline_events，本机 22 万+）/ 世界事件 /
    记忆摘要 / 关系 / 状态提交 / 对话轨迹。
  - 角色卡片：姓名、身份、世界观、模型选择，合并关系（亲密度/信任/安全/冲突/
    最近互动）与记忆摘要（摘要截断 + 高亮 5 条 + 强化值 + 记忆事件数）。
  - 最近时间线 / 世界事件各 12 条。
- 记忆中枢新增「数字生命」库（首页卡片 + 库面板），与三脑记忆并列；数据不可用时
  展示可读空态，不阻塞其他库。

## 二、最近 Herdsman 操作

- `App.HerdsmanOperations()`：读取 `~/.herdsman/skill-operations.json`，
  按创建时间倒序返回最近 20 条异步任务（生图/模型启动/下载/TTS 等）的
  状态、进度与产物数。
- 数字生命面板内展示「最近 Herdsman 操作」列表（类型中文标签、模型、进度、
  状态徽标、时间），长任务进度与产物一目了然。

## 三、测试与构建

- Go：新增数字生命只读解析用例（临时 SQLite 建表+夹具：计数/角色合并关系与
  摘要/摘要截断/时间线/世界事件/缺失库报错）+ 操作记录解析（排序/字段/非法 JSON/
  磁盘读取）；`go vet` + `go test ./internal/app` 全绿。
- 前端：新增 DigitalLifeLibrary 2 例（角色/关系/摘要/时间线/世界事件/操作渲染 +
  不可用空态），Vitest 239 → 241 全绿，`tsc -b` 通过。
- `wails build` 成功：gaea-v2.15.6.exe（33,484,800 字节），bindings 已再生。
- SHA256：`4f9d4ace9692326d549997ccf85bcc46ea2859e554f944c76ba00154ba60d306`

## 四、Herdsman 深挖路线图状态

P0 盘点 ✅ → P1 模型库 ✅ → P2 生命周期 ✅ → P3 本地翻译 ✅ → P4 检索升级+统计 ✅
→ P5 数字生命联动+操作 ✅。六个阶段全部落地。
