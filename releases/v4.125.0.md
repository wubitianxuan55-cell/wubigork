# v4.125.0 · 联动候选：办公摘要卡「设为当前计划」+ 拍板池三份设计文档落档

- 日期：2026-09-06
- 绑定面：**588 零变更**（drift PASS@588；设为当前计划走既有 ScheduleSave 通道）
- 性质：①联动候选池「另存为当前计划」兑现为摘要卡一键动作；②拍板池三项欠账（diff 确认卡/双工期口径/多工程管理）由并行子代理完成**拍板用设计文档**（调研+设计，不动代码）——拍板池从「待拍板无材料」转为「有方案可拍板」。

## 一、本版内容

### 1. 办公摘要卡「设为当前计划」（联动候选兑现）
- 非当前计划的 .gsched.json 摘要卡上新增一键「设为当前计划」：把该文件内容经 normalizeProject 复制为当前计划（走既有 `ScheduleSave`→Go Validate/CPM fail-closed 通道），成功后 `notifyScheduleFileChanged` 板块即时回读 + toast，卡片转「已复制为当前计划」态。
- 守卫：循环依赖计划（summary.ok=false）不提供入口——Go Save 必拒收，宁不显示不报错；仅非当前计划文件显示（当前计划的打开/周报动作不变）。
- i18n 三语 +4 键（schedCard.setAsCurrent/copied/copyFail 等）；测试 +1（复制写入断言+循环依赖入口隐藏）。

### 2. 拍板池三份设计文档落档（并行子代理，纯文档）
| 文档 | 推荐方案速览 | 拍板项 |
| --- | --- | --- |
| docs/gaea-schedule-diff-confirm-design-2026-09.md | **混合分级确认**：project 整量通道前置强制确认（复用 Gate 机制禁会话记忆）、ops 增量通道 ask 级审批卡升级为 diff 确认卡、补后置回滚入口；零绑定可行已核实（ToolDispatch 先于闸门，前端已持 args）；最大风险=TS simulateOps 镜像与 Go ApplyOps 语义漂移 | 8 项，分 4 刀（A 回滚入口→B diff 卡→C 引擎强制→D simulateOps） |
| docs/gaea-schedule-dual-duration-design-2026-09.md | **任务级单位标记 `durationUnit?: 'wd'\|'cd'`**（缺省 wd 零迁移零绑定）：换算=es 起按自然日走、完成边界 ceil 吸附工作日；v1 cd 任务收窄仅 FS 搭接；成本/基线用等效工作日跨度（ef−es）；4 刀（引擎→agent→UI→mspdi 真机池） | 9 项 |
| docs/gaea-schedule-multi-project-design-2026-09.md | **每文件一工程+索引文件**（进度计划/*.gsched.json + .gaea/schedule/index.json 注册表+当前指针）；当前指针收敛 Go 侧、agent 缺省 path 跟随；刀1 绑定面 588→**590**（+2）或附零新增备选（前端 ListDir+WriteFile 凑齐）；v1 不做 list_projects 工具 | 8 项 |

## 二、测试与门禁

| 门禁 | 结果 |
| --- | --- |
| vitest | 2213→**2214/2214**（+1，全绿） |
| tsc -b / eslint | 0 / 0 |
| Go | 零改动例行绿 |
| check-bindings-drift | PASS@588 |
| check-version-drift | OK（4.125.0） |
| 版本四处 | 4.125.0 |
| build.bat | 冒烟 /api/health 200 |

## 三、欠账清单（发布口径）

- 拍板池三项已具备拍板材料，等用户对三份文档的方案与拍板项表态后进入实施刀序。
- 资源成本刀4 mspdi 互通——绑真机池，不真机不发布。
- AOA 刀2（对齐辅助线/一键整理）观察后拍板。
- 联动候选余：完成率口径；真机池：真机端到端+联动跳转+AOA 拖拽手感。
