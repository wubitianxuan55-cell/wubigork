# v4.177.0 — 瘦身 P4 懒加载三刀收官：mermaid 动态化（H1）+ locales 按需（H7）+ SearchModal lazy（H8）（2026-09-09）

> 瘦身执行层第十二版：P4 运行刀收官——把 mermaid 607KB / locales 250KB / SearchModal 从急切加载图移出。
> 两线并发子代理（H1 + H7/H8，足迹互斥）+ 主代理收口（含 i18n 回退链修复 + 全量门禁）。

## 1. H1：mermaid 动态 import（Markdown.tsx + mermaidPng.ts）

- 两文件删静态 `import mermaid` → `ensureMermaid` 改异步 `await import("mermaid")`（模块级缓存，initialize 配置原样；MarkdownTheme 用 strict、mermaidPng 用 loose 各自保留）。
- MermaidBlock 渲染迁入 useEffect async IIFE（取消语义/失败降级与原同步路径一致）；renderMermaidToPng 的 await 移入 try（加载失败也落 "失败返回 null" 契约）。
- **全仓 mermaid 静态 import 清零**：entry 对 mermaid.core **零引用**（基线有静态引用）；仅 2 处动态 import（Markdown chunk + ImageGenPage chunk）；mermaid.core 566.9KB 独立 async chunk 按需。
- 测试零改动（5 个 Markdown 相关测试均不渲染 mermaid 围栏，jsdom 下动态 import 不触发）。

## 2. H7：locales 按需（i18n.tsx）

- **zh 静态保留**（首帧零异步）+ en/zh-TW 动态 `import("../locales/en")` 显式分支（Vite 静态解析出独立 chunk）；loadLocale 幂等（loadedLocales Set）。
- **同步 API 不变**：useT/t/setPref 签名零变动；Provider 切换语言时先同步渲染（未加载字典回退）→ chunk 就绪后 forceRender 整树重渲染，alive 防过期落地。
- **主代理收口修复（关键）**：`translate()` 回退链 `DICTS[locale] → DICTS.en → key` 改为 `DICTS[locale] → DICTS.zh → DICTS.en → key`——zh 静态恒可用兜底，非 React 调用方（lib/tools.ts 摘要口述，currentLocale 默认 en）在 en chunk 未加载时**裸键永不外泄**。
- 5 个测试补 `beforeAll(() => loadLocale('en'))`（ConsistencyPanel/mock-session-replay/DeliverablesPanel/MemCitationChip/Transcript.t74——模块级 t() 需 en chunk 就绪）+ ModelSwitcher renderT en 用例 await。

## 3. H8：SearchModal lazy（MainLayout.tsx）

- 静态 import → `React.lazy(() => import('../components/SearchModal'))` + Suspense fallback null；SearchModal.test 不经 MainLayout 挂载零改动。

## 4. 主代理收口

- 首跑全量 vitest 13 例失败（i18n en 断言时序 + tools.ts 裸键回退）——zh 兜底修复 + beforeAll 适配后全绿；1 例 FilePreviewModal 并发负载 flaky（单独复跑 15/15 绿，在册先例）。
- 足迹核验：11 项（4 源文件 + 7 测试适配），全在预期。
- **entry chunk 755.48KB**（基线 1193→**−37%**）；en=76.95KB/zh-TW=74.91KB/SearchModal 独立 chunk。

## 5. 度量看板

```
每空间一级板块数（工位 6/乐园 4）｜ entry chunk 755.48 kB gz 246.48（基线 1193→−37%）｜ 冷启动可交互(待真机) ｜ 常驻内存(待真机) ｜ exe 46.2 MB ｜ Go >50KB 源文件 0
```

## 6. 产物与冒烟

- 产物：`gaea-v4.177.0.exe`（46.15 MB），SHA256=`D027D29B5BADF120EB0FA06BF074E7E946B75004B3476A84D9E2EC5BD9E283CC`（releases/SHA256SUMS-v4.177.0.txt；**docs 补提交更正**：初版记录 `E98E49CD…` 为 build.bat strip 固化**前**的构建，更正值为本版入库 build.bat（-ldflags "-s -w" -trimpath）可复现产物，tag v4.177.0 不动——v4.101 补提交先例）。
- 冒烟：`scripts/smoke.ps1` → **/api/health 200**（冒烟进程已回收）。

## 7. P4 收官状态与欠账

- **P4 结构+运行刀收官**：Go >50KB 0；entry 1193→**755.48KB（−37%）**；MemoryHubPage 页壳 15.95KB；mock/office 51.2→1.1KB；wailsjsCompat 退役（v4.174）。
- 欠账：**exe strip 已随本版实施并实测销账**——build.bat 固化 `-ldflags "-s -w" -trimpath`（下轮起发布构建自动 strip）；实测 46.2→46.15MB（−0.09%）减量甚微：Go 符号表在 Wails 应用占比小，-trimpath 安全收益为主。壳内真机池（rail 切换器/home 最近文档/.gsched）、审计刀D 真机取证（printSvg print/拖拽/粘贴）、观察池刀3（工作台内嵌办公）待评估均不变。