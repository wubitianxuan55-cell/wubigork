# v4.153.0 · 双工期口径刀4：mspdi 互通（DurationFormat 8 导出/导入）（2026-09-08）

> 双工期四刀收官刀（设计 §3.6）：养护类 cd（日历天）任务进出 MS Project XML 不再失真——此前导出恒 `DurationFormat=7`、导入忽略 DurationFormat，Project 侧 28ed 任务导回被静默当 28 工作日（口径放大 +8 工作日）。纯前端，绑定面 **597 零变更**。**真机走查状态见文末——代码侧按 XSD 语义假说实现+mock 往返钉死，Project 往返重算行为待真机池验证。**

## 1. 导出（cd 任务）

- `durationUnit='cd'` 叶任务：`<DurationFormat>8</DurationFormat>`（elapsed 日历天）+ `<Duration>PT{自然日×24}H0M0S</Duration>`（28 自然日=PT672H）。wd 任务恒 7 + PT{d×8}H 逐位不变；分组行恒工作日口径（禁 cd 与数据层一致）。
- Start/Finish 本就按 wdToDate(es/ef) 出日历日期，cd 任务两端日期自动吻合真实日历跨度。
- 不导出任务日历（TaskCalendarUID，模型无承载，诚实不导）。

## 2. 导入

- `DurationFormat=8` → `durationUnit:'cd'`，时长=分钟÷1440（24h/自然日）；里程碑/汇总行不标 cd（工期恒 0，normalize 双保险）。
- 其余格式（7 天/9 周/未知枚举）维持现状按工作日 ÷480 折算，宽松容错沿现状（未知格式回落工作日，无单位标记）。
- 导入链经 normalizeProject（刀1 容错）→ cd 数值越界/分组行/里程碑自动回落 wd，引擎 fail-closed 兜底非 FS 搭接。

## 3. 内部重构

- `parseDurationDays` → `parseDurationMinutes`（分钟原值，按格式分流 ÷480 / ÷1440）；常量 `FORMAT_DAYS=7` / `FORMAT_ELAPSED_DAYS=8` / `MINUTES_PER_ELAPSED_DAY=1440` 显式化。

## 门禁与走查

- vitest 2596→**2601**（+5：导出口径/roundtrip 不丢口径不变值/Format 8 导入/未知格式回落工作日/elapsed 里程碑不标 cd）；tsc -b/eslint 0；drift PASS@597；版本三处 4.153.0；build+冒烟过（/api/health 200）。Go 零改动。
- **真机池（本机未装 MS Project，无法执行；沿用「等真机」观察池先例）**：①真机 Project 打开导出文件核对 8 的显示与重算、改值导回 diff；②`PT{自然日×24}H` 序列化假说钉死（XSD xsd:duration 语义允许，Project 实际写法待样本确认——若有出入仅影响 Export 端常数，Import 端 ÷1440 与分钟解析已按两口径兼容）；③WPS/斑马打开导出文件行为。真机走查通过前，Export 端对 Project 的实际兼容性视为**待验证**（Import 端是失真修复，方向性收益确定）。

## 产物

- `releases/gaea-v4.153.0.exe` — SHA256 `e55e9bad991153fd330c63cc74c19a6d6fdcc7feba91e084ff8b8e6b60fae706`（见 `SHA256SUMS-v4.153.0.txt`）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）
