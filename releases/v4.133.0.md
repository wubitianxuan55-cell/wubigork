# v4.133.0 · 进度计划刀D2：网络图上报件——双代号时标+工程标尺 / 单代号六格（含 AOA 逆推锚点修正）

> 差距清单 #3（docs/gaea-schedule-gap-vs-project-2026-09.md，对标 .gzp 时标网络上报件：重庆干休所件口径）。纯前端，绑定面 **588** 零变更（AOA 不入 Go 引擎，无镜像负担）。

## 一、既有引擎缺陷修正（图面照出的真问题，先于图面发布）

- **buildAoa 逆推锚点错位（aoa.ts）**：`total`（计算工期）在正推**之前**取事件注册初始 es（=单任务工期），长链计划下 total=30 而真实 Tc=141 → 整条逆推平移 −111，节点下方「最迟时间」集体假负、**刀G G3 的 AOA 侧 planFinish 锚点从未真实生效**（Tp 只与错误的 total 比较，Tp<total_init 才收紧=荒谬条件）。修法=total 移到正推之后。关键线路判定（相对比较）不受影响、ES/波形不受影响。
- 实证（?mock=demo 办公楼样例，无 deadline）：min(ls) −111→0，节点下方 ES/LS 恢复诚实；planFinish 锚点在真实长链计划上首次可生效。
- 回归用例 +3：长链 ls≥0 且 T 事件 es=ls=Tc；planFinish<Tc 时 S.ls=Tp−Tc（关键集含关键虚工作）；planFinish≥Tc 不收紧维持经典口径。刀G 既有断言未编码错误行为（其用例初始 es 恰=Tc），全量绿。

## 二、networkExport.ts 网络图上报图面（纯函数构建器）

- **buildAoaExportSvg 双代号时标网络**：水平轴=最早时间（1 格=1 天，发布物恒取时标口径）；**工程标尺四行**（工程日步长按总工期 5/10/20 自适应 + 总工期红刻度 / 月 `2026.9` / 日 / 星期），月界竖线贯通图面；日宽自适应 36~110px/天（200 天计划 22000px→11846px，不爆画布）；事件圈+编号+上 ES/下 LS；箭线复用刀H 纯函数（edgeSegs 标注口径/波形线=自由时差/过桥法），关键红粗、虚工作虚线；图例+口径注+图签。
- **buildPdmExportSvg 单代号网络**：拓扑分层+六格节点盒（ES/工期/EF·LS/总时差/LF，TF=0 红字）、绑定中的临界搭接红色贯通（isLinkBinding 同构副本）、虚拟 S/T 虚线框、无时标（逻辑关系图口径）；图例+图签。
- ganttExport.ts 抽共享导出件：EXP_COLORS/EXP_FONT/EXP_MARGIN/EXP_TITLE_H/esc/fitText/exportTitleSvg/exportSignSvg（三图面同源标题带与图签）。

## 三、ExportDialog 图面类型切换

- 导出弹窗增 Segmented：横道图 / 双代号网络图 / 单代号网络图（默认跟随当前视图，打开时重置）；按图种给缺省图名与口径提示文案；签署字段持久化不变。

## 门禁与走查

- vitest 2291→**2301**（+10：networkExport 7 / aoa 回归 3）；tsc -b、eslint 0（测试断言 hex 改引 EXP_COLORS）；go test 绿（零改动）；drift PASS@588；版本四处 4.133.0。
- 走查（?mock=demo 真浏览器）：三图面 PNG/PDF 全出「已导出」回执；页面内同源模块构建 AOA 图面目检——工程标尺四行（日号跳周末 1-4、7-8…/星期对齐/月标签 2026.9）、min(ls)=0、9 条波形全在虚工作自由时差位；PDM 图面目检——红框关键链贯通、红箭线 12 条、六格 TF=0 红字、标题带签署字段在位（截图复核）。

## 下一步

刀D3 Excel 导入导出（Go binding，588 会变）；刀C 余项（undo-redo/右键菜单/任务筛选/进度编辑入口）；小刀（任务路径分析+任务检查器）；PDF对比余项（bar 图面标注/编制说明注/里程碑旗标/签证台账）。
