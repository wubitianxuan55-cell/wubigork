# v4.154.0 · MPP 导入支持 Project 2013+ 变体（真机样本驱动修复）（2026-09-08）

> 用户投入真实工程文件「重庆干休所总进度计划1（开工）.mpp」（Project 2013+ 保存）当真机样本：**此前 ParseMpp 直接拒收**（「缺 CompObj」），强拆后任务名/工期/层级系统性错乱。本刀以字节级取证钉死 2013+ 变体与 2010 口径的五处漂移并修复——**该文件现已完整导入：38 任务（8 分组）/42 搭接/总工期 136 天，任务名零缺失，CPM 自洽**。纯 Go 内部引擎，绑定面 **597 零变更**。

## 真机取证钉死的 2013+ 变体口径（vs MPP14-2010）

| # | 口径 | 2010 | 2013+ | 取证方法 |
|---|---|---|---|---|
| 1 | 格式探测 | `\x01CompObj` 应用名 | **无 CompObj 流**——按工程目录编号判版（`   114`→MPP14，MPXJ 同款启发式），无 CompObj ⇒ appVer≥15 | 流清单全列 |
| 2 | 任务工期 | i32@42 | **i32@84**（@88 恒同值重复；38/38 行整除 4800 且数值全为整天） | 全行偏移扫描：唯一全行命中的候选 |
| 3 | Var2Data 回链键 | uid@0 | **id@4**（uid@0 与名字表大面积错位=陈旧键；换 id 后 38/38 全对上） | 名字表 uid 集与行 uid/id 集合比对 |
| 4 | 父链/层级 | parentUID@36 | **@36 恒 0 失效；大纲层级 i16@172**（序列 0,1,2,3,2,3…与 WBS 逐行吻合），父链由层级栈重建 | 小整数列扫描+逐行人工辨认 |
| 5 | 里程碑位 | FixedMeta byte8&0x20 | **byte8 恒 0xe3 失效；里程碑=零工期**（Project 语义） | meta 条目全字节 dump |

搭接表 TBkndCons：pred/succ 确认同为 **ID 键空间**（45 行全部命中任务 id 集），lag@14（刀4 已有口径）数值合理（14d/27d）——搭接 45→42 条经严格递增去重后全数回链。

## 诚实降级（宁缺勿错）

- **2013+ 资源/分配暂不解析**：资源行布局漂移（解析出「名为 27」的垃圾资源）、分配行 taskUID/resUID 偏移与 units 偏移未钉死——错数据比没数据更有害，跳过并留观察池（任务/搭接/层级/日历不受影响）。
- 日历例外日仍不解析（v4.140 既有口径）：文件原总工期口径 150 天 vs 本引擎 CPM 136 天的差异主要来自此（原文件或含节假日例外），自洽性 192 自然日 ≈ 136 工作日×7/5 可验。

## 门禁与走查

- `TestMppRealSamples2013` 门控样本测试落档（clones/mpp2013/，git-ignored，缺失 Skip）：钉 38 任务/8 分组/42 搭接/136 天/名称零缺失；**既有 `TestMppRealSamples` 三样本（MPP9/12/14-2010）零回归**（146/43/323 任务、资源分配俱全）。Go 全量绿；版本三处 4.154.0；drift PASS@597；build+冒烟过。
- 前端零改动（导入链 store→normalizeProject 不变）。

## 产物

- `releases/gaea-v4.154.0.exe` — SHA256 `162c038118d3b9db8885dab86d7afa2f494b4ac011bc3019327f2521e60e381c`（见 `SHA256SUMS-v4.154.0.txt`）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）
