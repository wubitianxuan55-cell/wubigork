# v4.201.0 — 办公会话排队：拖拽排序（2026-09-10）

> v4.200 排队项能改、能删、能插话，顺序却只能入队先后。本刀让排队中的信息可以拖拽重排发送顺序。

## 落地

纯前端，绑定面 **608 零变更**。HTML5 原生拖拽（看板/导图同款，零新依赖）。

- `moveItem` 纯函数：把 from 插到 to；**sending 钉住不可拖出**，pending 可拖到任意槽（含发送中那条前面，决定下一句谁先发）；
- 行左握把（GripVertical）：`cursor-grab`，点握把不触发撤回编辑；发送中行无握把；
- 拖到目标行高亮 inset accent，被拖行降透明度（只动 opacity/box-shadow）。

## 测试

moveItem 保序/钉住/越界；列表 dragStart→drop 回调 onReorder；sending 无握把；Composer 入两队后拖第二到队首顺序翻转。

## 门禁

vitest 318 文件 2781 例 exit 0 / tsc 0 / eslint 0 / 版本三处 sync 4.201.0。
