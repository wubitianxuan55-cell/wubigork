# v4.155.0 · 双工期欠账放开：SS/FF/SF×cd 全搭接（§3.2.5 重推，零不动点）（2026-09-08）

> 双工期设计（docs/gaea-schedule-dual-duration-design-2026-09.md）§6 欠账池首项销账：v4.150 刀1 曾 fail-closed 收窄「cd 任务仅允许 FS 搭接」，本刀按 §3.2.5 要求**实施前重推语义矩阵**——原矩阵四格「⚠ 需不动点」均系 wd 代数形误判，实际**零迭代可解**，八格全放开。TS↔Go 同批镜像，绑定面 **597 零变更**。

## 重推结论（为何不需要不动点）

| 手段 | 消解的格 | 原理 |
|---|---|---|
| **单调逆查** `cdEarliestStart/CdEarliestStart`（新原语，与 cdToEf/cdLatestStart 互为镜像三件套） | FF-to / SF-to | fwd=cdToEf 关于 es 单调不减（有平段），「完成 ≥ T」的最早开工=最小 s 使 fwd(s)≥T——逆像存在且确定，无需 wd 式 `es = ef−dur` 代数（那才会撞上「等效跨度随位置变」的假不动点） |
| **直接 ls 下界** | SS-from / SF-from | SS/SF 约束本就落在**开始**上（`ls ≤ to.ls−lag`），直接收集为 lsBound；wd 式先换算成 lf 上界（+durFrom）再减回来才是原矩阵担心的不动点源——不换算就没有 |

落地口径（两侧逐位镜像）：①正推 to=cd 时 FF/SF 走逆查、FS/SS 同 wd 式；②逆推 from=cd 拆双上界——lf 收 FS/FF（完成约束）、lsBound 收 SS/SF（开始约束），`ls = min(cdLatestStart(lf), lsBound)`，`lf` 保留 raw 上界（兼容 v4.150「fwd(ls)≤lf」pin）；③后继 **effLf**=cd 后继的 `cdToEf(ls)`——FF/SF 对前驱的传播读「实际最迟完成」才诚实（v1 全 FS 组合永不触及，旧 pin 零风险）；④wd 路径逐位不变（FS 项无条件改读 to.ls 与旧式恒等，快路径用例零回归钉死）。

## 闸与文案同步拆除

- Go Validate「cd 搭接仅 FS」闸、TS/Go 引擎级防御拒绝、analyze「cd 非 FS 疑旧口径残留」finding——三处全删；cd 余下四闸（枚举/分组行禁 cd/里程碑禁 cd/3650 上限）不动。
- 文案：scheduleApply 描述、schedule-edit 技能（锚点锁 37 不变）、types 注释、甘特工期 chip title 的「搭接仅 FS」全部更新；compact 无涉及。

## 测试与对拍

- **六张锚点表**（cdToEf/cdEarliestStart/cdLatestStart × cd=5/7，含平台 0-2→5、5-7→10）两侧逐字钉死；**用例 A-E**（FF+cd-to 带 lag / SS+cd-from / SF+cd-from 非平凡 / SS+cd-to 旧路径恒等 / FF 链+cd 中段 effLf）两侧镜像断言 es/ef/ls/lf/tf/critical/duration。
- golden 对拍 **62→65 例**（+3：set_links 给 cd 挂 SS(lag2)/SF(lag1)、upsert cd+FF 搭接，全成功例入 after 状态）；opsSim 双跑漂移即炸。
- 既有「cd 非 FS fail-closed」用例按新语义原位翻转（ok=true），其余既有用例零改动全绿。
- 契约笔误如实记录：用例 D 原稿把 SS 的正推期望误写成 FS 式数字，两侧子代理独立发现并按定稿语义（SS 正推=from.es+lag）收敛到同一组修正值——镜像无损。

## 门禁

Go 全量绿（+2 测试函数/5 子用例）、vitest **2601→2617**（+13 TS 新用例+3 golden 双跑）、tsc -b/eslint 0、drift PASS@597、版本三处 4.155.0、build+冒烟过。

## 产物

- `releases/gaea-v4.155.0.exe` — SHA256 `41d8d7f72746cbd52cc3448b9621be763ecd3c3829e162ef5d230cbf1bc93fc1`（见 `SHA256SUMS-v4.155.0.txt`）
- 冒烟：/api/health 200（scripts/smoke.ps1，18999）

## 欠账清单（如实列示）

- 观察池不变：mspdi 刀4 真机走查（Project/WPS/斑马）、2013+ 资源/分配键位、list_projects、E1 负数对称（动逆推才收口）——全绑真机。
- 双工期欠账池剩余：导入 Project 任务日历转 cd 迁移助手、斑马/定额导入口径探测、等效跨度灰显双读数（按需）。
