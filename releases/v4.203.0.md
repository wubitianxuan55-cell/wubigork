# v4.203.0 — 空间策略其余功能域键：总闸当场切（2026-09-10）

> v4.190 欠账收刀。七键写路径早已通，编辑 UI 却只放 gaea 主控——闲庭要给
> 小说/对话单独绑模型，只能改 toml。本刀把其余六键拉进同一张空间策略卡。

## 缺口

`GaeaSpaceProfileSet` 可写 `gaea/chat/whisper/novel/office/characterlib/routine`，
模型中心「空间策略」只给办公 Agent（gaea）编辑钮。其余功能域覆写仅在非空时
以 chip 显形，空键看不见，写不了。展望「书斋和闲庭可以各绑各的」差这一截。

## 落地

纯前端，绑定面 **608 零变更**（写路径 v4.190 已通，mock 七键本就收）。

- 每张空间卡「其余功能域」六键常显：已配显示引用，空键灰字「未配置（维持现状）」；
- 行级编辑闭环与 gaea 同款（带出当前值 → `GaeaSpaceProfileSet(space, key, ref)`
  → 视图随返回刷新；空串=清除；后端校验失败原样 `message.error`）；
- gaea 主控行与既有 testid 不动（`mc-strategy-*-edit/input/save`）。

## 测试

既有 6 例保留（chip 断言改为 novel 行 testid）。+2：小说键编辑闭环 /
空串清除回未配置。

## 门禁

vitest StrategySection 8/8 / tsc 0 / eslint 0 / 绑定名 608 零变更 /
版本三处 sync 4.203.0。

## 欠账

- 功能域×空间策略交叉矩阵（哪个功能域最终用哪个 provider）仍按需另刀；
- 壳内真机走查池不变（空间策略其余键编辑闭环挂真机池）。
