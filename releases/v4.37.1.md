# v4.37.1「D 刀收口 · 模型库卸载确认带释放大小」

> 发布日期：2026-09-02 ｜ tag：`v4.37.1` ｜ 基线：v4.37.0 ｜ 绑定面 **557 零变更**

## 交付（模型中心专项调研 D 刀——核伪后剩余增量）

- 模型库卸载确认文案带释放大小：`file_size` 存在时 description 显示「将删除已下载的模型文件，释放 X GB」（Jan 式「删除释放空间」的克制版）。

## 核伪剔除（调研提案中两项经 grep 核实已存在，不入刀）

- 「磁盘占用展示」：HerdsmanCatalogSection 已展示已装占用/磁盘余量/每模型 file_size（E1-4 磁盘治理早已落地）。
- 「llama.cpp /health 透出」：TestConnection 对 herdsman 已走 fetchModels 探活，状态已在总览/引擎卡展示。

## 验证

- go build/vet 全绿（Go 侧零改动）；tsc -b 0；vitest 模型中心目录绿；drift PASS（557 零变更）；版本四处 4.37.1。

## 欠账清单

- **沿旧**：A2 帧流/接管/动态权限卡；B2 真编辑 pptx；行级降噪折叠重构；palette 个性化排序；并行多子代理派发瞬间 task 卡空 ref；-race（本机无 gcc）；model-failover 文案 engineLabel 化；dev mock 补 Get/SetEngineFailover；自定义引擎用户价目（目录 v3 候选）。
