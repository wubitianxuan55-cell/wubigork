# v4.332.0 · 7.3-2 板块降级为任务视图（阶段七「层跃升」刀）

> 2026-09-17 发布。规格书：`进度计划/gaea-tasks-first-home-20260917.md`。
> 用户「开始吧」提前拍板解锁（原零功能周窗口 ≈09-23）。
> **形态标志：本版=阶段七「层跃升」版本（nextgen §10 口径；版本号续
> v4.x 列车不跳号）。**

## 论点

首页从「用哪个工具」（板块入口优先）翻转为「什么在等我」——收件箱+最近
上下文成为首屏，板块入口降为「能力的视图」。身位依据平台调研 §1.1：任务
持久化四家红海，「多入口统一收件箱+板块降级为任务视图」无人发布。

## 落地

- **homeLayout 开关**（appStore 持久化，缺省 classic）：现有体验零变化，
  层跃升由开关启用——渐进不重写。
- **TaskInboxBoard 内联板**：TaskInboxPanel 主体原样抽出，Modal 壳保留
  薄包装（面板 props/DOM/既有 9 用例零改动）。
- **TasksFirstHome**（两空间共用）：SpaceSwitch → 轻 masthead（印章/标题/
  lede+切回经典钮）→ **收件箱大区**（TaskInboxBoard 直嵌）→ 最近会话 →
  **能力视图**（deriveLauncherModules 全量 chips 点击直达——藏≠删）→
  纯同步口径脚注。
- **回退开关双位**：设置页 HomeLayoutPanel（SegmentedRow 正式位）+ 首页
  SpaceSwitch 条快捷钮互切；classic 走既有 Desk/Garden 零变化。
- 零新绑定（707 不动）；manifest/pageRegistry 零改动；三语 +12 键。

## 测试与门禁

- 前端 +10（TasksFirstHome 4 / ModuleLauncher 分支 2 / appStore 3）。
- 零回归实证：TaskInboxPanel 9/9、ModuleLauncher 13/13、SettingsPage/
  appStore 既有全绿零改动。
- 门禁：tsc -b 零错、eslint 零告警、vitest 全量 3180/3180 一次全绿、
  drift OK@707、版本三处 4.332.0。

## 坑（沉淀）

- 测试文件尾部 rfind('})') 追加会吞原 describe 闭合（Unexpected EOF）。
- useT 组件测试须 LocaleProvider 包裹。
- ESM 测试无 require——守卫函数走顶部 import。

## 观察池

「最近任务」独立区；能力 chips 分组/搜索；任务拖拽排序；晨报/脉息在
tasks 形态的取舍；默认形态翻转（用户试用后拍板）。
