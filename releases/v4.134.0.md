# v4.134.0 · 进度计划刀D3：Excel 导入导出——上报口径往返（绑定面 588→590）

> 差距清单 #2（docs/gaea-schedule-gap-vs-project-2026-09.md，官方桌面客户端「Excel 导入/导出」项；国内上报常态 Excel 横道表）。Go 侧 excelize（成本板块既有依赖）。

## 内容

- **internal/schedule/xlsx.go**（格式层不入引擎，无镜像负担）：
  - **ExportXlsx**：上报口径 7 列（序号/WBS/任务名称/工期(天)/开始/完成/前置）；CPM fail-closed（循环依赖不出表）；日期按工作日历换算（WdToDate）；分组行跨连续叶任务取 min ES/max EF 跨度；前置列 Project 式 `2FS+3` 引用（lag 负值 `-n`）；列宽/表头加粗。
  - **ImportXlsx**：表头行在前 10 行内按**别名归一识别**（任务名称/工作名称/工作内容…、工期/持续时间/工期(天)…、开始/开工日期/竣工日期…、前置/紧前工作/逻辑关系…、序号/标识号、wbs/编码/大纲）；层级按 WBS（存在「X.」子编码的行=分组，两级大纲收敛），无 WBS 列时空工期行=分组；工期文本提取（`5 个工作日`→5）；前置引用 `2`/`2FS`/`2SS+3`/`2fs-1`（大小写不敏感）按序号回链，跳过未知序号与涉及分组行的引用、同键去重；里程碑=0 工期叶任务；开始/完成列**不进模型**（仅取最早开始作开工日）——排程交回 CPM 引擎重算，与 mspdi 导入同律；Validate fail-closed 收口。
- **绑定 ×2**（office 门面，gen_bindings 重生成）：`GaeaScheduleExportXlsx(projectJSON) → base64`、`GaeaScheduleImportXlsx(xlsxBase64) → 计划 JSON`。前端下载/上传走 base64（Blob `<a download>` 与导出 XML 同一 UX）。
- **前端接线**：bridge.ts AppBindings+名称映射、spaceBindings 分面 +2 work（数量锁 301→**303**）、mock/office.ts 直通桩（浏览器 dev 用 JSON↔base64 模拟 xlsx 往返，行为可走查）、schedule/api.ts `exportScheduleXlsx`/`importScheduleXlsx`（分块 btoa/atob）、SchedulePage 工具栏「导入 Excel / 导出 Excel」按钮（FileExcelOutlined，复用 importMsg 反馈）。

## 门禁与走查

- Go +7 用例（xlsx_test.go）：往返一致（名称/层级/工期/里程碑/搭接回链/开工日）、表头别名、引用变体（含小写/未知序号跳过）、WBS 深度、无 WBS 分组判定、循环依赖导出 fail-closed、未知表头可读错误。go test ./... 绿；绑定面 **590**（drift PASS@590）。
- vitest 2301 全绿（spaceBindings 数量锁 +2）；tsc -b、eslint 0；版本四处 4.134.0；build 冒烟 1m4。
- 走查（?mock=demo 浏览器）：「导出 Excel」出成功回执；api 模块直连往返（导出→File→导入）20 行与工程名保持一致（真实 xlsx 渲染由 Go 用例钉死，mock 为诚实直通）。

## 下一步

刀C 余项（undo-redo/右键菜单/任务筛选/进度编辑入口）；小刀（任务路径分析+任务检查器）；PDF 对比余项（bar 图面标注/编制说明注/里程碑旗标/签证台账）。
