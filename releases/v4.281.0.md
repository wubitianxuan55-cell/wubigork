# v4.281.0 — 拆书导入 P1 接线：AI 反推大纲（预览载荷 + 幂等落库 + 创作间入口）（2026-09-13）

## 来源

续 v4.280.0（反推编排引擎）。本刀把 t2 P1 接成**用户可用的一条链**：
反推（AI）→ 预览载荷 → 确认 → **幂等合并进大纲**，入口在小说创作间 rail。

## 落地

### ① 后端（新 `internal/app/novel_import_ai.go`，绑定 646→648）

| 绑定 | 行为 |
|---|---|
| `NovelOutlineReconstruct()` | 读当前工程章节（标题取大纲节点、正文走 `ReadChapterAsStitch`，上限 200 章）→ Stage1 立项反推（模板 `book-import-project`，采样前 3 章 × 2000 rune）→ 分批章节大纲（`book-import-outline`，batchSize=5，**逐批独立降级**）→ 返回预览载荷，**零落库** |
| `NovelOutlineReconstructApply(itemsJSON)` | 按**章号**命中既有大纲节点，写 summary / scene_ideas / key_points / emotion；**幂等**（同载荷连跑两次结果一字不差）；不新建、不删除节点、**不碰章节正文**；角色名只进预览、不写角色 ID 字段（角色库匹配另刀）；空载荷或全部对不上**显式报错**，不静默成功 |

**降级诚实**：无模型 / 解析失败逐级回落规则兜底，原因写进 `warnings` 且 `aiUsed=false`；
单批失败只影响该批，不阻断整单（用户仍拿得到可用的兜底结构）。

### ② 前端（CreatePage 创作间 rail）

- 新按钮「AI 反推大纲」：一次点击 → 反推（长书约 1-3 分钟）→ 确认弹窗（显示推断题材 / 叙事视角 / 目标字数 + 前两条告警 + **明确「不覆盖正文、可重复执行」**）→ 应用 → 刷新大纲 + rail 消息「已应用 N 章大纲」。
- `bridge/novel.ts` 增 `OutlineReconstructPreview` / `OutlineReconstructItem` 类型（camelCase 形状锁）。

### ③ 接线面

`NovelB` 门面 +2（648）；`bindingNames.ts` 同步（drift OK）；`spaceBindings` 两方法归 `play`（数量锁 468→470）；
`wailsjs` 用 `wails generate module` 再生。

## 测试

- Go **+2**：① 无模型时全规则兜底且逐章字段完整（章号/概要/情感/scenes/key_points 齐备，`aiUsed=false` + warnings 非空）；
  ② 幂等与作用域：只写命中章号、未命中章不动、同一载荷连跑两次**字节一致**、空载荷与全不匹配**显式报错**。
- 前端：`spaceBindings` 4 例（锁 470）、`CreatePage` 9 例。

## 门禁

- `scripts/ci.ps1` **全绿**（exit 0）：Go `build`/`vet`/`test ./...` 全量通过；前端 `lint` 0 error（5 条既有 warning）、
  `tsc -b && vite build` 成功、vitest **345 文件 2994 例全绿**；E 系列回归守卫 OK；仓库卫生守卫 OK。
- `check-bindings-drift` OK@**648**（本刀 +2）；`check-version-drift` 三处一致 4.281.0。
- **坑复训**：`go run ./scripts/gen_bindings` 会把 `bindings_cost.go` / `bindings_office.go` 的**单参化改回变参**
  （E27 变参禁令当场红）——按既有纪律「再生后必须 git diff 全部门面文件」处置：`git checkout HEAD --` 还原该两文件后 E27 复绿。

## 产物

- `build\bin\gaea.exe` **49,455,616 B**（`wails build -s -ldflags "-s -w" -trimpath`）；
  SHA256 = `7FB09E36E6D1CB490DD2EFA486A2924BA735CAC579955F2ACB1A4628A882A693`；
  冒烟 `/api/health` **200** `{"status":"ok"}`；桌面副本同哈希。
- 归档：`releases/gaea-v4.281.0.exe` + `releases/SHA256SUMS-v4.281.0.txt`。

## 未做（下刀）

- **导入向导的预览 UI**（逐章核对 / 单章编辑后再应用）——本刀刻意取「一次反推 + 确认应用」的最短闭环；
- 幂等落库的**任务态**（`tasks` 表 `KindBookImport`，长书后台化 + 进度回传）；
- 角色名 → 角色库 ID 匹配（把 `characters` 真正接进大纲的角色字段）；
- tail 模式（只导入末尾 N 章）的导出入口。
