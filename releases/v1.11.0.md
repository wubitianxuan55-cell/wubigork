# gaea v1.11.0「界面体验深化 · 全站重设计」

> 发布：2026-08-02 · 构建 37,676,544 字节 · tag v1.11.0
>
> 设置中心外观细化升华（实时预览/三态显示/字体/密度/动效/强调色）+ 聊天 Markdown 消息体验 + 轻语面板 UI 重设计（角色状态头/气泡/情绪回复）+ 虚拟助手面板与角色卡详情重设计 + 轻语测试深化（21.8%）+ P3 archiveExporter 记忆归档导出。

## 本轮提交（10 个，c2a0aa7 → c93563c）

- `c2a0aa7` + `599fd8d`：轻语测试深化（33 测试，16.8%→21.8%，修 2 真实 bug）+ archiveExporter 归档导出 + 三模块 TOP 优化（模型三态徽章/设置全局搜索/即时生效统一标注）
- `80104e9`：外观设置细化升华（实时预览区 + hover 联动 + 三态显示模式含跟随系统）
- `64fff83` + `4fe71eb`：外观扩展非颜色维度（字体/密度/动效/强调色）+ effTokens useMemo 优化
- `0f139a0`：聊天板块消息体验升级（Markdown 渲染/分组/hover 操作栏/重新生成）
- `91547f9` + `1c1c746`：轻语面板 UI 重设计（角色状态头/消息气泡/等待期修复/情绪回复）
- `90fa689`：虚拟助手管理中心按钮卡片化
- `c93563c`：虚拟助手面板 + 角色卡详情重设计（粒子球/标签 chips/五维雷达并排）

## 修复的 bug

- paced_stream 死锁：流结束发完末段不 finishBubble → MarkDone 等待循环死锁（中文无断点文本必现）
- FirstDisplayUnitLen rune/字节偏移错位：中文句号处切乱码
- 轻语等待期空白+光标（streamText 从未 set）→ typing dots

## 构建与验证

- `wails build -clean` 成功（15.3s）；首次失败因 `tsc -b` 严格构建 3 处错误（CompanionAvatar 命名导出 / PaletteOutlined 图标名 / onMouseEnterCapture 不存在），已修复
- 全流程验证：go vet clean + go build ./... 全过 + go test whisper/app 全绿 + frontend tsc -b 0 错误

## 发布副本

- `releases/gaea-v1.11.0.exe`（本地，不入 git）
- 构建产物 `build/bin/gaea.exe`
