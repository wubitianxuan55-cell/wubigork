# v4.202.0 — 上下文页按会话读取：时间线/节点详情跟 UI 会话（2026-09-10）

> v4.181 欠账收刀。轨迹/Agent 网络已按 UI 会话读，上下文页时间线与节点详情
> 仍恒读内核 `ga.ctrl` 单例——切历史会话后看到的是内核会话的固定内容。

## 缺口

`ContextView` 已接 `sessionPath` 订 Agent 网络，时间线却走无参 `GaeaContextView()`，
节点详情走无参 `GaeaContextNodeDetail(seq)`。与 v4.181「UI 会话 ≠ 内核会话」
同一根因家族。

## 落地

- **Go**：`GaeaContextView(sessionPath ...string)` / `GaeaContextNodeDetail(seq, sessionPath ...string)`
  走 `resolveGaeaSessionPath`（显式路径优先，缺省回落内核，空串=缺省）；
  窗口大小仍用 `gaeaContextWindow()`（属内核配置与会话无关）。
- **绑定名 608 零变更**（变参，旧调用兼容）。OfficeB 门面透传。
- **前端**：`app.ContextView([path])`；inspector `useNodeDetails` 带路径，
  切会话清详情缓存（宁空勿错）。

## 测试

`TestGaeaContextViewAndNodeDetailExplicitSessionPath`：内核 nil 缺省空快照 /
NodeDetail 报会话未就绪；显式路径折叠出内容；空串回落缺省。
vitest：ContextView 带 sessionPath 调用 `[path]`。

## 门禁

Go 定向绿 / vitest 318 文件 2782 例 / tsc 0 / eslint 0 / 绑定名 608 零变更 /
版本三处 sync 4.202.0。
