# P2 重复代码消除 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 `internal/util` 统一收纳 12 处重复函数，删除死代码 `internal/xai/`。

**Architecture:** 新建 `internal/util/util.go` 提供 `ExtractJSON`/`Truncate`/`Max`/`Min` 四个导出函数，7 个调用方删除本地定义改为 import util 调用。Task 1 创建 util 包（前后无依赖），Task 2-4 并行替换 7 个调用方，Task 5 删除 xai 死代码。

**Tech Stack:** Go 1.26

## Global Constraints

- 函数体一字不改，仅移动位置和统一签名
- 所有调用方必须通过 `wails build`
- 每次 commit 独立可编译
- 不新建 util 以外的文件

**Spec:** `docs/superpowers/specs/2026-06-19-p2-dedup-design.md`

### Task 1: 新建 `internal/util/util.go`

**Files:**
- Create: `internal/util/util.go`

**Interfaces:**
- Produces: `util.ExtractJSON(s string) string`, `util.Truncate(s string, maxLen int) string`, `util.Max(a, b int) int`, `util.Min(a, b int) int`

- [ ] **Step 1: 创建 util.go**

写入 `internal/util/util.go`：

```go
package util

// ExtractJSON 从 AI 回复中提取第一个完整 JSON 对象
func ExtractJSON(s string) string {
	start, end := -1, -1
	for i, ch := range s {
		if ch == '{' && start == -1 {
			start = i
		}
		if ch == '}' {
			end = i
		}
	}
	if start >= 0 && end > start {
		return s[start : end+1]
	}
	return s
}

// Truncate 按 rune 截断字符串
func Truncate(s string, maxLen int) string {
	runes := []rune(s)
	if len(runes) <= maxLen {
		return s
	}
	return string(runes[:maxLen]) + "..."
}

// Max 返回两个 int 中较大值
func Max(a, b int) int {
	if a > b {
		return a
	}
	return b
}

// Min 返回两个 int 中较小值
func Min(a, b int) int {
	if a < b {
		return a
	}
	return b
}
```

- [ ] **Step 2: 编译验证**

Run: `cd /d/AI/wubigork && go build ./internal/util/`
Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add internal/util/util.go
git commit -m "feat: 新建 internal/util 包，收纳 ExtractJSON/Truncate/Max/Min"
```

### Task 2: 迁移 chapter + analysis 调用 util

**Files:**
- Modify: `internal/chapter/chapter.go`
- Modify: `internal/analysis/analysis.go`

**Interfaces:**
- Consumes: `util.ExtractJSON`, `util.Truncate`, `util.Max` (来自 Task 1)
- Produces: chapter/analysis 无本地 extractJSON/truncate/max 定义

- [ ] **Step 1: 替换 chapter.go**

`internal/chapter/chapter.go` 三处替换：

1. 删除本地 `max` 函数（line 279-281）
2. 删除本地 `truncate` 函数（line 284-290）
3. 删除本地 `extractJSON` 函数（line 292-302）
4. 添加 import：`"github.com/wubigork/wubigork/internal/util"`
5. `max(...)` → `util.Max(...)` （查找所有调用点替换）
6. `truncate(...)` → `util.Truncate(...)`
7. `extractJSON(...)` → `util.ExtractJSON(...)`

具体替换点（用 grep 验证后精确替换）：
- `max(targetWords*3, 4096)` → `util.Max(targetWords*3, 4096)` (line ~170)
- `truncate(prevChapter, 2000)` → `util.Truncate(prevChapter, 2000)` (line ~84)
- `truncate(projCtx.Worldview, 1000)` → `util.Truncate(projCtx.Worldview, 1000)` (line ~105)
- `extractJSON(reply)` → `util.ExtractJSON(reply)` (line ~115)

- [ ] **Step 2: 替换 analysis.go**

`internal/analysis/analysis.go` 两处替换：

1. 删除本地 `truncate` 函数（line 183-189）
2. 删除本地 `extractJSON` 函数（line 191-202）
3. 添加 util import
4. 所有 `truncate(` → `util.Truncate(`
5. 所有 `extractJSON(` → `util.ExtractJSON(`

- [ ] **Step 3: 构建验证**

Run: `cd /d/AI/wubigork && wails build`
Expected: 编译成功

- [ ] **Step 4: Commit**

```bash
git add internal/chapter/chapter.go internal/analysis/analysis.go
git commit -m "refactor: chapter/analysis 改用 util.ExtractJSON/Truncate/Max"
```

### Task 3: 迁移 character + worldview + outline 调用 util

**Files:**
- Modify: `internal/character/character.go`
- Modify: `internal/worldview/worldview.go`
- Modify: `internal/outline/outline.go`

**Interfaces:**
- Consumes: `util.ExtractJSON`, `util.Min` (来自 Task 1)

- [ ] **Step 1: 替换 character.go**

1. 删除本地 `extractJSON` 函数（line 430-441）
2. 删除本地 `min` 函数（line 447-452）
3. 添加 util import
4. 所有 `extractJSON(` → `util.ExtractJSON(`
5. 所有 `min(` → `util.Min(`

- [ ] **Step 2: 替换 worldview.go**

1. 删除本地 `extractJSON` 函数（line 442-453）
2. 删除本地 `min` 函数（line 254-259）
3. 添加 util import
4. 所有 `extractJSON(` → `util.ExtractJSON(`
5. 所有 `min(` → `util.Min(`

- [ ] **Step 3: 替换 outline.go**

1. 删除本地 `extractJSON` 函数（line 379-390）— 注意：Task 1C 修改了行号，当前实际行号以代码为准
2. 添加 util import
3. 所有 `extractJSON(` → `util.ExtractJSON(`

- [ ] **Step 4: 构建验证**

Run: `cd /d/AI/wubigork && wails build`
Expected: 编译成功

- [ ] **Step 5: Commit**

```bash
git add internal/character/character.go internal/worldview/worldview.go internal/outline/outline.go
git commit -m "refactor: character/worldview/outline 改用 util.ExtractJSON/Min"
```

### Task 4: 迁移 app + ai/context 调用 util

**Files:**
- Modify: `internal/app/app.go`
- Modify: `internal/ai/context.go`

**Interfaces:**
- Consumes: `util.Max`, `util.Truncate` (来自 Task 1)

- [ ] **Step 1: 替换 app.go**

1. 删除本地 `max` 函数（当前在文件末尾 ~line 823）
2. 添加 util import
3. 所有 `max(` → `util.Max(`

- [ ] **Step 2: 替换 context.go**

1. 删除本地 `truncate` 函数（line 182-188）
2. 添加 util import
3. 所有 `truncate(` → `util.Truncate(`

- [ ] **Step 3: 构建验证**

Run: `cd /d/AI/wubigork && wails build`
Expected: 编译成功

- [ ] **Step 4: Commit**

```bash
git add internal/app/app.go internal/ai/context.go
git commit -m "refactor: app/ai 改用 util.Max/Truncate"
```

### Task 5: 删除 `internal/xai/` 死代码

**Files:**
- Delete: `internal/xai/client.go`
- Delete: `internal/xai/types.go`

- [ ] **Step 1: 删除**

```bash
rm -rf internal/xai/
```

- [ ] **Step 2: 构建验证**

Run: `cd /d/AI/wubigork && wails build`
Expected: 编译成功

- [ ] **Step 3: Commit**

```bash
git add internal/xai/
git commit -m "refactor: 删除死代码 internal/xai/（已被 internal/ai/ 替代）"
```

---

## 执行顺序

Task 1 必须先执行（创建 util 包），Task 2-4 可在 Task 1 后任意顺序执行，Task 5 最后（独立操作）。

## 验证检查清单

- [ ] `wails build` 在每个 Task 后通过
- [ ] 所有 `extractJSON` 调用方行为不变（函数体未改）
- [ ] 无 import cycle（util 不依赖任何 internal 包）
- [ ] `grep -r "func extractJSON\|func truncate\|^func max\|^func min" internal/` 仅在 util.go 中有定义
