# 方案编写板块重设计 P2 — 招标解析管线 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将招标文件解析从「无来源的 JSON 提取」升级为「结构化字段 + 原文来源定位 + OCR 可插拔接口」的解析管线。

**Architecture:** 新增文档分页文本提取（PDF/DOCX/TXT）→ LLM 逐文件/分块提取字段与原文摘录（quote）→ 后端用确定性定位器把 quote 映射到文件 Markdown 偏移与页码 → 结果同时写入 `parse_results` 表与 `BidSummary`（兼容现有大纲/检查流程）→ 前端解析 Tab 改为结构化字段卡 + 原文预览抽屉。OCR 仅定义 `OCRProvider` 接口（P2 不接具体引擎）。

**Tech Stack:** Go 1.26 + modernc.org/sqlite + React/TS/AntD + Wails v2

---

## 任务总览

| 任务 | 内容 | 提交 |
|---|---|---|
| 1 | 文档分页文本提取（doctext.go） | `feat(office): 文档分页文本提取` |
| 2 | office.db SchemaV2（parse_results）+ Store API | `feat(office): office.db SchemaV2 parse_results` |
| 3 | 来源定位器 LocateQuote/LocatePage | `feat(office): 来源定位器` |
| 4 | 结构化解析模型 + ParseBidFile v2 | `feat(office): 招标解析管线 v2` |
| 5 | 绑定序列化兼容（btm/bsf JSON 往返） | `feat(office): 解析绑定与序列化兼容` |
| 6 | 前端解析卡片 + 原文定位抽屉 | `feat(office): 招标解析结构化卡片+原文定位` |
| 7 | 全量验证与 v1.15.0 发布 | `release: v1.15.0 招标解析管线` |

---

## Task 1: 文档分页文本提取

**Files:**
- Create: `internal/office/proposal/doctext.go`
- Create: `internal/office/proposal/doctext_test.go`
- Create: `internal/office/proposal/testdata/empty.pdf`（无文字单页 PDF 夹具）

- [ ] **Step 1: 写失败测试**

创建 `internal/office/proposal/doctext_test.go`：

```go
package proposal

import (
	"os"
	"path/filepath"
	"testing"
)

func TestExtractPageText_TXT(t *testing.T) {
	dir := t.TempDir()
	p := filepath.Join(dir, "a.txt")
	if err := os.WriteFile(p, []byte("第一行内容\n第二行内容"), 0644); err != nil {
		t.Fatal(err)
	}
	pages, err := ExtractPageText(p)
	if err != nil {
		t.Fatalf("ExtractPageText: %v", err)
	}
	if len(pages) != 1 || pages[0].Page != 0 || pages[0].Text != "第一行内容\n第二行内容" {
		t.Fatalf("TXT pages 异常: %+v", pages)
	}
}

func TestExtractPageText_DOCX(t *testing.T) {
	dir := t.TempDir()
	p := filepath.Join(dir, "b.docx")
	doc := newTestDocx(t, []string{"项目概况段落", "评分标准段落"})
	if err := doc.SaveToFile(p); err != nil {
		t.Fatal(err)
	}
	pages, err := ExtractPageText(p)
	if err != nil {
		t.Fatalf("ExtractPageText: %v", err)
	}
	if len(pages) != 1 || pages[0].Page != 0 {
		t.Fatalf("DOCX pages 异常: %+v", pages)
	}
	if !containsAny(pages[0].Text, "项目概况段落", "评分标准段落") {
		t.Errorf("DOCX 文本缺失: %q", pages[0].Text)
	}
}

func TestExtractPageText_PDF(t *testing.T) {
	pages, err := ExtractPageText("testdata/empty.pdf")
	if err != nil {
		t.Fatalf("ExtractPageText: %v", err)
	}
	if len(pages) != 1 || pages[0].Page != 1 {
		t.Fatalf("PDF pages 异常: %+v", pages)
	}
}
```

`newTestDocx` 测试辅助函数：

```go
func newTestDocx(t *testing.T, paras []string) *document.Document {
	t.Helper()
	doc := document.New()
	for _, s := range paras {
		doc.AddParagraph().AddRun().AddText(s)
	}
	return doc
}
```

`containsAny` 辅助函数：

```go
func containsAny(s string, subs ...string) bool {
	for _, sub := range subs {
		if strings.Contains(s, sub) {
			return true
		}
	}
	return false
}
```

测试文件 import：

```go
import (
	"os"
	"path/filepath"
	"strings"
	"testing"

	"github.com/carmel/gooxml/document"
)
```

创建 `internal/office/proposal/testdata/empty.pdf`（最小单页无文字 PDF）：

```pdf
%PDF-1.4
1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj
2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj
3 0 obj<</Type/Page/Parent 2 0 R/MediaBox[0 0 612 792]>>endobj
xref
0 4
0000000000 65535 f 
0000000009 00000 n 
0000000052 00000 n 
0000000101 00000 n 
trailer<</Size 4/Root 1 0 R>>
startxref
160
%%EOF
```

（`testdata` 目录下若有其他夹具，`git add testdata/empty.pdf` 单独提交。）

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/office/proposal/ -run TestExtractPageText -v`
Expected: FAIL（编译错误：`ExtractPageText` 未定义）。

- [ ] **Step 3: 实现 doctext.go**

创建 `internal/office/proposal/doctext.go`：

```go
// Package proposal — 文档分页文本提取（供来源定位使用）
package proposal

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/carmel/gooxml/document"
	"github.com/ledongthuc/pdf"
)

// PageText 单页文本
type PageText struct {
	Page int    `json:"page"`
	Text string `json:"text"`
}

// ExtractPageText 提取文件分页文本。
// PDF 按页返回真实页码；DOCX/TXT 无可靠分页信息，统一返回 Page=0。
func ExtractPageText(filePath string) ([]PageText, error) {
	ext := strings.ToLower(filepath.Ext(filePath))
	switch ext {
	case ".pdf":
		return extractPdfPages(filePath)
	case ".docx":
		text, err := extractDocxText(filePath)
		if err != nil {
			return nil, err
		}
		return []PageText{{Page: 0, Text: text}}, nil
	default:
		data, err := os.ReadFile(filePath)
		if err != nil {
			return nil, err
		}
		return []PageText{{Page: 0, Text: string(data)}}, nil
	}
}

func extractPdfPages(filePath string) ([]PageText, error) {
	f, r, err := pdf.Open(filePath)
	if err != nil {
		return nil, fmt.Errorf("打开 PDF 失败: %w", err)
	}
	defer f.Close()
	var out []PageText
	for i := 1; i <= r.NumPage(); i++ {
		page := r.Page(i)
		if page.V.IsNull() {
			continue
		}
		text, err := page.GetPlainText(nil)
		if err != nil {
			continue
		}
		out = append(out, PageText{Page: i, Text: text})
	}
	return out, nil
}

func extractDocxText(filePath string) (string, error) {
	doc, err := document.Open(filePath)
	if err != nil {
		return "", fmt.Errorf("打开 DOCX 失败: %w", err)
	}
	var sb strings.Builder
	for _, para := range doc.Paragraphs() {
		for _, run := range para.Runs() {
			sb.WriteString(run.Text())
		}
		sb.WriteString("\n")
	}
	return sb.String(), nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `go test ./internal/office/proposal/ -run TestExtractPageText -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/office/proposal/doctext.go internal/office/proposal/doctext_test.go internal/office/proposal/testdata/empty.pdf
git commit -m "feat(office): 文档分页文本提取"
```

---

## Task 2: office.db SchemaV2（parse_results）+ Store API

**Files:**
- Modify: `internal/office/db/schema.go`（新增 SchemaV2）
- Modify: `internal/office/db/database.go`（migrations 追加）
- Modify: `internal/office/db/database_test.go`（表清单 + user_version=2）
- Modify: `internal/office/proposal/types.go`（ParseResultItem、FileDoc.FileID）
- Modify: `internal/office/proposal/store.go`（SaveParseResults/ListParseResults、AddFile 返回 ID）
- Modify: `internal/office/proposal/store_test.go`、`internal/office/proposal/upload.go`
- Test: `internal/office/proposal/store_test.go`（新增解析结果 CRUD 测试）

- [ ] **Step 1: 先改数据库测试，确认失败**

在 `internal/office/db/database_test.go` 的表清单中加入 `"parse_results"`，并把版本断言改为 2：

```go
	for _, tbl := range []string{"projects", "proposals", "sections", "files", "versions", "templates", "parse_results", "schema_meta"} {
```

Run: `go test ./internal/office/db/... -v`
Expected: FAIL（`parse_results` 表不存在 / user_version 为 1）。

- [ ] **Step 2: 新增 SchemaV2 并注册迁移**

在 `internal/office/db/schema.go` 末尾追加：

```go
// SchemaV2 招标解析结果表：结构化字段 + 来源定位。
const SchemaV2 = `
CREATE TABLE IF NOT EXISTS parse_results (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  proposal_id TEXT NOT NULL REFERENCES proposals(id) ON DELETE CASCADE,
  file_id     TEXT NOT NULL DEFAULT '',
  field       TEXT NOT NULL,
  value       TEXT NOT NULL DEFAULT '',
  page        INTEGER NOT NULL DEFAULT 0,
  start       INTEGER NOT NULL DEFAULT 0,
  end         INTEGER NOT NULL DEFAULT 0,
  snippet     TEXT NOT NULL DEFAULT '',
  confidence  REAL NOT NULL DEFAULT 0,
  created_at  TEXT NOT NULL DEFAULT ''
);
CREATE INDEX IF NOT EXISTS idx_parse_results_proposal ON parse_results(proposal_id);
`
```

在 `internal/office/db/database.go` 修改迁移列表：

```go
var migrations = []string{SchemaV1, SchemaV2}
```

Run: `go test ./internal/office/db/... -v`
Expected: PASS。

- [ ] **Step 3: 写 Store 解析结果测试（RED）**

在 `internal/office/proposal/store_test.go` 追加：

```go
func TestParseResultsCRUD(t *testing.T) {
	s := newTestStore(t)
	proj, _ := s.EnsureDefaultProject()
	p, err := s.Create("方案", "blank", "", "其他", proj.ID, nil)
	if err != nil {
		t.Fatal(err)
	}
	items := []ParseResultItem{
		{FileID: "f1", Field: "duration", Value: "90 日历天", Page: 3, Start: 100, End: 120, Snippet: "工期 90 日历天", Confidence: 1},
		{FileID: "f1", Field: "techScoring", Value: "施工方案 20 分", Page: 5, Start: 300, End: 330, Snippet: "施工方案（20 分）", Confidence: 0.8},
	}
	if err := s.SaveParseResults(p.ID, items); err != nil {
		t.Fatalf("SaveParseResults: %v", err)
	}
	got, err := s.ListParseResults(p.ID)
	if err != nil {
		t.Fatalf("ListParseResults: %v", err)
	}
	if len(got) != 2 || got[0].Field != "duration" || got[0].Page != 3 {
		t.Fatalf("解析结果异常: %+v", got)
	}
	// 幂等重存：先删后插
	if err := s.SaveParseResults(p.ID, []ParseResultItem{{FileID: "f1", Field: "overview", Value: "项目概况", Snippet: "本项目为…"}}); err != nil {
		t.Fatal(err)
	}
	got2, _ := s.ListParseResults(p.ID)
	if len(got2) != 1 || got2[0].Field != "overview" {
		t.Fatalf("重存后异常: %+v", got2)
	}
}
```

Run: `go test ./internal/office/proposal/ -run TestParseResultsCRUD -v`
Expected: FAIL（编译错误：`ParseResultItem`/`SaveParseResults`/`ListParseResults` 未定义）。

- [ ] **Step 4: 扩展 types.go**

在 `internal/office/proposal/types.go` 的 `FileDoc` 中增加字段：

```go
type FileDoc struct {
	FileID   string `json:"fileId"`   // files 表 ID（旧数据为空时用 file-<index>）
	Name     string `json:"name"`
	Path     string `json:"path"`
	Markdown string `json:"markdown"`
	Size     int    `json:"size"`
}
```

文件末尾追加：

```go
// ParseResultItem 解析结果行（parse_results 表）
type ParseResultItem struct {
	FileID     string  `json:"fileId"`
	Field      string  `json:"field"`
	Value      string  `json:"value"`
	Page       int     `json:"page"`
	Start      int     `json:"start"`
	End        int     `json:"end"`
	Snippet    string  `json:"snippet"`
	Confidence float64 `json:"confidence"`
}
```

- [ ] **Step 5: 实现 Store API**

在 `internal/office/proposal/store.go` 中把 `AddFile` 改为返回 ID：

```go
// AddFile 登记附件，返回文件行 ID
func (s *Store) AddFile(proposalID, kind, name, path string, size int) (string, error) {
	if err := s.errIfUnavailable(); err != nil {
		return "", err
	}
	id := uuid.New().String()
	_, err := s.db.Exec(
		"INSERT INTO files(id, proposal_id, kind, name, path, size, created_at) VALUES(?,?,?,?,?,?,?)",
		id, proposalID, kind, name, path, size, now(),
	)
	if err != nil {
		return "", err
	}
	return id, nil
}
```

在 `AddFile` 之后追加：

```go
// SaveParseResults 全量替换方案解析结果（先删后插）
func (s *Store) SaveParseResults(proposalID string, items []ParseResultItem) error {
	if err := s.errIfUnavailable(); err != nil {
		return err
	}
	tx, err := s.db.Begin()
	if err != nil {
		return err
	}
	defer tx.Rollback()
	if _, err := tx.Exec("DELETE FROM parse_results WHERE proposal_id = ?", proposalID); err != nil {
		return err
	}
	for _, it := range items {
		if _, err := tx.Exec(
			`INSERT INTO parse_results(proposal_id, file_id, field, value, page, start, end, snippet, confidence, created_at)
			 VALUES(?,?,?,?,?,?,?,?,?,?)`,
			proposalID, it.FileID, it.Field, it.Value, it.Page, it.Start, it.End, it.Snippet, it.Confidence, now(),
		); err != nil {
			return err
		}
	}
	return tx.Commit()
}

// ListParseResults 列出方案解析结果
func (s *Store) ListParseResults(proposalID string) ([]ParseResultItem, error) {
	if err := s.errIfUnavailable(); err != nil {
		return nil, err
	}
	rows, err := s.db.Query(
		`SELECT file_id, field, value, page, start, end, snippet, confidence FROM parse_results WHERE proposal_id = ? ORDER BY id`,
		proposalID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []ParseResultItem
	for rows.Next() {
		var it ParseResultItem
		if err := rows.Scan(&it.FileID, &it.Field, &it.Value, &it.Page, &it.Start, &it.End, &it.Snippet, &it.Confidence); err != nil {
			return nil, err
		}
		out = append(out, it)
	}
	return out, rows.Err()
}
```

- [ ] **Step 6: 修改调用方**

`internal/office/proposal/upload.go` 的 `SaveUploadedFile` 改为接收返回的 ID 并写入 FileDoc：

```go
	fileID, err := s.store.AddFile(proposalID, "attachment", filepath.Base(path), path, len(data))
	if err != nil {
		return nil, err
	}
	p.BidSummary.RawFiles = append(p.BidSummary.RawFiles, FileDoc{
		FileID: fileID, Name: filepath.Base(path), Path: path, Size: len(data),
	})
```

`internal/office/proposal/store_test.go` 中 `TestStoreAddFile` 的调用改为：

```go
	if _, err := s.AddFile(p.ID, "attachment", "a.pdf", filepath.Join(s.FilesDir(), p.ID, "a.pdf"), 10); err != nil {
```

Run: `go test ./internal/office/proposal/... ./internal/office/db/...`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add internal/office/db internal/office/proposal/types.go internal/office/proposal/store.go internal/office/proposal/store_test.go internal/office/proposal/upload.go
git commit -m "feat(office): office.db SchemaV2 parse_results"
```

---

## Task 3: 来源定位器

**Files:**
- Create: `internal/office/proposal/locate.go`
- Create: `internal/office/proposal/locate_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/office/proposal/locate_test.go`：

```go
package proposal

import "testing"

func TestLocateQuote_Exact(t *testing.T) {
	md := "第一章 项目概况\n本项目位于华东地区。"
	start, end, ok := LocateQuote(md, "本项目位于华东地区")
	if !ok {
		t.Fatal("exact 未命中")
	}
	if md[start:end] != "本项目位于华东地区" {
		t.Errorf("偏移错误: %d-%d => %q", start, end, md[start:end])
	}
}

func TestLocateQuote_WhitespaceFlexible(t *testing.T) {
	md := "施工方案（20 分）\n要求：\n- 完整\n- 合理"
	start, end, ok := LocateQuote(md, "施工方案(20分)")
	if !ok {
		t.Fatal("归一化未命中")
	}
	if md[start:end] == "" {
		t.Error("空命中")
	}
}

func TestLocateQuote_NotFound(t *testing.T) {
	if _, _, ok := LocateQuote("完全无关的内容", "废标条款"); ok {
		t.Error("不应命中")
	}
}

func TestLocatePage(t *testing.T) {
	pages := []PageText{
		{Page: 1, Text: "第一章 项目概况"},
		{Page: 2, Text: "评分标准：施工方案 20 分"},
		{Page: 3, Text: "废标条款"},
	}
	if got := LocatePage(pages, "施工方案"); got != 2 {
		t.Errorf("LocatePage = %d, want 2", got)
	}
	if got := LocatePage(pages, "不存在的词"); got != 0 {
		t.Errorf("LocatePage 未命中应返回 0，实际 %d", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/office/proposal/ -run 'TestLocateQuote|TestLocatePage' -v`
Expected: FAIL（编译错误：`LocateQuote`/`LocatePage` 未定义）。

- [ ] **Step 3: 实现 locate.go**

创建 `internal/office/proposal/locate.go`：

```go
// Package proposal — 来源定位器：把 AI 摘录（quote）映射到原文偏移/页码
package proposal

import "strings"

type token struct {
	text  string
	start int
}

// tokenize 按空白切分并记录每个 token 在原文中的起始偏移
func tokenize(s string) []token {
	var out []token
	start := -1
	for i, r := range s {
		if r == ' ' || r == '\t' || r == '\n' || r == '\r' {
			if start >= 0 {
				out = append(out, token{text: s[start:i], start: start})
				start = -1
			}
			continue
		}
		if start < 0 {
			start = i
		}
	}
	if start >= 0 {
		out = append(out, token{text: s[start:], start: start})
	}
	return out
}

// LocateQuote 在 markdown 中定位 quote。
// 先精确匹配；失败后按空白归一化做 token 序列匹配（忽略空白差异）。
// 返回原文的 [start, end) 字节区间。
func LocateQuote(markdown, quote string) (int, int, bool) {
	if quote == "" {
		return 0, 0, false
	}
	if idx := strings.Index(markdown, quote); idx >= 0 {
		return idx, idx + len(quote), true
	}
	mdToks := tokenize(markdown)
	qToks := tokenize(quote)
	if len(qToks) == 0 {
		return 0, 0, false
	}
	for i := 0; i+len(qToks) <= len(mdToks); i++ {
		match := true
		for j := range qToks {
			if !strings.EqualFold(mdToks[i+j].text, qToks[j].text) {
				match = false
				break
			}
		}
		if match {
			last := i + len(qToks) - 1
			start := mdToks[i].start
			end := mdToks[last].start + len(mdToks[last].text)
			return start, end, true
		}
	}
	return 0, 0, false
}

// LocatePage 返回包含 quote 的页码（按页文本定位；未命中返回 0）
func LocatePage(pages []PageText, quote string) int {
	if quote == "" {
		return 0
	}
	for _, p := range pages {
		if p.Text == "" {
			continue
		}
		if _, _, ok := LocateQuote(p.Text, quote); ok {
			return p.Page
		}
	}
	return 0
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `go test ./internal/office/proposal/ -run 'TestLocateQuote|TestLocatePage' -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/office/proposal/locate.go internal/office/proposal/locate_test.go
git commit -m "feat(office): 来源定位器"
```

---

## Task 4: 结构化解析模型 + ParseBidFile v2

**Files:**
- Modify: `internal/office/proposal/types.go`（SourceRef/BidItem/BidSummary 扩展/ScoringItem.Sources）
- Create: `internal/office/proposal/parse.go`（新解析管线）
- Modify: `internal/office/proposal/service.go`（ParseBidFile 委托新管线）
- Modify: `internal/office/proposal/service_test.go`（解析测试）

- [ ] **Step 1: 扩展 types.go**

`ScoringItem` 增加来源：

```go
type ScoringItem struct {
	Name        string      `json:"name"`
	MaxScore    string      `json:"maxScore"`
	Requirement string      `json:"requirement"`
	Sources     []SourceRef `json:"sources,omitempty"`
}
```

`BidSummary` 增加字段：

```go
type BidSummary struct {
	TechScoring     []ScoringItem     `json:"techScoring"`
	KeyRequirements []string          `json:"keyRequirements"`
	Duration        string            `json:"duration"`
	RedLines        []string          `json:"redLines"`
	Overview        string            `json:"overview"`
	Extra           map[string]string `json:"extra"`
	RawMarkdown     string            `json:"rawMarkdown"`
	RawFiles        []FileDoc         `json:"rawFiles"`
	RawText         string            `json:"rawText"`
	Qualification   []BidItem         `json:"qualification,omitempty"` // 资质要求
	Format          []BidItem         `json:"format,omitempty"`        // 格式要求
	DarkRules       []BidItem         `json:"darkRules,omitempty"`     // 暗标要求
	RedLineItems    []BidItem         `json:"redLineItems,omitempty"`  // 废标条款（带来源）
	OverviewSources []SourceRef       `json:"overviewSources,omitempty"`
	DurationSources []SourceRef       `json:"durationSources,omitempty"`
	ParseStatus     string            `json:"parseStatus,omitempty"` // none|done|partial
}
```

文件末尾追加：

```go
// SourceRef 来源引用（文件 + 页码 + Markdown 偏移 + 原文摘录）
type SourceRef struct {
	FileID     string  `json:"fileId"`
	FileName   string  `json:"fileName"`
	Page       int     `json:"page"`
	Start      int     `json:"start"`
	End        int     `json:"end"`
	Snippet    string  `json:"snippet"`
	Confidence float64 `json:"confidence"`
}

// BidItem 带来源的要求项（资质/格式/暗标/废标）
type BidItem struct {
	Name    string      `json:"name"`
	Content string      `json:"content"`
	Sources []SourceRef `json:"sources"`
}
```

- [ ] **Step 2: 写解析管线测试（RED）**

在 `internal/office/proposal/service_test.go` 追加：

```go
func TestParseBidFileV2_WithSources(t *testing.T) {
	ai := &mockAI{replies: map[string]string{
		"招标文件": `{
  "overview": "本项目为污染场地修复",
  "overviewQuote": "本项目为污染场地修复工程",
  "duration": "90 日历天",
  "durationQuote": "工期要求：90 日历天",
  "qualification": [{"name":"环保工程专业承包资质","content":"需具备环保工程专业承包三级及以上","quote":"环保工程专业承包三级及以上"}],
  "techScoring": [{"name":"施工方案","maxScore":"20","requirement":"方案完整合理","quote":"施工方案 20 分"}],
  "keyRequirements": ["项目经理须常驻现场"],
  "redLines": [{"name":"废标条款","content":"投标文件未按要求签字盖章","quote":"未按要求签字盖章"}],
  "format": [{"name":"装订要求","content":"A4 双面打印","quote":"A4 双面打印"}],
  "darkRules": [{"name":"暗标要求","content":"不得出现单位名称","quote":"不得出现单位名称"}]
}`,
	}}
	s := newServiceAt(t, t.TempDir(), ai)
	p, _ := s.Create("方案", "blank", "", "其他")
	// 注入一个已转换的招标文件
	fileID, err := s.store.AddFile(p.ID, "tender", "招标文件.pdf", "x.pdf", 100)
	if err != nil {
		t.Fatal(err)
	}
	p.BidSummary = &BidSummary{
		RawFiles: []FileDoc{{
			FileID: fileID, Name: "招标文件.pdf", Path: "x.pdf",
			Markdown: "一、项目概况\n本项目为污染场地修复工程。\n二、工期要求\n工期要求：90 日历天。\n三、资质要求\n需具备环保工程专业承包三级及以上资质。\n四、评分标准\n施工方案 20 分。\n五、其他\n投标文件未按要求签字盖章作废标处理；A4 双面打印；暗标不得出现单位名称。",
		}},
		RawMarkdown: "一、项目概况\n本项目为污染场地修复工程。",
	}
	if err := s.store.Update(p); err != nil {
		t.Fatal(err)
	}

	got, err := s.ParseBidFile(context.Background(), p.ID)
	if err != nil {
		t.Fatalf("ParseBidFile: %v", err)
	}
	if got.BidSummary.ParseStatus != "done" {
		t.Errorf("ParseStatus = %q", got.BidSummary.ParseStatus)
	}
	if len(got.BidSummary.Qualification) != 1 || len(got.BidSummary.Qualification[0].Sources) == 0 {
		t.Fatalf("资质来源缺失: %+v", got.BidSummary.Qualification)
	}
	src := got.BidSummary.Qualification[0].Sources[0]
	if src.Snippet == "" || src.Confidence <= 0 {
		t.Errorf("来源异常: %+v", src)
	}
	if got.BidSummary.TechScoring[0].Sources[0].Page != 0 {
		t.Errorf("页码异常: %+v", got.BidSummary.TechScoring[0].Sources)
	}
	rows, err := s.store.ListParseResults(p.ID)
	if err != nil || len(rows) == 0 {
		t.Fatalf("parse_results 未落库: %v %+v", err, rows)
	}
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `go test ./internal/office/proposal/ -run TestParseBidFileV2_WithSources -v`
Expected: FAIL（测试失败或编译错误：新字段/新函数未定义）。

- [ ] **Step 4: 实现 parse.go**

创建 `internal/office/proposal/parse.go`：

```go
// Package proposal — 招标解析管线 v2：逐文件提取字段 + 来源定位
package proposal

import (
	"context"
	"encoding/json"
	"fmt"
	"strings"
)

const parseChunkSize = 12000

// parseFileResult AI 单次返回的解析结果（含原文摘录 quote）
type parseFileResult struct {
	Overview        string    `json:"overview"`
	OverviewQuote   string    `json:"overviewQuote"`
	Duration        string    `json:"duration"`
	DurationQuote   string    `json:"durationQuote"`
	Qualification   []BidItem `json:"qualification"`
	TechScoring     []struct {
		Name        string `json:"name"`
		MaxScore    string `json:"maxScore"`
		Requirement string `json:"requirement"`
		Quote       string `json:"quote"`
	} `json:"techScoring"`
	KeyRequirements []string  `json:"keyRequirements"`
	RedLines        []BidItem `json:"redLines"`
	Format          []BidItem `json:"format"`
	DarkRules       []BidItem `json:"darkRules"`
}

const parseSystemPrompt = `你是一位专业的招投标专家。基于给定招标文件（Markdown 格式），提取所有对投标方案编写有影响的关键信息。

返回纯 JSON，格式：
{
  "overview": "项目概况（150 字以内）",
  "overviewQuote": "项目概况在原文中的一字不差摘录（尽量完整的一句）",
  "duration": "工期要求",
  "durationQuote": "工期要求原文摘录",
  "qualification": [{"name":"资质项名称","content":"具体资质要求","quote":"原文摘录"}],
  "techScoring": [{"name":"评分项","maxScore":"分值","requirement":"具体要求","quote":"原文摘录"}],
  "keyRequirements": ["核心要求"],
  "redLines": [{"name":"废标条款名称","content":"废标条款内容","quote":"原文摘录"}],
  "format": [{"name":"格式要求名称","content":"格式要求内容","quote":"原文摘录"}],
  "darkRules": [{"name":"暗标要求名称","content":"暗标要求内容","quote":"原文摘录"}]
}

要求：
- quote 必须是文档中出现的原文片段（可含标点，尽量短但足以定位）
- 不存在的类别填空数组或空字符串
- 不要遗漏影响投标的关键信息`

// ParseBidFileV2 执行完整解析管线：逐文件分块提取 → 来源定位 → 落库
func (s *Service) ParseBidFileV2(ctx context.Context, proposalID string) (*Proposal, error) {
	if s.ai == nil {
		return nil, fmt.Errorf("AI 客户端未初始化")
	}
	p, err := s.store.Get(proposalID)
	if err != nil {
		return nil, err
	}
	if p.BidSummary == nil || len(p.BidSummary.RawFiles) == 0 {
		return nil, fmt.Errorf("请先导入招标文件并转换 Markdown")
	}

	merged := parseFileResult{}
	var resultItems []ParseResultItem
	partial := false
	for i, f := range p.BidSummary.RawFiles {
		if f.Markdown == "" {
			continue
		}
		fileID := f.FileID
		if fileID == "" {
			fileID = fmt.Sprintf("file-%d", i)
		}
		pages, _ := ExtractPageText(f.Path)
		if len(pages) == 0 {
			pages = []PageText{{Page: 0, Text: f.Markdown}}
		}
		res, ok := s.parseFileChunks(ctx, f.Markdown)
		if !ok {
			partial = true
			continue
		}
		merged = mergeParseResults(merged, res)
		resultItems = append(resultItems, resolveParseItems(fileID, f.Name, pages, f.Markdown, res)...)
	}
	if len(resultItems) == 0 {
		return nil, fmt.Errorf("AI 解析失败，未提取到任何字段")
	}
	if err := s.store.SaveParseResults(proposalID, resultItems); err != nil {
		return nil, err
	}
	if p.BidSummary == nil {
		p.BidSummary = &BidSummary{}
	}
	applyParseResult(p.BidSummary, merged)
	if partial {
		p.BidSummary.ParseStatus = "partial"
	} else {
		p.BidSummary.ParseStatus = "done"
	}
	p.UpdatedAt = now()
	if err := s.store.Update(p); err != nil {
		return nil, err
	}
	return p, nil
}

// parseFileChunks 对单个文件分块调用 AI 并合并
func (s *Service) parseFileChunks(ctx context.Context, markdown string) (parseFileResult, bool) {
	runes := []rune(markdown)
	if len(runes) <= parseChunkSize {
		reply, err := s.ai.ChatSimpleStream(ctx, "", parseSystemPrompt, "请解析以下招标文件：\n\n"+markdown)
		if err != nil {
			return parseFileResult{}, false
		}
		return decodeParseResult(reply), true
	}
	merged := parseFileResult{}
	any := false
	for start := 0; start < len(runes); start += parseChunkSize {
		end := start + parseChunkSize
		if end > len(runes) {
			end = len(runes)
		}
		chunk := string(runes[start:end])
		reply, err := s.ai.ChatSimpleStream(ctx, "", parseSystemPrompt,
			fmt.Sprintf("请解析以下招标文件片段（第 %d-%d 字，共 %d 字）：\n\n%s", start+1, end, len(runes), chunk))
		if err != nil {
			continue
		}
		merged = mergeParseResults(merged, decodeParseResult(reply))
		any = true
	}
	return merged, any
}

func decodeParseResult(reply string) parseFileResult {
	reply = extractJSON(reply)
	var res parseFileResult
	if err := json.Unmarshal([]byte(reply), &res); err != nil {
		return parseFileResult{}
	}
	return res
}

func mergeParseResults(a, b parseFileResult) parseFileResult {
	if a.Overview == "" {
		a.Overview = b.Overview
		a.OverviewQuote = b.OverviewQuote
	}
	if a.Duration == "" {
		a.Duration = b.Duration
		a.DurationQuote = b.DurationQuote
	}
	a.Qualification = appendUniqueBidItems(a.Qualification, b.Qualification)
	a.RedLines = appendUniqueBidItems(a.RedLines, b.RedLines)
	a.Format = appendUniqueBidItems(a.Format, b.Format)
	a.DarkRules = appendUniqueBidItems(a.DarkRules, b.DarkRules)
	for _, sc := range b.TechScoring {
		if !containsScoring(a.TechScoring, sc.Name) {
			a.TechScoring = append(a.TechScoring, sc)
		}
	}
	for _, kr := range b.KeyRequirements {
		if !containsString(a.KeyRequirements, kr) {
			a.KeyRequirements = append(a.KeyRequirements, kr)
		}
	}
	return a
}

func appendUniqueBidItems(dst, src []BidItem) []BidItem {
	for _, it := range src {
		if it.Name == "" && it.Content == "" {
			continue
		}
		if !containsBidItem(dst, it.Name, it.Content) {
			dst = append(dst, it)
		}
	}
	return dst
}

func containsBidItem(items []BidItem, name, content string) bool {
	for _, it := range items {
		if it.Name == name && it.Content == content {
			return true
		}
	}
	return false
}

func containsScoring(items []struct {
	Name        string `json:"name"`
	MaxScore    string `json:"maxScore"`
	Requirement string `json:"requirement"`
	Quote       string `json:"quote"`
}, name string) bool {
	for _, it := range items {
		if it.Name == name {
			return true
		}
	}
	return false
}

func containsString(list []string, s string) bool {
	for _, v := range list {
		if v == s {
			return true
		}
	}
	return false
}

// resolveParseItems 把摘录定位到文件 Markdown 偏移与页码，生成落库行
func resolveParseItems(fileID, fileName string, pages []PageText, markdown string, res parseFileResult) []ParseResultItem {
	var out []ParseResultItem
	add := func(field, value, quote string) {
		item := ParseResultItem{FileID: fileID, Field: field, Value: value}
		if quote != "" {
			start, end, ok := LocateQuote(markdown, quote)
			item.Snippet = quote
			switch {
			case ok && markdown[start:end] == quote:
				item.Confidence = 1
			case ok:
				item.Confidence = 0.8
			default:
				item.Confidence = 0.3
			}
			item.Start, item.End = start, end
			item.Page = LocatePage(pages, quote)
		}
		out = append(out, item)
	}
	add("overview", res.Overview, res.OverviewQuote)
	add("duration", res.Duration, res.DurationQuote)
	for _, it := range res.Qualification {
		add("qualification", it.Name+"："+it.Content, quoteOf(it))
	}
	for _, it := range res.TechScoring {
		add("techScoring", it.Name+"（"+it.MaxScore+"分）："+it.Requirement, it.Quote)
	}
	for _, kr := range res.KeyRequirements {
		add("keyRequirements", kr, "")
	}
	for _, it := range res.RedLines {
		add("redLines", it.Name+"："+it.Content, quoteOf(it))
	}
	for _, it := range res.Format {
		add("format", it.Name+"："+it.Content, quoteOf(it))
	}
	for _, it := range res.DarkRules {
		add("darkRules", it.Name+"："+it.Content, quoteOf(it))
	}
	return out
}

func quoteOf(it BidItem) string { return it.Sources[0].Snippet }

// applyParseResult 把解析结果映射到 BidSummary（保持旧字段兼容）
func applyParseResult(bs *BidSummary, res parseFileResult) {
	bs.Overview = res.Overview
	bs.Duration = res.Duration
	bs.Qualification = res.Qualification
	bs.Format = res.Format
	bs.DarkRules = res.DarkRules
	bs.RedLineItems = res.RedLines
	bs.RedLines = nil
	for _, it := range res.RedLines {
		bs.RedLines = append(bs.RedLines, it.Content)
	}
	bs.KeyRequirements = res.KeyRequirements
	bs.TechScoring = nil
	for _, sc := range res.TechScoring {
		bs.TechScoring = append(bs.TechScoring, ScoringItem{
			Name: sc.Name, MaxScore: sc.MaxScore, Requirement: sc.Requirement,
			Sources: refsFromQuote(sc.Quote),
		})
	}
	bs.OverviewSources = refsFromQuote(res.OverviewQuote)
	bs.DurationSources = refsFromQuote(res.DurationQuote)
}

// refsFromQuote 占位：真实来源在 resolveParseItems 中落库；
// BidSummary 内保留精简来源（snippet+confidence），页码/偏移以 parse_results 为准。
func refsFromQuote(quote string) []SourceRef {
	if quote == "" {
		return nil
	}
	return []SourceRef{{Snippet: quote}}
}
```

- [ ] **Step 5: 接线 ParseBidFile**

将 `service.go` 中 `ParseBidFile` 的函数体替换为委托：

```go
// ParseBidFile 执行结构化解析管线（v2）
func (s *Service) ParseBidFile(ctx context.Context, proposalID string) (*Proposal, error) {
	return s.ParseBidFileV2(ctx, proposalID)
}
```

（旧 `ParseBidFile` 大函数与 `applyBidSummary` 一并删除；`ParseBidFileV2` 由 parse.go 提供。）

修复 `quoteOf`：BidItem 来自 AI JSON 时 Sources 为空，改用专门的 quote 字段。将 `parseFileResult` 的 `RedLines/Format/DarkRules/Qualification` 类型改为带 quote 的结构，`applyParseResult`/`resolveParseItems` 相应调整：

> 注意：以下修订类型为准，Step 4 代码块中的 `parseFileResult`/`TechScoring` 内联结构/`quoteOf` 以本步定义为准，直接按本步实现，不要两套并存。

```go
type parseItem struct {
	Name    string `json:"name"`
	Content string `json:"content"`
	Quote   string `json:"quote"`
}

type parseFileResult struct {
	Overview        string    `json:"overview"`
	OverviewQuote   string    `json:"overviewQuote"`
	Duration        string    `json:"duration"`
	DurationQuote   string    `json:"durationQuote"`
	Qualification   []parseItem `json:"qualification"`
	TechScoring     []parseScoring `json:"techScoring"`
	KeyRequirements []string  `json:"keyRequirements"`
	RedLines        []parseItem `json:"redLines"`
	Format          []parseItem `json:"format"`
	DarkRules       []parseItem `json:"darkRules"`
}

type parseScoring struct {
	Name        string `json:"name"`
	MaxScore    string `json:"maxScore"`
	Requirement string `json:"requirement"`
	Quote       string `json:"quote"`
}
```

`mergeParseResults`/`containsScoring`/`appendUniqueBidItems` 改用 `parseItem`；`applyParseResult` 把 `parseItem` 映射为 `BidItem{Name, Content}` 并附带 `refsFromQuote(it.Quote)`；`resolveParseItems` 用 `it.Quote` 定位（删除 `quoteOf`）。

- [ ] **Step 6: 运行测试确认通过**

Run: `go test ./internal/office/proposal/ -run 'TestParseBidFileV2_WithSources|TestParseBidFile|TestGenerateOutline' -v`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add internal/office/proposal/types.go internal/office/proposal/parse.go internal/office/proposal/service.go internal/office/proposal/service_test.go
git commit -m "feat(office): 招标解析管线 v2"
```

---

## Task 5: 绑定序列化兼容

**Files:**
- Modify: `internal/app/office_handler.go`（btm/bsf 改为 JSON 往返）
- Modify: `internal/app/office_handler_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/app/office_handler_test.go` 追加：

```go
func TestBidSummaryRoundTripKeepsNewFields(t *testing.T) {
	bs := &proposal.BidSummary{
		Overview: "项目概况",
		Qualification: []proposal.BidItem{{Name: "资质", Content: "三级", Sources: []proposal.SourceRef{{Snippet: "原文"}}}},
		ParseStatus: "done",
	}
	m := btm(bs)
	got := bsf(m)
	if got.ParseStatus != "done" || len(got.Qualification) != 1 || got.Qualification[0].Sources[0].Snippet != "原文" {
		t.Fatalf("往返丢失新字段: %+v", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/app/ -run TestBidSummaryRoundTripKeepsNewFields -v`
Expected: FAIL（btm 手工 map 未包含新字段）。

- [ ] **Step 3: btm/bsf 改为 JSON 往返**

将 `office_handler.go` 中的 `btm` 替换为：

```go
func btm(bs *proposal.BidSummary) map[string]interface{} {
	data, _ := json.Marshal(bs)
	var m map[string]interface{}
	_ = json.Unmarshal(data, &m)
	return m
}
```

将 `bsf` 替换为：

```go
func bsf(m map[string]interface{}) *proposal.BidSummary {
	data, _ := json.Marshal(m)
	var bs proposal.BidSummary
	_ = json.Unmarshal(data, &bs)
	return &bs
}
```

`office_handler.go` 顶部确保 import `encoding/json`。

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/app/...`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/app/office_handler.go internal/app/office_handler_test.go
git commit -m "feat(office): 解析绑定与序列化兼容"
```

---

## Task 6: 前端解析卡片 + 原文定位抽屉

**Files:**
- Create: `frontend/src/components/office/ParseSummaryCards.tsx`
- Create: `frontend/src/components/office/SourcePreviewDrawer.tsx`
- Modify: `frontend/src/pages/OfficePage.tsx`（ParseTab 使用新组件）

- [ ] **Step 1: 创建 ParseSummaryCards.tsx**

创建 `frontend/src/components/office/ParseSummaryCards.tsx`：

```tsx
import React from 'react'
import { Button, Card, Divider, Input, Space, Tag, Typography } from 'antd'
import { FileTextOutlined, SaveOutlined } from '@ant-design/icons'

const { Text, Title } = Typography

export interface SrcRef { fileId?: string; fileName?: string; page?: number; snippet?: string }
export interface BidItem { name?: string; content?: string; sources?: SrcRef[] }

export interface BidSummaryShape {
  overview?: string
  duration?: string
  techScoring?: { name?: string; maxScore?: string; requirement?: string; sources?: SrcRef[] }[]
  keyRequirements?: string[]
  redLines?: string[]
  redLineItems?: BidItem[]
  qualification?: BidItem[]
  format?: BidItem[]
  darkRules?: BidItem[]
  overviewSources?: SrcRef[]
  durationSources?: SrcRef[]
  parseStatus?: string
}

function SrcChips({ sources, onJump }: { sources?: SrcRef[]; onJump: (s: SrcRef) => void }) {
  if (!sources?.length) return null
  return (
    <Space size={4} wrap style={{ marginTop: 4 }}>
      {sources.map((s, i) => (
        <Tag key={i} icon={<FileTextOutlined />} color="blue" style={{ cursor: 'pointer', fontSize: 11 }} onClick={() => onJump(s)}>
          {s.fileName || s.fileId || '来源'}{s.page ? ` · 第${s.page}页` : ''}
        </Tag>
      ))}
    </Space>
  )
}

function ItemList({ items, onJump }: { items?: BidItem[]; onJump: (s: SrcRef) => void }) {
  if (!items?.length) return null
  return (
    <div>
      {items.map((it, i) => (
        <div key={i} style={{ marginBottom: 8 }}>
          <Text strong>{it.name}</Text>
          {it.content ? <div style={{ fontSize: 13 }}>{it.content}</div> : null}
          <SrcChips sources={it.sources} onJump={onJump} />
        </div>
      ))}
    </div>
  )
}

export const ParseSummaryCards: React.FC<{
  bs: BidSummaryShape
  onChange: (bs: BidSummaryShape) => void
  onJump: (s: SrcRef) => void
}> = ({ bs, onChange, onJump }) => {
  const set = (patch: Partial<BidSummaryShape>) => onChange({ ...bs, ...patch })
  return (
    <div>
      <Card size="small" title="项目概况" style={{ marginBottom: 12 }}>
        <Input.TextArea value={bs.overview || ''} rows={3} onChange={e => set({ overview: e.target.value })} />
        <SrcChips sources={bs.overviewSources} onJump={onJump} />
      </Card>
      <Card size="small" title="工期要求" style={{ marginBottom: 12 }}>
        <Input value={bs.duration || ''} onChange={e => set({ duration: e.target.value })} />
        <SrcChips sources={bs.durationSources} onJump={onJump} />
      </Card>
      <Card size="small" title="评分标准" style={{ marginBottom: 12 }}>
        {(bs.techScoring || []).map((sc, i) => (
          <div key={i} style={{ marginBottom: 8 }}>
            <Space style={{ width: '100%' }}>
              <Input style={{ width: 180 }} value={sc.name || ''} placeholder="评分项" />
              <Input style={{ width: 80 }} value={sc.maxScore || ''} placeholder="分值" />
            </Space>
            <Input.TextArea value={sc.requirement || ''} rows={2} style={{ marginTop: 4 }} />
            <SrcChips sources={sc.sources} onJump={onJump} />
          </div>
        ))}
      </Card>
      <Card size="small" title="核心要求" style={{ marginBottom: 12 }}>
        {(bs.keyRequirements || []).map((kr, i) => (
          <div key={i} style={{ marginBottom: 4 }}>• {kr}</div>
        ))}
      </Card>
      <Card size="small" title="废标条款" style={{ marginBottom: 12 }}>
        <ItemList items={bs.redLineItems} onJump={onJump} />
        {!bs.redLineItems?.length && (bs.redLines || []).map((r, i) => <div key={i} style={{ marginBottom: 4 }}>• {r}</div>)}
      </Card>
      <Card size="small" title="资质要求" style={{ marginBottom: 12 }}>
        <ItemList items={bs.qualification} onJump={onJump} />
      </Card>
      <Card size="small" title="格式要求" style={{ marginBottom: 12 }}>
        <ItemList items={bs.format} onJump={onJump} />
      </Card>
      <Card size="small" title="暗标要求" style={{ marginBottom: 12 }}>
        <ItemList items={bs.darkRules} onJump={onJump} />
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: 创建 SourcePreviewDrawer.tsx**

创建 `frontend/src/components/office/SourcePreviewDrawer.tsx`：

```tsx
import React, { useEffect, useRef } from 'react'
import { Drawer, Typography } from 'antd'
import type { SrcRef } from './ParseSummaryCards'

const { Text } = Typography

export const SourcePreviewDrawer: React.FC<{
  open: boolean
  files?: { name?: string; markdown?: string }[]
  source?: SrcRef | null
  onClose: () => void
}> = ({ open, files, source, onClose }) => {
  const bodyRef = useRef<HTMLDivElement>(null)
  const file = (files || []).find(f => f.name === source?.fileName) || (files || [])[0]
  useEffect(() => {
    if (!open || !bodyRef.current || !source?.snippet) return
    const idx = file?.markdown?.indexOf(source.snippet)
    if (idx != null && idx >= 0 && bodyRef.current.firstChild) {
      const walker = document.createTreeWalker(bodyRef.current, NodeFilter.SHOW_TEXT)
      let node: Node | null
      let offset = 0
      while ((node = walker.nextNode())) {
        const len = node.textContent?.length || 0
        if (offset + len >= idx) {
          const range = document.createRange()
          range.setStart(node, idx - offset)
          range.setEnd(node, Math.min(idx - offset + (source.snippet?.length || 0), len))
          const sel = window.getSelection()
          sel?.removeAllRanges()
          sel?.addRange(range)
          node.parentElement?.scrollIntoView({ block: 'center' })
          break
        }
        offset += len
      }
    }
  }, [open, source, file])
  return (
    <Drawer title="招标文件原文" width={560} open={open} onClose={onClose}>
      <Text type="secondary" style={{ fontSize: 12 }}>{file?.name || '未选择文件'}</Text>
      <div ref={bodyRef} style={{ marginTop: 8, whiteSpace: 'pre-wrap', fontSize: 13, lineHeight: 1.8 }}>
        {file?.markdown || '（无原文内容）'}
      </div>
    </Drawer>
  )
}
```

- [ ] **Step 3: 改造 ParseTab**

在 `OfficePage.tsx` 顶部 import：

```tsx
import { ParseSummaryCards, SourcePreviewDrawer } from '../components/office/ParseSummaryCards'
```

将 `ParseTab` 的 JSON 文本框部分替换为卡片 + 抽屉。`ParseTab` 增加内部状态：

```tsx
const [editBS, setEditBS] = useState<BidSummaryShape>(sp?.bidSummary || {})
const [src, setSrc] = useState<SrcRef | null>(null)
useEffect(() => { setEditBS(sp?.bidSummary || {}) }, [sp?.bidSummary])
```

`hasSummary` 时渲染：

```tsx
<Card size="small" title="招标关键内容摘要（可编辑）" extra={<Button type="primary" size="small" icon={<SaveOutlined />} onClick={save}>保存</Button>}>
  <ParseSummaryCards bs={editBS} onChange={setEditBS} onJump={s => setSrc(s)} />
</Card>
<SourcePreviewDrawer open={!!src} files={sp?.bidSummary?.rawFiles} source={src} onClose={() => setSrc(null)} />
```

`save` 改为构造对象保存（替代 JSON 解析）：

```tsx
const save = async () => {
  if (!sid) return
  try {
    await App.ProposalUpdate({ ...sp, bidSummary: editBS } as any)
    onR()
    message.success('已保存')
  } catch (e: any) { message.error(String(e)) }
}
```

`BidSummaryShape`/`SrcRef` 类型从组件文件导入。

- [ ] **Step 4: 前端验证**

Run（工作目录 `frontend`）: `node .\node_modules\typescript\bin\tsc -b`
Expected: 0 errors。

Run: `node .\node_modules\vite\bin\vite.js build`
Expected: 构建成功。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/office frontend/src/pages/OfficePage.tsx
git commit -m "feat(office): 招标解析结构化卡片+原文定位"
```

---

## Task 7: 全量验证与 v1.15.0 发布

- [ ] **Step 1: 后端全量验证**

Run: `go vet ./...` 与 `go test ./...`
Expected: vet clean，全部 PASS。

- [ ] **Step 2: 前端构建验证**

Run: `cd frontend && node .\node_modules\typescript\bin\tsc -b && node .\node_modules\vite\bin\vite.js build`
Expected: 0 errors + 构建成功。

- [ ] **Step 3: 桌面构建**

Run: `$env:TMP='C:\AI\wubigrok\.tmp'; $env:TEMP='C:\AI\wubigrok\.tmp'; wails build`
Expected: `build/bin/gaea.exe` 生成成功。

- [ ] **Step 4: 手工验收（CDP 或桌面）**

1. 上传一份文字型招标 PDF → 转换 Markdown → AI 解析。
2. 解析 Tab 显示结构化卡片（概况/工期/评分/资质/废标/格式/暗标），每项带来源 chip。
3. 点击来源 chip → 打开原文抽屉并定位高亮摘录。
4. 修改字段 → 保存 → 重启后仍存在；大纲生成仍使用解析出的评分标准。

- [ ] **Step 5: 更新 CHANGELOG 与版本号**

`CHANGELOG.md` 顶部新增 `## v1.15.0「招标解析管线」` 节（按既有格式），`wails.json` 的 `productVersion` 改为 `1.15.0`。

- [ ] **Step 6: 提交**

```bash
git add CHANGELOG.md wails.json
git commit -m "release: v1.15.0 招标解析管线"
```

---

## 计划自检

- **Spec 覆盖**：§5.2 文档智能（多格式解析/结构化提取/来源定位/OCR 接口）→ Task 1-4；§5.1 parse_results 表 → Task 2；§6 前端解析卡片与原文定位 → Task 6；OCR 仅定义 `OCRProvider` 接口，P2 阶段先支持文字型 PDF（设计默认决策）。
- **范围控制**：图纸/清单解析、具体 OCR 引擎不纳入（gaea 其他板块/后续任务）。
- **类型一致性**：`FileDoc.FileID`、`ParseResultItem`、`SourceRef`、`BidItem`、`BidSummary` 新字段、`AddFile` 返回 ID，全链路（store→parse→btm/bsf→前端）一致。
- **占位符检查**：无 TBD/TODO；代码步骤均含完整实现或精确替换说明。
- **兼容性**：`BidSummary` 旧字段（techScoring/keyRequirements/duration/redLines/overview/rawFiles/rawMarkdown）全部保留；`ParseBidFile` 绑定签名不变。
