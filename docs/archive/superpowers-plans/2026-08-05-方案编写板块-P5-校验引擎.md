# 方案编写板块重设计 P5 — 校验引擎 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「覆盖/规范单次 LLM 检查」升级为可插拔校验引擎：结构化规则（废标响应/事实一致性/跨章重复/暗标格式/规范引用）+ AI 语义覆盖规则，输出统一检查报告（规则/严重级/状态/章节定位/证据）。

**Architecture:** `CheckRule` 接口 + `ruleFunc` 适配器 + `RunChecks` 运行器；规则集为 Service 方法（可访问 store/knowledge/facts）；`CheckAll` 聚合全部规则；既有 `CheckCoverage`/`CheckCompliance` 保留兼容并内部复用；前端导出 Tab 增加「全面检查」报告面板（分组、状态着色、章节定位跳转）。

**Tech Stack:** Go 1.26 + modernc.org/sqlite + React/TS/AntD + Wails v2

---

## 任务总览

| 任务 | 内容 | 提交 |
|---|---|---|
| 1 | CheckRule 框架 + 运行器 + 默认规则集 | `feat(office): 校验引擎框架` |
| 2 | 结构化规则（废标/一致性/重复/暗标/规范引用） | `feat(office): 结构化校验规则` |
| 3 | AI 覆盖规则 + CheckAll + 绑定 | `feat(office): 全面检查聚合与绑定` |
| 4 | 前端检查报告面板 | `feat(office): 校验报告前端` |
| 5 | 全量验证与 v1.18.0 发布 | `release: v1.18.0 校验引擎` |

---

## Task 1: CheckRule 框架 + 运行器

**Files:**
- Create: `internal/office/proposal/check.go`
- Create: `internal/office/proposal/check_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/office/proposal/check_test.go`：

```go
package proposal

import (
	"context"
	"testing"
)

func TestRunChecks_AllRulesExecuted(t *testing.T) {
	proj, _ := (&Store{}).EnsureDefaultProject() // 占位，实际用 service 创建
	_ = proj
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	p, _ := s.Create("方案", "soil-remediation-bid", "", "环保工程")
	rules := []CheckRule{
		ruleFunc{name: "r1", severity: "warning", fn: func(ctx context.Context, p *Proposal) ([]CheckItem, error) {
			return []CheckItem{{Rule: "r1", Severity: "warning", Status: "pass", Message: "ok"}}, nil
		}},
	}
	items := RunChecks(context.Background(), p, rules)
	if len(items) != 1 || items[0].Rule != "r1" || items[0].Status != "pass" {
		t.Fatalf("RunChecks 异常: %+v", items)
	}
}

func TestRunChecks_RuleErrorBecomesItem(t *testing.T) {
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	p, _ := s.Create("方案", "blank", "", "其他")
	rules := []CheckRule{
		ruleFunc{name: "boom", severity: "critical", fn: func(ctx context.Context, p *Proposal) ([]CheckItem, error) {
			return nil, errTestCheck
		}},
	}
	items := RunChecks(context.Background(), p, rules)
	if len(items) != 1 || items[0].Status != "error" || items[0].Message == "" {
		t.Fatalf("错误规则未转 item: %+v", items)
	}
}
```

辅助：

```go
var errTestCheck = &ruleError{msg: "boom"}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestRunChecks -v`
Expected: FAIL（编译错误）。

- [ ] **Step 3: 实现 check.go**

```go
// Package proposal — 校验引擎：可插拔规则 + 统一检查报告
package proposal

import (
	"context"
	"fmt"
)

// CheckItem 单条检查结果
type CheckItem struct {
	Rule      string `json:"rule"`
	Severity  string `json:"severity"` // critical | warning | info
	Status    string `json:"status"`   // pass | fail | warn | skip | error
	SectionID string `json:"sectionId,omitempty"`
	Message   string `json:"message"`
	Evidence  string `json:"evidence,omitempty"`
}

// CheckRule 校验规则接口
type CheckRule interface {
	Name() string
	Severity() string
	Run(ctx context.Context, p *Proposal) ([]CheckItem, error)
}

// ruleFunc 函数式规则适配器
type ruleFunc struct {
	name     string
	severity string
	fn       func(ctx context.Context, p *Proposal) ([]CheckItem, error)
}

func (r ruleFunc) Name() string     { return r.name }
func (r ruleFunc) Severity() string { return r.severity }
func (r ruleFunc) Run(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	return r.fn(ctx, p)
}

// RunChecks 顺序执行全部规则，错误规则转为 error item
func RunChecks(ctx context.Context, p *Proposal, rules []CheckRule) []CheckItem {
	var out []CheckItem
	for _, rule := range rules {
		items, err := rule.Run(ctx, p)
		if err != nil {
			out = append(out, CheckItem{
				Rule: rule.Name(), Severity: rule.Severity(), Status: "error",
				Message: "规则执行失败: " + err.Error(),
			})
			continue
		}
		for i := range items {
			if items[i].Rule == "" {
				items[i].Rule = rule.Name()
			}
			if items[i].Severity == "" {
				items[i].Severity = rule.Severity()
			}
			out = append(out, items[i])
		}
	}
	return out
}

type ruleError struct{ msg string }

func (e *ruleError) Error() string { return e.msg }
```

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/office/proposal/ -run TestRunChecks -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
gofmt -w internal/office/proposal/check.go internal/office/proposal/check_test.go
git add internal/office/proposal/check.go internal/office/proposal/check_test.go
git commit -m "feat(office): 校验引擎框架"
```

---

## Task 2: 结构化校验规则

**Files:**
- Create: `internal/office/proposal/check_rules.go`
- Modify: `internal/office/proposal/check_test.go`

- [ ] **Step 1: 写失败测试**

追加：

```go
func TestStructuredRules(t *testing.T) {
	st := newTestKnowledgeStore(t)
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	s.SetKnowledgeStoreForTest(st)
	proj, _ := s.store.EnsureDefaultProject()
	p, _ := s.store.Create("方案", "soil-remediation-bid", "", "环保工程", proj.ID, []ProposalSection{
		{Title: "第一章", Level: 1, Content: "修复工期为 90 日历天，采用固化稳定化工艺。GB 36600-2018 要求砷限值 60mg/kg。"},
		{Title: "第二章", Level: 1, Content: "修复工期为 90 日历天，采用固化稳定化工艺。GB 36600-2018 要求砷限值 60mg/kg。"},
	})
	p.BidSummary = &BidSummary{
		RedLines: []string{"投标文件未按要求签字盖章作废标处理"},
		DarkRules: []BidItem{{Name: "暗标", Content: "不得出现单位名称、不得加粗"}},
	}
	_ = s.store.SaveProjectFacts(proj.ID, map[string]string{"工期": "90 日历天", "业主单位": "某区生态环境局"})
	if err := s.store.Update(p); err != nil {
		t.Fatal(err)
	}
	items := RunChecks(context.Background(), p, s.structuredRules())
	byRule := map[string][]CheckItem{}
	for _, it := range items {
		byRule[it.Rule] = append(byRule[it.Rule], it)
	}
	if len(byRule["重复率检测"]) == 0 || byRule["重复率检测"][0].Status != "fail" {
		t.Fatalf("重复率规则异常: %+v", byRule["重复率检测"])
	}
	if len(byRule["暗标格式检查"]) == 0 {
		t.Fatalf("暗标规则未执行: %+v", items)
	}
	if len(byRule["废标条款响应"]) == 0 || byRule["废标条款响应"][0].Status != "warn" {
		t.Fatalf("废标响应异常: %+v", byRule["废标条款响应"])
	}
	if len(byRule["规范引用检查"]) == 0 {
		t.Fatalf("规范引用规则未执行")
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestStructuredRules -v`
Expected: FAIL（`structuredRules` 未定义）。

- [ ] **Step 3: 实现 check_rules.go**

```go
// Package proposal — 结构化校验规则（确定性，无需 LLM）
package proposal

import (
	"context"
	"regexp"
	"sort"
	"strings"
)

var specCodeRe = regexp.MustCompile(`(?:GB|HJ)\s?\d{2,5}(?:\.\d+)?(?:-\d{4})?`)

// structuredRules 返回确定性规则集
func (s *Service) structuredRules() []CheckRule {
	return []CheckRule{
		ruleFunc{name: "废标条款响应", severity: "critical", fn: s.runRedLineRule},
		ruleFunc{name: "数据一致性检查", severity: "critical", fn: s.runConsistencyRule},
		ruleFunc{name: "重复率检测", severity: "warning", fn: s.runDuplicateRule},
		ruleFunc{name: "暗标格式检查", severity: "critical", fn: s.runDarkFormatRule},
		ruleFunc{name: "规范引用检查", severity: "warning", fn: s.runSpecRefRule},
	}
}

// runRedLineRule 废标条款响应检查：方案内容未明确回应则 warn
func (s *Service) runRedLineRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	if p.BidSummary == nil {
		return nil, nil
	}
	content := Assemble(p)
	norm := strings.Join(strings.Fields(content), "")
	var out []CheckItem
	add := func(title, clause string, ok bool) {
		status := "pass"
		msg := "已明确响应：" + title
		if !ok {
			status = "warn"
			msg = "方案未明确响应废标条款「" + clause + "」，需人工复核"
		}
		out = append(out, CheckItem{Status: status, Message: msg, Evidence: clause})
	}
	items := p.BidSummary.RedLineItems
	if len(items) == 0 {
		for _, r := range p.BidSummary.RedLines {
			items = append(items, BidItem{Name: "废标条款", Content: r})
		}
	}
	for _, it := range items {
		key := it.Content
		if key == "" {
			key = it.Name
		}
		keyNorm := strings.Join(strings.Fields(key), "")
		add(it.Name, key, strings.Contains(norm, keyNorm))
	}
	return out, nil
}

// runConsistencyRule 项目事实基线一致性：未体现 warn，工期/单位冲突 fail
func (s *Service) runConsistencyRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	if p.ProjectID == "" {
		return nil, nil
	}
	facts, err := s.store.GetProjectFacts(p.ProjectID)
	if err != nil || len(facts) == 0 {
		return nil, nil
	}
	content := Assemble(p)
	norm := strings.Join(strings.Fields(content), "")
	var out []CheckItem
	for k, v := range facts {
		if strings.TrimSpace(v) == "" {
			continue
		}
		vNorm := strings.Join(strings.Fields(v), "")
		if !strings.Contains(norm, vNorm) {
			out = append(out, CheckItem{Status: "warn", Message: "项目事实「" + k + "」未在方案中体现：" + v})
		}
	}
	if d := facts["工期"]; d != "" {
		vals := distinctDurations(norm)
		if len(vals) > 1 {
			sort.Strings(vals)
			out = append(out, CheckItem{Status: "fail", Message: "工期前后不一致：" + strings.Join(vals, " vs "), Evidence: d})
		}
	}
	if u := facts["业主单位"]; u != "" {
		uNorm := strings.Join(strings.Fields(u), "")
		if strings.Contains(norm, uNorm) && hasDarkNoUnit(p) {
			out = append(out, CheckItem{Status: "fail", Message: "暗标场景下出现单位名称：" + u})
		}
	}
	return out, nil
}

var durationRe = regexp.MustCompile(`\d+\s*(?:日历天|天)`)

func distinctDurations(norm string) []string {
	seen := map[string]bool{}
	var out []string
	for _, m := range durationRe.FindAllString(norm, -1) {
		key := strings.Join(strings.Fields(m), "")
		if !seen[key] {
			seen[key] = true
			out = append(out, key)
		}
	}
	return out
}

// runDuplicateRule 跨章节重复：20 字 n-gram 集合交并比
func (s *Service) runDuplicateRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	type secInfo struct {
		id    string
		title string
		grams map[string]bool
	}
	var secs []secInfo
	for _, sec := range flattenSections(p.Sections) {
		content := strings.Join(strings.Fields(sec.Content), "")
		if len([]rune(content)) < 60 {
			continue
		}
		secs = append(secs, secInfo{id: sec.ID, title: sec.Title, grams: gramSet(content, 20)})
	}
	var out []CheckItem
	for i := 0; i < len(secs); i++ {
		for j := i + 1; j < len(secs); j++ {
			common := 0
			for g := range secs[i].grams {
				if secs[j].grams[g] {
					common++
				}
			}
			den := len(secs[i].grams)
			if len(secs[j].grams) < den {
				den = len(secs[j].grams)
			}
			if den == 0 {
				continue
			}
			ratio := float64(common) / float64(den)
			if ratio > 0.5 {
				out = append(out, CheckItem{Status: "fail", SectionID: secs[j].id,
					Message: "与章节「" + secs[i].title + "」重复率高（" + pct(ratio) + "）", Evidence: secs[i].title})
			} else if ratio > 0.3 {
				out = append(out, CheckItem{Status: "warn", SectionID: secs[j].id,
					Message: "与章节「" + secs[i].title + "」存在较多重复（" + pct(ratio) + "）", Evidence: secs[i].title})
			}
		}
	}
	return out, nil
}

func gramSet(s string, n int) map[string]bool {
	runes := []rune(s)
	out := make(map[string]bool)
	for i := 0; i+n <= len(runes); i++ {
		out[string(runes[i:i+n])] = true
	}
	return out
}

func pct(v float64) string {
	return fmt.Sprintf("%.0f%%", v*100)
}

// runDarkFormatRule 暗标格式：加粗/斜体/删除线/emoji/单位名称
func (s *Service) runDarkFormatRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	if p.BidSummary == nil || len(p.BidSummary.DarkRules) == 0 {
		return nil, nil
	}
	content := Assemble(p)
	var out []CheckItem
	if strings.Contains(content, "**") || strings.Contains(content, "__") {
		out = append(out, CheckItem{Status: "fail", Message: "检测到加粗标记（暗标禁止加粗）", Evidence: "**"})
	}
	if strings.Contains(content, "~~") {
		out = append(out, CheckItem{Status: "warn", Message: "检测到删除线标记（暗标建议移除）", Evidence: "~~"})
	}
	for _, r := range content {
		if r >= 0x1F300 && r <= 0x1FAFF {
			out = append(out, CheckItem{Status: "warn", Message: "检测到 emoji 符号（暗标禁止特殊符号）"})
			break
		}
	}
	return out, nil
}

func hasDarkNoUnit(p *Proposal) bool {
	for _, it := range p.BidSummary.DarkRules {
		if strings.Contains(it.Content, "单位名称") || strings.Contains(it.Content, "公司") {
			return true
		}
	}
	return false
}

// runSpecRefRule 规范引用：技术方案必须引用规范编号；引用编号应存在于知识库
func (s *Service) runSpecRefRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	content := Assemble(p)
	codes := specCodeRe.FindAllString(content, -1)
	var out []CheckItem
	if len(codes) == 0 {
		hasContent := false
		for _, sec := range flattenSections(p.Sections) {
			if sec.Content != "" {
				hasContent = true
				break
			}
		}
		if hasContent {
			out = append(out, CheckItem{Status: "warn", Message: "技术方案未引用任何规范标准编号（如 GB 36600、HJ 25.4）"})
		}
		return out, nil
	}
	if st := s.knowledgeStore(); st != nil {
		known := map[string]bool{}
		for _, e := range st.ReadAll() {
			known[strings.ToLower(strings.TrimSpace(e.Source))] = true
		}
		for _, c := range codes {
			if !known[strings.ToLower(c)] {
				out = append(out, CheckItem{Status: "warn", Message: "引用的规范「" + c + "」不在知识库中，请核实编号"})
			}
		}
	}
	return out, nil
}
```

`check_rules.go` import 增加 `"fmt"`。

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/office/proposal/ -run TestStructuredRules -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
gofmt -w internal/office/proposal/check_rules.go internal/office/proposal/check_test.go
git add internal/office/proposal/check_rules.go internal/office/proposal/check_test.go
git commit -m "feat(office): 结构化校验规则"
```

---

## Task 3: AI 覆盖规则 + CheckAll + 绑定

**Files:**
- Create: `internal/office/proposal/check_ai.go`
- Modify: `internal/office/proposal/check.go`（defaultRules 聚合）
- Modify: `internal/office/proposal/service.go`（CheckAll；CheckCoverage 复用）
- Modify: `internal/app/office_handler.go`（ProposalCheckAll）
- Modify: `internal/app/office_handler_test.go`

- [ ] **Step 1: 写失败测试**

`check_test.go` 追加：

```go
func TestCheckAll_AggregatesRules(t *testing.T) {
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	p, _ := s.Create("方案", "blank", "", "其他")
	_, items, err := s.CheckAll(context.Background(), p.ID)
	if err != nil {
		t.Fatalf("CheckAll: %v", err)
	}
	if len(items) == 0 {
		t.Fatal("CheckAll 无结果")
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestCheckAll -v`
Expected: FAIL。

- [ ] **Step 3: 实现 check_ai.go**

```go
// Package proposal — AI 语义覆盖规则
package proposal

import (
	"context"
	"encoding/json"
	"fmt"
	"strings"
)

// runCoverageRule 对照招标评分标准做语义覆盖检查（LLM）
func (s *Service) runCoverageRule(ctx context.Context, p *Proposal) ([]CheckItem, error) {
	if s.ai == nil || p.BidSummary == nil || len(p.BidSummary.TechScoring) == 0 {
		return nil, nil
	}
	var allContent strings.Builder
	for _, sec := range flattenSections(p.Sections) {
		if sec.Content != "" {
			allContent.WriteString(fmt.Sprintf("【%s】\n%s\n\n", sec.Title, sec.Content))
		}
	}
	var scoringList strings.Builder
	for _, item := range p.BidSummary.TechScoring {
		scoringList.WriteString(fmt.Sprintf("- %s（%s分）：%s\n", item.Name, item.MaxScore, item.Requirement))
	}
	sp := fmt.Sprintf("你是环保工程投标评审专家。对照评分标准检查方案：\n%s\n返回 JSON: [{\"name\":\"\",\"maxScore\":\"\",\"covered\":\"full|partial|none\",\"suggestion\":\"\"}]", scoringList.String())
	reply, err := s.ai.ChatSimpleStream(ctx, "", sp, allContent.String())
	if err != nil {
		return nil, err
	}
	reply = extractJSON(reply)
	var results []struct {
		Name       string `json:"name"`
		MaxScore   string `json:"maxScore"`
		Covered    string `json:"covered"`
		Suggestion string `json:"suggestion"`
	}
	if err := json.Unmarshal([]byte(reply), &results); err != nil {
		return nil, fmt.Errorf("解析覆盖检查失败: %w", err)
	}
	var out []CheckItem
	for _, r := range results {
		status := "pass"
		if r.Covered == "partial" {
			status = "warn"
		} else if r.Covered == "none" {
			status = "fail"
		}
		out = append(out, CheckItem{
			Status: status, Message: fmt.Sprintf("评分项「%s」（%s分）：%s", r.Name, r.MaxScore, r.Covered),
			Evidence: r.Suggestion,
		})
	}
	return out, nil
}
```

`check.go` 增加聚合规则集：

```go
// defaultRules 完整规则集：结构化 + AI 语义
func (s *Service) defaultRules() []CheckRule {
	return append(s.structuredRules(),
		ruleFunc{name: "评分覆盖检查", severity: "warning", fn: s.runCoverageRule})
}
```

`service.go` 增加：

```go
// CheckAll 运行全部校验规则，返回统一检查报告
func (s *Service) CheckAll(ctx context.Context, proposalID string) (*Proposal, []CheckItem, error) {
	p, err := s.store.Get(proposalID)
	if err != nil {
		return nil, nil, err
	}
	items := RunChecks(ctx, p, s.defaultRules())
	return p, items, nil
}
```

把既有 `CheckCoverage` 函数体替换为委托（保持返回类型兼容）：

```go
// CheckCoverage 语义覆盖检查（兼容旧绑定，内部走覆盖规则）
func (s *Service) CheckCoverage(ctx context.Context, proposalID string) (*Proposal, []CoverageResult, error) {
	p, items, err := s.CheckAll(ctx, proposalID)
	if err != nil {
		return nil, nil, err
	}
	var out []CoverageResult
	for _, it := range items {
		if it.Rule != "评分覆盖检查" {
			continue
		}
		out = append(out, CoverageResult{Name: it.Message, Covered: it.Status, Suggestion: it.Evidence})
	}
	return p, out, nil
}
```

- [ ] **Step 4: 绑定**

`office_handler.go`：

```go
func (a *officeState) ProposalCheckAll(pid string) (map[string]interface{}, error) {
	p, items, err := a.proposalSvc.CheckAll(a.ctx, pid)
	if err != nil {
		return nil, err
	}
	return map[string]interface{}{"proposal": toMap(p), "items": items}, nil
}
```

`office_handler_test.go` 追加断言（空方案 CheckAll 不报错且返回 items 数组）。

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/office/proposal/... ./internal/app/...`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
gofmt -w internal/office/proposal/check.go internal/office/proposal/check_ai.go internal/office/proposal/service.go internal/app/office_handler.go internal/app/office_handler_test.go
git add internal/office/proposal/check.go internal/office/proposal/check_ai.go internal/office/proposal/service.go internal/office/proposal/check_test.go internal/app/office_handler.go internal/app/office_handler_test.go
git commit -m "feat(office): 全面检查聚合与绑定"
```

---

## Task 4: 前端检查报告面板

**Files:**
- Modify: `frontend/src/pages/OfficePage.tsx`（ExportTab 全面检查 + 报告列表 + 章节定位）
- Regenerate: `frontend/wailsjs/go/app/App.d.ts`、`App.js`

- [ ] **Step 1: 后端绑定重新生成**

Run: `wails generate module`
Expected: `ProposalCheckAll` 出现在 App.d.ts。

- [ ] **Step 2: ExportTab 增加全面检查**

状态与处理：

```tsx
const [chk, setChk] = useState<any[]>([]); const [chkg, setChkg] = useState(false)
const hchk = async () => { if (!sid) return; setChkg(true); try { const r = await App.ProposalCheckAll(sid) as any; setChk(r.items || []); onR() } catch (e: any) { message.error(String(e)) }; setChkg(false) }
```

工具条增加按钮：`<Button type="primary" icon={<CheckCircleOutlined />} onClick={hchk} loading={chkg}>全面检查</Button>`

报告渲染（工具条下方）：

```tsx
{chk.length > 0 && (
  <Card size="small" title="校验报告" style={{ marginBottom: 12 }}>
    {chk.map((it, i) => (
      <div key={i} style={{ display: 'flex', gap: 8, alignItems: 'flex-start', padding: '6px 0', borderBottom: '1px solid var(--md-sys-color-outline-variant)' }}>
        <Tag color={it.status === 'pass' ? 'green' : it.status === 'fail' ? 'red' : it.status === 'warn' ? 'orange' : it.status === 'error' ? 'red' : 'default'} style={{ width: 54, textAlign: 'center', margin: 0 }}>
          {it.status === 'pass' ? '通过' : it.status === 'fail' ? '不通过' : it.status === 'warn' ? '提醒' : it.status === 'error' ? '异常' : '跳过'}
        </Tag>
        <div style={{ flex: 1, minWidth: 0 }}>
          <Text strong style={{ fontSize: 12 }}>{it.rule}</Text>
          <div style={{ fontSize: 12 }}>{it.message}</div>
          {it.evidence ? <Text type="secondary" style={{ fontSize: 11 }}>{it.evidence}</Text> : null}
          {it.sectionId ? <Button type="link" size="small" style={{ padding: 0 }} onClick={() => onGoto(it.sectionId)}>定位章节</Button> : null}
        </div>
      </div>
    ))}
  </Card>
)}
```

`ExportTab` 增加 props：`onGoto: (id: string) => void`；`OfficePage` 传入 `onGoto={(id) => { oss(id); setTab('write') }}`。

- [ ] **Step 3: 前端验证**

Run（工作目录 `frontend`）: `node .\node_modules\typescript\bin\tsc -b`
Expected: 0 errors。

Run: `node .\node_modules\vite\bin\vite.js build`
Expected: 构建成功。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/OfficePage.tsx frontend/wailsjs/go/app/App.d.ts frontend/wailsjs/go/app/App.js
git commit -m "feat(office): 校验报告前端"
```

---

## Task 5: 全量验证与 v1.18.0 发布

- [ ] **Step 1: 后端全量验证**

Run: `go vet ./...` 与 `go test ./...`
Expected: vet clean，全部 PASS。

- [ ] **Step 2: 前端构建验证**

Run: `cd frontend && node .\node_modules\typescript\bin\tsc -b && node .\node_modules\vite\bin\vite.js build`
Expected: 0 errors + 构建成功。

- [ ] **Step 3: 桌面构建**

Run: `$env:TMP='C:\AI\wubigrok\.tmp'; $env:TEMP='C:\AI\wubigrok\.tmp'; wails build`
Expected: `build/bin/gaea.exe` 生成成功。

- [ ] **Step 4: 手工验收（CDP 或桌面）**

1. 解析招标文件后生成部分章节 → 导出 Tab「全面检查」：评分覆盖（AI）、废标响应、数据一致性、重复率、暗标格式、规范引用全部出现。
2. 制造两章重复内容 → 重复率规则报 fail；工期事实与正文冲突 → 一致性规则报 fail；正文加粗 → 暗标规则报 fail。
3. 点击「定位章节」跳到对应章节。

- [ ] **Step 5: 更新 CHANGELOG 与版本号**

`CHANGELOG.md` 顶部新增 `## v1.18.0「校验引擎」` 节（按既有格式），`wails.json` 的 `productVersion` 改为 `1.18.0`。

- [ ] **Step 6: 提交**

```bash
git add CHANGELOG.md wails.json
git commit -m "release: v1.18.0 校验引擎"
```

---

## 计划自检

- **Spec 覆盖**：§5.5 校验引擎（CheckRule 接口 + 规则库：评分覆盖/废标项/资质工期一致性/重复率/规范引用/暗标格式；LLM 辅助语义）→ Task 1-3；前端检查报告逐项处理 → Task 4。
- **范围控制**：不改 P4 知识资产/不接 OCR/不做商务；既有 `CheckCoverage`/`CheckCompliance` 绑定兼容保留。
- **类型一致性**：`CheckItem`、`CheckRule`、`ruleFunc`、`CheckAll`、`ProposalCheckAll`、前端 `it.status/rule/message/evidence/sectionId` 全链路一致。
- **性能**：重复率用 20 字 n-gram 集合交并比（O(n+m)），不跑全量 LCS。
