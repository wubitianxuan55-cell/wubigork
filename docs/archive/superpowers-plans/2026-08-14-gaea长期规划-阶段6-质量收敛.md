# gaea 长期规划 · 阶段 6：质量收敛（审查驱动）（2026-08-14 定稿）

> 依据：2026-08-14 九板块独立子代理代码审查（chat / whisper / novel / imagegen /
> modelcenter / office / memory / misc / frontend 共 9 份报告，每份均带文件:行号证据，
> 证据已在起草规划章节时逐一 read/grep 复核）、v2.23.0 收官现状。
> 原则：延续阶段 0-5 纪律——不做磨皮擦痒；每条修复可度量、有验收；每刀独立发布
> （exe + SHA256SUMS + 冒烟 + 发布文档）；修复不重构，改动面最小化。

## 0. 一句话定位

前五阶段把底座（herdsman 可靠性）、门禁（CI）、安全（架构收敛）、数据（备份/成本/测评）、
运行体验（调度/速度/韧性）做扎实了。九板块审查给出的统一画像：**主路径强、边缘靠手工、
失败静默、巨型文件堆积**——没有 A 级短板也没有 C 级灾难，缺的是把「实测过」变成
「可回归地测」、把「静默」变成「可见」、把「声称」变成「实现」。阶段 6 主题 =
**质量收敛 · 可靠性 · 可观测性**。

## 1. 短板证据（九板块审查要点，每条有代码实证，详见各模块章节）

| # | 模块 | 核心短板 | 实证 |
|---|---|---|---|
| G1 | 基础层 | SSE 64KB 行上限断流、流无超时无重试、错误全静默、token 明文入日志 | `ai/client.go:437`（Scanner 默认 64KB）；`bridge.ts` 全文件无 try/catch；`store.ts:347/377/391/398` 静默 catch；`main.go:42-43` token 打日志 |
| G2 | 办公引擎 | docmd 页数计数 bug、TCCA/evidence 零测试、后端看门狗缺失、禁写清单硬编码 | `docmd.go:540`（/Type /Page 误匹配 /Type /Pages）；`agent_run.go:267`；`task.go:281-288` |
| G3 | 对话 chat | 流订阅竞态+无超时（sending 永久 true）、语音不落库、迁移常驻重试、导出未转义 | `ChatPage.tsx:485-487`；`:254-261`；`:145-173`；`chat_service.go:251-258` |
| G4 | 绘梦 | 取消不生效、flux 路由错配、历史图片丢失、t2v 格式误标、端口注入面 | `image_handler.go:63-74`；`:195-200`；`image_comfyui.go:209-213`；`:133-139`；`image_handler.go:868-884` |
| G5 | 轻语 | 测试密度 10/223、记忆错误静默、异步火忘不可观测、成人模式死开关、陈旧占位 | `memory_ingest.go:94-101`；`whisper_handler.go:205-220`；`orchestrator.go:91`；`plan_document_intent.go:1` |
| G6 | 模型中心 | refresh_token 明文（与 DPAPI 不一致）、汇率 7.2 写死、告警文案 bug、UI 42 个 useState+竞态、XAI_VOICES 四处重复 | `auth/token.go:78`；`modelengine/stats.go:175`；`herdsman/probe.go:221`；`ModelCenterPage.tsx` |
| G7 | 小说 | export 零测试+静默失败+硬编码作者、生成无中断无互斥、writeJSON 非原子、模板替换误伤 | `export.go:82-85/:131`；`create_chapter_handler.go:170/:79`；`project.go:428-433` |
| G8 | 记忆 | dream 绕过审批、facts 只软删不清理、索引 rune 硬切双口径、前端核心组件零测试 | `controller_memory.go:199-215`（SaveDreamFacts 不经 hardAskTools）；`memory.go:197-202` |
| G9 | 辅助合集 | 微信 0 测试+凭证失效空转+Stop 非幂等+凭证明文、OCR 兜底名不副实、config 非原子写、路径硬编码、token 走 URL | `clawbot.go:70-89/:167-172`；`manager.go:22`；`config.go:813-815`；`tts_service.go:15-18`；`bridge.go:118-120` |
| G10 | 前端收敛 | 巨型文件（mock.ts 73KB/ChatPage 963 行）、315 处 any、Go↔TS 漂移检查禁用、mock 锁虚构值、零虚拟化、双桥接 | `bridge.ts:691-692`（_CheckGenToApp 被注释）；`mock.ts:1413`；`eslint.config.js:31` |

## 2. 阶段任务表（按功能模块逐刀，基础层先行）

模块编号 = 刀序：T6-1 基础层 → T6-9 辅助合集；T6-10 前端收敛为贯穿任务
（每刀预留 20-30% 预算拆本模块巨型组件/清本模块 any，另设专项收尾刀）。
每刀一个版本（v2.24.0 起），可随时在刀间插入紧急修复。

### 第一刀 v2.24.0「基础层·可靠性」（G1）

### 基础层：AI 客户端与错误可见性（T6-1）

- **现状**：SSE 64KB 行上限、无超时/重试、代理未接入，多处静默吞错、token 明文入日志。

- **本轮目标**：AI 流长行/弱网/代理下稳定（SSE 单行 ≥1MB 不断流、可重试恢复）；错误可见（前端错误码+LogFrontendError、后端吞错清零）；日志脱敏。

- **迭代任务**：

**T6-1.1**【内容】SSE 改 bufio.Reader（≥1MB）、加超时+退避重试（仅连接/5xx）；接入 netclient 代理（ProxySpec→Transport.Proxy）。【涉及文件】internal/ai/client.go、internal/netclient/。【验收标准】超长行/超时重试/代理转发单测 ≥5；无代理测试全绿、门禁全绿。

**T6-1.2**【内容】bridge.ts invoke 错误归一化；store.ts 去静默 catch 改 LogFrontendError。【涉及文件】frontend/src/gaea/lib/bridge.ts、store.ts。【验收标准】tsc 0 errors、vitest ≥3；LogFrontendError ≥3、无静默 catch。

**T6-1.3**【内容】吞错记录：appendChatExchange 失败不掩盖、return nil 改 error；whisper 三文件失败必日志、去 `_=`；config.Load 解析上抛；token 脱敏（尾 4 位）。【涉及文件】internal/app/chat_service.go、whisper_handler.go、internal/whisper/memory_ingest.go、memory_consolidator.go、internal/config/config.go、main.go。【验收标准】错误路径/坏 JSON/脱敏单测 ≥6；grep 无吞错；门禁全绿。

- **明确不做**：持久化/断点续传；不改 SSE 协议；不接第三方监控；无重试 UI。

- **风险与依赖**：重试须幂等；吞错修复暴露错误，与 T6-1.2 联动；代理依赖 netclient 支持 Proxy；bridge 签名变需 gen_bindings。

### 第二刀 v2.25.0「办公引擎·正确性」（G2）

### 通用办公引擎（含 docmd）（T6-2）

- **现状**：docmd 页数误匹配 /Type /Pages、BT/ET 分页错位；TCCA 内核与 evidence 零测试；TurnResult 统计与流错误处理缺陷；后端看门狗缺失（v2.13.0 声称的看门狗仅前端实现）；running 期 Send 静默丢弃；子代理禁写清单硬编码 6 项；docmd.go 全仓最大而核心测试不足。

- **本轮目标**：修复分页计数/过滤与 TurnResult 语义，落地真实后端看门狗与 Send 排队，禁写清单改由工具注册表驱动，补齐三包核心测试。验收口径：全量 go test ./... 绿 + vet 干净；新增测试 ≥35；docmd/context/evidence 覆盖率 ≥70%/60%/60%；僵死 turn 实测 <5s 被终止。

- **迭代任务**：
  1. **T6-2.1 修复 PDF 页数统计与分页过滤**【内容】/Type /Page 精确匹配（排除 /Type /Pages）；文本块按页对象归类，BT/ET 不再自增页码；OCR 回退范围与「已截断」提示一致。【涉及文件】internal/office/docmd/docmd.go。【验收标准】新增 ≥6 用例（3 页含 /Pages 干扰 → 页数=3；pages=2-3 只返回对应页文本）；docmd 包测试全绿 + vet 干净。
  2. **T6-2.2 修复运行链路结果语义**【内容】blocked/suppressed 计入 Errors，Success 仅整轮未阻断且无错误时为 true；流错误路径先将已收部分文本写入会话再返回 err；step-- 加下限 0 并与 graceRound 边界复核，杜绝额外一轮。【涉及文件】internal/gaea/agent/agent_run.go、TurnResult 定义处。【验收标准】新增 ≥8 单测（全 blocked 轮 → Success=false；流中断保留部分文本且返回 err；step 负数不触发额外 grace 轮）；agent 包测试绿 + vet 干净。
  3. **T6-2.3 TCCA 与 evidence 补测**【内容】为 TCCA 内核（internal/gaea/context 的 manager/store/flow/store_file/metrics）与 evidence 补核心路径测试：缓存形状指纹、命中/未命中、持久化 round-trip、证据匹配去重。【涉及文件】internal/gaea/context/*_test.go、internal/gaea/evidence/evidence_test.go（均新增）。【验收标准】新增 ≥12 测试，两包 go test -cover ≥60%，全量 go test ./... 绿。
  4. **T6-2.4 落地后端看门狗**【内容】进程内运行态看门狗：turn 墙钟超时或推进停滞 → 强制终止并 Emit TurnDone(Err)；阈值配置化防误杀长任务；changelog 表述与实现对齐。【涉及文件】internal/gaea/control/controller.go（或新增 watchdog.go）、releases/CHANGELOG-v2.13.0.txt。【验收标准】新增 ≥4 测试（停滞 turn 被终止并发出 TurnDone(Err)）；注入停滞 provider 实测 <5s 终止。
  5. **T6-2.5 Send 排队与禁写清单注册表化**【内容】running 期 Send 改限长队列（8 条），turn 结束后按序执行、队满拒绝并 notice；子代理禁写集由工具注册表「持久化写」标记自动推导，替换 task.go 手写 6 项。【涉及文件】internal/gaea/control/controller.go、internal/gaea/agent/task.go、工具注册处。【验收标准】新增 ≥5 测试（入队/执行/队满/新持久化写工具自动纳入禁写）；现有 6 项集合不变；全量测试绿。
  6. **T6-2.6 docmd.go 拆分**【内容】1429 行 docmd.go 按职责拆分（PDF 解析/Office 转换/OCR 编排），先补测试后拆、行为不变。【涉及文件】internal/office/docmd/*.go。【验收标准】核心函数行覆盖 ≥70%；拆分后现有测试全绿、vet 干净。

- **明确不做**：不改 UI/绑定面（无 gen_bindings 变动）；不引入第三方 PDF 引擎；不优化 docx/pptx 转换质量（仅修分页/计数）；不做跨进程守护（看门狗仅进程内）；不重写 TCCA 缓存策略。

- **风险与依赖**：分页修复需真实多页 PDF fixture（含压缩流/干扰对象），控制体积与 CI 时长；TCCA 覆盖依赖可注入性，若含全局状态需先小幅重构；看门狗与并发取消机制耦合，阈值需配置化；禁写清单改动涉子代理权限面，需回归审批链路；docmd 拆分属大文件重构，先测试后拆、分步提交。

### 第三刀 v2.26.0「对话·流可靠」（G3）

### 对话 chat（T6-3）

- **现状**：流订阅首帧竞态且 Promise 无超时会永久挂起；语音不落库、旧迁移常驻重试、导出未转义；多处落库/列表错误被吞。
- **本轮目标**：流订阅首帧不丢、异常/超时必收尾（sending 恒可复位）；语音识别/回复落库、刷新不丢；旧键迁移仅执行一次且失败可观测、导入事务化；导出转义安全。修复均配新增测试且门禁全绿：Go 全量包 ok + vet 干净、前端 tsc 0 errors + vitest 全量通过、绑定面变动重跑 gen_bindings 并通过完备性测试。
- **迭代任务**：
  1. **T6-3.1 流订阅竞态与超时**【内容】ChatPage.tsx:485-487 先 EventsOn 订阅再 await ChatStreamPlain（或缓冲首帧）；流 Promise 加可配超时（默认 30s 无帧即失败）与 finally 复位 sending；chat_service.go:82 go 侧保证 emit 前订阅就绪或首帧可重放。【涉及文件】frontend/src/pages/ChatPage.tsx、internal/app/chat_service.go。【验收标准】新增前端测试 ≥2（订阅先于启动、超时后 sending=false 且 finally 执行，vitest fake timers）；实测模拟永不结束/不发 error 流，sending 在超时阈值内复位、发送框恢复可用。
  2. **T6-3.2 落库错误透传**【内容】appendChatExchange（:164-171）失败改返回 error，:131/:158 不再静默，:132-137 落库失败改 emit error 而非 done；ChatTopicsList（:175-179）/ChatMessagesList（:224）吞错改返回 error。【涉及文件】internal/app/chat_service.go。【验收标准】新增 Go 单测 ≥2（落库失败不 emit done、错误正确返回）；前端失败路径用例 ≥1；只读 DB 实测前端提示失败而非成功。
  3. **T6-3.3 语音持久化与资源清理**【内容】ChatPage.tsx:254-261 语音识别/回复改经后端 AppendExchange 事务落库而非仅内存；:575 朗读 URL 在 finally/卸载时 revokeObjectURL；:520-530 模拟打字循环加取消标志，切话题/卸载即中止。【涉及文件】frontend/src/pages/ChatPage.tsx、internal/app/chat_service.go。【验收标准】新增前端测试 ≥3（URL revoke、循环取消、语音经落库接口调用）；实测重启后端并刷新页面后语音消息仍在且可回放。
  4. **T6-3.4 迁移一次性与导入事务**【内容】:51-61 旧键迁移加持久化已执行标记，:145-173 migrateLegacyTopics 失败记日志不再静默重试（去除 :166 catch(_){} 吞错）；ChatImportTopic（chat_service.go:302）逐条落库改单事务、失败全量回滚。【涉及文件】frontend/src/pages/ChatPage.tsx、internal/app/chat_service.go。【验收标准】新增测试 ≥2（二次初始化迁移仅执行一次、导入中途失败无部分落库）；实测连续两次启动仅见一次迁移日志、无重复数据。
  5. **T6-3.5 导出转义与死绑定核实**【内容】:257 消息原文写前转义 Markdown 敏感字符（#、`、<、>、|）；补 sanitizeChatFilename（:273）单测；ChatGeneral（chat_handler.go:5-15）前端 0 调用但 module_bindings.go:16 主脑派发仍依赖、gen_bindings/main.go:145 有登记——仅补测试与说明，不做删除。【涉及文件】internal/app/chat_service.go、internal/app/chat_handler.go、internal/app/module_bindings.go。【验收标准】新增 Go 单测 ≥3（转义矩阵 ≥2 组、非法文件名、ChatGeneral 派发路径）；go test ./... 全绿。
  6. **T6-3.6 全量回归**【内容】上述修复合并后统一回归。【涉及文件】chat 模块全部改动。【验收标准】Go 全量包 ok + vet 干净；前端 tsc 0 errors + vitest 全量通过；绑定面无漂移（如涉 gen_bindings 变动则重生成且完备性测试通过）。
- **明确不做**：不重构对话 UI/交互与聊天模式切换；不更换语音识别/TTS 引擎或模型；不回溯修复历史脏数据（仅保证不再重试）；不改 chat 数据库 schema（语音落库若需新列，归数据层模块先行评估）；不新增导出格式（仅转义）。
- **风险与依赖**：流时序修复依赖事件通道 emit 时序契约，需基础层事件模块先行确认；语音落库若需新字段依赖 schema/迁移模块先行；ChatGeneral 与主脑助手模块（module_bindings.go）耦合，禁止删除；迁移标记与话题列表共享状态，注意与相关模块改动冲突。

### 第四刀 v2.27.0「绘梦·链路真实生效」（G4）

### 绘梦（文生图）（T6-4）

- **现状**：审查确认 7 项缺陷：取消仅 cancel context 不中断 ComfyUI 任务、checkHistory 无 ctx；flux 无工作流、落 default 分支实际跑 Krea2；历史剥离 image 且落盘路径返回值被丢弃；t2v webp 按 .mp4 下载命名且预览不播；尺寸解析 Sscanf 不校验；findstr 拼接端口可注入 cmd 误杀进程；核心链路零测试（仅 10 个纯函数）。中文错误提示、LoRA 动态同步、孤儿实例自动重启为保留亮点。
- **本轮目标**：取消 ≤2s 内真实终止 ComfyUI 任务并释放 GPU；flux 选择提交真实 Flux 工作流；重启后历史图片可恢复、下载/设为剧照可用；轮询/取消/上传/下载链路有 httptest mock 覆盖；Go 全量 ok+vet 干净、tsc 0 errors、vitest 全量通过、绑定面变更重生成 gen_bindings。
- **迭代任务**：
  - **T6-4.1 取消真实生效**【内容】CancelImageGeneration 增调 ComfyUI /interrupt/ 中断当前任务、按 promptID 从 /queue 删除排队项；checkHistory 改用 http.NewRequestWithContext 携带 ctx，取消后轮询即刻退出并回写状态。【涉及文件】internal/app/image_handler.go、internal/ai/image_comfyui.go、frontend/src/pages/ImageGenPage.tsx。【验收标准】httptest mock ≥4 用例（interrupt 已调用/队列删除/ctx 传播/重复取消幂等）；实测取消后任务 ≤2s 终止、GPU 释放、前端即时「已取消」。
  - **T6-4.2 flux 名实相符**【内容】ComfyUI 端补 Flux 工作流（FLUX.1-schnell/dev + UNETLoader），或从 comfyAlways 移除 flux；模型→工作流改显式映射表，default 仅接受白名单模型、其余返回中文错误。【涉及文件】internal/ai/image_comfyui.go、internal/app/image_handler.go。【验收标准】映射表单测覆盖全部 comfyAlways 模型无静默降级；flux 提交载荷含 Flux 节点；go test ./internal/... ok + vet 干净。
  - **T6-4.3 历史图片可恢复**【内容】imageItem 增 FilePath 字段，saveImageToDisk 返回值入历史；saveHistoryMeta 保留 image/path；重启加载时无 base64 则按路径读盘转 dataURL 回填。【涉及文件】internal/app/image_handler.go、frontend/src/pages/ImageGenPage.tsx。【验收标准】E2E：生成→重启→历史缩略图显示→下载出图→设为剧照成功；vitest ≥2 用例（保留 path/加载回填）；Go 单测：返回路径非空且文件存在。
  - **T6-4.4 格式与尺寸校验**【内容】video 下载改 .webp 命名、预览改兼容 webp 动画组件；Sscanf 检查错误并钳制尺寸 64–2048，非法输入返回中文错误。【涉及文件】frontend/src/pages/ImageGenPage.tsx、internal/ai/image_comfyui.go。【验收标准】vitest 1 用例断言 video 下载名 .webp；Go 单测 ≥3 用例（"abc"/"99999x1" 报错不静默 1024）。
  - **T6-4.5 端口命令注入**【内容】findProcessByPort 弃用 cmd/findstr 拼接，改 exec.Command("netstat","-ano") 参数数组传参解析（或 net.Listen 探测 + PID 解析）；port 白名单纯数字 1–65535。【涉及文件】internal/app/image_handler.go。【验收标准】Go 单测 ≥3 用例（"|calc" 等特殊字符不注入、非法端口返回 0、正常端口解析正确）；实测 8188 正常检出。
  - **T6-4.6 核心链路测试补全**【内容】ComfyUI 客户端注入可替换 http.Client（httptest server），补提交/轮询/取消/上传/下载用例；前端补生成状态机 vitest。【涉及文件】internal/ai/image_comfyui.go、新增 internal/ai/image_comfyui_test.go、frontend/src/pages/ImageGenPage.tsx。【验收标准】Go 新增 ≥10 用例且四条链路各 ≥2；前端新增 ≥4 用例；Go 全量 ok+vet 干净、tsc 0 errors、vitest 全量通过；绑定面签名变动时 gen_bindings 重生成 + TestBindingsCompleteness 通过。
- **明确不做**：不接入新模型/下载新权重（flux 仅名实相符）；不重写 ComfyUI 协议层为 SDK；不做画廊 UI 重构；不扩展视频引擎（仅修 webp 输出与预览）；不动 ComfyUI 安装/自定义节点管理。
- **风险与依赖**：ComfyUI 版本间 /interrupt、/queue 删除行为差异需兼容探测；WebView2 对 webp 动画预览兼容性待实测；大图 base64 与路径混存需分级策略防 localStorage 溢出；历史恢复依赖既有 ImageSaveDir/小说 images 目录（基础层已有，无阻塞）；「设为剧照」依赖角色库 portrait 接口（既有）；绑定面变更依赖 gen_bindings + 完备性测试门禁。

### 第五刀 v2.28.0「轻语·测试与可观测」（G5）

### 轻语 whisper（T6-5）

- **现状**：223 源文件仅 10 个测试文件，emotion_fusion 等 9 组核心文件 0 测试；LLM/解析失败静默 return、记忆写协程不可观测、成人模式硬编码 true、db 层重复 PRAGMA 与陈旧占位并存。
- **本轮目标**：测试密度达办公引擎同级（emotion_fusion 函数覆盖 100%、其余 ≥90%），go test -race ./internal/whisper/... 与 go vet ./... 全绿；所有 LLM/解析/持久化失败路径均有 error 日志且单测可断言。异步写失败可观测；成人模式「永久开启」决策成文；PRAGMA 单一来源、GetDatabase 返回 error、FTS 失败有日志；无指向不存在文件的占位注释。
- **迭代任务**（未注路径均在 internal/whisper/）：
  - **T6-5.1 补测试**【内容】为 0 测试文件（memory_consolidator/memory_contradiction/memory_self_editor/vector_store/dispatch_router/agent_loop_runner/canon_*/desktop_*）及 emotion_fusion.go 全部 17 函数补单测。【涉及文件】上述源文件及新建 *_test.go。【验收标准】新增用例 ≥45 个；emotion_fusion 函数覆盖 100%、其余 ≥90%（go test -cover）；go test -race 与 go vet 全绿。
  - **T6-5.2 错误可见化**【内容】memory_ingest.go:94-101、memory_consolidator.go:47-49 静默 return 改为 slog.Error+返回 error；whisper_handler.go:203/:759 的 `_=` 丢弃改为记日志。【涉及文件】internal/whisper/memory_ingest.go、memory_consolidator.go、internal/app/whisper_handler.go。【验收标准】grep 断言 4 处不再静默；失败路径单测断言日志捕获 ≥1 条 error；门禁全绿。
  - **T6-5.3 异步火忘可观测**【内容】记忆写协程（internal/app/whisper_handler.go:205-220）增加失败回传：orchestrator 暴露 WriteErrors() 计数，失败记 slog.Error。【涉及文件】internal/app/whisper_handler.go、记忆写入相关文件。【验收标准】注入失败单测断言错误计数 ≥1 且 error 日志出现；正常路径 -race 无数据竞争。
  - **T6-5.4 成人模式决策**【内容】推荐「文档化永久开启」：新增 ADULT_MODE.md，orchestrator.go:91 注释引用该文档；WhisperSetAdultMode 删除或显式记录「参数被忽略」。【涉及文件】internal/whisper/adult_mode.go、orchestrator.go、internal/app/whisper_handler.go、ADULT_MODE.md。【验收标准】决策文档存在且 orchestrator.go:91 注释引用；无静默忽略参数的代码路径。
  - **T6-5.5 db 细节收敛**【内容】database.go 去重 DSN/PRAGMA（保留 PRAGMA 循环为唯一来源）；GetDatabase 改为返回 (*sql.DB, error) 并同步全部调用方；V11 FTS 全量同步失败（internal/app/whisper_handler.go:759 链路）记日志。【涉及文件】internal/whisper/db/database.go 及调用方、internal/app/whisper_handler.go。【验收标准】每条 PRAGMA 在 database.go 仅出现 1 次（grep 断言）；签名变更后编译通过且调用方均处理 error；FTS 失败注入测试断言出现 error 日志。
  - **T6-5.6 陈旧占位清理**【内容】plan_document_intent.go:1 注释指向不存在的 plan_document.go——更新为真实目标或删除；paper_card_companion/desktop_mode_policy/desktop_opening 三个 merged 占位核实目标存在后删除。【涉及文件】上述 4 个占位文件。【验收标准】脚本断言包内「merged into/plan_document.go」注释目标均存在；删除后 go build ./... 通过。
- **明确不做**：不改记忆/情感算法语义、不换向量检索实现、不改 schema/V11 FTS 机制、不动前端与绑定面（无 gen_bindings 变更）。
- **风险与依赖**：补测需 mock LLM/sqlite 基建，若缺失依赖基础层先行；GetDatabase 签名变更波及全部调用方需同步；成人模式为产品决策，需与 ackem adult-mode.ts 对齐确认；写协程错误回传需 -race 验证；占位删除需确认无引用。

### 第六刀 v2.29.0「模型中心·密钥与 UI」（G6）

### 模型中心 + Herdsman（T6-6）

- **现状**：审查发现 4 处待修：refresh_token 明文落盘（internal/auth/token.go:78）、汇率 7.2 写死（internal/modelengine/stats.go:175）、probe 告警误打印 ConfigPath（internal/herdsman/probe.go:221）、页面 40+ 个 useState 且刷新/切分类竞态、XAI_VOICES 四处重复。
- **本轮目标**：refresh_token 与 DeepSeek key 同等 DPAPI 加密落盘、旧明文平滑迁移；汇率可配置实时生效；UI 按分类拆分（顶层 ≤15）且无竞态；XAI_VOICES 单源。验收：Go 全量 ok+vet 干净、tsc/eslint 0 errors、vitest 全量通过、绑定重生成+完备性测试绿。
- **迭代任务**（路径约定：Go 相对仓库根，前端相对 frontend/src）：
  - **T6-6.1 密钥一致性**【内容】internal/auth/token.go 读写经 internal/gaea/secure 的 DPAPI 加密（dpapi: 前缀），旧明文兼容读并自动重写加密。【涉及文件】internal/auth/token.go、token_test.go（扩展）。【验收标准】新增测试 ≥2（落盘无明文、旧明文迁移）；go test ./internal/auth/... + vet 干净；实测 .gaea_token.json 无明文。
  - **T6-6.2 汇率配置**【内容】usdToCny 改配置项（默认 7.2），汇总与 UsdToCny 透传读配置；ModelPanel 加汇率输入。【涉及文件】internal/modelengine/stats.go、stats_test.go、components/settings/ModelPanel.tsx。【验收标准】新增测试 ≥2（默认值、改 7.0 折算生效）；stats_test.go:64 断言改注入；gen_bindings 重生成+完备性测试绿；实测改汇率汇总即时变化。
  - **T6-6.3 告警文案**【内容】probe.go:221 改打印真实目录路径（filepath.Dir(ConfigPath)）。【涉及文件】internal/herdsman/probe.go、probe_test.go。【验收标准】新增断言 ≥1（含目录路径、不含 config.yaml 字样）；go test ./internal/herdsman/... + vet 干净。
  - **T6-6.4 UI 拆分与单源**【内容】40+ 个 useState 按 5 分类 Section 下沉，顶层仅留全局态；XAI_VOICES 仅 modelcenter/utils.tsx 定义，另两处改引用。【涉及文件】ModelCenterPage.tsx、modelcenter/utils.tsx、components/VoiceSettingsPanel.tsx、components/settings/ChatPanel.tsx。【验收标准】顶层 useState ≤15；grep XAI_VOICES 仅 1 处；tsc/eslint 0 errors、vitest 全量通过；实测五页签切换各 <300ms。
  - **T6-6.5 竞态修复**【内容】refreshLocalModels 加请求序号守卫，切分类后丢弃过期结果；5s 定时器随 category 重置。【涉及文件】ModelCenterPage.tsx。【验收标准】新增 vitest 竞态测试 ≥1（延迟 mock 下旧结果不覆盖新分类）；实测快速连切 5 分类 UI 与最终分类一致。
- **明确不做**：OAuth PKCE 流程改造（仅改存储）；汇率自动联网获取；引入状态管理库；XAI_VOICES 资产扩充；keep-warm 探针、换模预估纯函数、统计批量落盘+30 天裁剪不动。
- **风险与依赖**：迁移期解密失败会掉登录——保留旧明文回退读路径并重写；DPAPI 仅 Windows，非 Windows 需明文降级分支。T6-6.2 依赖绑定扩展需重生成；T6-6.4/T6-6.5 同文件强耦合建议同轮实施；汇率常量改动先破 stats_test.go 断言，须测试先行。

### 第七刀 v2.30.0「小说·导出与原子性」（G7）

### 小说创作（T6-7）

- **现状**：export 包零测试且实现粗糙（静默失败、硬编码作者、写盘不建父目录、两套分段器不一致、文件名不消毒），流式生成无中断无互斥，writeJSON 非原子，前端事件处理粗糙、20 组件仅 1 测试。
- **本轮目标**：导出四格式行为对齐且全量单测覆盖（新增 ≥12 Go 测试）；生成可一键停止并拒绝并发写同章；所有落盘原子化；CreatePage 拆分后 vitest 全绿。门禁：go test 全量 ok + vet 干净、tsc 0 errors、gen_bindings 重新生成且完备性测试通过。
- **迭代任务**：
  - **T6-7.1 导出整改与测试**【内容】internal/export/export.go:82-85 读取失败改 slog.Warn + 失败计数；:131 作者取自项目元数据；统一 chapterToHTML/markdownToHTML 为单一分段器；TXT 补世界观与 MD 对齐；各导出前建父目录；sanitizeFilename 增 Windows 保留名与尾部点/空格剔除。【涉及文件】internal/export/export.go、internal/export/html.go、internal/export/export_test.go（新建）。【验收标准】新增 ≥12 测试覆盖失败分支、保留名消毒、段落一致性、父目录创建；go test ./... ok 且 vet 干净。
  - **T6-7.2 生成中断与互斥**【内容】streamCreateChapter 改请求级 context.WithCancel；新增 CancelCreateChapter 绑定 + 前端停止按钮，取消时落盘已生成部分；按章节互斥，并发请求拒绝并返回明确错误。【涉及文件】internal/app/create_chapter_handler.go、internal/app/app.go、gen_bindings 输出。【验收标准】新增 ≥4 并发/取消测试（并发写同一 NNN.md 仅一次成功；取消后无写入）；绑定重新生成且完备性测试通过；实测取消响应 <1s。
  - **T6-7.3 落盘原子化**【内容】internal/project/project.go:428 writeJSON 及章节写入统一改临时文件 + os.Rename（同目录同卷保证原子），失败时清理临时文件、保留旧文件。【涉及文件】internal/project/project.go。【验收标准】新增 ≥3 单测（成功替换/覆盖已存在/写入失败不破坏原文件）；全量 go test ok。
  - **T6-7.4 模板替换精确化**【内容】create_chapter_handler.go:79 的 ReplaceAll("5000") 改为替换字数占位符，杜绝误伤模板中其他 "5000" 字样。【涉及文件】internal/app/create_chapter_handler.go、模板源文件。【验收标准】新增 ≥2 单测：模板含两处 "5000" 时仅字数位被替换；回归测试全绿。
  - **T6-7.5 前端拆分与状态收敛**【内容】CreatePage（791 行）按事件处理/生成表单/流式展示拆组件；(data as any) 字符串 switch 改 discriminated union 类型分发；Status 枚举补前端校验。【涉及文件】frontend/src/pages/CreatePage.tsx、frontend/src/components/novel/**、frontend/src/pages/CreatePage.test.tsx（新建）。【验收标准】新增 vitest ≥6；tsc 0 errors、vitest 全量通过；CreatePage 单文件 ≤300 行；「停止」按钮配合 T6-7.2 实测可中断。
- **明确不做**：不改生成质量与提示词效果；不做断点续传与进度持久化；不改 EPUB 排版样式与封面生成；不动大纲树重排、角色双库去重等亮点既有行为；不做导出目录选择 UI 增强。
- **风险与依赖**：EPUB 依赖第三方库，分段器统一后需回归 EPUB 输出（低风险）；取消实时性依赖 ai.ChatStream 对 ctx 取消的即时响应，需实测确认（依赖基础层 ai 包）；gen_bindings 变更影响前端类型，须与 T6-7.5 联调；并发互斥粒度需结合项目级多开模型评审（依赖基础层先行）。

### 第八刀 v2.31.0「记忆·生命周期与审计」（G8）

### 记忆中枢/知识库（T6-8）

- **现状**：自动做梦写入绕过 hardAskTools 审批、facts 只软删不清理、索引按 rune 硬切与 Block() 4096 字节双口径、GraphView 与 WhisperMemoryLibrary 零测试；三脑架构、审批门禁与预算分层落地良好（亮点保留）。
- **本轮目标**：dream 写入路径决策明确并全程可审计；facts 具备可度量的清理与归档上限；索引截断尊重行/markdown 链接边界并与 Block 预算统一口径；两个前端组件补齐测试。变更后 Go 全量 go test ok+vet 干净、前端 tsc 0 errors+vitest 全量通过、绑定面变动重新生成 gen_bindings。
- **迭代任务**：
  - **T6-8.1 dream 路径决策与审计**【内容】决策自动做梦（后台 goroutine 异步触发，90s 超时）与显式 /dream extract 是否走审批。默认放行：后台流程无法等待人工确认，/dream extract 本身即用户显式触发；但每次 SaveDreamFacts 落审计日志（source=auto_dream|explicit、条数），决策写入代码注释与文档；若改走审批则将 dream 写入纳入 hardAskTools。【涉及文件】internal/app/gaea_dream.go、internal/gaea/control/controller_memory.go、controller_approval.go。【验收标准】决策文档化；审计日志测试 ≥2（自动/显式两路径各断言 1 条审计行）；全量 go test ok+vet 干净。
  - **T6-8.2 facts 生命周期清理**【内容】为归档事实定保留策略与滚动清理（如归档超 90 天硬删或同源合并，溯源字段迁移至审计侧），ListArchived 加上限/分页防全量返回，清理任务可手动触发并留日志。【涉及文件】internal/gaea/memory/sqlite.go、store.go、migrate.go。【验收标准】新增清理与分页测试 ≥4；清理后表行数回落且 List 不受影响；若绑定面变更需 gen_bindings 重新生成+完备性测试；全量 go test ok+vet 干净。
  - **T6-8.3 索引截断按边界**【内容】capMemoryIndex 改为按行/段落边界截断且不切断 markdown 链接（]( 成对保护），与 Block() 的 4096 字节阈值统一口径（统一为字节，或显式双口径注释并存）。【涉及文件】internal/memory/memory.go、recall.go。【验收标准】单元测试 ≥5：链接完整性、段落截断、预算内字节上限、rune≠byte 边界（中文/emoji）；注入上下文 ≤ 预算的实测断言；既有 recall_test.go 预算用例回归通过；全量 go test ok+vet 干净。
  - **T6-8.4 前端组件补测**【内容】为 GraphView（212 行）与 WhisperMemoryLibrary（326 行）补 vitest：渲染、空态、数据驱动节点/列表、交互回调。【涉及文件】frontend/src/gaea/components/memoryhub/GraphView.tsx、WhisperMemoryLibrary.tsx 及新建 *.test.tsx。【验收标准】新增用例 ≥10 且两组件均覆盖；tsc 0 errors；vitest 全量通过；若 props/绑定类型变更需 gen_bindings 复核。
- **明确不做**：不做 bge-m3 增量向量化重写；不做三脑架构与跨脑接口重构；不做前端 UI 重设计；不处理会话归档（属会话模块）；不引入分布式存储。
- **风险与依赖**：T6-8.1 依赖产品定夺，若收紧需评估后台 90s 流程等待审批的可行性（可能阻塞自动整理）；T6-8.2 依赖 migration 版本管理先行，硬删前须确认溯源数据落审计侧；T6-8.3 口径统一可能影响召回质量，需以 recall 预算测试回归兜底；T6-8.4 依赖 mermaid/jsdom 兼容性，如不可行降级为纯逻辑+快照测试。

### 第九刀 v2.32.0「辅助合集·名实相符」（G9）

### 辅助合集（微信/OCR/配置/备份/安全）（T6-9）

- **现状**：微信零测试、凭证失效后 5 分钟空转且 Stop 不幂等；OCR「Windows 兜底」仅文案；配置写入非原子；CosyVoice 路径端口硬编码；token 经 URL 传参。
- **本轮目标**：微信经决策进入可维护状态（修复或冻结并文档化）；OCR 兜底名实相符；.gaea_config.json 原子写且损坏可恢复；路径端口全部可配置；token 不再经 URL 传输。门禁：Go 全包 ok+vet 干净，新增测试 ≥10 全绿。
- **迭代任务**：
  - T6-9.1 生命周期与失效自愈：【内容】Stop 幂等化，Start 支持重启；会话过期退出空转，置 sessionExpired 并暴露重新扫码钩子。【涉及文件】internal/channels/weixin/clawbot.go（:70-89,:167-172）。【验收标准】新增测试 ≥3：二次 Stop 不 panic、Stop 后重启恢复轮询、过期触发钩子不再空转；vet 干净。
  - T6-9.2 凭据与表治理：【内容】wxToken 改 DPAPI 加密（复用 API key 方案）；weixin_* 4 表确认用途，无用则随迁移删除。【涉及文件】internal/channels/weixin/manager.go（:22）、internal/whisper/db/schema_v9.go（:6-28）。【验收标准】grep 确认明文 wxToken 不落盘；DPAPI 加解密测试 ≥2；表变更过 schema 迁移测试。
  - T6-9.3 OCR 兜底名实相符：【内容】实现 Windows 原生 OCR 或删 single_prompt.go:43 文案；OvisOCR2 不可用降级而非报错；60s 未就绪 kill 子进程树防孤儿。【涉及文件】internal/office/docmd/docmd.go（:833-840,:894-900）、internal/office/single_prompt.go（:43）。【验收标准】新增测试 ≥2（超时杀进程、降级链路）；实测超时无 llama-server 残留。
  - T6-9.4 配置原子写：【内容】Save 改临时文件+fsync+rename；Load 遇 JSON 错误备份损坏文件并报可恢复错误。【涉及文件】internal/config/config.go（:813-815）。【验收标准】新增测试 ≥2（中断不截断、损坏恢复）；实测连写 100 次无半写文件。
  - T6-9.5 路径端口可配置：【内容】CosyVoice 目录/8010 端口、OvisOCR2 目录入配置；启动失败指数退避重试。【涉及文件】internal/app/tts_service.go（:15-18,:82-85）、internal/config/config.go。【验收标准】默认值不变、改配置可换机运行；实测启动失败自动重试 ≥2 次；测试 ≥2。
  - T6-9.6 token 改 header：【内容】客户端停用 URL 传 token，改 Authorization Bearer；服务端保留 header 校验、移除 query 兜底。【涉及文件】internal/httpbridge/bridge.go（:118-120）。【验收标准】grep 无 URL 传 token；测试 1：URL 带 token 被拒；常量时间比较测试保持通过。
- **明确不做**：微信多账号与前端扫码 UI 重做；OCR 换引擎/模型训练；备份、TTS 保活、dirSize 缓存、DPAPI 等亮点不重构（仅回归）；不做配置历史迁移工具。
- **风险与依赖**：修复/冻结需产品确认，冻结则 T6-9.1/T6-9.2 缩为文档化+回归；OCR 降级依赖 RapidOCR 链路健壮性；删表影响 schema 迁移链；DPAPI 依赖基础层先行；改绑定面需重跑 gen_bindings 并过完备性测试。

### 贯穿任务 + 第十刀 v2.33.0「前端收敛」（G10）

### 前端整体收敛（T6-10）

- **现状**：8 个巨型文件、315 处 any、漂移检查禁用、mock 虚构值被测试锁死、双桥接与遗留迁移并存。
- **本轮目标**：拆分后无单文件超 400 行；any 清零且门禁强制；漂移检查恢复并接绑定完备性测试；mock 契约与 Go 对齐；两零测试组件补覆盖，迁移一次性化。本文「门禁全绿」= tsc 0 errors + vitest 全量通过 + Go ok/vet 干净。
- **迭代任务**：
  - T6-10.1 巨型文件拆分：【内容】按职责拆分 8 个巨型文件至 ≤400 行/文件；mock.ts 按域拆分并落实 11 个 no-op。【涉及文件】frontend/src/pages/*、frontend/src/gaea/components/*、frontend/src/gaea/lib/mock.ts 拆分产物。【验收标准】脚本统计无超限文件；no-op 归零；门禁全绿。
  - T6-10.2 any 清零：【内容】eslint.config.js:31 no-explicit-any 恢复 error 进 CI，消灭 315 处 any；any 相关 as 断言清零。【涉及文件】frontend/src、eslint.config.js、.github/workflows/ci.yml。【验收标准】eslint 0 any；新增 any 即 CI 红；门禁全绿。
  - T6-10.3 漂移检查恢复：【内容】取消 bridge.ts:691-692 注释恢复 _CheckGenToApp 入 typecheck；绑定面变动走 gen_bindings 重生成 + 完备性对照测试。【涉及文件】frontend/src/gaea/lib/bridge.ts、frontend/src/api/bridge.ts、scripts/gen_bindings。【验收标准】typecheck 绿；完备性测试 ≥5；新增绑定未同步即 CI 红（演示一次）。
  - T6-10.4 mock 契约对齐：【内容】RetrievalEvalRun 按 Go 评估真实返回取样，改写 mock-contract-e5.test.ts:39-50 为契约校验；Models() ref 与 Go 引擎 ID 拼接一致；Meta label 对齐。【涉及文件】frontend/src/gaea/lib/mock.ts、mock-contract-e5.test.ts。【验收标准】后端回归可被契约测试暴露（演示一次）；门禁全绿。
  - T6-10.5 虚拟化与性能：【内容】Sidebar.tsx:265 改 react-window 虚拟滚动；CostLibraryView 补 memo；高频输入补 debounce ≥2 处（Composer 之外）。【涉及文件】Sidebar.tsx、CostLibraryView.tsx、Composer.tsx。【验收标准】1000 条滚动 ≥55fps（实测记录）；重渲染降 ≥50%（Profiler）；debounce 测试 ≥2。
  - T6-10.6 桥接归一：【内容】gaea/lib/bridge.ts 为主，api/bridge.ts 消费方迁移后删除；新增绑定单处注册。【涉及文件】两 bridge.ts 及消费方。【验收标准】删旧代理后门禁全绿；注册表单一。
  - T6-10.7 零测试组件与迁移：【内容】GraphView、WhisperMemoryLibrary（538 行）补行为测试；ChatPage 六旧键 + migrateLegacyTopics 改一次性（带版本号），失败记日志不吞掉重试。【涉及文件】GraphView.tsx、WhisperMemoryLibrary.tsx、ChatPage.tsx。【验收标准】两组件新增测试 ≥6、行覆盖 ≥60%；迁移后不再每次初始化执行（实测无重复写）；门禁全绿。
- **明确不做**：不重做 UI 视觉/主题；不引入新状态库；不迁 Wails v3；不逐一重构 5022 处 as 断言（仅清 any 相关）；不改 Go 业务逻辑；179 个 mock 方法不逐一重写实现。
- **风险与依赖**：T6-10.4 依赖检索测评/模型模块先给真实契约；T6-10.3 依赖 gen_bindings 流程可用；T6-10.5 需验 react-window 与滚动容器兼容；T6-10.6 依赖全部消费方迁移（跨前端子模块）；拆分可能回归，依赖行为测试基线先行绿。

## 3. 版本节奏

- **v2.24.0**：第一刀 基础层·可靠性（T6-1）——SSE/超时/代理/错误可见/日志脱敏
- **v2.25.0**：第二刀 办公引擎·正确性（T6-2）——docmd 分页/看门狗/禁写注册表/TCCA+evidence 补测
- **v2.26.0**：第三刀 对话·流可靠（T6-3）——订阅时序/超时/语音落库/迁移一次性/导出转义
- **v2.27.0**：第四刀 绘梦·链路真实生效（T6-4）——取消/flux/历史/格式/注入面/测试
- **v2.28.0**：第五刀 轻语·测试与可观测（T6-5）——补测 45+/错误可见/成人模式决策/db 收敛
- **v2.29.0**：第六刀 模型中心·密钥与 UI（T6-6）——token DPAPI/汇率配置/UI 拆分/竞态
- **v2.30.0**：第七刀 小说·导出与原子性（T6-7）——export 整改/生成中断/原子写/模板占位符
- **v2.31.0**：第八刀 记忆·生命周期与审计（T6-8）——dream 审计/facts 清理/索引边界/前端补测
- **v2.32.0**：第九刀 辅助合集·名实相符（T6-9）——微信决策/OCR 兜底/config 原子写/路径可配置/token header
- **v2.33.0**：第十刀 前端收敛（T6-10）——巨型组件拆分收尾/any 清零/漂移检查恢复/mock 契约化
- 前端收敛贯穿：每刀执行时预留 20-30% 预算拆本模块巨型组件（ChatPage/CreatePage/ImageGenPage/ModelCenterPage/Composer 等）与清理本模块 any
- 每刀独立发布（exe + SHA256SUMS + 冒烟 + 发布文档），可随时在刀间插入紧急修复
- 若带宽吃紧，优先保 T6-1（基础层）与 T6-2.1/T6-4.1（正确性缺陷），T6-10 可降级为仅恢复漂移检查

## 4. 明确不做（战略聚焦）

- 不加新功能板块（沿用阶段 0-5 原则；本阶段全部为既有模块修复）
- 不做 UI 重构/换肤/动效/布局打磨（巨型组件拆分属可维护性收敛，非视觉重构）
- 不动 herdsman 本体、不追新模型热点（先受控测评、再接入纪律不变）
- 不重写三脑记忆/引擎架构（修复不重构；docmd 拆分仅按职责切文件，行为不变）
- 不做云端功能与移动端（个人使用 + 移动端冻结）
- 不回溯修复历史脏数据（仅保证不再产生新脏数据）

## 5. 工程门禁（沿用阶段 0-5 既定 + 新增）

- Go：变更面逐包 go test + vet + gofmt；scripts/test-all.ps1 全量兜底；本阶段新增 -race 要求（并发改动模块：whisper/control/tasks）
- 前端：tsc -b、eslint 0 errors、vitest 全量、vite build
- 绑定面：新增方法后 `go run ./scripts/gen_bindings` + TestBindingsCompleteness 兜底
- 发布：scripts/smoke.ps1 冒烟；releases 保留最近 5 版；CHANGELOG/README/wails.json/build/windows/info.json/versioninfo.rc 同步
- **新增门禁 A（错误可见性）**：新代码不得新增静默吞错——后端无 `_ =` 丢弃错误、无无日志 return 错误路径；前端无空 catch、错误必经 LogFrontendError 或用户可见 notice（代码审查 + grep 抽查）
- **新增门禁 B（声称与实现对齐）**：每刀发布的 changelog 条目必须能在代码中定位到实现（自检清单，防止"看门狗"类声称落空）
- 沙箱铁律沿用：wails build 前端编译挂起 → `cd frontend && npm run build` + `wails build -s`；.ps1 脚本 UTF-8 带 BOM

## 6. 执行记录

- 2026-08-14：九板块独立子代理审查完成（9 份报告，全部带文件:行号证据）；本规划定稿（模块章节由 10 个规划子代理并行起草，证据逐一复核）；已登记 .gaea/progress.md。
- 执行顺序：每刀开始前，将本规划对应模块章节细化为执行任务清单（沿用阶段 5 做法：子代理并行实施 + 契约层独占 + gen_bindings 父代理统一执行）。
