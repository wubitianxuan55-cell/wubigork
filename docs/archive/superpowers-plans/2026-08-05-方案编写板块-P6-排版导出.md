# 方案编写板块重设计 P6 — 排版导出 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 docx 导出从「朴素标题+正文」升级为可配置排版引擎：封面/目录/标题层级/页眉页脚页码/Markdown 表格与代码块；按章节导出；暗标规则库（内置土壤修复通用规则，导出自动清理加粗/斜体/emoji/特殊字符）。

**Architecture:** `ExportOptions`（封面/目录/暗标规则/字号/行距）驱动 `renderDocx`；Markdown 解析为块（段落/标题/列表/表格/代码）后渲染；`ApplyDarkRule` 在渲染前清理内容；暗标规则存 office.db SchemaV5 `dark_rules` 表（可增删改）；按章节导出复用同一渲染器（子树装配）。

**Tech Stack:** Go 1.26 + gooxml + modernc.org/sqlite + React/TS/AntD + Wails v2

---

## 任务总览

| 任务 | 内容 | 提交 |
|---|---|---|
| 1 | docx 渲染器重构（封面/目录/页眉页脚/表格/代码块） | `feat(office): docx 排版引擎` |
| 2 | Markdown 块解析（md_blocks.go） | 并入 Task 1 提交 |
| 3 | 暗标规则库（SchemaV5 + ApplyDarkRule） | `feat(office): 暗标规则库` |
| 4 | 按章节导出 + 导出选项绑定 | `feat(office): 按章节导出与导出选项` |
| 5 | 前端导出设置/暗标规则管理/单章导出 | `feat(office): 导出设置前端` |
| 6 | 全量验证与 v1.19.0 发布 | `release: v1.19.0 排版导出` |

---

## Task 1+2: docx 渲染器与 Markdown 块解析

**Files:**
- Create: `internal/office/proposal/md_blocks.go`
- Create: `internal/office/proposal/export_docx.go`
- Create: `internal/office/proposal/export_docx_test.go`
- Modify: `internal/office/proposal/export.go`（ExportDocx 委托新引擎，保留兼容）

- [ ] **Step 1: 写失败测试**

```go
package proposal

import (
	"os"
	"testing"

	"github.com/carmel/gooxml/document"
)

func TestRenderDocx_CoverTOCAndTable(t *testing.T) {
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	proj, _ := s.store.EnsureDefaultProject()
	p, _ := s.store.Create("某修复投标方案", "soil-remediation-bid", "需求", "环保工程", proj.ID, []ProposalSection{
		{Title: "第一章 项目概况", Level: 1, Content: "项目位于某区。\n\n| 项目 | 内容 |\n|---|---|\n| 面积 | 10000 m² |"},
		{Title: "第二章 技术路线", Level: 1, Content: "采用固化稳定化工艺。"},
	})
	path, err := renderDocxToFile(p, ExportOptions{IncludeCover: true, IncludeTOC: true}, s.store.ExportDir())
	if err != nil {
		t.Fatalf("renderDocxToFile: %v", err)
	}
	doc, err := document.Open(path)
	if err != nil {
		t.Fatalf("打开 docx 失败: %v", err)
	}
	if len(doc.Tables()) != 1 {
		t.Fatalf("表格数 = %d, want 1", len(doc.Tables()))
	}
	text := ""
	for _, para := range doc.Paragraphs() {
		for _, run := range para.Runs() {
			text += run.Text()
		}
	}
	for _, want := range []string{"某修复投标方案", "第一章", "第二章", "1.1"} {
		if !containsAny(text, want) {
			t.Errorf("文档缺少 %q", want)
		}
	}
	_ = os.Remove(path)
}

func TestExportSectionDocx_OnlySubtree(t *testing.T) {
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	proj, _ := s.store.EnsureDefaultProject()
	p, _ := s.store.Create("方案", "blank", "", "其他", proj.ID, []ProposalSection{
		{Title: "第一章", Level: 1, Children: []ProposalSection{
			{ID: "secA", Title: "1.1 背景", Level: 2, Content: "背景内容"},
		}},
		{Title: "第二章", Level: 1, Content: "第二章内容"},
	})
	path, err := s.ExportSectionDocx(p.ID, "secA", ExportOptions{})
	if err != nil {
		t.Fatalf("ExportSectionDocx: %v", err)
	}
	doc, err := document.Open(path)
	if err != nil {
		t.Fatal(err)
	}
	text := ""
	for _, para := range doc.Paragraphs() {
		for _, run := range para.Runs() {
			text += run.Text()
		}
	}
	if !containsAny(text, "1.1 背景", "背景内容") {
		t.Fatalf("单章导出内容异常: %s", text)
	}
	if containsAny(text, "第二章内容") {
		t.Fatal("单章导出混入其他章节")
	}
	_ = os.Remove(path)
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run 'TestRenderDocx|TestExportSectionDocx' -v`
Expected: FAIL（编译错误）。

- [ ] **Step 3: 实现 md_blocks.go**

```go
// Package proposal — Markdown 块解析（供 docx 渲染）
package proposal

import "strings"

type mdBlock struct {
	kind  string // heading / para / list / table / code
	level int
	text  string
	rows  [][]string
	lang  string
}

func parseMarkdownBlocks(md string) []mdBlock {
	lines := strings.Split(md, "\n")
	var blocks []mdBlock
	var cur *mdBlock
	flush := func() {
		if cur != nil {
			blocks = append(blocks, *cur)
			cur = nil
		}
	}
	for _, raw := range lines {
		line := strings.TrimRight(raw, "\r")
		trimmed := strings.TrimSpace(line)
		if trimmed == "" {
			flush()
			continue
		}
		if strings.HasPrefix(trimmed, "```") {
			if cur != nil && cur.kind == "code" {
				flush()
				continue
			}
			flush()
			cur = &mdBlock{kind: "code", lang: strings.TrimPrefix(trimmed, "```")}
			continue
		}
		if cur != nil && cur.kind == "code" {
			cur.text += line + "\n"
			continue
		}
		if strings.HasPrefix(trimmed, "#") {
			flush()
			n := 0
			for n < len(trimmed) && trimmed[n] == '#' {
				n++
			}
			blocks = append(blocks, mdBlock{kind: "heading", level: n, text: strings.TrimSpace(trimmed[n:])})
			continue
		}
		if strings.HasPrefix(trimmed, "|") && strings.Contains(trimmed, "|") {
			cells := strings.Split(strings.Trim(trimmed, "|"), "|")
			var row []string
			for _, c := range cells {
				row = append(row, strings.TrimSpace(c))
			}
			allDash := true
			for _, c := range row {
				if strings.Trim(c, "-: ") != "" {
					allDash = false
					break
				}
			}
			if allDash {
				continue // 分隔行
			}
			if cur == nil || cur.kind != "table" {
				flush()
				cur = &mdBlock{kind: "table"}
			}
			cur.rows = append(cur.rows, row)
			continue
		}
		if strings.HasPrefix(trimmed, "- ") || strings.HasPrefix(trimmed, "* ") {
			flush()
			blocks = append(blocks, mdBlock{kind: "list", text: strings.TrimSpace(trimmed[2:])})
			continue
		}
		if cur == nil || cur.kind != "para" {
			flush()
			cur = &mdBlock{kind: "para"}
		}
		if cur.text != "" {
			cur.text += "\n"
		}
		cur.text += line
	}
	flush()
	return blocks
}
```

- [ ] **Step 4: 实现 export_docx.go**

```go
// Package proposal — docx 排版引擎
package proposal

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/carmel/gooxml/document"
	"github.com/carmel/gooxml/measurement"
	"github.com/carmel/gooxml/schema/soo/wml"
)

// ExportOptions 导出选项
type ExportOptions struct {
	IncludeCover bool
	IncludeTOC   bool
	DarkRuleID   string
	FontSize     float64 // 正文磅值，0=默认 12
	LineSpacing  float64 // 行距倍数，0=默认 1.5
}

// DefaultExportOptions 默认导出选项
func DefaultExportOptions() ExportOptions {
	return ExportOptions{IncludeCover: true, IncludeTOC: true, FontSize: 12, LineSpacing: 1.5}
}

// renderDocxToFile 渲染方案为 docx 并保存到 exports 目录
func renderDocxToFile(p *Proposal, opts ExportOptions, exportDir string) (string, error) {
	if err := os.MkdirAll(exportDir, 0755); err != nil {
		return "", err
	}
	path := filepath.Join(exportDir, p.ID+".docx")
	doc, err := buildDocx(p, opts)
	if err != nil {
		return "", err
	}
	if err := doc.SaveToFile(path); err != nil {
		return "", err
	}
	return path, nil
}

func buildDocx(p *Proposal, opts ExportOptions) (*document.Document, error) {
	doc := document.New()
	if opts.FontSize <= 0 {
		opts.FontSize = 12
	}
	if opts.LineSpacing <= 0 {
		opts.LineSpacing = 1.5
	}
	setupPage(doc)
	addHeaderFooter(doc, p.Title)
	if opts.IncludeCover {
		addCover(doc, p)
	}
	if opts.IncludeTOC {
		addTOC(doc, p)
	}
	darkRule, _ := getDarkRuleFor(p, opts.DarkRuleID)
	walk := func(ss []ProposalSection, prefix string, chapter int) {
		idx := 0
		for _, sec := range ss {
			idx++
			num, markdownLevel := numbering(sec, prefix, chapter, idx)
			content := sec.Content
			if darkRule != nil {
				content = darkRule.Apply(content)
			}
			heading := doc.AddParagraph()
			run := heading.AddRun()
			run.AddText(num + " " + sec.Title)
			pr := run.Properties()
			pr.SetBold(darkRule == nil || !darkRule.NoBold)
			pr.SetSize(measurement.Points(markdownSize(markdownLevel, opts.FontSize)))
			pr.SetFont("宋体")
			for _, block := range parseMarkdownBlocks(content) {
				renderBlock(doc, block, opts)
			}
			childChapter := chapter
			if sec.Level == 1 {
				childChapter = idx
			}
			walk(sec.Children, num, childChapter)
		}
	}
	walk(p.Sections, "", 0)
	return doc, nil
}
```

（辅助函数：`setupPage` 设置 A4 与页边距；`addHeaderFooter` 页眉方案标题 + 页脚 PAGE 字段；`addCover` 居中排版“方案标题/类型/日期”；`addTOC` 静态目录列表；`numbering` 复用 Assemble 编号；`renderBlock` 渲染段落/列表/表格/代码块，正文首行缩进与行距；`getDarkRuleFor` 从暗标规则库取规则。代码块以等宽字体渲染为段落；行内 `**bold**`/`*italic*`/`` `code` `` 在渲染段落时解析为对应 run 样式（暗标禁用时跳过加粗/斜体）。）

- [ ] **Step 5: ExportDocx 委托 + 章节导出**

`export.go` 的 `ExportDocx` 改为：

```go
// ExportDocx 导出方案为 Word（默认排版选项）
func (s *Service) ExportDocx(proposalID string) (string, error) {
	return s.ExportDocxWithOptions(proposalID, DefaultExportOptions())
}

// ExportDocxWithOptions 按选项导出方案
func (s *Service) ExportDocxWithOptions(proposalID string, opts ExportOptions) (string, error) {
	p, err := s.store.Get(proposalID)
	if err != nil {
		return "", err
	}
	return renderDocxToFile(p, opts, s.store.ExportDir())
}

// ExportSectionDocx 导出单章节（含子章节）
func (s *Service) ExportSectionDocx(proposalID, sectionID string, opts ExportOptions) (string, error) {
	p, err := s.store.Get(proposalID)
	if err != nil {
		return "", err
	}
	sec := findSectionByID(p.Sections, sectionID)
	if sec == nil {
		return "", fmt.Errorf("章节未找到: %s", sectionID)
	}
	mini := &Proposal{ID: p.ID + "-" + sectionID, Title: sec.Title, Template: p.Template, Sections: []ProposalSection{*sec}}
	return renderDocxToFile(mini, opts, s.store.ExportDir())
}
```

- [ ] **Step 6: 运行测试**

Run: `go test ./internal/office/proposal/ -run 'TestRenderDocx|TestExportSectionDocx' -v`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
gofmt -w internal/office/proposal/md_blocks.go internal/office/proposal/export_docx.go internal/office/proposal/export.go internal/office/proposal/export_docx_test.go
git add internal/office/proposal/md_blocks.go internal/office/proposal/export_docx.go internal/office/proposal/export.go internal/office/proposal/export_docx_test.go
git commit -m "feat(office): docx 排版引擎"
```

---

## Task 3: 暗标规则库

**Files:**
- Modify: `internal/office/db/schema.go`（SchemaV5 dark_rules）
- Modify: `internal/office/db/database.go`（migrations）
- Modify: `internal/office/db/database_test.go`
- Create: `internal/office/proposal/dark_rule.go`
- Modify: `internal/office/proposal/store.go`（dark_rules CRUD）
- Modify: `internal/office/proposal/store_test.go`、`internal/office/proposal/export_docx_test.go`

- [ ] **Step 1: 写失败测试**

```go
func TestDarkRuleSeedApplyAndStore(t *testing.T) {
	dbT := t.TempDir()
	st := newTestStoreAt(t, dbT)
	if err := st.SeedDarkRules(); err != nil {
		t.Fatalf("SeedDarkRules: %v", err)
	}
	rules, err := st.ListDarkRules()
	if err != nil || len(rules) == 0 {
		t.Fatalf("ListDarkRules: %v %+v", err, rules)
	}
	rule := rules[0]
	got := rule.Apply("正文 **加粗** 和 ~~删除~~ 与 🎉 emoji\n\n\n多余空行")
	if containsAny(got, "**", "~~", "🎉") {
		t.Fatalf("暗标清理失败: %q", got)
	}
}
```

`newTestStoreAt` 直接建 store（`officedb.GetDatabase(filepath.Join(dir,"office"))` 后 `NewStore`）。

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestDarkRuleSeedApplyAndStore -v`
Expected: FAIL。

- [ ] **Step 3: SchemaV5**

```sql
CREATE TABLE IF NOT EXISTS dark_rules (
  id          TEXT PRIMARY KEY,
  name        TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  options     TEXT NOT NULL DEFAULT '{}',
  enabled     INTEGER NOT NULL DEFAULT 1,
  created_at  TEXT NOT NULL DEFAULT '',
  updated_at  TEXT NOT NULL DEFAULT ''
);
```

`migrations` 追加 SchemaV5；`database_test.go` 表清单加 `dark_rules`。

- [ ] **Step 4: 实现 dark_rule.go**

```go
// Package proposal — 暗标规则库
package proposal

import (
	"encoding/json"
	"strings"
	"unicode"
)

// DarkRuleOptions 暗标格式限制
type DarkRuleOptions struct {
	NoBold       bool `json:"noBold"`
	NoItalic     bool `json:"noItalic"`
	NoUnderline  bool `json:"noUnderline"`
	NoColor      bool `json:"noColor"`
	NoEmoji      bool `json:"noEmoji"`
	NoSpecial    bool `json:"noSpecial"`
	NoEmptyLines bool `json:"noEmptyLines"`
}

// DarkRule 暗标规则
type DarkRule struct {
	ID          string          `json:"id"`
	Name        string          `json:"name"`
	Description string          `json:"description"`
	Options     DarkRuleOptions `json:"options"`
	Enabled     bool            `json:"enabled"`
}

// Apply 按规则清理 Markdown 内容
func (r DarkRule) Apply(md string) string {
	if r.Options.NoBold {
		md = strings.ReplaceAll(md, "**", "")
		md = strings.ReplaceAll(md, "__", "")
	}
	if r.Options.NoItalic {
		md = strings.ReplaceAll(md, "*", "")
		md = strings.ReplaceAll(md, "_", "")
	}
	if r.Options.NoUnderline {
		md = strings.ReplaceAll(md, "<u>", "")
		md = strings.ReplaceAll(md, "</u>", "")
		md = strings.ReplaceAll(md, "~~", "")
	}
	if r.Options.NoEmoji {
		md = stripEmoji(md)
	}
	if r.Options.NoSpecial {
		md = stripSpecial(md)
	}
	if r.Options.NoEmptyLines {
		lines := strings.Split(md, "\n")
		var out []string
		prevEmpty := false
		for _, l := range lines {
			empty := strings.TrimSpace(l) == ""
			if empty && prevEmpty {
				continue
			}
			out = append(out, l)
			prevEmpty = empty
		}
		md = strings.Join(out, "\n")
	}
	return md
}

func stripEmoji(s string) string {
	var b strings.Builder
	for _, r := range s {
		if r >= 0x1F300 && r <= 0x1FAFF || r >= 0x2600 && r <= 0x27BF {
			continue
		}
		b.WriteRune(r)
	}
	return b.String()
}

func stripSpecial(s string) string {
	var b strings.Builder
	for _, r := range s {
		if unicode.IsControl(r) {
			continue
		}
		b.WriteRune(r)
	}
	return b.String()
}

func marshalOptions(o DarkRuleOptions) string {
	data, _ := json.Marshal(o)
	return string(data)
}
```

`store.go`：`SeedDarkRules`（内置“土壤修复通用暗标规则”：NoBold/NoItalic/NoColor/NoEmoji/NoSpecial/NoEmptyLines=true，幂等 upsert）、`ListDarkRules`、`SaveDarkRule`、`DeleteDarkRule`。

`getDarkRuleFor`（export_docx.go）：从 store 按 ID 读规则；ID 为空返回 nil；`buildDocx` 中 `darkRule := getDarkRuleFor(...)` 返回 `*DarkRule`（store 查询失败回退 nil）。

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/office/proposal/ -run 'TestDarkRule|TestRenderDocx' -v`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
gofmt -w internal/office/db internal/office/proposal/dark_rule.go internal/office/proposal/store.go internal/office/proposal/store_test.go internal/office/proposal/export_docx.go internal/office/proposal/export_docx_test.go
git add internal/office/db internal/office/proposal/dark_rule.go internal/office/proposal/store.go internal/office/proposal/store_test.go internal/office/proposal/export_docx.go internal/office/proposal/export_docx_test.go
git commit -m "feat(office): 暗标规则库"
```

---

## Task 4: 按章节导出 + 绑定

**Files:**
- Modify: `internal/app/office_handler.go`（ProposalExportDocxWithOptions/ProposalExportSectionDocx/ProposalDarkRulesList/Save/Delete）
- Modify: `internal/app/office_handler_test.go`

- [ ] **Step 1: 绑定**

```go
func (a *officeState) ProposalExportDocxWithOptions(pid string, includeCover, includeTOC bool, darkRuleID string) (string, error) {
	opts := proposal.DefaultExportOptions()
	opts.IncludeCover = includeCover
	opts.IncludeTOC = includeTOC
	opts.DarkRuleID = darkRuleID
	return a.proposalSvc.ExportDocxWithOptions(pid, opts)
}

func (a *officeState) ProposalExportSectionDocx(pid, sid string) (string, error) {
	return a.proposalSvc.ExportSectionDocx(pid, sid, proposal.DefaultExportOptions())
}

func (a *officeState) ProposalDarkRulesList() []map[string]interface{} { ... }
func (a *officeState) ProposalDarkRuleSave(m map[string]interface{}) error { ... }
func (a *officeState) ProposalDarkRuleDelete(id string) error { ... }
```

`ProposalDarkRuleSave` 用 JSON 往返把 map 转为 `proposal.DarkRule` 后 `SaveDarkRule`。

- [ ] **Step 2: 测试**

追加绑定测试：导出带封面 docx 路径非空；暗标规则 Save/List/Delete 走通。

Run: `go test ./internal/app/...`
Expected: PASS。

- [ ] **Step 3: 提交**

```bash
gofmt -w internal/app/office_handler.go internal/app/office_handler_test.go
git add internal/app/office_handler.go internal/app/office_handler_test.go
git commit -m "feat(office): 按章节导出与导出选项绑定"
```

---

## Task 5: 前端导出设置

**Files:**
- Modify: `frontend/src/pages/OfficePage.tsx`（ExportTab 导出设置 Modal、暗标规则管理、单章导出）
- Regenerate: `frontend/wailsjs/go/app/App.d.ts`、`App.js`

- [ ] **Step 1: 重新生成绑定**

Run: `wails generate module`

- [ ] **Step 2: ExportTab**

状态：`expOpen`、`includeCover`、`includeTOC`、`darkRuleID`、`darkRules`、`darkOpen`、`secID`（单章导出选择）。

工具条增加：「导出设置」按钮（Modal：封面/目录 Switch + 暗标规则 Select + 「导出 Word」确认走 `ProposalExportDocxWithOptions`）、「单章导出」Select（章节列表）+ 按钮（`ProposalExportSectionDocx`）、「暗标规则」按钮（Modal：规则列表/新增/编辑/删除，选项 Checkbox）。

- [ ] **Step 3: 前端验证**

Run: `node .\node_modules\typescript\bin\tsc -b` 与 `node .\node_modules\vite\bin\vite.js build`
Expected: 0 errors + 成功。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/OfficePage.tsx frontend/wailsjs/go/app/App.d.ts frontend/wailsjs/go/app/App.js
git commit -m "feat(office): 导出设置前端"
```

---

## Task 6: 全量验证与 v1.19.0 发布

- [ ] **Step 1**: `go vet ./...` + `go test ./...` 全绿
- [ ] **Step 2**: `tsc -b` + `vite build` 成功
- [ ] **Step 3**: `wails build` 成功
- [ ] **Step 4**: 手工验收：导出带封面/目录/页码 docx（WPS/Word 打开检查）；暗标规则开启后无加粗/emoji；单章导出仅含该章节。
- [ ] **Step 5**: CHANGELOG `## v1.19.0「排版导出」` + `wails.json` 1.19.0
- [ ] **Step 6**: 提交 `release: v1.19.0 排版导出`

---

## 计划自检

- **Spec 覆盖**：§5.6 排版导出（docx 模板化/封面/目录/页码/页眉页脚/表格图片样式/暗标规则库/按章节导出）→ Task 1-5；§10 验收（暗标规则导出）→ Task 3/6。
- **范围控制**：图片（流程示意图）渲染暂以代码块文本呈现，渲染引擎后置；PDF 导出沿用现有 MD→其他通道。
- **兼容性**：`ExportDocx` 签名不变（默认选项）；`ExportOptions` 新增。
