# gaea 2.0 P1「模型路由」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立第一等公民的模型路由（每功能绑定 + 显式降级链 + 可观测），并把 1.x 散落的模型调用点收敛到路由，杜绝"绕过功能绑定回落全局/陈旧模型"（E03/E09/E10）。

**Architecture:** 在 `internal/app` 内新增 `routeModel(feature)` 路由器：降级链 = 功能绑定（`cfg.GetFeatureModel`）→ 全局活跃（`GetActiveEngine/GetActiveModel`）→ 首个可用引擎；每次路由发 `model.route` 事件。再把 novel 系（章节/分支/风格）、whisper 的直连 `a.cfg.Model` 调用点改为经 `routeModel`。前端在模型中心页展示"当前生效（来源）"。

**Tech Stack:** Go 1.26.3、Wails v2.13.0、React 18 + antd 5（前端小改）、`internal/ai` 客户端。

**前置事实（已核实）：**

- 1.x 已有功能级绑定：`config.KeyFunc{Novel,Whisper,Office,Gaea,Chat}Engine/Model`、`cfg.GetFeatureModel/SetFeatureModel`、`app.featureModelKeys`。
- 已收敛的调用点：`chat_handler.go:8`（chat）、`office_handler.go:448-450`（office，含 E10 注释）、`gaea_handler.go:124`（gaea bridge，E10 修复）、`whisper_handler.go:145`。
- 绕过点（直读 `a.cfg.Model` 或空模型）：`create_chapter_handler.go:133,139`（E03 本体）、`plot_branch_handler.go:99,218`、`copilot_handler.go:16`（CmdKEdit 无 EngineID 参数，列为已知限制）、`platform_handler.go:153`（style 分析）、`whisper_handler.go:22`（whisperState.Chat 空模型）。
- `ai.Client`：`ChatStream(ctx, *ChatRequest{Model, EngineID, Messages})`、`ChatSimpleStreamWithOptions(ctx, model, sys, user, ChatSimpleOptions{EngineID})`。
- 降级链参考：`GetActiveEngine`（client 空时返回 "xai"）、`GetActiveModel`（xai 回退 `cfg.Model`）。
- `core` 结构：`ctx/cfg/client/engineMgr`；测试用 `&core{cfg: ..., engineMgr: modelengine.NewManager("","")}` 构造（见 `feature_model_handler_test.go`）。

---

### Task 1: ModelRouter 核心（routeModel + 单元测试）

**Files:**
- Create: `internal/app/model_router.go`
- Create: `internal/app/model_router_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/app/model_router_test.go`：

```go
package app

import (
	"testing"

	"github.com/gaea/gaea/internal/config"
	"github.com/gaea/gaea/internal/modelengine"
)

func newRouterTestCore(t *testing.T) *core {
	t.Helper()
	home := t.TempDir()
	t.Setenv("USERPROFILE", home)
	t.Setenv("HOME", home)
	c := &core{cfg: &config.Config{Model: "grok-4.20"}, engineMgr: modelengine.NewManager("", "")}
	engines := []modelengine.EngineConfig{
		{ID: "herdsman", Enabled: true, Models: []modelengine.ModelInfo{{ID: "qwen3-8b"}}},
		{ID: "xai", Enabled: true, Models: []modelengine.ModelInfo{{ID: "grok-4.20"}}},
	}
	for _, e := range engines {
		if err := c.engineMgr.SaveEngine(e); err != nil {
			t.Fatalf("SaveEngine(%s): %v", e.ID, err)
		}
	}
	return c
}

// 功能绑定优先：绑定 novel → herdsman/qwen3-8b，即使 cfg.Model 是陈旧模型名。
func TestRouteModelFeatureBindingWins(t *testing.T) {
	c := newRouterTestCore(t)
	c.cfg.Model = "stale-xai-model"
	if err := c.SetFeatureModel("novel", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	eng, model, source := c.routeModel("novel")
	if eng != "herdsman" || model != "qwen3-8b" || source != "feature" {
		t.Fatalf("route = (%q,%q,%q)", eng, model, source)
	}
}

// 未绑定 → 全局活跃（client 为空时 = xai + cfg.Model）。
func TestRouteModelGlobalFallback(t *testing.T) {
	c := newRouterTestCore(t)
	eng, model, source := c.routeModel("novel")
	if eng != "xai" || model != "grok-4.20" || source != "global" {
		t.Fatalf("route = (%q,%q,%q)", eng, model, source)
	}
}

// 绑定引擎被禁用 → 降级全局。
func TestRouteModelDisabledBindingFallsBack(t *testing.T) {
	c := newRouterTestCore(t)
	if err := c.SetFeatureModel("novel", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	herd, _ := c.engineMgr.GetEngine("herdsman")
	herd.Enabled = false
	if err := c.engineMgr.SaveEngine(*herd); err != nil {
		t.Fatal(err)
	}
	eng, model, source := c.routeModel("novel")
	if eng != "xai" || model != "grok-4.20" || source != "global" {
		t.Fatalf("route = (%q,%q,%q)", eng, model, source)
	}
}

// 全局活跃引擎也不可用 → 首个启用引擎兜底。
func TestRouteModelFirstEnabledFallback(t *testing.T) {
	c := newRouterTestCore(t)
	xai, _ := c.engineMgr.GetEngine("xai")
	xai.Enabled = false
	if err := c.engineMgr.SaveEngine(*xai); err != nil {
		t.Fatal(err)
	}
	eng, _, source := c.routeModel("whisper")
	if eng != "herdsman" || source != "fallback" {
		t.Fatalf("route = (%q,%q)", eng, source)
	}
}

// 未知功能域：不 panic，降级链同全局。
func TestRouteModelUnknownFeatureFallsBack(t *testing.T) {
	c := newRouterTestCore(t)
	if _, _, source := c.routeModel("does-not-exist"); source != "global" {
		t.Fatalf("source = %q", source)
	}
}
```

- [ ] **Step 2: 运行确认失败**

```powershell
go test ./internal/app/ -run TestRouteModel -count=1
```

预期：编译失败（`c.routeModel undefined`）。

- [ ] **Step 3: 实现 `internal/app/model_router.go`**

```go
package app

// routeModel 解析功能域生效的 (engine, model, source)。
// 降级链：功能绑定 → 全局活跃 → 首个可用引擎。
// source: feature | global | fallback（供前端与诊断展示）。
func (c *core) routeModel(feature string) (engine, model, source string) {
	// 1. 功能绑定（引擎必须存在且启用）
	if eng, m := c.cfg.GetFeatureModel(feature); eng != "" && m != "" {
		if e, ok := c.engineMgr.GetEngine(eng); ok && e.Enabled {
			c.emitModelRoute(feature, eng, m, "feature")
			return eng, m, "feature"
		}
	}
	// 2. 全局活跃引擎（存在且启用）
	if eng := c.GetActiveEngine(); eng != "" {
		if e, ok := c.engineMgr.GetEngine(eng); ok && e.Enabled {
			model = c.GetActiveModel()
			if model == "" {
				if dm, err := c.engineMgr.GetDefaultModel(eng); err == nil {
					model = dm
				}
			}
			if model == "" && eng == "xai" {
				model = c.cfg.Model
			}
			c.emitModelRoute(feature, eng, model, "global")
			return eng, model, "global"
		}
	}
	// 3. 首个启用引擎兜底
	for _, e := range c.engineMgr.GetEngines() {
		if !e.Enabled {
			continue
		}
		m, _ := c.engineMgr.GetDefaultModel(e.ID)
		c.emitModelRoute(feature, e.ID, m, "fallback")
		return e.ID, m, "fallback"
	}
	c.emitModelRoute(feature, "", "", "")
	return "", "", ""
}

// emitModelRoute 发布路由决策（ctx 未启动时跳过，测试安全）。
func (c *core) emitModelRoute(feature, engine, model, source string) {
	if c.ctx == nil {
		return
	}
	c.emit("model.route", map[string]interface{}{
		"feature": feature, "engine": engine, "model": model, "source": source,
	})
}
```

- [ ] **Step 4: 运行测试确认通过**

```powershell
go test ./internal/app/ -run TestRouteModel -count=1
```

预期：全部 PASS。

- [ ] **Step 5: 提交**

```powershell
git add internal/app/model_router.go internal/app/model_router_test.go
git commit -m "feat(app): ModelRouter 核心 routeModel（功能绑定→全局→首个可用，可观测 model.route 事件）"
```

---

### Task 2: 收敛 novel 系调用点（E03）

**Files:**
- Modify: `internal/app/create_chapter_handler.go:125-145`
- Modify: `internal/app/plot_branch_handler.go:90-105`、`210-225`
- Modify: `internal/app/platform_handler.go:145-160`
- 检查：`internal/app/outline_handler.go`（outlineAgent 模型来源）

- [ ] **Step 1: 收敛 create_chapter_handler**

在 `create_chapter_handler.go` 中，把 `// 发送 AI 请求` 附近的 `a.emit("xai-output", ...)` 与 `a.client.ChatStream(...)` 改为使用路由结果。编辑后该区域应如下：

```go
		// 小说功能级模型绑定（E03：不再直读 a.cfg.Model，防止把陈旧模型名发给活跃引擎）
		eng, model, _ := a.routeModel("novel")
		a.emit("xai-output", map[string]interface{}{
			"type":   "request",
			"model":  model,
			"system": systemPrompt,
			"user":   currentPrompt,
		})

		chunks, err := a.client.ChatStream(a.ctx, &ai.ChatRequest{
			Model:    model,
			EngineID: eng,
			Messages: []ai.ChatMessage{{Role: "system", Content: systemPrompt}, {Role: "user", Content: currentPrompt}},
		})
```

注意：`model` 变量名若与函数内其他 `model` 冲突，改为 `featEng, featModel` 并同步下方引用。

- [ ] **Step 2: 收敛 plot_branch_handler 两处**

在 `plot_branch_handler.go` 的两处（约 99 行与 218 行），把：

```go
reply, err := a.client.ChatSimpleStream(a.ctx, a.cfg.Model, systemPrompt, userPrompt)
```

替换为：

```go
eng, model, _ := a.routeModel("novel")
reply, err := a.client.ChatSimpleStreamWithOptions(a.ctx, model, systemPrompt, userPrompt, ai.ChatSimpleOptions{EngineID: eng})
```

- [ ] **Step 3: 收敛 platform_handler（风格分析）**

在 `platform_handler.go:153` 附近，把：

```go
analyzer := style.NewAnalyzer(pm, a.client, a.cfg.Model)
```

替换为：

```go
_, featModel, _ := a.routeModel("novel")
analyzer := style.NewAnalyzer(pm, a.client, featModel)
```

- [ ] **Step 4: 检查 outlineAgent 模型来源**

```powershell
rg -n "outlineAgent|outline\.New|NewOutlineAgent" internal/app -g '*.go'
```

若 outlineAgent 在构造时传入 `a.cfg.Model`：改为在构造点先 `_, featModel, _ := a.routeModel("novel")` 再传入；若其模型来自其他机制，把结论写进提交说明，不强行改。

- [ ] **Step 5: 验证**

```powershell
go build ./...; go test ./internal/app/ -count=1
rg -n "a\.cfg\.Model" internal/app/create_chapter_handler.go internal/app/plot_branch_handler.go internal/app/platform_handler.go
```

预期：build/test 全绿；三文件不再直读 `a.cfg.Model`。

- [ ] **Step 6: 提交**

```powershell
git add internal/app/create_chapter_handler.go internal/app/plot_branch_handler.go internal/app/platform_handler.go
git commit -m "feat(app): novel 系 AI 调用收敛到 routeModel（章节/分支/风格，E03 不再回退全局陈旧模型）"
```

---

### Task 3: 收敛 whisper 调用点

**Files:**
- Modify: `internal/app/whisper_handler.go:20-24`

- [ ] **Step 1: 修改 whisperState.Chat**

把：

```go
func (a *whisperState) Chat(systemPrompt, userPrompt string) (string, error) {
	return a.client.ChatSimpleStream(a.ctx, "", systemPrompt, userPrompt)
}
```

替换为：

```go
func (a *whisperState) Chat(systemPrompt, userPrompt string) (string, error) {
	eng, model, _ := a.routeModel("whisper")
	return a.client.ChatSimpleStreamWithOptions(a.ctx, model, systemPrompt, userPrompt, ai.ChatSimpleOptions{EngineID: eng})
}
```

- [ ] **Step 2: 验证**

```powershell
go build ./...; go test ./internal/app/ ./internal/whisper/... -count=1
```

预期：全绿。

- [ ] **Step 3: 提交**

```powershell
git add internal/app/whisper_handler.go
git commit -m "feat(app): whisper 聊天收敛到 routeModel（不再空模型直连活跃引擎）"
```

---

### Task 4: E03/E09/E10 回归 + GetModelRoute 绑定 + 前端展示

**Files:**
- Create: `internal/app/regression_model_route_test.go`
- Modify: `internal/app/model_router.go`（追加绑定方法）
- Modify: `frontend/src/pages/ModelCenterPage.tsx`

- [ ] **Step 1: 写回归测试**

创建 `internal/app/regression_model_route_test.go`：

```go
package app

import (
	"strings"
	"testing"
)

// E03：小说功能绑定后，路由不得回退全局陈旧模型名。
func TestRegressionE03NovelBindingBeatsStaleGlobal(t *testing.T) {
	c := newRouterTestCore(t)
	c.cfg.Model = "stale-xai-model"
	if err := c.SetFeatureModel("novel", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	eng, model, _ := c.routeModel("novel")
	if eng != "herdsman" || model != "qwen3-8b" {
		t.Fatalf("E03 回归失败: (%q,%q)", eng, model)
	}
}

// E09：方案功能绑定后，office 路由使用绑定模型。
func TestRegressionE09OfficeBindingEffective(t *testing.T) {
	c := newRouterTestCore(t)
	c.cfg.Model = "stale-xai-model"
	if err := c.SetFeatureModel("office", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	_, model, source := c.routeModel("office")
	if model != "qwen3-8b" || source != "feature" {
		t.Fatalf("E09 回归失败: (%q,%q)", model, source)
	}
}

// E10：办公 bridge 功能绑定后，gaea 路由使用绑定模型。
func TestRegressionE10GaeaBindingEffective(t *testing.T) {
	c := newRouterTestCore(t)
	c.cfg.Model = "stale-xai-model"
	if err := c.SetFeatureModel("gaea", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	_, model, source := c.routeModel("gaea")
	if model != "qwen3-8b" || source != "feature" {
		t.Fatalf("E10 回归失败: (%q,%q)", model, source)
	}
}

// 路由事件可观测：source 字段区分 feature/global/fallback。
func TestRegressionModelRouteSourceLabels(t *testing.T) {
	c := newRouterTestCore(t)
	if err := c.SetFeatureModel("whisper", "herdsman", "qwen3-8b"); err != nil {
		t.Fatal(err)
	}
	_, _, source := c.routeModel("whisper")
	if source != "feature" {
		t.Fatalf("source = %q", source)
	}
	if !strings.Contains("feature global fallback", source) {
		t.Fatalf("非法 source: %q", source)
	}
}
```

- [ ] **Step 2: 追加 `GetModelRoute` 绑定**

在 `internal/app/model_router.go` 末尾追加：

```go
// GetModelRoute 返回功能域当前生效路由（Wails 绑定，前端模型中心展示用）。
func (a *App) GetModelRoute(feature string) (string, error) {
	eng, model, source := a.routeModel(feature)
	b, _ := json.Marshal(map[string]string{
		"feature": feature, "engine": eng, "model": model, "source": source,
	})
	return string(b), nil
}
```

若 `model_router.go` 尚未导入 `encoding/json`，在文件头加入 `"encoding/json"`。

- [ ] **Step 3: 运行回归测试**

```powershell
go test ./internal/app/ -run 'TestRegressionE03|TestRegressionE09|TestRegressionE10|TestRegressionModelRoute|TestRouteModel' -count=1
```

预期：全部 PASS。

- [ ] **Step 4: 前端模型中心展示"当前生效"**

在 `frontend/src/pages/ModelCenterPage.tsx` 中，找到功能绑定卡渲染处（`const bound = ...` 附近），为每个功能卡增加一行当前生效信息。在组件内新增状态与加载逻辑：

```tsx
const [routes, setRoutes] = useState<Record<string, { engine: string; model: string; source: string }>>({});
useEffect(() => {
  let alive = true;
  (async () => {
    const bind = (window as any).go?.app?.App;
    if (!bind?.GetModelRoute) return;
    const next: Record<string, { engine: string; model: string; source: string }> = {};
    for (const f of FEATURES) {
      try {
        next[f.key] = JSON.parse(await bind.GetModelRoute(f.key));
      } catch { /* 忽略单功能失败 */ }
    }
    if (alive) setRoutes(next);
  })();
  return () => { alive = false; };
}, []);
```

在功能卡内（`bound ? \`当前：...\` : '尚未绑定...'` 那一行之后）追加：

```tsx
{routes[f.key] && (
  <Typography.Text style={{ color: C('color-text-secondary'), fontSize: 11, display: 'block' }}>
    当前生效：{routes[f.key].engine || '-'} / {routes[f.key].model || '-'}（{routes[f.key].source || '-'}）
  </Typography.Text>
)}
```

说明：`FEATURES` 为文件中已存在的功能清单常量；若常量名不同（如 `featureList`），用实际名称。该展示依赖 Wails 重新生成 `GetModelRoute` 绑定（`wails build` 时生成），tsc 阶段使用 `(window as any)` 规避类型。

- [ ] **Step 5: 前端验证 + 提交**

```powershell
Set-Location frontend
npx.cmd tsc -b
Set-Location ..
go build ./...
```

预期：tsc 与 go build 全绿。

```powershell
git add internal/app/model_router.go internal/app/regression_model_route_test.go frontend/src/pages/ModelCenterPage.tsx
git commit -m "feat: E03/E09/E10 模型路由回归 + GetModelRoute 绑定 + 模型中心当前生效展示"
```

---

### Task 5: 全量验证与记录

**Files:**
- Modify: `.gaea/progress.md`

- [ ] **Step 1: CI 闸门**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci.ps1
```

预期：`CI OK`，`git status --short` 为空。

- [ ] **Step 2: 记录进度**

在 `.gaea/progress.md` 的 P0 段落下追加：

```markdown
## 2.0 P1 模型路由（✅ 已完成）

- routeModel 降级链：功能绑定 → 全局活跃 → 首个可用引擎；model.route 事件可观测
- novel 系收敛：章节/分支/风格（E03）；whisper 收敛（空模型直连修复）
- office/gaea 绑定回归（E09/E10）；GetModelRoute 绑定 + 模型中心"当前生效"展示
- 已知限制：CmdKEdit 无 EngineID 参数（模型名已路由，引擎随活跃引擎）
```

- [ ] **Step 3: 提交**

```powershell
git add .gaea/progress.md
git commit -m "docs: P1 模型路由完成记录"
```

---

## 验收清单

| 项 | 判据 |
|----|------|
| 路由 | 功能绑定 > 全局 > 首个可用，source 可区分，`model.route` 事件 |
| 不绕过 | novel/whisper 调用点不再直读 `a.cfg.Model` 或空模型（rg 无命中） |
| 回归 | E03/E09/E10 测试通过；既有 app/whisper 测试全绿 |
| 绑定 | `GetModelRoute` 可用，模型中心展示当前生效与来源 |
| 全量 | `scripts/ci.ps1` 输出 CI OK，工作区干净 |
