# gaea 2.0 P5「发布 2.0.1」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 以 v1.21.0 + P0–P4 基线发布 2.0.1：版本号、CHANGELOG、发布文档与校验和、备份脚本，全量验收。

**Architecture:** 版本号三处（wails.json / versioninfo.rc / CHANGELOG）→ 发布文档 `releases/v2.0.1.md`（含 `wails build` 产物 SHA256）→ 新增 `scripts/backup.ps1` 数据备份（whisper_data / novels / office / 配置）→ 全量 CI。

**Tech Stack:** Go 1.26.3、Wails v2.13.0、PowerShell。

**前置事实（已核实）：**

- `wails.json` `info.productVersion = "1.21.0"`；`versioninfo.rc` 为 `1,13,0,0`（陈旧）。
- `CHANGELOG.md` 最新条目 `v1.21.0`。
- 1.x 数据目录：`whisper_data/`（仓库内）、用户配置 `.gaea_config.json`（用户目录）、小说 `novels/`（gitignore）、方案 `office/` 位于 whisper_data 下。

---

### Task 1: 版本号升级

**Files:**
- Modify: `wails.json`
- Modify: `versioninfo.rc`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: wails.json**

把 `"productVersion": "1.21.0"` 改为 `"productVersion": "2.0.1"`。

- [ ] **Step 2: versioninfo.rc**

把 `FILEVERSION 1,13,0,0` / `PRODUCTVERSION 1,13,0,0` 改为 `2,0,1,0`，`VALUE "FileVersion", "1.13.0"` 改为 `"2.0.1"`。

- [ ] **Step 3: CHANGELOG 顶部插入 v2.0.1 条目**

```markdown
## v2.0.1「三脑底座」(2026-08-07)

> 在 v1.21.0 基础上迭代：模型路由、三脑记忆、主脑可选编排、基线加固。

- 基线：frontend/package.json 纳入版本控制；scripts/ci.ps1 闸门；E01-E24 评估集 + 首批回归
- 模型：routeModel 降级链（功能绑定→全局→首个可用）；novel/whisper 调用收敛；模型中心"当前生效"
- 记忆：BrainStore 三脑统一访问 + brain_links 跨脑关联；记忆中枢三脑检索
- 编排：ModuleRegistry + RunModule + MainBrainChat（可选入口，不经由模块直达路径）
- 互联：方案生成自动注入跨脑记忆；3.0 模块协议文档预留
- 验证：go build/vet/test 全绿 + tsc + vite build + wails build（见 releases/v2.0.1.md）
```

- [ ] **Step 4: 提交**

```powershell
git add wails.json versioninfo.rc CHANGELOG.md
git commit -m "release: 版本号升级 2.0.1（wails/versioninfo/CHANGELOG）"
```

---

### Task 2: 发布文档与校验和

**Files:**
- Create: `releases/v2.0.1.md`
- Create: `releases/SHA256SUMS-v2.0.1.txt`（wails build 后生成）

- [ ] **Step 1: 写发布文档**

创建 `releases/v2.0.1.md`（内容：版本摘要、P0–P4 交付清单、安装说明、校验和命令）。

- [ ] **Step 2: wails build**

```powershell
$env:TMP = 'C:\AI\wubigrok\.tmp'; $env:TEMP = $env:TMP
wails build
```

若本机无 wails CLI：记录 `wails build` 为发布前置步骤，`SHA256SUMS-v2.0.1.txt` 以占位说明代替（不伪造校验和）。

- [ ] **Step 3: 生成校验和（构建成功时）**

```powershell
Get-FileHash 'build\bin\gaea.exe' -Algorithm SHA256 | ForEach-Object { "$($_.Hash.ToLower())  gaea.exe" } | Set-Content 'releases\SHA256SUMS-v2.0.1.txt'
```

- [ ] **Step 4: 提交**

```powershell
git add releases/v2.0.1.md releases/SHA256SUMS-v2.0.1.txt
git commit -m "release: 2.0.1 发布文档与校验和"
```

---

### Task 3: 备份脚本

**Files:**
- Create: `scripts/backup.ps1`

- [ ] **Step 1: 写脚本**

创建 `scripts/backup.ps1`：

```powershell
# gaea 2.0.1 数据备份：whisper_data / novels / office / 用户配置 → 时间戳目录
$ErrorActionPreference = 'Stop'
$root = Split-Path -Parent $PSScriptRoot
$stamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$out = Join-Path $root ("backups\" + $stamp)
New-Item -ItemType Directory -Force -Path $out | Out-Null

$items = @(
  (Join-Path $root 'whisper_data'),
  (Join-Path $root 'novels'),
  (Join-Path $root '.gaea_config.json')
)
foreach ($src in $items) {
  if (Test-Path -LiteralPath $src) {
    Copy-Item -LiteralPath $src -Destination $out -Recurse -Force
    Write-Output "backed up $src"
  }
}
Write-Output "备份完成：$out"
```

- [ ] **Step 2: 运行验证**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\backup.ps1
```

预期：输出备份目录，目录内包含 whisper_data 等。

- [ ] **Step 3: 提交**

```powershell
git add scripts/backup.ps1
git commit -m "ops: 数据备份脚本 scripts/backup.ps1（whisper/novels/配置 → 时间戳目录）"
```

---

### Task 4: 全量验收与记录

- [ ] **Step 1: CI 闸门**：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci.ps1`，预期 `CI OK` 且工作区干净。
- [ ] **Step 2: 记录**：`.gaea/progress.md` 追加 P5 段（2.0.1 发布、备份脚本、校验和状态）。
- [ ] **Step 3: 提交**：`git commit -m "docs: P5 发布 2.0.1 完成记录"`
