# gaea 2.0 P2「三脑记忆」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 1.x 已有的"三脑"记忆存储（主脑画像 Hephaestus.db profile / 左脑办公记忆 facts / 右脑轻语 hermes.db）形式化为统一 `BrainStore`，新增跨脑关联索引与跨脑检索，实现"轻语记住 → 方案可用"的耦合场景，且现有数据零迁移。

**Architecture:** 在 `internal/app` 新增 `brain_store.go`：统一接口 `Read/Write/Search/Link/CrossRefs`，三个适配器分别包住现有存储（主脑=`gaea/memory.ProfileStore` + `knowledge.Store`；右脑=`whisper/db/repos` 事实函数；左脑=方案 `proposal.Service` + 主脑 facts），跨脑关联用新 `brain_links` 表（Hephaestus.db，`CREATE TABLE IF NOT EXISTS`，零迁移）。适配器面向最小接口，测试用 fake 注入。

**Tech Stack:** Go 1.26.3、modernc SQLite（经现有 `gaea/db`、`whisper/db`）、React 18 + antd 5（前端三脑区块）。

**前置事实（已核实）：**

- 1.x 记忆中枢已按三脑呈现：`internal/app/memory_hub.go:22` 注释"左脑办公记忆（Hephaestus.db facts）、主脑全局画像（profile）、知识库（knowledge）、右脑轻语记忆（hermes.db 只读）"。
- 主脑画像：`gaea/memory.ProfileStore`（`Save/Get/Delete/List`，Hephaestus.db `profile` 表）；`hubProfileStore()` 已接线。
- 知识库：`gaea/knowledge.Store`（`Save/Get/Delete/List/ReadAll`）。
- 右脑事实：`whisper/db/repos` 包级函数 `LoadFactsFromDB(dataRoot)` / `InsertFact(dataRoot, whisper.MemoryFact)` / `DeleteFactFromDB`；`whisper.MemoryFact` 含 Subject/Summary/Domain/Tier 等字段。
- 左脑方案：`proposal.Service.List() []Proposal`（ID/Title/Category/Requirements/BidSummary）。
- 办公记忆 facts：Hephaestus.db `facts` 表（`gaea/memory` SQLite 后端）。
- `App` 嵌入 `*core/*writingState/*mediaState/*whisperState/*officeState`，可直接取现有服务实例。

---

### Task 1: BrainStore 接口与核心

**Files:**
- Create: `internal/app/brain_store.go`
- Create: `internal/app/brain_store_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/app/brain_store_test.go`：

```go
package app

import (
	"strings"
	"testing"
)

func TestBrainStoreReadWriteSearch(t *testing.T) {
	mainFake := &fakeMainBrain{rows: map[string]string{}}
	rightFake := &fakeRightBrain{rows: map[string]string{}}
	leftFake := &fakeLeftBrain{rows: map[string]string{}}
	bs := &BrainStore{main: mainFake, left: leftFake, right: rightFake}

	if err := bs.Write("brain.right", "甲方A", "偏好", "保守报价"); err != nil {
		t.Fatal(err)
	}
	hits, err := bs.Search("甲方A 报价", "brain.right")
	if err != nil {
		t.Fatal(err)
	}
	if len(hits) == 0 || !strings.Contains(hits[0].Text, "保守报价") {
		t.Fatalf("hits = %+v", hits)
	}
}

func TestBrainStoreLinkAndCrossRefs(t *testing.T) {
	mainFake := &fakeMainBrain{rows: map[string]string{}}
	bs := &BrainStore{main: mainFake, left: &fakeLeftBrain{}, right: &fakeRightBrain{}}
	if err := bs.Link("甲方A", "brain.right", "fact-1"); err != nil {
		t.Fatal(err)
	}
	if err := bs.Link("甲方A", "brain.left", "proposal:p1"); err != nil {
		t.Fatal(err)
	}
	refs, err := bs.CrossRefs("甲方A")
	if err != nil {
		t.Fatal(err)
	}
	if len(refs) != 2 {
		t.Fatalf("refs = %+v", refs)
	}
}
```

同文件定义 fake 适配器（放在测试文件内）：

```go
type fakeMainBrain struct{ rows map[string]string }
func (f *fakeMainBrain) Read(entity string) ([]Fact, error) { return nil, nil }
func (f *fakeMainBrain) Write(entity, attribute, value string) error { f.rows[entity+"|"+attribute] = value; return nil }
func (f *fakeMainBrain) Search(query string) ([]Hit, error) { return nil, nil }
func (f *fakeMainBrain) Links() *LinkStore { return nil }

type fakeRightBrain struct{ rows map[string]string }
func (f *fakeRightBrain) Read(entity string) ([]Fact, error) { return nil, nil }
func (f *fakeRightBrain) Write(entity, attribute, value string) error { f.rows[entity+"|"+attribute] = value; return nil }
func (f *fakeRightBrain) Search(query string) ([]Hit, error) {
	for k, v := range f.rows {
		if strings.Contains(k, query) || strings.Contains(v, query) {
			return []Hit{{Brain: "brain.right", Entity: strings.SplitN(k, "|", 2)[0], Text: v}}, nil
		}
	}
	return nil, nil
}

type fakeLeftBrain struct{ rows map[string]string }
func (f *fakeLeftBrain) Read(entity string) ([]Fact, error) { return nil, nil }
func (f *fakeLeftBrain) Write(entity, attribute, value string) error { return nil }
func (f *fakeLeftBrain) Search(query string) ([]Hit, error) { return nil, nil }
```

- [ ] **Step 2: 运行确认失败**

```powershell
go test ./internal/app/ -run TestBrainStore -count=1
```

预期：编译失败（`BrainStore` undefined）。

- [ ] **Step 3: 实现 `internal/app/brain_store.go`**

```go
package app

import "strings"

// Brain 命名空间（与 1.x 记忆中枢三脑架构一致）。
const (
	BrainMain  = "brain.main"
	BrainLeft  = "brain.left"
	BrainRight = "brain.right"
)

// Fact 是跨脑统一事实。
type Fact struct {
	Brain     string `json:"brain"`
	Entity    string `json:"entity"`
	Attribute string `json:"attribute"`
	Value     string `json:"value"`
}

// Hit 是跨脑检索命中。
type Hit struct {
	Brain  string  `json:"brain"`
	Entity string  `json:"entity"`
	Text   string  `json:"text"`
	Score  float64 `json:"score"`
}

// Ref 是主脑跨脑关联引用。
type Ref struct {
	Brain string `json:"brain"`
	Ref   string `json:"ref"`
}

// brainAdapter 是单脑适配器的最小接口（测试可注入 fake）。
type brainAdapter interface {
	Read(entity string) ([]Fact, error)
	Write(entity, attribute, value string) error
	Search(query string) ([]Hit, error)
}

// BrainStore 三脑统一访问层。
type BrainStore struct {
	main  brainAdapter
	left  brainAdapter
	right brainAdapter
	links *LinkStore
}

func (b *BrainStore) Read(brain, entity string) ([]Fact, error) {
	ad := b.adapter(brain)
	if ad == nil {
		return []Fact{}, nil
	}
	return ad.Read(entity)
}

func (b *BrainStore) Write(brain, entity, attribute, value string) error {
	ad := b.adapter(brain)
	if ad == nil {
		return nil
	}
	return ad.Write(entity, attribute, value)
}

// Search 跨脑检索；brains 为空 = 三脑全搜。
func (b *BrainStore) Search(query string, brains ...string) ([]Hit, error) {
	names := brains
	if len(names) == 0 {
		names = []string{BrainMain, BrainLeft, BrainRight}
	}
	var out []Hit
	for _, n := range names {
		ad := b.adapter(n)
		if ad == nil {
			continue
		}
		hits, err := ad.Search(query)
		if err != nil {
			return nil, err
		}
		out = append(out, hits...)
	}
	return out, nil
}

func (b *BrainStore) Link(entity, brain, ref string) error {
	if b.links == nil {
		return nil
	}
	return b.links.Add(entity, brain, ref)
}

func (b *BrainStore) CrossRefs(entity string) ([]Ref, error) {
	if b.links == nil {
		return []Ref{}, nil
	}
	return b.links.ListByEntity(entity)
}

func (b *BrainStore) adapter(brain string) brainAdapter {
	switch brain {
	case BrainMain:
		return b.main
	case BrainLeft:
		return b.left
	case BrainRight:
		return b.right
	}
	return nil
}

// matchAny 供适配器做朴素关键词匹配。
func matchAny(query, texts ...string) bool {
	for _, t := range texts {
		if strings.Contains(t, query) {
			return true
		}
	}
	return false
}
```

- [ ] **Step 4: 运行测试确认通过**

```powershell
go test ./internal/app/ -run TestBrainStore -count=1
```

预期：全部 PASS。

- [ ] **Step 5: 提交**

```powershell
git add internal/app/brain_store.go internal/app/brain_store_test.go
git commit -m "feat(app): BrainStore 三脑统一接口（Read/Write/Search/Link/CrossRefs）"
```

---

### Task 2: 主脑适配器 + 跨脑关联索引

**Files:**
- Create: `internal/app/brain_main.go`
- Create: `internal/app/brain_link_store.go`
- Create: `internal/app/brain_main_test.go`

- [ ] **Step 1: 实现 LinkStore（brain_links 表）**

创建 `internal/app/brain_link_store.go`：

```go
package app

import (
	"database/sql"
)

// LinkStore 主脑跨脑关联索引（Hephaestus.db brain_links 表，零迁移建表）。
type LinkStore struct {
	db *sql.DB
}

func NewLinkStore(db *sql.DB) *LinkStore {
	if db == nil {
		return &LinkStore{}
	}
	_, _ = db.Exec(`CREATE TABLE IF NOT EXISTS brain_links (
		entity TEXT NOT NULL,
		brain TEXT NOT NULL,
		ref TEXT NOT NULL,
		created_at TEXT NOT NULL DEFAULT (datetime('now')),
		PRIMARY KEY (entity, brain, ref)
	)`)
	return &LinkStore{db: db}
}

func (l *LinkStore) Add(entity, brain, ref string) error {
	if l.db == nil {
		return nil
	}
	_, err := l.db.Exec(
		`INSERT OR IGNORE INTO brain_links(entity, brain, ref) VALUES(?,?,?)`,
		entity, brain, ref)
	return err
}

func (l *LinkStore) ListByEntity(entity string) ([]Ref, error) {
	if l.db == nil {
		return []Ref{}, nil
	}
	rows, err := l.db.Query(`SELECT brain, ref FROM brain_links WHERE entity = ? ORDER BY brain, ref`, entity)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Ref
	for rows.Next() {
		var r Ref
		if err := rows.Scan(&r.Brain, &r.Ref); err != nil {
			return nil, err
		}
		out = append(out, r)
	}
	return out, rows.Err()
}
```

- [ ] **Step 2: 实现主脑适配器**

创建 `internal/app/brain_main.go`：

```go
package app

import (
	"github.com/gaea/gaea/internal/gaea/knowledge"
	"github.com/gaea/gaea/internal/gaea/memory"
)

// mainBrain 主脑：全局画像（profile）+ 知识库（knowledge）。
type mainBrain struct {
	profile *memory.ProfileStore
	kb      *knowledge.Store
}

func (m *mainBrain) Read(entity string) ([]Fact, error) {
	var out []Fact
	if m.profile != nil {
		if mem, ok := m.profile.Get(entity); ok {
			out = append(out, Fact{Brain: BrainMain, Entity: mem.Name, Attribute: "profile", Value: mem.Body})
		}
	}
	if m.kb != nil {
		if e, err := m.kb.Get(entity); err == nil && e != nil {
			out = append(out, Fact{Brain: BrainMain, Entity: e.Name, Attribute: "knowledge", Value: e.Body})
		}
	}
	return out, nil
}

func (m *mainBrain) Write(entity, attribute, value string) error {
	if m.profile == nil {
		return nil
	}
	return m.profile.Save(memory.Memory{Name: entity, Title: attribute, Description: value, Type: "user", Body: value})
}

func (m *mainBrain) Search(query string) ([]Hit, error) {
	var out []Hit
	if m.profile != nil {
		if all, err := m.profile.List(); err == nil {
			for _, mem := range all {
				if matchAny(query, mem.Name, mem.Title, mem.Description, mem.Body) {
					out = append(out, Hit{Brain: BrainMain, Entity: mem.Name, Text: mem.Body})
				}
			}
		}
	}
	if m.kb != nil {
		for _, s := range m.kb.List() {
			if matchAny(query, s.Name, s.Title, s.Category) {
				out = append(out, Hit{Brain: BrainMain, Entity: s.Name, Text: s.Title})
			}
		}
	}
	return out, nil
}
```

说明：`memory.Memory` 与 `knowledge.EntrySummary` 的字段名以 `internal/gaea/memory`、`internal/gaea/knowledge` 实际定义为准（`memory_hub.go` 有构造示例）；若有出入，最小修正字段名。

- [ ] **Step 3: 写主脑测试（真实 SQLite）**

创建 `internal/app/brain_main_test.go`：

```go
package app

import (
	"testing"

	gaeadb "github.com/gaea/gaea/internal/gaea/db"
	"github.com/gaea/gaea/internal/gaea/memory"
)

func TestMainBrainProfileRoundTrip(t *testing.T) {
	dir := t.TempDir()
	gdb := gaeadb.GetDatabase(dir)
	if gdb == nil {
		t.Fatal("gaea db unavailable")
	}
	mb := &mainBrain{profile: memory.NewProfileStore(gdb), kb: nil}
	if err := mb.Write("甲方A", "偏好", "保守报价"); err != nil {
		t.Fatal(err)
	}
	hits, err := mb.Search("保守报价")
	if err != nil || len(hits) == 0 {
		t.Fatalf("hits=%+v err=%v", hits, err)
	}
}

func TestLinkStoreRoundTrip(t *testing.T) {
	dir := t.TempDir()
	gdb := gaeadb.GetDatabase(dir)
	ls := NewLinkStore(gdb)
	if err := ls.Add("甲方A", BrainRight, "fact-1"); err != nil {
		t.Fatal(err)
	}
	if err := ls.Add("甲方A", BrainLeft, "proposal:p1"); err != nil {
		t.Fatal(err)
	}
	refs, err := ls.ListByEntity("甲方A")
	if err != nil || len(refs) != 2 {
		t.Fatalf("refs=%+v err=%v", refs, err)
	}
}
```

- [ ] **Step 4: 验证**

```powershell
go test ./internal/app/ -run 'TestBrainStore|TestMainBrain|TestLinkStore' -count=1
```

预期：全部 PASS。

- [ ] **Step 5: 提交**

```powershell
git add internal/app/brain_main.go internal/app/brain_link_store.go internal/app/brain_main_test.go
git commit -m "feat(app): 主脑适配器（画像+知识库）+ brain_links 跨脑关联索引（零迁移建表）"
```

---

### Task 3: 右脑适配器（轻语 hermes.db）

**Files:**
- Create: `internal/app/brain_right.go`
- Create: `internal/app/brain_right_test.go`

- [ ] **Step 1: 实现右脑适配器**

创建 `internal/app/brain_right.go`：

```go
package app

import (
	"github.com/gaea/gaea/internal/whisper"
	whisperdb "github.com/gaea/gaea/internal/whisper/db/repos"
)

// rightBrain 右脑：轻语 hermes.db 记忆事实（只做统一读写，不迁移数据）。
type rightBrain struct {
	dataRoot string
}

func (r *rightBrain) Read(entity string) ([]Fact, error) {
	var out []Fact
	for _, f := range whisperdb.LoadFactsFromDB(r.dataRoot) {
		if f.Subject == entity {
			out = append(out, Fact{Brain: BrainRight, Entity: f.Subject, Attribute: f.Subcategory, Value: f.Summary})
		}
	}
	return out, nil
}

func (r *rightBrain) Write(entity, attribute, value string) error {
	return whisperdb.InsertFact(r.dataRoot, whisper.MemoryFact{
		Domain:      "brain",
		Subcategory: attribute,
		Subject:     entity,
		Summary:     value,
		Weight:      1,
		Confidence:  1,
		Status:      "active",
		FactLayer:   "brain",
	})
}

func (r *rightBrain) Search(query string) ([]Hit, error) {
	var out []Hit
	for _, f := range whisperdb.LoadFactsFromDB(r.dataRoot) {
		if matchAny(query, f.Subject, f.Summary, f.Subcategory) {
			out = append(out, Hit{Brain: BrainRight, Entity: f.Subject, Text: f.Summary})
		}
	}
	return out, nil
}
```

说明：`whisper.MemoryFact` 的 `FactLayer` 字段若在 v1.21.0 类型中不存在，删除该字段赋值（以实际类型为准）。

- [ ] **Step 2: 写右脑测试（临时 hermes.db）**

创建 `internal/app/brain_right_test.go`：

```go
package app

import (
	"testing"

	whisperdb "github.com/gaea/gaea/internal/whisper/db"
)

func TestRightBrainWriteSearch(t *testing.T) {
	dir := t.TempDir()
	// 用 whisper db 初始化临时 hermes.db（路径语义与 whisperdb.LoadFactsFromDB 一致）
	_ = whisperdb.GetDatabase(dir)
	rb := &rightBrain{dataRoot: dir}
	if err := rb.Write("甲方A", "偏好", "保守报价"); err != nil {
		t.Fatal(err)
	}
	hits, err := rb.Search("报价")
	if err != nil || len(hits) == 0 {
		t.Fatalf("hits=%+v err=%v", hits, err)
	}
	if hits[0].Entity != "甲方A" {
		t.Fatalf("entity = %q", hits[0].Entity)
	}
}
```

说明：若 `whisper/db.GetDatabase` 的签名或初始化方式与假设不符，以 `internal/whisper/db/database.go` 实际 API 为准（右脑测试的核心是"临时目录 → 写入 → 读回"闭环）。

- [ ] **Step 3: 验证**

```powershell
go test ./internal/app/ -run 'TestRightBrain' -count=1
```

预期：PASS。

- [ ] **Step 4: 提交**

```powershell
git add internal/app/brain_right.go internal/app/brain_right_test.go
git commit -m "feat(app): 右脑适配器（轻语 hermes.db 事实统一读写，零迁移）"
```

---

### Task 4: 左脑适配器 + App 装配与绑定

**Files:**
- Create: `internal/app/brain_left.go`
- Create: `internal/app/brain_left_test.go`
- Modify: `internal/app/app.go`（装配 BrainStore）
- Modify: `internal/app/model_router.go` 附近追加绑定（或新建 `internal/app/brain_bindings.go`）

- [ ] **Step 1: 实现左脑适配器（面向最小接口）**

创建 `internal/app/brain_left.go`：

```go
package app

import "github.com/gaea/gaea/internal/office/proposal"

// leftSource 左脑数据源（测试可注入 fake；App 装配时用真实实现）。
type leftSource interface {
	ListProposals() ([]proposal.Proposal, error)
}

type leftBrain struct {
	src leftSource
}

func (l *leftBrain) Read(entity string) ([]Fact, error) {
	ps, err := l.src.ListProposals()
	if err != nil {
		return nil, err
	}
	var out []Fact
	for _, p := range ps {
		if p.Title == entity || p.ID == entity {
			out = append(out, Fact{Brain: BrainLeft, Entity: p.Title, Attribute: "proposal", Value: p.Requirements})
		}
	}
	return out, nil
}

func (l *leftBrain) Write(entity, attribute, value string) error {
	return nil // 左脑办公记忆以现有业务写入为准，BrainStore 不做直写
}

func (l *leftBrain) Search(query string) ([]Hit, error) {
	ps, err := l.src.ListProposals()
	if err != nil {
		return nil, err
	}
	var out []Hit
	for _, p := range ps {
		if matchAny(query, p.Title, p.Category, p.Requirements) {
			out = append(out, Hit{Brain: BrainLeft, Entity: p.Title, Text: p.Requirements})
		}
	}
	return out, nil
}
```

- [ ] **Step 2: 写左脑测试（fake 数据源）**

创建 `internal/app/brain_left_test.go`：

```go
package app

import (
	"testing"

	"github.com/gaea/gaea/internal/office/proposal"
)

type fakeLeftSource struct{ ps []proposal.Proposal }

func (f *fakeLeftSource) ListProposals() ([]proposal.Proposal, error) { return f.ps, nil }

func TestLeftBrainSearchProposals(t *testing.T) {
	lb := &leftBrain{src: &fakeLeftSource{ps: []proposal.Proposal{
		{ID: "p1", Title: "土壤修复标书", Category: "环保工程", Requirements: "按保守报价原则编制"},
	}}}
	hits, err := lb.Search("保守报价")
	if err != nil || len(hits) == 0 {
		t.Fatalf("hits=%+v err=%v", hits, err)
	}
	if hits[0].Entity != "土壤修复标书" {
		t.Fatalf("entity = %q", hits[0].Entity)
	}
}
```

- [ ] **Step 3: App 装配 BrainStore**

在 `internal/app/app.go` 的 `New` 尾部（`return &App{...}` 之前）新增：

```go
	// 三脑统一访问层（零迁移：适配器直接包现有存储）
	userDir := config.MemoryUserDir()
	gdb := gaeadb.GetDatabase(userDir)
	app.brain = &BrainStore{
		main:  &mainBrain{profile: memory.NewProfileStore(gdb), kb: kbStore},
		left:  &leftBrain{src: leftSrc},
		right: &rightBrain{dataRoot: whisperDataRoot},
		links: NewLinkStore(gdb),
	}
```

说明：`kbStore`（知识库 store）、`leftSrc`（包装 officeState 的 proposal Service 的 `ListProposals` 实现）、`whisperDataRoot`、`config.MemoryUserDir()` 等以 `app.go`/`memory_hub.go` 中已有变量与方法为准；若 `New` 函数已很长，把装配拆到独立方法 `a.initBrain()` 并在这里调用。`App` 结构体增加字段 `brain *BrainStore`。

- [ ] **Step 4: 追加绑定**

创建 `internal/app/brain_bindings.go`：

```go
package app

import (
	"encoding/json"
	"strings"
)

// BrainWrite 写入指定脑的事实（主脑/右脑可写；左脑为只读业务域）。
func (a *App) BrainWrite(brain, entity, attribute, value string) error {
	if a.brain == nil {
		return nil
	}
	return a.brain.Write(brain, entity, attribute, value)
}

// BrainSearch 跨脑检索（brains 逗号分隔，空 = 三脑全搜）。
func (a *App) BrainSearch(query, brains string) (string, error) {
	var names []string
	if brains != "" {
		for _, n := range strings.Split(brains, ",") {
			if n = strings.TrimSpace(n); n != "" {
				names = append(names, n)
			}
		}
	}
	var hits []Hit
	if a.brain != nil {
		var err error
		hits, err = a.brain.Search(query, names...)
		if err != nil {
			return "", err
		}
	}
	b, _ := json.Marshal(hits)
	return string(b), nil
}

// BrainCrossRefs 返回实体的跨脑关联。
func (a *App) BrainCrossRefs(entity string) (string, error) {
	var refs []Ref
	if a.brain != nil {
		var err error
		refs, err = a.brain.CrossRefs(entity)
		if err != nil {
			return "", err
		}
	}
	b, _ := json.Marshal(refs)
	return string(b), nil
}
```

- [ ] **Step 5: 验证**

```powershell
go build ./...; go test ./internal/app/ -run 'TestBrain|TestLeftBrain|TestRightBrain|TestMainBrain|TestLinkStore' -count=1
```

预期：全绿。

- [ ] **Step 6: 提交**

```powershell
git add internal/app/brain_left.go internal/app/brain_left_test.go internal/app/brain_bindings.go internal/app/app.go
git commit -m "feat(app): 左脑适配器（方案）+ BrainStore 装配与 BrainWrite/BrainSearch/BrainCrossRefs 绑定"
```

---

### Task 5: 跨脑验收场景 + 前端三脑区块

**Files:**
- Create: `internal/app/brain_acceptance_test.go`
- Modify: `frontend/src/views/MemoryCenterView.tsx`（或 `frontend/src/pages/MemoryHubPage.tsx`，以实际文件为准）

- [ ] **Step 1: 写验收场景测试**

创建 `internal/app/brain_acceptance_test.go`：

```go
package app

import (
	"testing"

	"github.com/gaea/gaea/internal/office/proposal"
)

// 验收场景：轻语（右脑）记住甲方偏好 → 跨脑检索在方案（左脑）写作时可命中。
func TestBrainAcceptanceRightToLeftScenario(t *testing.T) {
	mainFake := &fakeMainBrain{rows: map[string]string{}}
	rightFake := &fakeRightBrain{rows: map[string]string{}}
	leftFake := &fakeLeftBrain{}
	// 用一个可注入 links 的内存版（此处用 nil 表示关联索引单独验证）
	bs := &BrainStore{main: mainFake, left: leftFake, right: rightFake}
	leftBrain := &leftBrain{src: &fakeLeftSource{ps: []proposal.Proposal{
		{ID: "p1", Title: "土壤修复标书", Category: "环保工程", Requirements: "报价策略见甲方偏好"},
	}}}
	_ = leftBrain

	if err := bs.Write(BrainRight, "甲方A", "偏好", "保守报价"); err != nil {
		t.Fatal(err)
	}
	hits, err := bs.Search("甲方A 报价", BrainRight)
	if err != nil || len(hits) == 0 {
		t.Fatalf("右脑命中失败: %+v %v", hits, err)
	}
	// 左脑方案也应按关键词命中（同一查询可命中两脑）。
	all, err := bs.Search("报价")
	if err != nil || len(all) == 0 {
		t.Fatalf("跨脑检索失败: %+v %v", all, err)
	}
}
```

说明：该测试验证跨脑检索语义；真实"方案写作注入"由 P3 主脑助手阶段把 `BrainSearch` 结果注入 office 生成上下文。

- [ ] **Step 2: 前端三脑区块**

在记忆中枢页（`frontend/src/views/MemoryCenterView.tsx` 或 `frontend/src/pages/MemoryHubPage.tsx`）追加一个"三脑检索"卡片：

```tsx
<Card size="small" title="三脑检索" style={{ marginBottom: 16 }}>
  <Space.Compact style={{ width: '100%' }}>
    <Input
      placeholder="跨脑检索：如 甲方A 报价"
      value={brainQuery}
      onChange={e => setBrainQuery(e.target.value)}
      onPressEnter={() => runBrainSearch()}
    />
    <Button type="primary" onClick={runBrainSearch}>检索</Button>
  </Space.Compact>
  {brainHits.length > 0 && (
    <div style={{ marginTop: 12 }}>
      {brainHits.map((h, i) => (
        <div key={i} style={{ padding: '6px 0', borderBottom: '1px solid var(--border-subtle)', fontSize: 12 }}>
          <Tag color={h.brain === 'brain.right' ? 'purple' : h.brain === 'brain.left' ? 'blue' : 'green'}>
            {h.brain === 'brain.right' ? '右脑' : h.brain === 'brain.left' ? '左脑' : '主脑'}
          </Tag>
          {h.entity}：{h.text}
        </div>
      ))}
    </div>
  )}
</Card>
```

配套状态与函数（放在该组件内）：

```tsx
const [brainQuery, setBrainQuery] = useState('')
const [brainHits, setBrainHits] = useState<Array<{ brain: string; entity: string; text: string }>>([])
const runBrainSearch = async () => {
  const bind = (window as any).go?.app?.App
  if (!bind?.BrainSearch) return
  try {
    setBrainHits(JSON.parse(await bind.BrainSearch(brainQuery, '')))
  } catch { /* ignore */ }
}
```

- [ ] **Step 3: 前端验证**

```powershell
Set-Location frontend
npx.cmd tsc -b
Set-Location ..
go build ./...
```

预期：全绿。

- [ ] **Step 4: 提交**

```powershell
git add internal/app/brain_acceptance_test.go frontend/src/views/MemoryCenterView.tsx frontend/src/pages/MemoryHubPage.tsx
git commit -m "feat: 三脑跨脑检索验收场景 + 记忆中枢三脑检索区块"
```

（按实际修改的文件调整 `git add` 列表。）

---

### Task 6: 全量验证与记录

**Files:**
- Modify: `.gaea/progress.md`

- [ ] **Step 1: CI 闸门**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci.ps1
```

预期：`CI OK`，`git status --short` 为空。

- [ ] **Step 2: 记录进度**

在 `.gaea/progress.md` 的 P1 段落下追加：

```markdown
## 2.0 P2 三脑记忆（✅ 已完成）

- BrainStore 统一访问层：Read/Write/Search/Link/CrossRefs
- 主脑适配器（画像+知识库）、右脑适配器（hermes.db）、左脑适配器（方案）
- brain_links 跨脑关联索引（Hephaestus.db 零迁移建表）
- BrainWrite/BrainSearch/BrainCrossRefs 绑定 + 记忆中枢三脑检索区块
- 验收：右脑"甲方A 保守报价" → 跨脑检索命中；现有数据零迁移（适配器只读写不迁移）
```

- [ ] **Step 3: 提交**

```powershell
git add .gaea/progress.md
git commit -m "docs: P2 三脑记忆完成记录"
```

---

## 验收清单

| 项 | 判据 |
|----|------|
| 统一访问 | `BrainStore` Read/Write/Search 三脑可用，测试覆盖 |
| 关联索引 | `Link/CrossRefs` 经 brain_links 表持久化（重开库可读回） |
| 跨脑场景 | 右脑写入"甲方A 保守报价" → 跨脑检索命中 |
| 零迁移 | 适配器只读/写现有存储，不迁移、不复制数据 |
| 绑定 | BrainWrite/BrainSearch/BrainCrossRefs 可用，前端三脑检索区块展示 |
| 全量 | `scripts/ci.ps1` 输出 CI OK，工作区干净 |
