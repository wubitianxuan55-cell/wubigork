# gaea 2.0 P3「主脑助手」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 GAEA 升级为三脑协调者：新增 `ModuleRegistry`（模块注册 + 统一派发）、规则意图识别与 `MainBrainChat` 统一入口（取两脑资料 → 派发模块 → 汇总），并加前端主脑对话入口。1.x 办公 agent 能力保留。

**Architecture:** 在 `internal/app` 新增 `module_registry.go`（`Module{ID, Intents, Handle}` + `Dispatch`）与 `main_brain.go`（`classifyMainBrainIntent` 规则识别 + `MainBrainChat`）。注册表在 `App.initModules()` 装配真实闭包（包住现有 `WhisperChat` / `CreateChapter` / `ProposalCreate` / `GenerateFreeImage` / `ChatGeneral`）；`MainBrainChat` 先规则识别意图，再 `BrainSearch` 取材料注入上下文，派发模块后返回结构化结果。前端新增主脑对话页与首页入口。

**Tech Stack:** Go 1.26.3、Wails v2.13.0、React 18 + antd 5。

**前置事实（已核实）：**

- 模块入口（均嵌入 `App`）：`whisperState.WhisperChat(userMsg, personalityID)`、`writingState.CreateChapter(setting, prevSummary, plotReq, chapterNum, branchFromNodeID, skillName)`、`officeState.ProposalCreate(title, tmpl, req, cat, projectID)`、`mediaState.GenerateFreeImage(prompt, negative, size, style, model, seed, n, lora)`、`App.ChatGeneral(userMsg)`。
- `BrainStore`（P2 已交付）：`Search(query, brains...)` 跨脑检索。
- `routeModel`（P1 已交付）：`routeModel("gaea")` 取主脑模型绑定。

---

### Task 1: ModuleRegistry + RunModule 绑定

**Files:**
- Create: `internal/app/module_registry.go`
- Create: `internal/app/module_registry_test.go`
- Modify: `internal/app/app.go`（App 字段 + `initModules()`）
- Create: `internal/app/module_bindings.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/app/module_registry_test.go`：

```go
package app

import (
	"strings"
	"testing"
)

func TestModuleRegistryDispatch(t *testing.T) {
	reg := NewModuleRegistry()
	if err := reg.Register(Module{
		ID: "office", Name: "方案",
		Intents: []string{"create"},
		Handle: func(input map[string]any) (map[string]any, error) {
			return map[string]any{"created": true, "title": input["title"]}, nil
		},
	}); err != nil {
		t.Fatal(err)
	}
	out, err := reg.Dispatch("office", "create", map[string]any{"title": "土壤修复标书"})
	if err != nil {
		t.Fatal(err)
	}
	if out["created"] != true || out["title"] != "土壤修复标书" {
		t.Fatalf("out = %+v", out)
	}
	if _, err := reg.Dispatch("novel", "create_chapter", nil); err == nil || !strings.Contains(err.Error(), "unknown module") {
		t.Fatalf("unknown module err = %v", err)
	}
	if _, err := reg.Dispatch("office", "unknown_intent", nil); err == nil || !strings.Contains(err.Error(), "unknown intent") {
		t.Fatalf("unknown intent err = %v", err)
	}
}
```

- [ ] **Step 2: 运行确认失败**

```powershell
go test ./internal/app/ -run TestModuleRegistry -count=1
```

预期：编译失败（`NewModuleRegistry` undefined）。

- [ ] **Step 3: 实现 `internal/app/module_registry.go`**

```go
package app

import "fmt"

// Module 是一个可派发模块：Handle 接收 input map，返回结构化输出。
type Module struct {
	ID      string
	Name    string
	Intents []string
	Handle  func(input map[string]any) (map[string]any, error)
}

// ModuleRegistry 模块注册表（主脑派发用）。
type ModuleRegistry struct {
	modules map[string]Module
}

func NewModuleRegistry() *ModuleRegistry {
	return &ModuleRegistry{modules: map[string]Module{}}
}

func (r *ModuleRegistry) Register(m Module) error {
	if m.ID == "" || m.Handle == nil {
		return fmt.Errorf("module needs id and handle")
	}
	if _, dup := r.modules[m.ID]; dup {
		return fmt.Errorf("duplicate module %q", m.ID)
	}
	r.modules[m.ID] = m
	return nil
}

func (r *ModuleRegistry) Dispatch(moduleID, intent string, input map[string]any) (map[string]any, error) {
	m, ok := r.modules[moduleID]
	if !ok {
		return nil, fmt.Errorf("unknown module %q", moduleID)
	}
	for _, i := range m.Intents {
		if i == intent {
			return m.Handle(input)
		}
	}
	return nil, fmt.Errorf("unknown intent %q for module %q", intent, moduleID)
}

func (r *ModuleRegistry) Has(moduleID string) bool {
	_, ok := r.modules[moduleID]
	return ok
}
```

- [ ] **Step 4: 实现 App 装配与绑定**

在 `internal/app/app.go` 的 `App` 结构体追加字段 `modules *ModuleRegistry`；在 `New` 中 `a.proposalSvc = proposal.NewService(...)` 之后调用 `a.initModules()`。

创建 `internal/app/module_bindings.go`：

```go
package app

import (
	"encoding/json"
	"fmt"
)

// initModules 注册主脑可派发的模块（闭包包住现有 App 方法）。
func (a *App) initModules() {
	a.modules = NewModuleRegistry()
	_ = a.modules.Register(Module{
		ID: "gaea", Name: "主脑",
		Intents: []string{"chat"},
		Handle: func(input map[string]any) (map[string]any, error) {
			msg, _ := input["message"].(string)
			out, err := a.ChatGeneral(msg)
			if err != nil {
				return nil, err
			}
			return out, nil
		},
	})
	_ = a.modules.Register(Module{
		ID: "whisper", Name: "轻语",
		Intents: []string{"chat"},
		Handle: func(input map[string]any) (map[string]any, error) {
			msg, _ := input["message"].(string)
			pid, _ := input["personality_id"].(string)
			if pid == "" {
				pid = "轻语"
			}
			return a.WhisperChat(msg, pid)
		},
	})
	_ = a.modules.Register(Module{
		ID: "novel", Name: "小说",
		Intents: []string{"create_chapter"},
		Handle: func(input map[string]any) (map[string]any, error) {
			setting, _ := input["setting"].(string)
			plotReq, _ := input["plot_req"].(string)
			num := intField(input, "chapter_num", 1)
			return a.CreateChapter(setting, "", plotReq, num, "", "")
		},
	})
	_ = a.modules.Register(Module{
		ID: "office", Name: "方案",
		Intents: []string{"create"},
		Handle: func(input map[string]any) (map[string]any, error) {
			title, _ := input["title"].(string)
			tmpl, _ := input["template"].(string)
			req, _ := input["requirements"].(string)
			cat, _ := input["category"].(string)
			pid, _ := input["project_id"].(string)
			return a.ProposalCreate(title, tmpl, req, cat, pid)
		},
	})
	_ = a.modules.Register(Module{
		ID: "imagegen", Name: "绘梦",
		Intents: []string{"generate"},
		Handle: func(input map[string]any) (map[string]any, error) {
			prompt, _ := input["prompt"].(string)
			negative, _ := input["negative"].(string)
			size, _ := input["size"].(string)
			style, _ := input["style"].(string)
			model, _ := input["model"].(string)
			return a.GenerateFreeImage(prompt, negative, size, style, model,
				intField(input, "seed", 0), intField(input, "n", 1), "")
		},
	})
}

func intField(input map[string]any, key string, def int) int {
	if v, ok := input[key].(float64); ok {
		return int(v)
	}
	if v, ok := input[key].(int); ok {
		return v
	}
	return def
}

// RunModule 统一派发（Wails 绑定）。
func (a *App) RunModule(moduleID, intent, inputJSON string) (string, error) {
	if a.modules == nil {
		return "", fmt.Errorf("modules not initialized")
	}
	var input map[string]any
	if inputJSON != "" {
		if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
			return "", err
		}
	}
	out, err := a.modules.Dispatch(moduleID, intent, input)
	if err != nil {
		return "", err
	}
	b, _ := json.Marshal(out)
	return string(b), nil
}
```

- [ ] **Step 5: 验证**

```powershell
go build ./...; go test ./internal/app/ -run TestModuleRegistry -count=1
```

预期：全绿。

- [ ] **Step 6: 提交**

```powershell
git add internal/app/module_registry.go internal/app/module_registry_test.go internal/app/module_bindings.go internal/app/app.go
git commit -m "feat(app): ModuleRegistry 模块注册与统一派发 + RunModule 绑定（gaea/whisper/novel/office/imagegen）"
```

---

### Task 2: 主脑意图识别

**Files:**
- Create: `internal/app/main_brain.go`
- Create: `internal/app/main_brain_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/app/main_brain_test.go`：

```go
package app

import "testing"

func TestClassifyMainBrainIntent(t *testing.T) {
	cases := []struct {
		msg    string
		module string
		intent string
	}{
		{"帮我把这个标书写了", "office", "create"},
		{"写一份土壤修复方案", "office", "create"},
		{"生成第三章", "novel", "create_chapter"},
		{"写小说章节", "novel", "create_chapter"},
		{"和轻语聊聊天", "whisper", "chat"},
		{"画一张星空图", "imagegen", "generate"},
		{"生成一幅水墨画", "imagegen", "generate"},
		{"今天天气怎么样", "gaea", "chat"},
	}
	for _, tc := range cases {
		module, intent := classifyMainBrainIntent(tc.msg)
		if module != tc.module || intent != tc.intent {
			t.Errorf("%q → (%q,%q), want (%q,%q)", tc.msg, module, intent, tc.module, tc.intent)
		}
	}
}
```

- [ ] **Step 2: 运行确认失败**

```powershell
go test ./internal/app/ -run TestClassifyMainBrainIntent -count=1
```

预期：编译失败。

- [ ] **Step 3: 实现意图识别（追加到 `internal/app/main_brain.go`）**

```go
package app

import "strings"

// classifyMainBrainIntent 规则识别主脑意图：关键词命中模块与意图；默认 gaea.chat。
func classifyMainBrainIntent(msg string) (moduleID, intent string) {
	lower := strings.ToLower(msg)
	switch {
	case containsAny(lower, "标书", "招标", "方案", "报价", "proposal", "tender"):
		return "office", "create"
	case containsAny(lower, "章节", "小说", "大纲", "角色", "chapter", "novel"):
		return "novel", "create_chapter"
	case containsAny(lower, "轻语", "聊天", "陪", "whisper"):
		return "whisper", "chat"
	case containsAny(lower, "画", "图", "生图", "绘梦", "image", "generate"):
		return "imagegen", "generate"
	default:
		return "gaea", "chat"
	}
}

func containsAny(s string, subs ...string) bool {
	for _, sub := range subs {
		if strings.Contains(s, sub) {
			return true
		}
	}
	return false
}
```

注意：`containsAny` 若与既有函数重名（`rg -n "func containsAny" internal/app`），改用 `mainBrainContainsAny`。

- [ ] **Step 4: 验证**

```powershell
go test ./internal/app/ -run TestClassifyMainBrainIntent -count=1
```

预期：PASS。

- [ ] **Step 5: 提交**

```powershell
git add internal/app/main_brain.go internal/app/main_brain_test.go
git commit -m "feat(app): 主脑意图识别（规则分类 → office/novel/whisper/imagegen/gaea）"
```

---

### Task 3: MainBrainChat 统一入口

**Files:**
- Modify: `internal/app/main_brain.go`（追加 MainBrainChat）
- Create: `internal/app/main_brain_chat_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/app/main_brain_chat_test.go`：

```go
package app

import (
	"strings"
	"testing"
)

func TestMainBrainChatDispatchWithMaterials(t *testing.T) {
	a := &App{}
	a.modules = NewModuleRegistry()
	_ = a.modules.Register(Module{
		ID: "office", Name: "方案", Intents: []string{"create"},
		Handle: func(input map[string]any) (map[string]any, error) {
			return map[string]any{"created": true, "title": input["title"]}, nil
		},
	})
	a.brain = &BrainStore{
		main:  &fakeMainBrain{rows: map[string]string{}},
		left:  &fakeLeftBrain{},
		right: &fakeRightBrain{rows: map[string]string{"甲方A|偏好": "保守报价"}},
	}
	out, err := a.MainBrainChat("标书报价")
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(out, `"module":"office"`) || !strings.Contains(out, `"intent":"create"`) {
		t.Fatalf("out = %s", out)
	}
	// 材料注入：右脑命中应出现在结果里。
	if !strings.Contains(out, "保守报价") {
		t.Fatalf("materials 未注入: %s", out)
	}
}
```

- [ ] **Step 2: 运行确认失败**

```powershell
go test ./internal/app/ -run TestMainBrainChat -count=1
```

预期：编译失败（`MainBrainChat` undefined）。

- [ ] **Step 3: 实现 MainBrainChat（追加到 `internal/app/main_brain.go`）**

```go
// MainBrainChat 主脑统一入口：规则识别意图 → BrainSearch 取两脑材料 → 派发模块 → 汇总。
func (a *App) MainBrainChat(message string) (string, error) {
	moduleID, intent := classifyMainBrainIntent(message)
	var materials []Hit
	if a.brain != nil {
		materials, _ = a.brain.Search(message)
		if len(materials) > 5 {
			materials = materials[:5]
		}
	}
	result := map[string]any{
		"module": moduleID, "intent": intent, "materials": materials,
	}
	if a.modules != nil && a.modules.Has(moduleID) {
		out, err := a.modules.Dispatch(moduleID, intent, map[string]any{
			"message": message, "title": message, "requirements": message, "prompt": message,
		})
		if err != nil {
			result["error"] = err.Error()
		} else {
			result["output"] = out
			if text, ok := out["reply"].(string); ok {
				result["reply"] = text
			}
		}
	}
	b, _ := json.MarshalIndent(result, "", "  ")
	return string(b), nil
}
```

说明：`main_brain.go` 需要追加 `"encoding/json"` 导入；材料注入的完整"方案写作上下文融合"由 P4 场景深化，本步先保证材料随派发结果返回并可在前端展示。

- [ ] **Step 4: 验证**

```powershell
go test ./internal/app/ -run 'TestMainBrainChat|TestClassifyMainBrainIntent' -count=1
```

预期：全绿。

- [ ] **Step 5: 提交**

```powershell
git add internal/app/main_brain.go internal/app/main_brain_chat_test.go
git commit -m "feat(app): MainBrainChat 主脑统一入口（意图识别 + 跨脑材料 + 模块派发）"
```

---

### Task 4: 前端主脑对话入口

**Files:**
- Create: `frontend/src/pages/MainBrainPage.tsx`
- Modify: `frontend/src/App.tsx`（首页入口或顶栏）

- [ ] **Step 1: 创建主脑对话页**

创建 `frontend/src/pages/MainBrainPage.tsx`：

```tsx
import React, { useState } from "react";
import { Typography, Input, Button, Card, Tag } from "antd";

interface BrainHit { brain: string; entity: string; text: string }
interface BrainResult {
  module: string;
  intent: string;
  reply?: string;
  output?: Record<string, any>;
  error?: string;
  materials?: BrainHit[];
}

export default function MainBrainPage() {
  const [msg, setMsg] = useState("");
  const [busy, setBusy] = useState(false);
  const [result, setResult] = useState<BrainResult | null>(null);

  const send = async () => {
    if (!msg.trim()) return;
    setBusy(true);
    try {
      const bind = (window as any).go?.app?.App;
      if (!bind?.MainBrainChat) throw new Error("binding unavailable");
      setResult(JSON.parse(await bind.MainBrainChat(msg)));
    } catch (e: any) {
      setResult({ module: "", intent: "", error: String(e?.message ?? e) });
    } finally {
      setBusy(false);
    }
  };

  return (
    <div style={{ maxWidth: 720, margin: "0 auto", padding: 24 }}>
      <Typography.Title level={4} style={{ color: "var(--color-text)" }}>主脑</Typography.Title>
      <Card size="small" style={{ marginBottom: 16 }}>
        <Input.TextArea
          rows={3}
          value={msg}
          onChange={(e) => setMsg(e.target.value)}
          placeholder="一句话调度：如「帮我把土壤修复标书写了」「和轻语聊聊」"
        />
        <Button type="primary" style={{ marginTop: 10 }} loading={busy} onClick={send}>
          交给主脑
        </Button>
      </Card>
      {result && (
        <Card size="small">
          <div style={{ marginBottom: 8 }}>
            <Tag color={result.module === "office" ? "blue" : result.module === "whisper" ? "purple" : result.module === "novel" ? "orange" : result.module === "imagegen" ? "green" : "default"}>
              {result.module || "未知"}
            </Tag>
            <Tag>{result.intent}</Tag>
          </div>
          {result.reply && <div style={{ marginBottom: 8 }}>{result.reply}</div>}
          {result.output && <pre style={{ fontSize: 12, whiteSpace: "pre-wrap" }}>{JSON.stringify(result.output, null, 2)}</pre>}
          {result.error && <div style={{ color: "#ef4444", fontSize: 12 }}>{result.error}</div>}
          {result.materials && result.materials.length > 0 && (
            <div style={{ marginTop: 8, fontSize: 12, color: "var(--color-text-dim)" }}>
              跨脑材料：
              {result.materials.map((m, i) => (
                <div key={i}>{m.entity}：{m.text}</div>
              ))}
            </div>
          )}
        </Card>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 挂载入口**

在 `frontend/src/App.tsx` 中：导入 `MainBrainPage`，在视图切换处增加 `"mainbrain"` 视图（参考现有视图注册方式，如 `NovelView` 的挂载与 `validViews`/`defaultEntityId`/`TopBar` 入口；入口文案"主脑"）。具体挂载点以 `App.tsx` 现有视图机制为准。

- [ ] **Step 3: 前端验证**

```powershell
Set-Location frontend
npx.cmd tsc -b
Set-Location ..
go build ./...
```

预期：全绿。

- [ ] **Step 4: 提交**

```powershell
git add frontend/src/pages/MainBrainPage.tsx frontend/src/App.tsx
git commit -m "feat(frontend): 主脑对话页与首页入口（一句话调度 + 跨脑材料展示）"
```

---

### Task 5: 全量验证与记录

**Files:**
- Modify: `.gaea/progress.md`

- [ ] **Step 1: CI 闸门**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\ci.ps1
```

预期：`CI OK`，`git status --short` 为空。

- [ ] **Step 2: 记录进度**

在 `.gaea/progress.md` 的 P2 段落下追加：

```markdown
## 2.0 P3 主脑助手（✅ 已完成）

- ModuleRegistry 模块注册与统一派发 + RunModule 绑定（gaea/whisper/novel/office/imagegen）
- 主脑意图识别（规则分类）+ MainBrainChat 统一入口（跨脑材料 + 模块派发 + 汇总）
- 前端主脑对话页与首页入口
- 验收：一句话任务派发到正确模块，跨脑材料随结果返回
```

- [ ] **Step 3: 提交**

```powershell
git add .gaea/progress.md
git commit -m "docs: P3 主脑助手完成记录"
```

---

## 验收清单

| 项 | 判据 |
|----|------|
| 注册表 | 5 模块注册、`RunModule` 可派发；未知模块/意图报错 |
| 意图识别 | 规则分类测试通过（office/novel/whisper/imagegen/gaea） |
| 主脑入口 | `MainBrainChat` 返回 module/intent/materials/output；测试覆盖 |
| 前端 | 主脑对话页可用，tsc 通过 |
| 全量 | `scripts/ci.ps1` 输出 CI OK，工作区干净 |
