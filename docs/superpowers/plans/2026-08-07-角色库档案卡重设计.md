# 角色库「人物档案卡」重设计 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把角色库列表页的卡片重设计为“人物档案卡”，同时保持全部现有功能与信息架构不变。

**Architecture:** 新增独立档案卡组件 `CharacterCard`（含独立 CSS 与组件测试），列表页改为渲染该组件；首次为前端引入 Vitest 测试设施，仅用于新组件测试，不改动现有页面逻辑。

**Tech Stack:** React 18 + TypeScript + antd 5 + Vite 6 + Vitest 3 + @testing-library/react。

**Spec:** `docs/superpowers/specs/2026-08-07-角色库档案卡重设计-design.md`

---

## File Structure

- `frontend/package.json`（修改）：新增 `test` 脚本与 devDependencies。
- `frontend/vite.config.ts`（修改）：`defineConfig` 改从 `vitest/config` 引入，新增 `test` 配置。
- `frontend/src/test/setup.ts`（新增）：测试后自动 cleanup。
- `frontend/src/components/characterlib/CharacterCard.tsx`（新增）：档案卡组件。
- `frontend/src/components/characterlib/character-card.css`（新增）：档案卡样式。
- `frontend/src/components/characterlib/CharacterCard.test.tsx`（新增）：组件测试。
- `frontend/src/pages/CharacterLibraryPage.tsx`（修改）：网格改用 `CharacterCard`，头部工具栏轻量整理，跟踪“当前人格”。

---

## Task 1: Vitest 测试设施

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/vite.config.ts`
- Create: `frontend/src/test/setup.ts`

- [ ] **Step 1: 修改 package.json，新增 test 脚本**

在 `frontend/package.json` 的 `scripts` 中加入：

```json
"test": "vitest run",
```

- [ ] **Step 2: 安装依赖**

在 `frontend` 目录执行：

```bash
npm.cmd install -D vitest jsdom @testing-library/react
```

预期：`package.json` 的 `devDependencies` 出现 `vitest`、`jsdom`、`@testing-library/react`。

- [ ] **Step 3: 新增 setup 文件**

创建 `frontend/src/test/setup.ts`：

```ts
import { afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

afterEach(() => {
  cleanup()
})
```

- [ ] **Step 4: 修改 vite.config.ts 接入 test 配置**

把第一行 `import { defineConfig } from 'vite'` 改为：

```ts
import { defineConfig } from 'vitest/config'
```

并在 `defineConfig({...})` 内、`plugins` 同级追加：

```ts
test: {
  environment: 'jsdom',
  setupFiles: './src/test/setup.ts',
  css: true,
},
```

- [ ] **Step 5: 运行 test 验证设施可用**

在 `frontend` 目录执行：

```bash
npm.cmd run test
```

预期：`No test files found` 且退出码 0（尚无测试文件，属正常）。

- [ ] **Step 6: 提交**

```bash
git add frontend/package.json frontend/package-lock.json frontend/vite.config.ts frontend/src/test/setup.ts
git commit -m "test: 引入 Vitest 前端测试设施"
```

---

## Task 2: CharacterCard 档案卡组件（TDD）

**Files:**
- Create: `frontend/src/components/characterlib/CharacterCard.test.tsx`
- Create: `frontend/src/components/characterlib/CharacterCard.tsx`
- Create: `frontend/src/components/characterlib/character-card.css`

- [ ] **Step 1: 编写失败测试**

创建 `frontend/src/components/characterlib/CharacterCard.test.tsx`：

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { CharacterCard } from './CharacterCard'
import type { LibraryCharacter } from '../../api/characterlib'

function makeCharacter(overrides: Partial<LibraryCharacter> = {}): LibraryCharacter {
  return {
    id: 'c1',
    name: '苏念',
    kind: 'custom',
    gender: 'female',
    age: '23',
    tags: ['剑修', '女主'],
    portraitUrl: '',
    roleType: 'protagonist',
    arc: '从逃避到直面宿命。',
    status: 'Alive',
    chatEnabled: true,
    dims: { T: 80, I: 60, S: 50, O: 70, R: 40 },
    createdAt: '',
    updatedAt: '',
    hidden: false,
    ...overrides,
  }
}

function noop() {}

const baseProps = {
  onEdit: noop,
  onSetPersona: noop,
  onMemory: noop,
  onAssociate: noop,
  onDissociate: noop,
  onDelete: noop,
}

describe('CharacterCard', () => {
  it('渲染档案编号与类型标签', () => {
    render(<CharacterCard character={makeCharacter()} index={1} {...baseProps} />)
    expect(screen.getByText('NO.002')).toBeTruthy()
    expect(screen.getByText('自定义')).toBeTruthy()
  })

  it('渲染元数据行：定位 · 性别 · 年龄 · 状态', () => {
    render(<CharacterCard character={makeCharacter()} index={0} {...baseProps} />)
    expect(screen.getByText('主角 · 女性 · 23 · 存活')).toBeTruthy()
  })

  it('有弧线时渲染引言，无弧线时不渲染', () => {
    const { rerender } = render(<CharacterCard character={makeCharacter()} index={0} {...baseProps} />)
    expect(screen.getByText('从逃避到直面宿命。')).toBeTruthy()
    rerender(<CharacterCard character={makeCharacter({ arc: undefined })} index={0} {...baseProps} />)
    expect(screen.queryByText('从逃避到直面宿命。')).toBeNull()
  })

  it('渲染 # 前缀标签，最多 3 个', () => {
    render(<CharacterCard character={makeCharacter({ tags: ['剑修', '女主', '冷面', '第四'] })} index={0} {...baseProps} />)
    expect(screen.getByText('#剑修')).toBeTruthy()
    expect(screen.getByText('#女主')).toBeTruthy()
    expect(screen.queryByText('#第四')).toBeNull()
  })

  it('有立绘时渲染图片，无立绘时渲染首字占位', () => {
    const { container } = render(<CharacterCard character={makeCharacter({ portraitUrl: 'https://x/1.png' })} index={0} {...baseProps} />)
    expect(container.querySelector('img')?.getAttribute('src')).toBe('https://x/1.png')
    const { container: c2 } = render(<CharacterCard character={makeCharacter({ portraitUrl: '' })} index={0} {...baseProps} />)
    expect(c2.querySelector('.ccard-placeholder')?.textContent).toBe('苏')
  })

  it('仅可聊天角色渲染五维雷达与可聊天标记', () => {
    const { container } = render(<CharacterCard character={makeCharacter()} index={0} {...baseProps} />)
    expect(container.querySelector('svg')).toBeTruthy()
    expect(container.querySelector('.ccard-chat')?.textContent).toBe('可聊天')
    const { container: c2 } = render(<CharacterCard character={makeCharacter({ chatEnabled: false })} index={0} {...baseProps} />)
    expect(c2.querySelector('svg')).toBeNull()
    expect(c2.querySelector('.ccard-chat')).toBeNull()
  })

  it('点击操作按钮回调对应 handler', () => {
    const c = makeCharacter()
    const onEdit = vi.fn()
    const onSetPersona = vi.fn()
    render(<CharacterCard character={c} index={0} onEdit={onEdit} onSetPersona={onSetPersona} onMemory={noop} onAssociate={noop} onDissociate={noop} onDelete={noop} />)
    fireEvent.click(screen.getByTitle('编辑'))
    expect(onEdit).toHaveBeenCalledWith(c)
    fireEvent.click(screen.getByTitle('设为当前聊天人格'))
    expect(onSetPersona).toHaveBeenCalledWith(c)
  })

  it('isCurrentPersona 时显示当前标记', () => {
    render(<CharacterCard character={makeCharacter()} index={0} isCurrentPersona {...baseProps} />)
    expect(screen.getByText('当前人格')).toBeTruthy()
  })

  it('inProject 时显示已加入标记与按钮', () => {
    render(<CharacterCard character={makeCharacter()} index={0} inProject hasProject {...baseProps} />)
    expect(screen.getAllByText('已加入').length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: 运行测试，验证失败**

在 `frontend` 目录执行：

```bash
npm.cmd run test
```

预期：FAIL，原因 `Cannot find module './CharacterCard'`（组件尚不存在，属预期失败）。

- [ ] **Step 3: 实现 CharacterCard 组件**

创建 `frontend/src/components/characterlib/CharacterCard.tsx`：

```tsx
import React from 'react'
import { Button, Popconfirm, Tag } from 'antd'
import {
  EditOutlined, SwapOutlined, DatabaseOutlined, DeleteOutlined, ReadOutlined,
} from '@ant-design/icons'
import TisorRadar from '../TisorRadar'
import type { LibraryCharacter } from '../../api/characterlib'
import { C } from '../../utils/theme'
import './character-card.css'

const KIND_META: Record<string, { label: string; color: string }> = {
  builtin: { label: '内置', color: 'gold' },
  custom: { label: '自定义', color: 'green' },
  assistant: { label: '助手', color: 'geekblue' },
}

const ROLE_LABELS: Record<string, string> = {
  protagonist: '主角',
  antagonist: '反派',
  supporting: '配角',
  minor: '龙套',
}

const GENDER_LABELS: Record<string, string> = {
  female: '女性',
  male: '男性',
  neutral: '中性',
}

const STATUS_LABELS: Record<string, string> = {
  Alive: '存活',
  Dead: '已故',
  Missing: '失踪',
  Transformed: '变身',
}

interface CharacterCardProps {
  character: LibraryCharacter
  index: number
  inProject?: boolean
  isCurrentPersona?: boolean
  hasProject?: boolean
  onEdit: (c: LibraryCharacter) => void
  onSetPersona: (c: LibraryCharacter) => void
  onMemory: (c: LibraryCharacter) => void
  onAssociate: (c: LibraryCharacter) => void
  onDissociate: (c: LibraryCharacter) => void
  onDelete: (c: LibraryCharacter) => void
}

export const CharacterCard: React.FC<CharacterCardProps> = ({
  character: c,
  index,
  inProject = false,
  isCurrentPersona = false,
  hasProject = false,
  onEdit,
  onSetPersona,
  onMemory,
  onAssociate,
  onDissociate,
  onDelete,
}) => {
  const km = KIND_META[c.kind] || KIND_META.custom
  const meta = [
    c.roleType ? ROLE_LABELS[c.roleType] || c.roleType : '',
    c.gender ? GENDER_LABELS[c.gender] || c.gender : '',
    c.age,
    c.status ? STATUS_LABELS[c.status] || c.status : '',
    inProject ? '已加入' : '',
  ].filter(Boolean).join(' · ')

  return (
    <div className="ccard">
      {isCurrentPersona && <span className="ccard-current">当前人格</span>}

      <div className="ccard-head">
        <span className="ccard-no">NO.{String(index + 1).padStart(3, '0')}</span>
        <span className="ccard-head-right">
          <Tag color={km.color} className="ccard-kind-tag">{km.label}</Tag>
          {c.chatEnabled && (
            <span className="ccard-chat"><i className="ccard-chat-dot" />可聊天</span>
          )}
        </span>
      </div>

      <div className="ccard-portrait ccard-sheen">
        {c.portraitUrl ? (
          <img className="ccard-portrait-img" src={c.portraitUrl} alt={c.name} />
        ) : (
          <div className="ccard-placeholder"><span>{c.name.slice(0, 1) || '?'}</span></div>
        )}
      </div>

      <div className="ccard-body">
        <h3 className="ccard-name">{c.name}</h3>
        {meta && <p className="ccard-meta">{meta}</p>}
        {c.arc && <p className="ccard-arc">{c.arc}</p>}
        {c.tags && c.tags.length > 0 && (
          <div className="ccard-tags">
            {c.tags.slice(0, 3).map(t => (
              <span key={t} className="ccard-tag">#{t}</span>
            ))}
          </div>
        )}
      </div>

      <div className="ccard-foot">
        {c.chatEnabled && (
          <TisorRadar dims={c.dims} size={52} color="#f472b6" showLabels={false} />
        )}
        <div className="ccard-actions">
          <Button size="small" type="text" icon={<EditOutlined />} title="编辑"
            onClick={() => onEdit(c)} />
          <Button size="small" type="text" icon={<SwapOutlined />} title="设为当前聊天人格"
            disabled={!c.chatEnabled} onClick={() => onSetPersona(c)} />
          {c.chatEnabled && (
            <Button size="small" type="text" icon={<DatabaseOutlined />} title="查看状态 / 记忆 / 追踪"
              onClick={() => onMemory(c)} />
          )}
          {hasProject && (
            inProject ? (
              <Button size="small" type="text" icon={<ReadOutlined />} title="从当前项目移除"
                onClick={() => onDissociate(c)}>已加入</Button>
            ) : (
              <Button size="small" type="text" icon={<ReadOutlined />} title="加入当前项目"
                onClick={() => onAssociate(c)}>加入项目</Button>
            )
          )}
          <Popconfirm
            title={c.kind === 'builtin' ? `隐藏「${c.name}」？` : `删除「${c.name}」？删除会同时清理项目引用与聊天通道`}
            okText={c.kind === 'builtin' ? '隐藏' : '删除'} cancelText="取消"
            onConfirm={() => onDelete(c)}>
            <Button size="small" type="text" danger icon={<DeleteOutlined />} title="删除"
              style={{ color: C('color-text-secondary') }} />
          </Popconfirm>
        </div>
      </div>
    </div>
  )
}

export default CharacterCard
```

- [ ] **Step 4: 实现档案卡样式**

创建 `frontend/src/components/characterlib/character-card.css`：

```css
/* 角色库档案卡：竖版人物设定卡 */
.ccard {
  position: relative;
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  border-radius: 16px;
  border: 1px solid var(--md-sys-color-outline-variant);
  background: var(--gaea-glass-bg, var(--md-sys-color-surface-container));
  overflow: hidden;
  transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}

.ccard:hover {
  transform: translateY(-3px);
  border-color: #f472b6;
  box-shadow: 0 14px 34px rgba(0, 0, 0, 0.18), 0 0 0 1px rgba(244, 114, 182, 0.14);
}

.ccard:active {
  transform: scale(0.98);
}

/* 档案眉 */
.ccard-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding: 10px 14px 8px;
  border-bottom: 1px solid var(--md-sys-color-outline-variant);
  flex-shrink: 0;
}

.ccard-no {
  font-size: 10px;
  letter-spacing: 0.08em;
  color: var(--md-sys-color-text-secondary);
  font-variant-numeric: tabular-nums;
}

.ccard-head-right {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  min-width: 0;
}

.ccard-kind-tag {
  margin: 0;
  font-size: 9px;
  line-height: 15px;
}

.ccard-chat {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 9.5px;
  color: #f472b6;
  white-space: nowrap;
}

.ccard-chat-dot {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: #f472b6;
  box-shadow: 0 0 6px rgba(244, 114, 182, 0.7);
}

/* 当前人格标记 */
.ccard-current {
  position: absolute;
  top: 8px;
  left: 8px;
  z-index: 3;
  padding: 2px 8px;
  border-radius: 999px;
  font-size: 9.5px;
  font-weight: 600;
  color: #fff;
  background: rgba(244, 114, 182, 0.9);
  box-shadow: 0 2px 10px rgba(244, 114, 182, 0.4);
}

/* 立绘横幅 */
.ccard-portrait {
  position: relative;
  aspect-ratio: 4 / 3;
  overflow: hidden;
  flex-shrink: 0;
}

.ccard-portrait-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.ccard-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 46px;
  font-weight: 700;
  color: rgba(244, 114, 182, 0.55);
  background:
    radial-gradient(120% 120% at 15% 0%, rgba(244, 114, 182, 0.16), transparent 55%),
    linear-gradient(145deg, rgba(244, 114, 182, 0.08), transparent 60%),
    var(--md-sys-color-surface-container-high);
}

.ccard-sheen::after {
  content: '';
  position: absolute;
  inset: 0;
  z-index: 2;
  pointer-events: none;
  background: linear-gradient(105deg, transparent 40%, rgba(255, 255, 255, 0.13) 50%, transparent 60%);
  transform: translateX(-130%);
  transition: transform 450ms ease;
}

.ccard:hover .ccard-sheen::after {
  transform: translateX(130%);
}

/* 档案正文 */
.ccard-body {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 12px 14px 10px;
  flex: 1;
  min-height: 0;
}

.ccard-name {
  margin: 0;
  font-size: 19px;
  font-weight: 700;
  line-height: 1.25;
  color: var(--md-sys-color-text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.ccard-meta {
  margin: 0;
  font-size: 10.5px;
  line-height: 1.5;
  color: var(--md-sys-color-text-secondary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.ccard-arc {
  margin: 2px 0 0;
  padding-left: 8px;
  border-left: 2px solid rgba(244, 114, 182, 0.45);
  font-size: 11px;
  line-height: 1.55;
  color: var(--md-sys-color-text);
  opacity: 0.82;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.ccard-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 4px 8px;
  margin-top: 2px;
}

.ccard-tag {
  font-size: 10px;
  color: var(--gaea-glow, var(--md-sys-color-primary));
  letter-spacing: 0.02em;
  opacity: 0.9;
}

/* 底部：雷达 + 操作 */
.ccard-foot {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding: 8px 14px 10px;
  border-top: 1px solid var(--md-sys-color-outline-variant);
  margin-top: auto;
  flex-shrink: 0;
}

.ccard-actions {
  display: flex;
  align-items: center;
  gap: 2px;
  min-width: 0;
  opacity: 0;
  transform: translateY(3px);
  transition: opacity 160ms ease, transform 160ms ease;
}

.ccard:hover .ccard-actions,
.ccard:focus-within .ccard-actions {
  opacity: 1;
  transform: none;
}

@media (prefers-reduced-motion: reduce) {
  .ccard,
  .ccard-sheen::after,
  .ccard-actions {
    transition: none !important;
  }
  .ccard:hover {
    transform: none;
  }
}
```

- [ ] **Step 5: 运行测试，验证通过**

在 `frontend` 目录执行：

```bash
npm.cmd run test
```

预期：9 个用例全部 PASS。

- [ ] **Step 6: 类型检查**

在 `frontend` 目录执行：

```bash
npx.cmd tsc -b
```

预期：无类型错误。

- [ ] **Step 7: 提交**

```bash
git add frontend/src/components/characterlib/CharacterCard.tsx frontend/src/components/characterlib/character-card.css frontend/src/components/characterlib/CharacterCard.test.tsx
git commit -m "feat: 角色库档案卡组件 CharacterCard"
```

---

## Task 3: 接入列表页

**Files:**
- Modify: `frontend/src/pages/CharacterLibraryPage.tsx`

- [ ] **Step 1: 修改导入与常量**

在 `CharacterLibraryPage.tsx` 顶部：

1. 移除 `Tag` 的 import（列表页不再直接用）。
2. 删除 `KIND_META` 与 `ROLE_LABELS` 常量（已移入卡片组件）。
3. 移除以下已不再使用的 import（旧卡片内联代码删除后仅剩这些地方用到）：`EditOutlined`、`DeleteOutlined`、`SwapOutlined`、`UserOutlined`、`DatabaseOutlined`、`ReadOutlined`、`TisorRadar`。
4. 新增 import：

```tsx
import CharacterCard from '../components/characterlib/CharacterCard'
```

5. 在组件内新增“当前人格”状态：

```tsx
const [currentPersona, setCurrentPersona] = useState<string>(() => {
  try { return localStorage.getItem(PERSONALITY_KEY) || '' } catch { return '' }
})
```

6. `handleSetPersona` 内新增一行：

```tsx
setCurrentPersona(c.id)
```

- [ ] **Step 2: 替换网格渲染**

把 `items.map(c => { ... })` 内整段 `<Col>...</Col>` 替换为：

```tsx
{items.map((c, idx) => {
  const inProject = projectRefs.has(c.id)
  return (
    <Col key={c.id} xs={24} sm={12} lg={8} xl={6}>
      <CharacterCard
        character={c}
        index={idx}
        inProject={inProject}
        isCurrentPersona={currentPersona === c.id}
        hasProject={hasProject}
        onEdit={openEdit}
        onSetPersona={handleSetPersona}
        onMemory={setMemoryChar}
        onAssociate={handleAssociate}
        onDissociate={handleDissociate}
        onDelete={handleDelete}
      />
    </Col>
  )
})}
```

注意：`openEdit`、`handleSetPersona`、`setMemoryChar`、`handleAssociate`、`handleDissociate`、`handleDelete` 的签名与卡片 props 兼容（都接收 `LibraryCharacter`）。

- [ ] **Step 3: 头部工具栏轻量整理**

把头部容器改为底部发丝线分隔、纵向留白更稳：

```tsx
<div style={{
  display: 'flex', alignItems: 'center', gap: 10,
  marginBottom: 12, paddingBottom: 12,
  borderBottom: '1px solid var(--md-sys-color-outline-variant)',
  flexShrink: 0, flexWrap: 'wrap',
}}>
```

其余按钮、输入、筛选保持不变。

- [ ] **Step 4: 运行测试与类型检查**

```bash
npm.cmd run test
npx.cmd tsc -b
```

预期：测试全绿、无类型错误。

- [ ] **Step 5: 前端构建验证**

```bash
npm.cmd run build
```

预期：构建成功，`../dist` 更新。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/pages/CharacterLibraryPage.tsx
git commit -m "feat: 角色库列表页接入档案卡"
```

---

## Task 4: 端到端验证

**Files:** 无代码改动（验证 + 必要时修复）

- [ ] **Step 1: 构建桌面应用并启动**

在项目根目录：

```bash
$env:TMP='C:\AI\wubigrok\.tmp'; $env:TEMP='C:\AI\wubigrok\.tmp'
wails build
Copy-Item build\bin\gaea.exe "$env:USERPROFILE\Desktop\gaea.exe" -Force
Start-Process "$env:USERPROFILE\Desktop\gaea.exe"
```

预期：构建成功，应用启动。

- [ ] **Step 2: UI 验证档案卡渲染**

通过 UIA 自动化导航到 角色库 页面，检查：
- 列表中出现 `NO.001` 编号、档案卡卡片。
- 搜索/筛选/新建/导入按钮仍可点击。
- 悬停卡片操作区可见（可用键盘 `Tab` 聚焦验证 `:focus-within`）。

- [ ] **Step 3: 回归验证聊天联动**

在角色库点击某可聊天角色的“设为当前聊天人格”，确认聊天页人格同步（`gaea-persona-changed` 事件）。

- [ ] **Step 4: 提交（若有修复）**

```bash
git add -A
git commit -m "fix: 角色库档案卡重设计收尾"
```

---

## Self-Review

- **Spec 覆盖**：档案眉/立绘/正文/底部结构 → Task 2；响应式列与间距 → Task 3 网格；排版/色彩/圆角 → Task 2 CSS；动效与 reduced-motion → Task 2 CSS；加载/空态/错误态 → 沿用现有页面逻辑（列表骨架未在 spec 中承诺新增，空态/错误态保留原样）；当前人格/已加入标记 → Task 2 测试与 Task 3 状态；工具栏与分页轻量整理 → Task 3 Step 3；无障碍（title/focus-within）→ Task 2 组件与 CSS。
- **占位符扫描**：所有代码步骤均含完整实现，无 TBD。
- **类型一致性**：`CharacterCardProps` 各回调签名与列表页既有 handler 一致；`index` 从 0 开始、编号显示 `NO.001`（测试用 index=1 断言 `NO.002` 验证 +1 逻辑）；`dims` 类型与 `TisorRadar` 兼容。
