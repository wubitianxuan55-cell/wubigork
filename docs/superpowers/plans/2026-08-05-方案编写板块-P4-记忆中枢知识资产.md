# 方案编写板块重设计 P4 — 记忆中枢知识资产接入 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把规范条文、素材、历史方案统一接入 gaea 记忆中枢（Hephaestus.db knowledge 表 + knowledge.Service），`spec_query` 改读知识库，方案完成可归档回写并提供同类型参考；模板库落库。

**Architecture:** proposal.Service 持有可注入的 knowledge.Store（默认走 knowledge.Global()，测试可覆盖）；内置 `specIndex` 与 `SoilRemediationKB` 幂等入库（Category=规范标准/经验总结）；素材以 Category=素材库 入库（tags: 业绩/人员/设备/段落）；历史方案以 Category=设计方案 + tag legacy-proposal 归档，SectionContext 自动注入同类型参考摘要；office.db templates 表 seed 并作为 ListTemplates 数据源。

**Tech Stack:** Go 1.26 + modernc.org/sqlite + React/TS/AntD + Wails v2

---

## 任务总览

| 任务 | 内容 | 提交 |
|---|---|---|
| 1 | 规范条文与领域知识入库（Seed + 检索 API） | `feat(office): 规范知识入库记忆中枢` |
| 2 | 素材库 API（业绩/人员/设备/段落） | `feat(office): 素材库接入记忆中枢` |
| 3 | 历史方案归档与同类型参考 | `feat(office): 历史方案归档与参考` |
| 4 | 模板库落库 | `feat(office): 模板库落库` |
| 5 | spec_query 改读记忆中枢 | `feat(office): spec_query 改读知识库` |
| 6 | 绑定与前端（归档/素材库） | `feat(office): 知识资产前端入口` |
| 7 | 全量验证与 v1.17.0 发布 | `release: v1.17.0 记忆中枢知识资产` |

---

## Task 1: 规范条文与领域知识入库

**Files:**
- Modify: `internal/gaea/tool/builtin/spec_query.go`（导出 SpecEntry/SpecLibrary）
- Create: `internal/office/proposal/knowledge_assets.go`（EnsureSpecAssets/SearchSpecs/Store 注入）
- Create: `internal/office/proposal/knowledge_assets_test.go`

- [ ] **Step 1: 写失败测试**

创建 `internal/office/proposal/knowledge_assets_test.go`：

```go
package proposal

import (
	"testing"

	"github.com/gaea/gaea/internal/gaea/knowledge"
)

func newTestKnowledgeStore(t *testing.T) *knowledge.Store {
	t.Helper()
	st, err := knowledge.Open(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	return st
}

func TestEnsureSpecAssets_SeedsAndSearch(t *testing.T) {
	st := newTestKnowledgeStore(t)
	svc := &Service{}
	svc.SetKnowledgeStoreForTest(st)
	if err := svc.EnsureSpecAssets(); err != nil {
		t.Fatalf("EnsureSpecAssets: %v", err)
	}
	// 幂等
	if err := svc.EnsureSpecAssets(); err != nil {
		t.Fatalf("EnsureSpecAssets 二次: %v", err)
	}
	res := svc.SearchSpecs("砷 筛选值")
	if len(res) == 0 {
		t.Fatal("SearchSpecs 无结果")
	}
	found := false
	for _, r := range res {
		if containsAny(r.Title, "GB 36600") && containsAny(r.Body, "砷") {
			found = true
		}
	}
	if !found {
		t.Fatalf("未命中 GB 36600 砷条文: %+v", res)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/office/proposal/ -run TestEnsureSpecAssets -v`
Expected: FAIL（编译错误：`SetKnowledgeStoreForTest`/`EnsureSpecAssets`/`SearchSpecs` 未定义）。

- [ ] **Step 3: 导出 specIndex**

在 `internal/gaea/tool/builtin/spec_query.go` 中把 `type specEntry struct` 重命名为 `type SpecEntry struct`，`var specIndex = []specEntry{` 改为 `var specIndex = []SpecEntry{`，并新增：

```go
// SpecLibrary 返回内置土壤修复规范索引（供知识库入库使用）
func SpecLibrary() []SpecEntry {
	out := make([]SpecEntry, len(specIndex))
	copy(out, specIndex)
	return out
}
```

（`Execute` 内 `[]specEntry` 类型引用同步改为 `[]SpecEntry`。）

- [ ] **Step 4: 实现 knowledge_assets.go**

创建 `internal/office/proposal/knowledge_assets.go`：

```go
// Package proposal — 记忆中枢知识资产接入（规范/素材/历史方案）
package proposal

import (
	"fmt"
	"strings"

	"github.com/gaea/gaea/internal/gaea/knowledge"
	"github.com/gaea/gaea/internal/gaea/tool/builtin"
)

// SetKnowledgeStoreForTest 覆盖知识库存储（测试隔离）
func (s *Service) SetKnowledgeStoreForTest(st *knowledge.Store) { s.kb = st }

// knowledgeStore 返回可用知识库存储；未注入时走全局单例，失败返回 nil
func (s *Service) knowledgeStore() *knowledge.Store {
	if s.kb != nil {
		return s.kb
	}
	st, err := knowledge.Global().Store()
	if err != nil {
		return nil
	}
	return st
}

// EnsureSpecAssets 把内置规范索引与土壤修复技术知识幂等写入记忆中枢
func (s *Service) EnsureSpecAssets() error {
	st := s.knowledgeStore()
	if st == nil {
		return fmt.Errorf("知识库不可用")
	}
	for _, e := range builtin.SpecLibrary() {
		name := "spec-" + slugSpec(e.Code+"-"+e.Clause)
		body := e.Content
		if e.Explanation != "" {
			body += "\n\n💡 解释：" + e.Explanation
		}
		entry := knowledge.Entry{
			Name: name, Title: fmt.Sprintf("%s %s %s", e.Code, e.Clause, e.Title),
			Category: knowledge.CatStandard, Tags: []string{e.Category},
			Status: "已发布", Source: e.Code, Body: body,
		}
		if err := st.Save(entry); err != nil {
			return err
		}
	}
	// 土壤修复通用技术知识
	soil := knowledge.Entry{
		Name: "soil-remediation-tech", Title: "土壤修复常用技术与规范标准",
		Category: knowledge.CatExperience, Tags: []string{"土壤修复", "技术比选"},
		Status: "已发布", Source: "gaea 内置", Body: SoilRemediationKB,
	}
	return st.Save(soil)
}

func slugSpec(s string) string {
	var b strings.Builder
	for _, r := range s {
		if r >= 'a' && r <= 'z' || r >= 'A' && r <= 'Z' || r >= '0' && r <= '9' {
			b.WriteRune(r)
		} else {
			b.WriteByte('-')
		}
	}
	return strings.Trim(b.String(), "-")
}

// SpecRef 规范检索结果
type SpecRef struct {
	Name  string `json:"name"`
	Title string `json:"title"`
	Code  string `json:"code"`
	Body  string `json:"body"`
}

// SearchSpecs 在记忆中枢检索规范条文（Top 5）
func (s *Service) SearchSpecs(query string) []SpecRef {
	st := s.knowledgeStore()
	if st == nil || strings.TrimSpace(query) == "" {
		return nil
	}
	entries := knowledge.Search(st, query, knowledge.Filter{Category: knowledge.CatStandard})
	var out []SpecRef
	for _, e := range entries {
		if len(out) >= 5 {
			break
		}
		out = append(out, SpecRef{Name: e.Name, Title: e.Title, Code: e.Source, Body: e.Body})
	}
	return out
}
```

`Service` 结构体增加字段 `kb *knowledge.Store`（`service.go`）。

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/office/proposal/ -run TestEnsureSpecAssets -v`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
gofmt -w internal/gaea/tool/builtin/spec_query.go internal/office/proposal/knowledge_assets.go internal/office/proposal/knowledge_assets_test.go internal/office/proposal/service.go
go test ./internal/gaea/tool/builtin/... ./internal/office/proposal/...
git add internal/gaea/tool/builtin/spec_query.go internal/office/proposal/knowledge_assets.go internal/office/proposal/knowledge_assets_test.go internal/office/proposal/service.go
git commit -m "feat(office): 规范知识入库记忆中枢"
```

---

## Task 2: 素材库 API

**Files:**
- Modify: `internal/office/proposal/knowledge_assets.go`（AssetRef/AddAsset/ListAssets/SearchAssets/RemoveAsset）
- Modify: `internal/office/proposal/knowledge_assets_test.go`

- [ ] **Step 1: 写失败测试**

在 `knowledge_assets_test.go` 追加：

```go
func TestAssetsCRUD(t *testing.T) {
	st := newTestKnowledgeStore(t)
	svc := &Service{}
	svc.SetKnowledgeStoreForTest(st)
	if err := svc.AddAsset("某公司土壤修复业绩", []string{"业绩"}, "2023 年完成某化工厂污染场地修复 5 万 m³"); err != nil {
		t.Fatalf("AddAsset: %v", err)
	}
	if err := svc.AddAsset("项目经理张三", []string{"人员"}, "高级工程师，10 年修复经验"); err != nil {
		t.Fatal(err)
	}
	all := svc.ListAssets()
	if len(all) != 2 {
		t.Fatalf("ListAssets = %d, want 2", len(all))
	}
	hit := svc.SearchAssets("修复", "")
	if len(hit) == 0 {
		t.Fatal("SearchAssets 无结果")
	}
	byTag := svc.SearchAssets("", "人员")
	if len(byTag) != 1 || byTag[0].Title != "项目经理张三" {
		t.Fatalf("按标签检索异常: %+v", byTag)
	}
	if err := svc.RemoveAsset(all[0].Name); err != nil {
		t.Fatal(err)
	}
	if len(svc.ListAssets()) != 1 {
		t.Fatalf("删除后数量异常")
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestAssetsCRUD -v`
Expected: FAIL（编译错误：AddAsset/ListAssets 等未定义）。

- [ ] **Step 3: 实现素材 API**

在 `knowledge_assets.go` 追加：

```go
// officeAssetsCategory 素材库在记忆中枢中的分类
const officeAssetsCategory = "素材库"

// AssetRef 素材条目
type AssetRef struct {
	Name  string   `json:"name"`
	Title string   `json:"title"`
	Tags  []string `json:"tags"`
	Body  string   `json:"body"`
}

// AddAsset 新增素材（业绩/人员/设备/常用段落）
func (s *Service) AddAsset(title string, tags []string, body string) error {
	st := s.knowledgeStore()
	if st == nil {
		return fmt.Errorf("知识库不可用")
	}
	if title == "" || body == "" {
		return fmt.Errorf("title 与 body 不能为空")
	}
	return st.Save(knowledge.Entry{
		Name: "asset-" + slugSpec(title) + "-" + nowStamp(),
		Title: title, Category: officeAssetsCategory, Tags: tags,
		Status: "已发布", Body: body,
	})
}

// ListAssets 列出全部素材
func (s *Service) ListAssets() []AssetRef {
	st := s.knowledgeStore()
	if st == nil {
		return nil
	}
	var out []AssetRef
	for _, e := range st.ReadAll() {
		if e.Category != officeAssetsCategory {
			continue
		}
		out = append(out, AssetRef{Name: e.Name, Title: e.Title, Tags: e.Tags, Body: e.Body})
	}
	return out
}

// SearchAssets 检索素材（query 与 tag 可空）
func (s *Service) SearchAssets(query, tag string) []AssetRef {
	st := s.knowledgeStore()
	if st == nil {
		return nil
	}
	filter := knowledge.Filter{Category: officeAssetsCategory, Tag: tag}
	var out []AssetRef
	for _, e := range knowledge.Search(st, query, filter) {
		out = append(out, AssetRef{Name: e.Name, Title: e.Title, Tags: e.Tags, Body: e.Body})
	}
	return out
}

// RemoveAsset 删除素材
func (s *Service) RemoveAsset(name string) error {
	st := s.knowledgeStore()
	if st == nil {
		return fmt.Errorf("知识库不可用")
	}
	return st.Delete(name)
}
```

`nowStamp` 辅助（`knowledge_assets.go`）：

```go
func nowStamp() string {
	return strings.ReplaceAll(strings.ReplaceAll(now(), " ", "-"), ":", "-")
}
```

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/office/proposal/ -run TestAssetsCRUD -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
gofmt -w internal/office/proposal/knowledge_assets.go internal/office/proposal/knowledge_assets_test.go
git add internal/office/proposal/knowledge_assets.go internal/office/proposal/knowledge_assets_test.go
git commit -m "feat(office): 素材库接入记忆中枢"
```

---

## Task 3: 历史方案归档与同类型参考

**Files:**
- Modify: `internal/office/proposal/knowledge_assets.go`（ArchiveProposal/SearchLegacyProposals/legacyRefFor）
- Modify: `internal/office/proposal/context.go`（SectionContext 注入历史方案参考）
- Modify: `internal/office/proposal/knowledge_assets_test.go`

- [ ] **Step 1: 写失败测试**

追加：

```go
func TestArchiveProposal_And_Reference(t *testing.T) {
	st := newTestKnowledgeStore(t)
	s := newServiceAt(t, t.TempDir(), &mockAI{def: "mock"})
	s.SetKnowledgeStoreForTest(st)
	proj, _ := s.store.EnsureDefaultProject()
	p, _ := s.store.Create("某修复投标方案", "soil-remediation-bid", "需求", "环保工程", proj.ID, []ProposalSection{
		{Title: "第一章", Level: 1, Content: "项目概况正文"},
	})
	name, err := s.ArchiveProposal(p.ID)
	if err != nil {
		t.Fatalf("ArchiveProposal: %v", err)
	}
	if name == "" {
		t.Fatal("归档名为空")
	}
	refs := s.SearchLegacyProposals("修复", "环保工程")
	if len(refs) == 0 {
		t.Fatal("历史方案检索无结果")
	}
	ref := s.legacyRefFor("soil-remediation-bid", "环保工程")
	if ref == "" {
		t.Fatal("同类型参考为空")
	}
	if !containsAny(ref, "某修复投标方案", "项目概况正文") {
		t.Fatalf("参考内容异常: %s", ref)
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestArchiveProposal -v`
Expected: FAIL。

- [ ] **Step 3: 实现归档与检索**

追加：

```go
// ArchiveProposal 把方案装配全文归档到记忆中枢（幂等覆盖），返回条目名
func (s *Service) ArchiveProposal(proposalID string) (string, error) {
	st := s.knowledgeStore()
	if st == nil {
		return "", fmt.Errorf("知识库不可用")
	}
	p, err := s.store.Get(proposalID)
	if err != nil {
		return "", err
	}
	name := "proposal-" + p.ID
	entry := knowledge.Entry{
		Name: name, Title: "【历史方案】" + p.Title,
		Category: knowledge.CatDesign,
		Tags:     []string{"legacy-proposal", p.Category, p.Template},
		Status:   "已发布", Source: p.Category, Body: Assemble(p),
	}
	if err := st.Save(entry); err != nil {
		return "", err
	}
	return name, nil
}

// SearchLegacyProposals 检索历史方案
func (s *Service) SearchLegacyProposals(query string) []AssetRef {
	st := s.knowledgeStore()
	if st == nil {
		return nil
	}
	var out []AssetRef
	for _, e := range knowledge.Search(st, query, knowledge.Filter{Category: knowledge.CatDesign, Tag: "legacy-proposal"}) {
		out = append(out, AssetRef{Name: e.Name, Title: e.Title, Tags: e.Tags, Body: e.Body})
	}
	return out
}

// legacyRefFor 返回同模板/同分类历史方案的参考摘要（最多 600 字）
func (s *Service) legacyRefFor(template, category string) string {
	st := s.knowledgeStore()
	if st == nil {
		return ""
	}
	for _, e := range st.ReadAll() {
		if e.Category != knowledge.CatDesign || !hasTag(e.Tags, "legacy-proposal") {
			continue
		}
		if template != "" && hasTag(e.Tags, template) {
			return truncate(e.Body, 600)
		}
		if category != "" && hasTag(e.Tags, category) {
			return truncate(e.Body, 600)
		}
	}
	return ""
}
```

`hasTag` 辅助（文件内）：

```go
func hasTag(tags []string, tag string) bool {
	for _, t := range tags {
		if t == tag {
			return true
		}
	}
	return false
}
```

- [ ] **Step 4: SectionContext 注入历史方案参考**

在 `context.go` 的 `SectionContext` 中，`【项目事实基线】` 块之后追加：

```go
	if p.Template != "" || p.Category != "" {
		if ref := s.legacyRefFor(p.Template, p.Category); ref != "" {
			parts = append(parts, "\n【历史方案参考（同类型，仅供结构/措辞参考，不得抄袭）】\n"+ref)
		}
	}
```

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/office/proposal/ -run 'TestArchiveProposal|TestSectionContext' -v`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
gofmt -w internal/office/proposal/knowledge_assets.go internal/office/proposal/context.go internal/office/proposal/knowledge_assets_test.go
git add internal/office/proposal/knowledge_assets.go internal/office/proposal/context.go internal/office/proposal/knowledge_assets_test.go
git commit -m "feat(office): 历史方案归档与参考"
```

---

## Task 4: 模板库落库

**Files:**
- Modify: `internal/office/proposal/store.go`（SeedTemplates/ListTemplatesDB）
- Modify: `internal/office/proposal/service.go`（NewService seed；ListTemplates 读库）
- Modify: `internal/office/proposal/store_test.go`

- [ ] **Step 1: 写失败测试**

追加：

```go
func TestTemplateSeedAndList(t *testing.T) {
	s := newTestStore(t)
	if err := s.SeedTemplates(); err != nil {
		t.Fatalf("SeedTemplates: %v", err)
	}
	if err := s.SeedTemplates(); err != nil {
		t.Fatalf("SeedTemplates 二次: %v", err)
	}
	list, err := s.ListTemplatesDB()
	if err != nil {
		t.Fatal(err)
	}
	found := false
	for _, tmpl := range list {
		if tmpl.ID == "soil-remediation-bid" && len(tmpl.Sections) == 15 {
			found = true
		}
	}
	if !found {
		t.Fatalf("土壤修复模板未入库: %+v", list)
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/office/proposal/ -run TestTemplateSeedAndList -v`
Expected: FAIL。

- [ ] **Step 3: 实现**

`store.go` 追加：

```go
// SeedTemplates 把默认模板幂等写入 templates 表
func (s *Store) SeedTemplates() error {
	if err := s.errIfUnavailable(); err != nil {
		return err
	}
	for _, t := range DefaultTemplates {
		data, err := json.Marshal(t.Sections)
		if err != nil {
			return err
		}
		if _, err := s.db.Exec(
			`INSERT INTO templates(id, name, description, sections, created_at, updated_at)
			 VALUES(?,?,?,?,?,?)
			 ON CONFLICT(id) DO UPDATE SET name=excluded.name, description=excluded.description, sections=excluded.sections, updated_at=excluded.updated_at`,
			t.ID, t.Name, t.Description, string(data), now(), now(),
		); err != nil {
			return err
		}
	}
	return nil
}

// ListTemplatesDB 从库中读取模板
func (s *Store) ListTemplatesDB() ([]Template, error) {
	if err := s.errIfUnavailable(); err != nil {
		return nil, err
	}
	rows, err := s.db.Query("SELECT id, name, description, sections FROM templates ORDER BY rowid")
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var out []Template
	for rows.Next() {
		var t Template
		var raw string
		if err := rows.Scan(&t.ID, &t.Name, &t.Description, &raw); err != nil {
			return nil, err
		}
		_ = json.Unmarshal([]byte(raw), &t.Sections)
		out = append(out, t)
	}
	return out, rows.Err()
}
```

`service.go`：`NewService` 中 `st := NewStore(...)` 之后追加：

```go
	if err := st.SeedTemplates(); err != nil {
		log.Printf("[proposal] 模板初始化失败: %v", err)
	}
```

`ListTemplates` 改为：

```go
// ListTemplates 列出模板（优先读库，失败回退默认）
func (s *Service) ListTemplates() []Template {
	if s.store != nil {
		if list, err := s.store.ListTemplatesDB(); err == nil && len(list) > 0 {
			return list
		}
	}
	return DefaultTemplates
}
```

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/office/proposal/ -run 'TestTemplateSeedAndList|TestListTemplates' -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
gofmt -w internal/office/proposal/store.go internal/office/proposal/service.go internal/office/proposal/store_test.go
git add internal/office/proposal/store.go internal/office/proposal/service.go internal/office/proposal/store_test.go
git commit -m "feat(office): 模板库落库"
```

---

## Task 5: spec_query 改读记忆中枢

**Files:**
- Modify: `internal/gaea/tool/builtin/spec_query.go`（Execute 优先读知识库）
- Create: `internal/gaea/tool/builtin/spec_query_test.go`

- [ ] **Step 1: 写失败测试**

```go
package builtin

import (
	"context"
	"encoding/json"
	"strings"
	"testing"

	"github.com/gaea/gaea/internal/gaea/knowledge"
)

func TestSpecQueryReadsKnowledgeBase(t *testing.T) {
	st, err := knowledge.Open(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	knowledge.SetStoreForTest(st)
	t.Cleanup(knowledge.ResetForTest)
	if err := st.Save(knowledge.Entry{
		Name: "spec-gb36600-1", Title: "GB 36600-2018 表1 建设用地标准",
		Category: knowledge.CatStandard, Tags: []string{"标准"},
		Source: "GB 36600-2018", Body: "砷(As)筛选值：一类20mg/kg、二类60mg/kg。",
	}); err != nil {
		t.Fatal(err)
	}
	out, err := (specQuery{}).Execute(context.Background(), json.RawMessage(`{"question":"砷 筛选值"}`))
	if err != nil {
		t.Fatalf("Execute: %v", err)
	}
	if !strings.Contains(out, "GB 36600") {
		t.Fatalf("未命中知识库条文: %s", out)
	}
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/gaea/tool/builtin/ -run TestSpecQueryReadsKnowledgeBase -v`
Expected: FAIL（当前 Execute 不读知识库，输出为内置索引格式——若仍包含 GB 36600 会误通过；因此断言改为输出包含知识库自定义标题「建设用地标准」）。

> 修正断言：`strings.Contains(out, "建设用地标准")`，内置索引标题为「土壤环境质量标准 建设用地」，与知识库标题区分。

- [ ] **Step 3: 修改 Execute**

在 `specQuery.Execute` 开头（解析参数后）插入知识库优先逻辑：

```go
	if st, err := knowledge.Global().Store(); err == nil && st != nil {
		if entries := knowledge.Search(st, p.Question, knowledge.Filter{Category: knowledge.CatStandard}); len(entries) > 0 {
			var b strings.Builder
			fmt.Fprintf(&b, "🔍 查询「%s」找到 %d 条相关规范条文（知识库）：\n\n", p.Question, len(entries))
			for i, e := range entries {
				if i >= 5 {
					break
				}
				fmt.Fprintf(&b, "━━━ [%d/%d] %s ━━━\n", i+1, len(entries), e.Title)
				fmt.Fprintf(&b, "📋 原文：%s\n", e.Body)
				if i < 4 && i < len(entries)-1 {
					fmt.Fprintf(&b, "\n")
				}
			}
			return tool.WrapText(b.String()), nil
		}
	}
```

import 增加 `"github.com/gaea/gaea/internal/gaea/knowledge"`。

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/gaea/tool/builtin/ -run TestSpecQueryReadsKnowledgeBase -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
gofmt -w internal/gaea/tool/builtin/spec_query.go internal/gaea/tool/builtin/spec_query_test.go
git add internal/gaea/tool/builtin/spec_query.go internal/gaea/tool/builtin/spec_query_test.go
git commit -m "feat(office): spec_query 改读知识库"
```

---

## Task 6: 绑定与前端

**Files:**
- Modify: `internal/app/office_handler.go`（ProposalArchive、ProposalAssets*）
- Modify: `frontend/src/pages/OfficePage.tsx`（导出 Tab 归档按钮、素材库 Modal）
- Regenerate: `frontend/wailsjs/go/app/App.d.ts`、`App.js`

- [ ] **Step 1: 后端绑定**

`office_handler.go` 新增：

```go
func (a *officeState) ProposalArchive(pid string) (string, error) {
	return a.proposalSvc.ArchiveProposal(pid)
}

func (a *officeState) ProposalAssetsList() []map[string]interface{} {
	if a.proposalSvc == nil {
		return nil
	}
	var r []map[string]interface{}
	for _, x := range a.proposalSvc.ListAssets() {
		r = append(r, assetToMap(x))
	}
	return r
}

func (a *officeState) ProposalAssetAdd(title string, tags []string, body string) error {
	return a.proposalSvc.AddAsset(title, tags, body)
}

func (a *officeState) ProposalAssetRemove(name string) error {
	return a.proposalSvc.RemoveAsset(name)
}
```

辅助：

```go
func assetToMap(a proposal.AssetRef) map[string]interface{} {
	return map[string]interface{}{"name": a.Name, "title": a.Title, "tags": a.Tags, "body": a.Body}
}
```

（新增绑定测试：归档后 `ProposalArchive` 返回非空名称；素材增删查走通。）

- [ ] **Step 2: 重新生成绑定**

Run: `wails generate module`
Expected: `ProposalArchive`/`ProposalAssetsList`/`ProposalAssetAdd`/`ProposalAssetRemove` 出现在 App.d.ts。

- [ ] **Step 3: 前端 ExportTab 归档按钮**

`ExportTab` 工具条增加：

```tsx
<Button icon={<BookOutlined />} onClick={harchive} loading={archiving}>归档到记忆中枢</Button>
```

状态与处理：

```tsx
const [archiving, setArchiving] = useState(false)
const harchive = async () => {
  if (!sid) return
  setArchiving(true)
  try {
    const name = await App.ProposalArchive(sid)
    message.success('已归档：' + name)
  } catch (e: any) { message.error(String(e)) }
  setArchiving(false)
}
```

- [ ] **Step 4: 前端素材库 Modal**

`OfficePage` 增加 `assetsOpen`/`assets`/`assetTitle`/`assetTags`/`assetBody` 状态；从右上角「项目事实」按钮旁增加「素材库」按钮；Modal 内容：

```tsx
<Modal title="素材库（业绩/人员/设备/段落）" open={assetsOpen} onCancel={() => setAssetsOpen(false)} footer={null} width={680}>
  <Space style={{ marginBottom: 12 }} wrap>
    <Input placeholder="搜索素材" style={{ width: 220 }} onChange={e => loadAssets(e.target.value)} />
    <Input value={assetTitle} placeholder="标题（如：某公司业绩）" style={{ width: 200 }} onChange={e => setAssetTitle(e.target.value)} />
    <Input value={assetTags} placeholder="标签，逗号分隔" style={{ width: 160 }} onChange={e => setAssetTags(e.target.value)} />
    <Button type="primary" size="small" onClick={addAsset}>新增</Button>
  </Space>
  <Input.TextArea value={assetBody} rows={2} placeholder="正文内容" onChange={e => setAssetBody(e.target.value)} />
  <div style={{ marginTop: 12, maxHeight: 380, overflow: 'auto' }}>
    {assets.map((x, i) => (
      <Card key={i} size="small" style={{ marginBottom: 8 }} title={x.title} extra={<Button type="text" size="small" danger onClick={() => removeAsset(x.name)}>删除</Button>}>
        {(x.tags || []).map(t => <Tag key={t} style={{ marginRight: 4 }}>{t}</Tag>)}
        <div style={{ fontSize: 13, whiteSpace: 'pre-wrap' }}>{x.body}</div>
      </Card>
    ))}
  </div>
</Modal>
```

`loadAssets`（可带 query，后端仅 ListAssets，前端过滤）、`addAsset`（`App.ProposalAssetAdd(assetTitle, assetTags.split(',').map(s => s.trim()).filter(Boolean), assetBody)`）、`removeAsset`。

- [ ] **Step 5: 前端验证**

Run（工作目录 `frontend`）: `node .\node_modules\typescript\bin\tsc -b`
Expected: 0 errors。

Run: `node .\node_modules\vite\bin\vite.js build`
Expected: 构建成功。

- [ ] **Step 6: 提交**

```bash
git add internal/app/office_handler.go internal/app/office_handler_test.go frontend/src/pages/OfficePage.tsx frontend/wailsjs/go/app/App.d.ts frontend/wailsjs/go/app/App.js
git commit -m "feat(office): 知识资产前端入口"
```

---

## Task 7: 全量验证与 v1.17.0 发布

- [ ] **Step 1: 后端全量验证**

Run: `go vet ./...` 与 `go test ./...`
Expected: vet clean，全部 PASS。

- [ ] **Step 2: 前端构建验证**

Run: `cd frontend && node .\node_modules\typescript\bin\tsc -b && node .\node_modules\vite\bin\vite.js build`
Expected: 0 errors + 构建成功。

- [ ] **Step 3: 桌面构建**

Run: `$env:TMP='C:\AI\wubigrok\.tmp'; $env:TEMP='C:\AI\wubigrok\.tmp'; wails build`
Expected: `build/bin/gaea.exe` 生成成功。

- [ ] **Step 4: 手工验收（CDP 或桌面）**

1. 首次使用方案板块后，记忆中枢「知识库」出现 15+ 规范条文（GB 36600/HJ 25.x 等）与「土壤修复常用技术」。
2. 办公 agent 中 `spec_query` 提问「砷 筛选值」返回知识库条文（标题含「建设用地标准」）。
3. 素材库 Modal 可增删查素材；生成章节时上下文注入同类型历史方案参考（若有归档）。
4. 方案导出 Tab 点「归档到记忆中枢」→ 知识库出现「【历史方案】…」；再建同类方案生成时引用该参考。

- [ ] **Step 5: 更新 CHANGELOG 与版本号**

`CHANGELOG.md` 顶部新增 `## v1.17.0「记忆中枢知识资产」` 节（按既有格式），`wails.json` 的 `productVersion` 改为 `1.17.0`。

- [ ] **Step 6: 提交**

```bash
git add CHANGELOG.md wails.json
git commit -m "release: v1.17.0 记忆中枢知识资产"
```

---

## 计划自检

- **Spec 覆盖**：§5.3 知识资产（规范/素材/历史方案统一集成记忆中枢，不单独造库）→ Task 1-3；spec_query/spec_judge 数据一致性 → Task 5（spec_judge 保持确定性判定表，限值数据与知识库条文一致）；模板库保留为 office.db → Task 4；§6 归档/素材入口 → Task 6。
- **范围控制**：不实现商机监控/多用户/云同步；不做图纸/清单/成本（其他板块）。
- **类型一致性**：`AssetRef`、`SpecRef`、`ProposalArchive`、`ProposalAssets*` 绑定与前端一致；`Service.kb *knowledge.Store` 注入点统一。
- **测试隔离**：所有触碰知识库的测试使用 `knowledge.Open(t.TempDir())` + `SetKnowledgeStoreForTest`/`SetStoreForTest`，绝不触碰真实 Hephaestus.db。
- **占位符检查**：无 TBD/TODO；每个代码步骤含完整实现或精确替换说明。
